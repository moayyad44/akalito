# أكليتو (Akleto) — سياق المشروع الكامل

## عن المشروع
تطبيق أردني لتوصيل مكونات الوجبات (مش وجبات جاهزة). المستخدم يختار وجبات يريد طبخها، التطبيق يحسب المكونات بالكميات الصحيحة عبر خوارزمية دمج ذكي، ويوصّل المكونات من أقرب متجر شريك.

**المستخدمون الأربعة:** زبون، سائق، متجر شريك، أدمن.
**صاحب المشروع:** مؤيد — ليس مبرمجاً، يتوقع من Claude تنفيذ كل الجانب التقني.

## الهوية البصرية
- خلفية: #FDF8F3 كريمي (أبداً أبيض نقي) — للتطبيقات العامة (الزبون، المتجر، السائق)
- عنابي: #A23E48 (اللون الرئيسي)
- خوخي: #F5A65B (ثانوي)
- زيتوني: #65743A (للطزاجة/الصحة فقط — ما تستخدمه لأي شي ثاني)
- ممنوع الزهري أو البرتقالي الفاقع
- لوحة الأدمن تحديداً: باليت غامق مختلف (خلفية #130609، أرقام تتوهج بألوان) — قرار مقصود لأنها لوحة تحكم داخلية، مش واجهة للزبون
- خط Cairo (نص) + Tajawal (عناوين)، RTL كامل
- حواف مدورة 8px+ دائماً

## Firebase
- مشروع: akleto-prod (Firestore + Email/Password Auth + Storage)
- Collections الموجودة:
  - meals: 5 وجبات (منسف، مقلوبة، مسخن، فلافل، فتة) — schema: name_ar, category, difficulty, prep_time_min, base_price, servings_default, description, image_url, is_active, order_count
  - ingredients: 25+ مكوّن — schema: name_ar, unit, category, price_per_unit, image_url, is_active
  - meal_ingredients: سجلات ربط (meal_id, ingredient_id, quantity_per_person) — بنية خوارزمية الدمج
  - stores / orders / drivers / notifications: فاضية، بانتظار بيانات حقيقية

## الملفات على GitHub (repo: moayyad44/akalito)
| الملف | الوصف |
|-------|--------|
| akleto-customer.html | تطبيق الزبون **الفعلي المبني** (ملف واحد SPA) — تسجيل بسيط (اسم+هاتف، بدون Auth كامل)، تصفح وجبات، تفاصيل الوجبة مع تعديل عدد الأشخاص، سلة، **الدمج الذكي الفعلي** لمكونات كل الوجبات المختارة، تأكيد الطلب (يُنشئ سجل بـ orders)، سجل الطلبات بصفحة الحساب. هوية بصرية كريمي/عنابي/خوخي/زيتوني منفصلة عن تصميم الأدمن الغامق |
| akleto-admin.html | لوحة الأدمن — نظرة عامة، Auth gate، إضافة وجبة (وربط مكوناتها)، نموذج إضافة مكوّن سريع (للاستخدام أثناء إضافة وجبة) |
| akleto-admin-ingredients.html | صفحة مستقلة لإدارة المكونات — عرض القائمة الكاملة + إضافة + حذف |
| akleto-admin-meals.html | صفحة مستقلة لإدارة الوجبات — شبكة بطاقات، ضغط على وجبة يفتح تفاصيلها ويقدر يعدلها (بما فيها مكوناتها)، إضافة، حذف |
| akleto-admin-customers.html | صفحة عرض الزبائن (تُملأ تلقائياً عند بناء تطبيق الزبون) + إمكانية حظر/رفع حظر زبون |
| akleto-admin-orders.html | صفحة إدارة الطلبات — تحديث لحظي (onSnapshot)، فلاتر بالحالة (قيد المعالجة/تجهيز/**جاهز للاستلام**/بالطريق/تم/ملغي)، تفاصيل كل طلب (الوجبات + المكونات المدمجة + المتجر المخصص + معلومات السائق المستلم إن وُجد + السعر)، تحديث حالة الطلب |
| akleto-admin-stores.html | صفحة إدارة المتاجر الشريكة — إضافة/تعديل/حذف متجر (اسم، هاتف، عنوان، إحداثيات lat/lng)، توليد PIN فريد لدخول موظفي المتجر، عرض/تفعيل/تعطيل، قائمة الموظفين المسجّلين بكل متجر |
| akleto-admin-units.html | 🆕 صفحة إدارة "الأوزان والمقاييس" — عرض/إضافة/تعديل/حذف وحدات الوصفة (رشة، معلقة، كوب...) لكل نوع قياس (وزن/حجم/عدد)، وتعديل اسم نوع القياس ووحدة الأساس. مربوطة بمجموعة `measure_config` بـ Firestore |
| akleto-driver.html | تطبيق السائق **الفعلي المبني** (ملف واحد SPA، هوية بصرية كريمي/عنابي مطابقة لتطبيق الزبون) — تسجيل بسيط (اسم+هاتف، بدون Auth، بينشئ سجل بـ collection جديد `drivers`)، شاشة "الطلبات المتاحة" (كل طلب status=ready يظهر لحظياً + اسم المتجر لمكان الاستلام)، استلام الطلب عبر Firestore Transaction (يمنع سائقين ياخذوا نفس الطلب بنفس اللحظة)، شاشة "توصيلاتي" (قيد التوصيل + سجل سابق)، زر "تم التوصيل"، شاشة حسابي |
| firebase-init.js | إعدادات Firebase المشتركة (config, db, auth, storage, uploadImage, withTimeout, runTransaction) — تستورده كل صفحات الأدمن + تطبيق السائق + تطبيق الزبون بدل تكرار الإعدادات. 🆕 فيه كمان نظام "أنواع القياس" (`MEASURE_TYPES`, `DEFAULT_MEASURE_TYPES`, `loadMeasureTypes()`, `convertToBase()`) — مربوط الآن بمجموعة `measure_config` بـ Firestore بدل ما يكون Object ثابت بالكود |
| admin-style.css | كل تنسيقات لوحة الأدمن (Dark/Light mode) بملف واحد مشترك بين صفحات الأدمن |
| akleto-roadmap.html | خارطة طريق تفاعلية للمشروع |
| context.md | ملف السياق هذا |

**ملاحظة:** لا يوجد حالياً ملفات akleto-firebase-manager.html أو akleto-firebase-setup.html بالـ repo — إذا احتجناها لازم ننشئها من جديد.

**بنية صفحات الأدمن (مهم لأي صفحة جديدة تنضاف):** كل صفحة أدمن جديدة (مثلاً الطلبات، المتاجر) لازم:
1. تعمل `<link rel="stylesheet" href="admin-style.css">` بدل تكرار الـ CSS
2. تستورد `db, auth, ...` من `./firebase-init.js` بدل تكرار Firebase config
3. تعمل auth gate بسيط: `onAuthStateChanged` يحوّل لـ `akleto-admin.html` لو ما في مستخدم مسجّل دخول
4. تكرر نفس الـ sidebar HTML (بحدد `active` على العنصر المناسب) — لسا مو مستخرج لملف مشترك، فلازم تنسخه يدوياً من أي صفحة أدمن موجودة
5. ⚠️ **`akleto-admin.html` فيه نسخة مكرّرة كاملة من نموذجي "إضافة وجبة" و"إضافة مكوّن"** (منطق مستقل عن `akleto-admin-meals.html`/`akleto-admin-ingredients.html`، مش استدعاء مشترك) — أي تغيير بمخطط بيانات المكونات أو الوجبات (حقول جديدة، تغيير أسماء حقول...) **لازم يتكرر يدوياً بثلاث أماكن**: الصفحة المستقلة + `akleto-admin.html`. نسيان مزامنة `akleto-admin.html` بيخلق تضارب بيانات صامت (نموذج بيكتب مخطط قديم بينما باقي النظام يتوقع الجديد).

## قرارات MVP
- 10-15 وجبة، 2-3 متاجر بعمّان
- ~20 مستخدم للاختبار الداخلي
- ✅ تطبيق السائق صار موجود (بُني بتاريخ لاحق — شوف قسم "الملفات" وآخر نقطة توقفنا)
- مستبعد مبدئياً: الدفع الإلكتروني، برنامج الولاء
- التطبيق: صفحات HTML خام تُنشر عبر GitHub Pages (مش PWA رسمي بعد)

## الأدوات (محدّثة)
- **البناء:** ملفات HTML/JS منفردة (بدون أي أداة No-Code) — **لا نستخدم FlutterFlow إطلاقاً**، هذا قرار نهائي
- **القاعدة الخلفية:** Firebase (Firestore, Auth, Storage)
- **الاستضافة الحالية:** GitHub Pages (مجاني، شغال الآن)
- **GoDaddy:** محجوز للدومين المخصص لاحقاً — مو مستخدم حالياً، رح نفعّله بمرحلة لاحقة بعد الـ MVP
- **VS Code + Live Server:** إلزامي لفتح ملفات Firebase محلياً (بروتوكول file:// ما بيشتغل)
- **Figma:** للتصميم
- ميزانية تسويق مستقبلية: 100-300 دينار/شهر (انستغرام، تيك توك، واتساب)

## آخر نقطة توقفنا (محدّثة)
1. ✅ **صفحة الطلبات بلوحة الأدمن جاهزة** — akleto-admin-orders.html: تعرض كل الطلبات القادمة من تطبيق الزبون **لحظياً** (onSnapshot، مش getDocs، فأي طلب جديد بيظهر فوراً بدون تحديث الصفحة)، فلاتر سريعة بالحالة، الضغط على طلب يفتح تفاصيله (الوجبات المطلوبة + قائمة المكونات المدمجة + السعر التقديري) مع إمكانية تغيير الحالة (pending → preparing → delivering → done / cancelled)
2. **حلقة الاستخدام الأساسية الكاملة صارت شغالة بالكامل تقنياً:** زبون يفتح akleto-customer.html ← يختار وجبات ← يتأكد الطلب ← يظهر فوراً بصفحة الطلبات بالأدمن ← الأدمن يحدّث الحالة
3. القائمة الجانبية بكل صفحات الأدمن الخمسة (admin, orders, meals, ingredients, customers) صارت مربوطة ببعض بروابط حقيقية بدون استثناء

4. ✅ **صفحة الطلبات صارت متجاوبة (responsive) بشكل كامل بين الموبايل والكمبيوتر**: أُضيفت حاوية `.orders-content-max` (max-width: 1400px, margin: auto) حول الفلاتر وقائمة البطاقات عشان ما تتمدد البطاقات بشكل مبالغ فيه على شاشات الكمبيوتر العريضة، مع breakpoint إضافي بين 601px-900px (تابلت) يفرض عمودين ثابتين بدل الاعتماد فقط على auto-fill. شبكة البطاقات على الموبايل (تحت 480px) تبقى عمود واحد كما كانت.

5. ✅ **إصلاح باغ حقيقي مؤكد بالصورة**: على الموبايل، عند فتح القائمة الجانبية (sidebar) كانت طبقة التعتيم الغامقة خلفها (`.sidebar-overlay`) ما بتظهر، فتضل الصفحة اللي تحتها ظاهرة وقابلة للتفاعل جنب القائمة. السبب: خاصية CSS `inset: 0` (المستخدمة بـ 4 عناصر: sidebar-overlay, modal-overlay, fullscreen-page, login-gate بملف admin-style.css + عنصر مشابه بـ akleto-customer.html) مش مدعومة على بعض متصفحات أندرويد الأقدم/الأوفر، فالعنصر ما ياخد حجم الشاشة كاملة. تم استبدالها بـ `top:0; left:0; right:0; bottom:0;` الصريحة بكل المواضع الخمسة — إصلاح بملف CSS مشترك واحد بينطبق تلقائياً على كل صفحات الأدمن.

6. ✅ **إصلاح مشكلة "الحجم كبير وغير مناسب" بصفحة الطلبات على الموبايل**: السبب المرجّح هو خاصية معروفة بمتصفحات أندرويد الأقدم اسمها **font boosting** — بتكبّر النص تلقائياً بشكل غير متوقع على الشاشات الضيقة بغض النظر عن أحجام الخط المحددة بالـ CSS. تم تعطيلها بإضافة `text-size-adjust: 100%` على `html` بكل صفحات الأدمن (admin-style.css) وتطبيق الزبون. بالإضافة: أضفت `overflow-x: hidden` على html/body لمنع أي تمدد أفقي غير مقصود، وأضفت رقم إصدار `?v=3` لرابط admin-style.css بكل صفحات الأدمن لضمان تحميل النسخة المحدثة دائماً بدل نسخة قديمة من كاش المتصفح.

7. 🗑️ **تم حذف ملف `akleto-admin-orders.html` نهائياً** (بناءً على طلب صريح مؤكَّد). ⚠️ رابط "الطلبات" لسا موجود بالقائمة الجانبية بكل صفحات الأدمن الباقية (`akleto-admin.html`, `akleto-admin-meals.html`, `akleto-admin-ingredients.html`, `akleto-admin-customers.html`) وبيأدي لصفحة 404 لأنه ما انحذف عمداً بناءً على طلب المستخدم. بيانات الطلبات نفسها ما زالت موجودة بـ Firestore (collection `orders`) — بس ما في واجهة حالياً لعرضها أو إدارتها.

8. ♻️ **تم إنشاء/استرجاع صفحة `akleto-admin-orders.html` من جديد** — بنفس آخر نسخة سليمة كانت موجودة قبل الحذف (بكل إصلاحات التجاوب مع الموبايل المتراكمة: شبكة متجاوبة، حاوية max-width، إصلاح font-boosting، كسر كاش CSS). رابط "الطلبات" بالقائمة الجانبية شغال الآن بشكل صحيح بكل الصفحات.

9. 🆕 **إعادة بناء `akleto-admin-orders.html` من الصفر بتصميم جديد كلياً** (بناءً على طلب صريح، مش استرجاع من النسخة القديمة):
   - **قائمة الطلبات:** عمود واحد افتراضياً (mobile-first) يتحول لشبكة أعمدة (auto-fill minmax 320px) على الشاشات ≥700px، محاطة بحاوية max-width:1100px.
   - **بطاقة الطلب:** تصميم جديد بخط ملوّن على الحافة (border-inline-start) بلون الحالة بدل بادج تقليدي، لعرض أسرع وأخف.
   - **تفاصيل الطلب:** بدل المودال التقليدي بمنتصف الشاشة، صار **Bottom Sheet** (ورقة تنزلق من الأسفل) على الموبايل — نفس نمط "bottom sheet لتفاصيل الوجبة" المستخدم بتطبيق الزبون، وعلى الديسكتوب (≥640px) تتحول تلقائياً لبطاقة بمنتصف الشاشة.
   - **تحديث الحالة:** زر "نقل للحالة التالية" ديناميكي (pending→preparing→delivering→done) + زر إلغاء، بدل select يدوي.
   - **تحديث لحظي:** onSnapshot بدل getDocs، فأي طلب جديد يظهر أوتوماتيك بدون Refresh.
   - يعتمد على الشل المشترك (sidebar/topbar/admin-style.css) لضمان الاتساق مع باقي صفحات الأدمن.

10. 🆕 **بناء تطبيق السائق (akleto-driver.html) من الصفر:**
   - **قرارات مؤكَّدة من مؤيد:** (أ) تسجيل السائق بسيط بالاسم+الهاتف بدون Auth كامل (نفس نمط تطبيق الزبون تماماً — بينشئ سجل جديد بكل تسجيل، وبيحفظ الـ id بالـ localStorage). (ب) السائق ياخذ الطلبات بنفسه (self-claim) — يشوف كل الطلبات "الجاهزة للاستلام" ويضغط زر ليستلم الطلب، **مافي تعيين يدوي من الأدمن حالياً**.
   - **تعديل مهم بدورة حياة الطلب:** أُضيفت حالة جديدة `ready` (جاهز للاستلام) بين `preparing` و `delivering`. الأدمن ينقل الطلب من `preparing` إلى `ready` لما يخلص تجهيزه، وعندها بيظهر تلقائياً بتطبيق السائق. لما سائق يضغط "استلام الطلب"، بينتقل تلقائياً لـ `delivering` مع إرفاق `driver_id, driver_name, driver_phone, picked_up_at`. تم تحديث `akleto-admin-orders.html` (STATUS map + NEXT_STATUS + فلتر جديد + عرض معلومات السائق بتذكرة الطلب) و `admin-style.css` (لون بنفسجي `--purple` جديد لهاي الحالة).
   - **استلام الطلب محمي بـ Firestore Transaction** (`runTransaction`، أُضيفت حديثاً لـ `firebase-init.js`): بيتأكد إن الطلب لسا `ready` قبل ما يحوّله لسائق، فما ينحصل تعارض لو سائقين ضغطوا بنفس اللحظة.
   - **Collection جديد فعلياً مستخدم:** `drivers` (name, phone, is_active, deliveries_count, created_at) — كان موجود بس فاضي بالـ context القديم.
   - **⚠️ نقطة ناقصة معروفة (لازم ننتبه لها قبل الإطلاق الفعلي):** ما في حالياً أي حقل عنوان/موقع للزبون بتطبيق الزبون ولا بالطلب — السائق حالياً بس شايف اسم ورقم هاتف الزبون بتفاصيل الطلب، ولازم يتصل فيه يستفسر عن العنوان. هاي مقبولة مؤقتاً لأن عدد المستخدمين التجريبيين قليل ومعروفين، بس لازم تنحل قبل أي إطلاق أوسع.
   - **لا يوجد بعد** صفحة أدمن لإدارة/تعيين السائقين يدوياً (رابط "السائقين" بالقائمة الجانبية لسا placeholder غير فعّال) — مو مطلوب حالياً لأن السائق self-claim.

11. ✅ **صفحة `akleto-admin-drivers.html` جديدة بلوحة الأدمن — إدارة السائقين:**
   - نفس نمط `akleto-admin-customers.html` تماماً (getDocs، لا live listener لأنها لسا قائمة تُدار يدوياً بدون حاجة لتحديث لحظي).
   - تعرض كل سائق مسجّل بـ collection `drivers`: الاسم، الهاتف، عدد التوصيلات (`deliveries_count`)، تاريخ الانضمام (`created_at`)، مرتبة الأحدث تسجيلاً أولاً.
   - زر حظر/رفع حظر لكل سائق — بيعدّل الحقل `is_active` (السائق المحظور `is_active:false`). ⚠️ **ملاحظة مهمة:** تطبيق السائق (`akleto-driver.html`) حالياً **ما بيتحقق من `is_active` عند تسجيل الدخول أو استلام الطلبات** — يعني الحظر بلوحة الأدمن ما له أثر فعلي على تطبيق السائق حالياً. لازم إضافة هذا التحقق بـ `akleto-driver.html` لاحقاً عشان الحظر يصير فعّال فعلياً (نفس الملاحظة تنطبق أساساً على حظر الزبائن بـ `akleto-admin-customers.html`).
   - رابط "السائقين" بالقائمة الجانبية صار فعّال بكل صفحات الأدمن الست (admin, orders, meals, ingredients, customers, drivers) بدل الـ placeholder، وزر "إدارة الكل" ببطاقة السائقين بصفحة النظرة العامة (`akleto-admin.html`) صار يوجّه لهاي الصفحة.
   - **ملاحظة:** `deliveries_count` بتبقى دايماً 0 حالياً لأنه ما في أي منطق بالكود يزيدها لما طلب ينتقل لحالة `done` — لسا نقطة ناقصة معروفة لازم تُحل لاحقاً (يفترض تحديثها بـ `akleto-admin-orders.html` أو بـ transaction عند تسليم الطلب).

12. 🆕 **قرار معماري مؤكَّد: حساب المتجر مش تطبيق منفصل — هو نفس تطبيق الزبون (`akleto-customer.html`) مع صلاحية إضافية:**
   - **الآلية:** الأدمن يحوّل حساب زبون موجود (بصفحة `akleto-admin-customers.html`) لحساب متجر عبر زر "تحويل لمتجر" — بيطلب اسم المتجر + العنوان + PIN، وبينشئ سجل بـ collection `stores` (نفس معرف الزبون `id` — ربط 1:1 مباشر) بجانب حقلين جدد على وثيقة الزبون نفسها بـ `customers`: `is_store: true` و `store_pin` (كلمة مرور بسيطة يحددها الأدمن).
   - **schema/stores/{customerId}:** name, phone, address, is_active (نشط/غير نشط — بيتحكم فيه المتجر لاحقاً من تطبيقه), rating (فاضي حالياً), created_at. هاي البنية متوافقة مع الكود الموجود مسبقاً بصفحة `akleto-admin.html` (renderStores) اللي كان يتوقع نفس الحقول من قبل — ما احتجنا نعدله.
   - **قرارات مؤكَّدة من مؤيد:**
     (أ) دخول المتجر: **رقم هاتف + PIN** (أضمن من نمط الزبون العادي localStorage-only) — لسا لازم نبني شاشة "دخول كمتجر" بتطبيق الزبون.
     (ب) حساب المتجر بيشوف تبويبات المتجر **بالإضافة لتبويبات التسوق العادية** (يقدر يتسوق كزبون كمان لو حاب).
   - **افتراض من Claude لسا بانتظار تأكيد نهائي:** استلام الطلبات من المتجر بنفس نمط self-claim متل السائق (كل الطلبات الجديدة تظهر لكل المتاجر النشطة، أول متجر يضغط "استلمت التجهيز" ياخذه) — بديل عن تعيين يدوي، لتفادي الحاجة لحقل عنوان/موقع زبون غير موجود لسا لحساب "أقرب متجر".
   - **✅ تم إنجازه:** الخطوة 1 فقط — تحويل حساب زبون لمتجر من لوحة الأدمن (`akleto-admin-customers.html` + إضافة `setDoc` لـ `firebase-init.js`).
   - **⏳ الخطوات المتبقية (بالترتيب):**
     1. إضافة `store_id` على وثيقة الطلب + Transaction لاستلام المتجر للطلب (شبيه بمنطق استلام السائق)
     2. شاشة "دخول كمتجر" بتطبيق الزبون (هاتف + PIN)
     3. تبويب "الطلبات" الخاص بالمتجر (الطلبات الجديدة + تبديل متاح/غير متاح)
     4. تبويب "منتجاتي" (المتجر يأشر أي مكونات من `ingredients` متوفرة عنده)
     5. تبويب "بوابة المتجر" (تقارير مبيعات/أرباح مبنية على الطلبات المرتبطة بـ store_id — آخر شي لأنها تحتاج بيانات متراكمة)

12. 🔄 **تعديل جوهري على قرار "حساب المتجر" (يلغي نقطة 12 القديمة أعلاه بخصوص تحويل زبون موجود):**
   - **القرار النهائي المعتمد من مؤيد:** المتجر مش تحويل حساب زبون موجود — هو **حساب مستقل بالكامل** يُنشئه الأدمن من الصفر بصفحة جديدة `akleto-admin-stores.html` (وليس من صفحة الزبائن). ✅ تراجعنا فعلياً عن زر "تحويل لمتجر" اللي كان انضاف بصفحة الزبائن وحذفناه بالكامل (رجّعنا `akleto-admin-customers.html` لنسختها الأصلية).
   - **آلية العمل الكاملة المبنية الآن:**
     1. **الأدمن** ينشئ متجر جديد من `akleto-admin-stores.html` (اسم، هاتف، عنوان/موقع) — **رمز PIN من 4 أرقام يتولّد تلقائياً وبشكل فريد** (مو الأدمن يكتبه)، ويُخزّن بوثيقة `stores/{id}` (name, phone, address, pin, is_active, rating, created_at).
     2. الصفحة تعرض لكل متجر: الاسم، العنوان، الهاتف، رمز PIN (مع زر نسخ)، عدد الموظفين المسجّلين، قائمة أسماء/أرقام الموظفين (expandable)، وزر تفعيل/تعطيل المتجر.
     3. **تطبيق الزبون** (`akleto-customer.html`) صار فيه زر "🏪 الدخول كتاجر" بشاشة الترحيب — بيفتح شاشة دخول منفصلة (اسم + هاتف + PIN).
     4. عند الدخول: نبحث عن متجر بنفس الـ PIN (`where('pin','==',...)`) — لو موجود ونشط، نبحث/ننشئ سجل موظف بـ collection جديد `store_employees` (store_id, name, phone, created_at) بنفس رقم الهاتف المُدخل (لدعم أكثر من موظف لنفس المتجر، كل وحد بحسابه الخاص لكن نفس الـ PIN).
     5. الجلسة تُحفظ محلياً (localStorage) بشكل منفصل تماماً عن جلسة الزبون العادية — يعني نفس الشخص يقدر يكون مسجّل كزبون وكموظف متجر بنفس الوقت على نفس الجهاز (بحسب قرار مؤيد: حساب المتجر بيقدر يتسوّق كزبون كمان).
     6. بعد الدخول كمتجر، يظهر تبويب جديد "🏪 متجري" بالتنقل السفلي (bottom nav) بجانب تبويبات الزبون العادية (الرئيسية/سلتي/حسابي).
   - **⏳ شاشة "متجري" حالياً Placeholder فقط (قشرة/Shell بدون منطق فعلي):** فيها 3 بطاقات "قريباً" (طلبات المتجر، منتجاتي، بوابة المتجر) + زر خروج من حساب المتجر. المنطق الفعلي لهاي الشاشات الثلاث لسا ما انبنى — بينتظر تأكيد آلية ربط الطلبات بالمتجر (self-claim مقترح من Claude، لسا ما تأكد نهائياً من مؤيد).
   - **الملفات المتأثرة:** `akleto-admin-stores.html` (جديدة)، `akleto-customer.html` (شاشة دخول متجر + شاشة متجري + تبويب bottom-nav)، رابط "المتاجر الشريكة" صار فعّال بكل صفحات الأدمن السبعة بدل placeholder.

13. 🆕 **قائمة جانبية (drawer) بتطبيق الزبون** — استبدلت زر تسجيل الخروج المنفرد اللي كان بشاشة حسابي. زر ☰ بأعلى شاشتي الرئيسية وحسابي بيفتح قائمة منزلقة من اليمين فيها: الرئيسية، سلتي، طلباتي وحسابي، الدخول كتاجر (أو "🏪 اسم المتجر" لو مسجّل دخول كموظف متجر)، وتسجيل الخروج بالأسفل بلون مميز.

14. 🔄 **تصحيح قرار سابق: وضع المتجر صار حصري بالكامل — لا يقدر يتسوق كزبون.**
   - كان قرار سابق إنه حساب المتجر يقدر يتسوق كزبون بجانب تبويباته، وهذا الآن **مُلغى**: أي مستخدم مسجّل دخول كموظف متجر (`storeId` موجود) بيشوف شاشة "متجري" فقط، وما بيقدر يوصل لشاشات التسوق (الرئيسية/سلتي/حسابي) إطلاقاً — حتى لو نفس الجهاز عنده حساب زبون محفوظ محلياً.
   - `switchScreen()` صار فيه حارس (`if (storeId && name !== 'store') return;`) يمنع أي تنقل لشاشات التسوق أثناء وضع المتجر.
   - `bottomNav` ينخفي بالكامل بوضع المتجر (شاشة "متجري" ما فيها شريط تنقل سفلي، بس زر "خروج من حساب المتجر" جواها).
   - حُذف تبويب `nav-store` من شريط التنقل السفلي لأنه صار غير مستخدم (وضع المتجر ما عاد يحتاج تبويب توأم مع شاشات التسوق).
   - عند استرجاع الجلسة من localStorage عند فتح الصفحة: لو في جلسة متجر محفوظة، بتاخذ أولوية مطلقة وما بيتم عرض واجهة التسوق العادية إطلاقاً بغض النظر عن وجود جلسة زبون محفوظة بنفس الجهاز.

15. 🆕 **إعادة تصميم نظام المقادير من الصفر: "نوع القياس + العبوة الحقيقية + وحدة الوصفة المطبخية"** (بديل نهائي — تجاوز محاولتين سابقتين أُلغيتا: أولاً حقل `min_purchase_qty` بسيط، وثانياً تصنيف الوحدات لـ"مغلّفة/سائبة". القرار النهائي بعد نقاش مباشر مع مؤيد لأن الحلول السابقة عقّدت شاشة إدخال المكوّن أو ما عالجت حالة "رشة ملح من كيس 250غ" بشكل صحيح):

   **المشكلة الأصلية:** بعض المكونات تُستخدم بالوصفة بكمية صغيرة جداً (رشة ملح) بينما أصغر وحدة مبيعة بالسوق أكبر بكثير (كيس ملح 250غ). محتاجين ندخل الوجبة بوحدة مطبخية طبيعية (رشة) وفي نفس الوقت نعرف نحسب كم عبوة سوق فعلياً لازم نشتري.

   **الحل المعتمد — 3 مفاهيم منفصلة:**
   - **نوع القياس** (`measure_type`): `weight` (وزن) / `volume` (حجم) / `count` (عدد) — خاصية على كل مكوّن بشاشة المكونات.
   - **العبوة الحقيقية** (كيف يُشترى المكوّن فعلياً بالسوق): `package_label` (اسم حر: "كيس"، "علبة"، "كرتونة"، "حبة") + `package_qty` (الكمية بوحدة الأساس — غرام لو وزن، مل لو حجم، عدد لو عدد) + `price_per_package` (سعر العبوة الكاملة).
   - **وحدة الوصفة** (كيف تُدخل الكمية بشاشة الوجبة): قائمة وحدات مطبخية جاهزة حسب نوع القياس (رشة/معلقة صغيرة/معلقة كبيرة/غرام/كيلو للوزن، معلقة صغيرة/معلقة كبيرة/مل/لتر/كأس صغير/كأس كبير للحجم، حبة للعدد) — كل وحدة إلها عامل تحويل ثابت لوحدة الأساس.

   **`firebase-init.js` — مصدر الحقيقة المشترك الجديد:**
   - `export const MEASURE_TYPES` = كائن يحدد لكل نوع قياس: `label`, `baseUnitLabel`, و`recipeUnits` (خريطة اسم الوحدة ← عامل التحويل لوحدة الأساس). مثال: `weight.recipeUnits = {'رشة':1, 'معلقة صغيرة':5, 'معلقة كبيرة':15, 'غرام':1, 'كيلو':1000}`.
   - `export function convertToBase(measureType, recipeUnit, qty)` — يحوّل كمية بوحدة وصفة لكمية بوحدة الأساس. **مستوردة بكل من `akleto-admin-ingredients.html`, `akleto-admin-meals.html`, `akleto-customer.html`, و`akleto-admin.html`** لضمان عدم اختلاف جدول التحويل بين الصفحات.

   **مخطط بيانات `ingredients` الجديد** (استُبدل بالكامل، القديم `unit`/`price_per_unit` أُزيل): `measure_type`, `package_label`, `package_qty`, `price_per_package`.

   **مخطط بيانات `meal_ingredients` الجديد** (استُبدل `quantity_per_person` بحقلين): `recipe_unit` (اسم وحدة الوصفة المختارة، مثل "رشة") و `recipe_qty` (الكمية بهاي الوحدة، مثل 1).

   **شاشة المكوّن (`akleto-admin-ingredients.html` و نسخة `akleto-admin.html` المطابقة):** نموذج إضافة مكوّن صار: الاسم → نوع القياس (select) → التصنيف → اسم العبوة (نص حر) → كمية العبوة (label بتتغيّر تلقائياً حسب نوع القياس: غرام/مل/حبة) → سعر العبوة كاملة → صورة.

   **شاشة الوجبة (`akleto-admin-meals.html` و نسخة `akleto-admin.html` المطابقة):** كل صف مكوّن فيه: حقل بحث اسم المكوّن (datalist كالسابق) → حقل كمية رقمي → **select وحدة وصفة** يتحدّث تلقائياً حسب نوع قياس المكوّن المختار (بدون إعادة رسم كامل الصف — بس تحديث الـ select بالـ DOM مباشرة عشان ما نخسر تركيز حقل الكتابة). مثال إدخال: "ملح" + "1" + "رشة".

   **`akleto-customer.html` — خوارزمية الدمج الجديدة (`computeMergedIngredients`):**
   1. لكل وجبة مختارة، `getMealIngredients` تجيب `recipe_unit`+`recipe_qty` من الربط، وتحوّلها لـ`baseQtyPerPerson` عبر `convertToBase`.
   2. تُجمع `baseQtyPerPerson × عدد الأشخاص` لكل مكوّن عبر كل الوجبات بالطلب (بوحدة الأساس: غرام/مل/حبة).
   3. `purchaseQty = Math.ceil(baseQty_الإجمالي / package_qty)` — عدد العبوات الكاملة الواجب شراؤها (ما يمكن شراء نص عبوة).
   4. شاشة السلة تعرض `purchaseQty × package_label` (مثال: "1 كيس")، ولو الكمية المشتراة أكبر من المطلوب فعلياً تظهر ملاحظة توضيحية: "الوصفة تحتاج تقريباً X غرام — رح تشتري Y كيس (Z غرام)".
   5. **السعر التقديري = `purchaseQty × price_per_package`** (سعر ما يُشترى فعلياً، مش كسر نظري).

   **بنية الطلب بـ Firestore:** كل عنصر بـ `merged_ingredients` صار: `ingredient_id`, `name`, `package_label`, `purchase_quantity` (عدد العبوات)، `base_qty_needed` (الكمية الفعلية اللي تحتاجها الوصفة بوحدة الأساس، للمرجعية)، `base_unit_label`.

   **`akleto-admin-orders.html`:** تفاصيل الطلب تعرض `purchase_quantity × package_label` كرقم رئيسي (هذا يلي لازم يُشترى)، مع ملاحظة صغيرة تحته توضح الكمية الفعلية اللي تحتاجها الوصفة — مفيدة للمتجر عند التسوق الفعلي.

   **⚠️ ملاحظة حاسمة — بدء نظافة:** مؤيد أكّد إنه نبلش نظافة تماماً (لا يوجد بيانات إنتاجية حالياً تستاهل ترحيل). **أي مكوّن أو وجبة قديمة بقاعدة البيانات من قبل هاد التغيير أصبحت غير متوافقة مع الشاشات الجديدة ولازم تُدخل من جديد بالكامل** (نوع القياس، العبوة، ووحدة كل مقدار بكل وجبة) — الحقول القديمة (`unit`, `price_per_unit`, `quantity_per_person`) ما عاد أي شاشة تقرأها أو تكتبها.

16. 🆕 **تعديل المكونات بالضغط عليها (`akleto-admin-ingredients.html`):** كل صف بقائمة المكونات صار قابل للضغط (`cursor:pointer`) ويفتح نفس مودال الإضافة لكن بوضع تعديل (`currentEditingIngredientId`) — العنوان يصير "✏️ تعديل: [الاسم]"، الحقول تتعبى من بيانات المكوّن الحالي، وزر "🗑️ حذف المكوّن" يظهر بأسفل المودال. الحفظ يستخدم `updateDoc` بدل `addDoc`. زر الحذف "✕" على الصف نفسه لسا شغال بشكل منفصل (`event.stopPropagation()` يمنعه من فتح مودال التعديل بالغلط).

17. 🆕 **سلة تطبيق الزبون صارت قابلة للتعديل يدوياً (`akleto-customer.html`):**
   - كل سطر بقائمة "المكونات المدمجة" بالسلة صار يعرض: سعر العبوة الواحدة، stepper (−/+) لزيادة/إنقاص عدد العبوات، السعر الإجمالي للسطر، وزر ✕ لحذف المكوّن بالكامل من السلة.
   - زر جديد "➕ ضيف مكوّن للشراء" بأسفل القائمة يفتح شيت (`ingredientPickerSheet`) فيه بحث + قائمة كل المكونات المتوفرة (اسم + سعر العبوة) — الضغط "أضف +" يحط المكوّن بالسلة حتى لو مش جزء من أي وصفة مختارة.
   - **حالة جديدة `cartOverrides`** (كائن: `ingredientId → عدد عبوات نهائي`): يطبّق فوق نتيجة الدمج التلقائي من الوصفات (`computeMergedIngredients`). لو المفتاح موجود بـ`cartOverrides` بيتجاوز الرقم المحسوب تلقائياً؛ لو قيمته 0 يختفي السطر (يعني الزبون حذفه يدوياً حتى لو الوصفة بتحتاجه).
   - **دالة `getFinalCartIngredients()`** هي المصدر الوحيد لقائمة السلة الفعلية الآن (تدمج المحسوب تلقائياً + التعديلات اليدوية + المكونات المضافة يدوياً اللي مو من أي وصفة) — تُستخدم بكل من عرض السلة (`renderCartScreen`) وإرسال الطلب (`placeOrder`)، بدل الاعتماد المباشر على `computeMergedIngredients()`.
   - `cartOverrides` تتصفّر تلقائياً بعد إرسال الطلب بنجاح، وكمان لو الزبون حذف كل الوجبات من السلة (رجعت فاضية).
   - **ملاحظة "الوصفة تحتاج تقريباً..."** (تظهر لما التقريب لعبوة كاملة أكبر من الحاجة الفعلية) بطّلت تظهر خطأً على المكونات المُضافة يدوياً (اللي أصلاً `baseQty = 0` لأنها مو من وصفة).

18. 🆕 **إدارة توفر المنتجات بالمتجر (\"منتجاتي\") + توزيع الطلبات تلقائياً على أقرب متجر متوفر فيه كل المكونات:**

   **الفكرة:** كل متجر شريك صار يحدد بنفسه (من تطبيق الزبون بوضع المتجر) أي المكونات متوفرة عنده فعلياً وبأي سعر، وعملية توزيع الطلب على متجر تصير تلقائية بالكامل بناءً على: (أ) توفر **كل** مكونات الطلب عند المتجر، و(ب) الأقرب جغرافياً للزبون — **الشرطان لازم يتحققا معاً**، الأولوية ليست فقط للمسافة.

   **مجموعة Firestore جديدة: `store_ingredients`** — معرّف الوثيقة `{storeId}_{ingredientId}` (يمنع تكرار)، الحقول: `store_id`, `ingredient_id`, `is_available` (bool), `price_per_package` (سعر المتجر الخاص، قد يختلف عن سعر المكوّن العام), `updated_at`.
   - **عدم وجود وثيقة** لمكوّن معيّن عند متجر = المكوّن **\"جديد\"** (لم يراجعه المتجر بعد).
   - وجود وثيقة بـ `is_available: true` = **\"متوفر\"**. وجود وثيقة بـ `is_available: false` = **\"غير متوفر\"**.

   **`akleto-customer.html` — شاشة \"منتجاتي\" بوضع المتجر (`storeProductsScreen`):**
   - بطاقة \"🧺 منتجاتي\" بشاشة المتجر الرئيسية صارت قابلة للضغط وتفتح شاشة كاملة (`openStoreProducts()` / `closeStoreProducts()`).
   - 3 تبويبات بعدّاد لكل واحد: 🆕 جديدة / ✅ متوفرة / 🚫 غير متوفرة (`storeProductsTab`, `switchStoreProductsTab()`).
   - شبكة بطاقات كبيرة بصور واضحة (`product-grid` / `product-tile`، عمودين، صورة 140px) — تُبنى من `ingredientsCache` (تحمّل عند فتح الشاشة لو غير محمّلة) مقسّمة لـ3 مجموعات عبر `bucketStoreProducts()` بمقارنة كل مكوّن مع `storeIngredientsCache` (سجلات `store_ingredients` الخاصة بهاد المتجر فقط، تُحمّل بـ`loadStoreProductsData()`).
   - الضغط على أي بطاقة يفتح شيت تعديل واحد مشترك (`storeProductSheet` / `openStoreProductSheet()`) بيتغيّر نص وأزراره حسب حالة المكوّن الحالية:
     - **جديد:** حقل سعر (افتراضي = سعر المكوّن العام) + زرّين \"✅ متوفر عندي بهاي السعر\" / \"🚫 غير متوفر عندي\".
     - **متوفر:** زرّين \"💾 حفظ السعر\" (تعديل السعر فقط) / \"🚫 وضعه غير متوفر\".
     - **غير متوفر:** زرّين \"✅ متوفر الآن بهاي السعر\" / \"🚫 يبقى غير متوفر\".
   - الحفظ عبر `upsertStoreIngredient()` يستخدم `setDoc(..., {merge:true})` بمعرّف `{storeId}_{ingredientId}` — يحدّث `storeIngredientsCache` محلياً فوراً بدون إعادة تحميل، وينقل البطاقة تلقائياً للتبويب الصحيح.
   - **مهم:** `setDoc` صار مستورد بـ`akleto-customer.html` (كان غير مستخدم بهاد الملف سابقاً — مُصدَّر أصلاً من `firebase-init.js`).

   **`akleto-admin-stores.html` — إحداثيات المتجر:** نموذج إضافة متجر صار فيه حقلي `lat`/`lng` رقميين + زر \"📍 استخدم موقعي الحالي\" (`navigator.geolocation`, يفترض إنه الأدمن واقف فعلياً بالمتجر وقت الإدخال) — تُحفظ كـ`lat`/`lng` بوثيقة `stores`. بطاقة المتجر بلوحة الأدمن تعرض تحذير أحمر واضح لو الإحداثيات ناقصة (\"⚠️ ما رح يشارك بحساب أقرب متجر\") لأن أي متجر بدون إحداثيات ما ينضم لحساب الأقرب إطلاقاً (يُستبعد عملياً من الترشيح لو في متاجر ثانية بإحداثيات).

   **`akleto-customer.html` — منطق توزيع الطلب (بدون أي حقل عنوان محفوظ للزبون — تحديد موقع حي وقت الطلب فقط):**
   - `getCustomerCoords()`: يطلب موقع الزبون الحالي عبر `navigator.geolocation` (مرة وحدة، يُخزَّن بـ`customerCoords` لتفادي تكرار طلب الصلاحية بنفس الجلسة)؛ لو رفض الزبون الصلاحية أو غير مدعوم، يرجع `null` والنظام يكمل بدون حساب مسافة (يختار أول متجر مؤهّل بدل الأقرب).
   - `loadStoresAndAvailability()`: يجيب كل المتاجر النشطة + كل سجلات `store_ingredients` بـ `is_available == true` بكل المتاجر (استعلام واحد لكل، بدون كاش دائم — يُعاد التحميل بكل مرة عشان البيانات تضل حديثة لحظة الطلب).
   - `findBestStoreForIngredients(ingredientIds)`: يبني خريطة متجر ← مجموعة مكونات متوفرة، يفلتر المتاجر اللي عندها **كل** مكونات السلة (`.every()`)، ولو أكتر من مرشح ومتوفر موقع الزبون يحسب المسافة (`haversineKm`) ويختار الأقرب (المتاجر بدون `lat`/`lng` تُحسب مسافتها `Infinity` فتُستبعد عملياً عند وجود بدائل). يرجع أيضاً `priceMap` (سعر كل مكوّن عند هداك المتجر تحديداً) لاستخدامه بالتسعير الفعلي.
   - **شاشة السلة:** بعد كل `renderCartScreen()` يُستدعى `updateStoreAssignmentNote()` تلقائياً — يعرض بصندوق `#storeAssignmentNote` إما \"🏪 رح تشتري من: [اسم المتجر]\" أو تحذير واضح \"⚠️ ما في متجر يقدر يجهز كل مكونات طلبك حالياً\" لو ما في مرشح مؤهّل.
   - **`placeOrder()`:** يعيد استدعاء `findBestStoreForIngredients()` مباشرة قبل الإرسال (تفادي بيانات قديمة) — لو ما في متجر مؤهّل يوقف الإرسال بتوست تحذيري ولا يُنشئ الطلب إطلاقاً. لو في متجر مؤهّل: **السعر الفعلي بكل عنصر بـ`merged_ingredients` يصير سعر ذاك المتجر تحديداً** (`priceMap`) لا السعر العام للمكوّن لو مختلف، ويُحفظ `store_id` و`store_name` بوثيقة الطلب. شاشة النجاح تعرض اسم المتجر المختار بالنص.

   **`akleto-admin-orders.html`:** تفاصيل الطلب وبطاقته بالقائمة صارت تعرض اسم المتجر (`store_name`) لو موجود.

   **`akleto-driver.html`:** بطاقات الطلبات المتاحة والتوصيلات النشطة + ورقة تفاصيل الطلب صارت تعرض اسم المتجر كـ\"مكان الاستلام\" — مفيد جداً الآن إنه في أكتر من متجر فعلياً (سابقاً ما كان في أي إشارة لمكان استلام لأنه افتراضياً كان محل واحد).

   **⚠️ فجوات معروفة متبقية:**
   - لسا ما في حقل عنوان محفوظ للزبون (نقطة 10 بالتاريخ القديم) — حساب \"أقرب متجر\" حالياً يعتمد كلياً على صلاحية الموقع الحيّة وقت فتح السلة/الطلب، مش عنوان مسجّل. لو الزبون رفض صلاحية الموقع، التوزيع يتجاهل المسافة تماماً ويختار أول متجر مؤهّل بس (بدون أي أولوية جغرافية).
   - لازم كل المتاجر الموجودة حالياً تُدخل إحداثياتها يدوياً من لوحة الأدمن — ✅ صار ممكن مباشرة الآن (شوف نقطة 19 تحت: تعديل بيانات المتجر بالضغط على بطاقته).
   - كل المتاجر تبلش بدون أي سجل بـ`store_ingredients` (كل المكونات \"جديدة\" افتراضياً) — لازم كل متجر يراجع تبويب \"🆕 جديدة\" بشاشة \"منتجاتي\" ويحدد التوفر والسعر لكل مكوّن قبل ما يقدر يستقبل أي طلب فعلياً (وإلا ما رح يظهر كمرشح إطلاقاً لأي طلب).


20. 🆕 **إدارة "الأوزان والمقاييس" من لوحة الأدمن — صار نظام `MEASURE_TYPES` مربوط بـ Firestore بدل Object ثابت بالكود:**

   **المشكلة:** نظام وحدات الوصفة (نقطة 15 أعلاه — رشة/معلقة/كأس...) كان معرّف كـ`const MEASURE_TYPES` ثابت جوا `firebase-init.js`، فأي إضافة وحدة جديدة (متل "كوب" أو "نص كوب") أو تعديل عامل تحويل كانت تحتاج تعديل كود ونشر جديد.

   **الحل — مجموعة Firestore جديدة `measure_config`:** 3 وثائق ثابتة بمعرّف `weight` / `volume` / `count`، كل وحدة فيها: `label` (اسم نوع القياس)، `base_unit_label` (اسم وحدة الأساس)، `units` (array مرتب من `{name, factor}` — كل عنصر وحدة وصفة وعامل تحويلها لوحدة الأساس).

   **`firebase-init.js`:**
   - `export const DEFAULT_MEASURE_TYPES` = نفس القيم القديمة الثابتة، تُستخدم فقط للبذر التلقائي أول مرة.
   - `export let MEASURE_TYPES` (صار `let` مو `const`) — يبلش بقيم `DEFAULT_MEASURE_TYPES` كـ fallback فوري، وتُستبدل بالكامل بعد نجاح `loadMeasureTypes()`.
   - `export async function loadMeasureTypes()` — يجيب `measure_config`؛ لو فاضية (أول مرة) يبذرها بـ`writeBatch` من `DEFAULT_MEASURE_TYPES`؛ غير هيك يبني `MEASURE_TYPES` من بيانات Firestore (تحويل `units` array لـObject map `recipeUnits` بالترتيب نفسه). **بفضل الـ ES Modules live bindings، إعادة تعيين `MEASURE_TYPES` جوا `firebase-init.js` تنعكس تلقائياً بكل صفحة استوردتها بدون أي كود إضافي.**
   - `convertToBase()` ما تغيّر منطقها، بس صارت تقرأ من `MEASURE_TYPES` المحدّثة ديناميكياً.

   **كل صفحة تستورد `MEASURE_TYPES` صار لازم تستدعي `await loadMeasureTypes()` قبل أي استخدام لها** (عشان تنعكس تعديلات الأدمن): تمت الإضافة بـ`akleto-admin-ingredients.html` (قبل `loadIngredients()`)، `akleto-admin-meals.html` (قبل `loadIngredients()`+`loadMeals()`)، `akleto-admin.html` (أول سطر بـ`loadDashboard()`)، و`akleto-customer.html` (ضمن `Promise.all` بـ`loadInitialData()`).

   **صفحة جديدة `akleto-admin-units.html` (تبويب "⚖️ الأوزان والمقاييس" بالسايدبار — أُضيف لكل صفحات الأدمن):**
   - 3 بطاقات (وزن/حجم/عدد)، كل وحدة صف قابل للضغط يفتح مودال تعديل (اسم + عامل التحويل)، مع زر "🗑️ حذف" (مُعطّل لو هاي آخر وحدة متبقية بنوع القياس — لازم يبقى وحدة عالأقل).
   - زر "➕ إضافة وحدة جديدة" بكل بطاقة يفتح نفس المودال بوضع إضافة.
   - زر "✏️ تعديل الاسم/وحدة الأساس" بأعلى كل بطاقة يفتح مودال منفصل لتعديل `label`/`base_unit_label` (تسمية فقط، ما يأثر على آلية الحساب).
   - **تحذير أمان قبل الحذف/إعادة التسمية:** بيستعلم `meal_ingredients` عن عدد المقادير المستخدم فيها اسم الوحدة تحديداً (`where('recipe_unit','==', name)`) ويعرض تحذير واضح بعدد الوصفات المتأثرة قبل التأكيد — لأنه تغيير/حذف اسم وحدة مستخدمة بوصفات موجودة بيخلي `convertToBase()` ما يلاقي عامل التحويل ويرجع 1 افتراضياً (خطأ صامت بحساب الكمية الحقيقية المطلوب شراؤها).
   - الحفظ فوري (زر واحد لكل تغيير، مافي "حفظ الكل") — نفس نمط الشاشات السابقة (منتجاتي، المكونات).

   **⚠️ نطاق هذا التغيير:** بيغطي بس **وحدات الوصفة** (رشة/معلقة/كوب...) جوا الأنواع الثلاثة الموجودة (وزن/حجم/عدد). إضافة نوع قياس رابع كلياً (غير وزن/حجم/عدد) *مو* مدعوم من هاي الشاشة — هيك تغيير بده يلمس أيضاً `<select>` نوع القياس بشاشة إضافة مكوّن (`akleto-admin-ingredients.html` و`akleto-admin.html`) اللي لسا فيها 3 `<option>` مكتوبين يدوياً بالكود.


1. **اختبار حلقة استلام وتوصيل الطلب فعلياً من جهاز حقيقي** (أهم خطوة قادمة الآن) — جرّب: أدمن ينقل طلب لـ ready ← يفتح على جهاز ثاني تطبيق السائق ← يضغط استلام ← يتحول delivering ← يضغط تم التوصيل ← يتأكد إنه done بلوحة الأدمن
2. حل نقطة العنوان الناقصة (نقطة 10 بالتاريخ + الفجوة المذكورة بنقطة 18) — إضافة حقل عنوان محفوظ للزبون كبديل/تكملة للموقع الحي وقت الطلب
3. ✅ ~~صفحة "المتاجر الشريكة" — إضافة/تعديل متجر + ربط الطلبات بمتجر~~ — تمت (شوف نقطة 18: التوزيع صار تلقائي بالكامل حسب التوفر + القرب)
4. ✅ ~~صفحة أدمن لإدارة السائقين~~ — تمت (شوف نقطة 11 أعلاه). المتبقي: تفعيل تحقق `is_active` فعلياً بتطبيق السائق، وزيادة `deliveries_count` تلقائياً عند إتمام التوصيل.
5. بناء نظام دخول حقيقي لتطبيق الزبون (بدل الاسم+الهاتف البسيط) قبل الإطلاق الفعلي
6. تحسينات واجهة تطبيق الزبون بناءً على ملاحظات المستخدمين التجريبيين
7. 🆕 **إعادة إدخال كل المكونات والوجبات من الصفر بالنظام الجديد** (نوع القياس + العبوة الحقيقية لكل مكوّن، ووحدة الوصفة المناسبة لكل مقدار بكل وجبة) — بدون هاي الخطوة قاعدة البيانات فاضية عملياً من منظور الشاشات الجديدة
19. 🆕 **تعديل بيانات المتجر بالضغط عليها (`akleto-admin-stores.html`):** كل بطاقة متجر بالقائمة صارت قابلة للضغط (`cursor:pointer`, `onclick="openEditStoreModal('id')"`) وتفتح نفس مودال الإضافة لكن بوضع تعديل:
   - العنوان يصير "✏️ تعديل: [الاسم]"، الحقول (الاسم، الهاتف، العنوان، lat، lng) تتعبى من بيانات المتجر الحالي، زر الحفظ يصير "حفظ التعديلات ✓"، وتلميح الـPIN ينخفي (PIN ثابت وما يتغيّر بالتعديل).
   - زر "🗑️ حذف المتجر نهائياً" يظهر بأسفل المودال بوضع التعديل فقط (`deleteStoreFromModal()` — يستخدم `deleteDoc` بعد تأكيد).
   - الحفظ عبر `saveStore()` صار يتفرّع: لو `currentEditingStoreId` موجود يستخدم `updateDoc` (بدون تغيير `pin`/`is_active`/`created_at`)، وإلا `addDoc` كالسابق.
   - أزرار "نسخ PIN" و"تفعيل/تعطيل" و"عرض الموظفين ▾" داخل البطاقة صارت تستخدم `event.stopPropagation()` عشان ما تفتح مودال التعديل بالغلط عند الضغط عليها.
   - **إصلاح جانبي مهم:** تم اكتشاف إنه class="hidden" كان مستخدم بكذا مكان (قائمة الموظفين المنسدلة مثلاً) بدون أي تعريف CSS فعلي لها بـ`admin-style.css` (خلل موجود من قبل، ما كان مؤثر بشكل واضح). أُضيفت قاعدة `.hidden { display: none !important; }` لـ`admin-style.css` وتم رفع `?v=6` بكل صفحات الأدمن.

8. 🆕 إدخال إحداثيات (`lat`/`lng`) لكل المتاجر الموجودة حالياً من لوحة الأدمن، ومراجعة كل متجر لشاشة "منتجاتي" وتحديد توفر/أسعار مكوناته — بدون هاتين الخطوتين ولا متجر رح يظهر كمرشح لأي طلب
21. 🆕 **تفعيل "طلبات المتجر" بحساب التاجر (`akleto-customer.html`, وضع المتجر) — استقبال الطلبات الواردة والتأشير بجهوزيتها للسائق:**

   بطاقة "📦 طلبات المتجر" بشاشة المتجر الرئيسية صارت فعّالة (كانت "قريباً") وتفتح شاشة كاملة (`storeOrdersScreen`).

   - **اتصال لحظي (`onSnapshot`)** على `collection('orders')` مفلترة بـ`where('store_id','==', storeId)` — يبلش وقت الدخول لحساب المتجر (`enterStoreMode()` → `startStoreOrdersListener()`) ويُفصل عند الخروج (`logoutStore()`)، فبيضل شغال بالخلفية حتى لو التاجر مو فاتح شاشة الطلبات — عشان بادج (نقطة حمراء) على بطاقة "طلبات المتجر" تنعرض بعدد الطلبات الواردة الجديدة بأي وقت وهو بالشاشة الرئيسية لحسابه.
   - **تبويبين:** "📥 واردة" (الحالات `pending`/`preparing` — تحتاج إجراء من التاجر) و"🗂️ سجل الطلبات" (`ready`/`delivering`/`done`/`cancelled` — للمرجعية فقط، بدون إجراء).
   - كل طلب بالقائمة بطاقة مختصرة (رقم الطلب، الوقت، عدد الوجبات/المكونات، حالة ملوّنة). الضغط عليها يفتح شيت تفاصيل (`storeOrderSheet`) بيعرض **قائمة المكونات المطلوبة بالكميات** (`merged_ingredients` نفسها المحفوظة بالطلب وقت التأكيد — تشمل السعر الخاص بهاد المتجر تحديداً بفضل نقطة 18) + الإجمالي التقديري.
   - **زر "✅ جهّزنا المكونات — جاهز للسائق"** يظهر بس على الطلبات الواردة (`pending`/`preparing`) وينفّذ `updateDoc(status:'ready')` مباشرة — بعدها الطلب يختفي من "واردة" وينتقل لـ"سجل الطلبات"، ويظهر تلقائياً بشاشة "الطلبات المتاحة" بتطبيق السائق (نفس حالة `ready` اللي أدمن كان يحطها يدوياً بالسابق — الآن المتجر نفسه قادر يحطها).
   - **ملاحظة مهمة:** التاجر ما بيشوف اسم/رقم الزبون بهاي الشاشة (فقط رقم الطلب + المكونات + السعر) — حفاظاً على خصوصية الزبون، بما إنه التواصل مع الزبون مسؤولية السائق مو التاجر.
   - تمت إضافة `onSnapshot` لقائمة imports `akleto-customer.html`، وهيلبرز `ticketCode()`/`timeAgo()` (نفس المنطق المستخدم بـ`akleto-driver.html`).

   **⚠️ ملاحظة على تدفق الحالة:** لسا ما في تمييز بالواجهة بين `pending` و`preparing` من طرف المتجر (نفس التبويب "واردة" يجمعهم) — التاجر بيضغط "جهّزنا المكونات" فينتقل الطلب مباشرة لـ`ready` بغض النظر إذا كان `pending` أو `preparing` قبلها. هيك بسيط ومقصود حالياً، بس ممكن لاحقاً نضيف خطوة وسط "بدأنا التجهيز" (`pending`→`preparing`) لو احتجنا تمييز أدق بالتقارير.

22. 🆕 **تحويل الزبون والسائق ولوحة التحكم إلى PWA (تطبيقات قابلة للتثبيت على الشاشة الرئيسية):**

   **السبب:** بدل بناء APK حقيقي (يحتاج Android Studio/SDK وتوقيع، غير متاح بالـ sandbox)، اعتمدنا PWA — يسمح للمستخدم يضغط "أضف للشاشة الرئيسية" من المتصفح فيفتح كأيقونة تطبيق مستقلة (بدون شريط متصفح، splash screen، شغال مباشرة من GitHub Pages الحالي، بدون Play Store).

   **3 مانيفستات منفصلة (كل تطبيق يُثبّت بشكل مستقل بأيقونة واسم خاص فيه):**
   - `manifest-customer.json` — start_url `akleto-customer.html`، ثيم عنابي #A23E48 على خلفية كريمي #FDF8F3.
   - `manifest-driver.json` — start_url `akleto-driver.html`، نفس هوية الزبون البصرية.
   - `manifest-admin.json` — start_url `akleto-admin.html`، ثيم غامق #130609 (يطابق هوية لوحة الأدمن المنفصلة).

   **أيقونات مُصمَّمة حسب الدور** (`icons/` — مقاسات 192/512/180 لكل تطبيق، مولّدة برمجياً):
   - الزبون: سلة تسوق بيضاء على خلفية عنابي.
   - السائق: مقود سيارة أبيض على خلفية عنابي (نفس اللون، شكل مختلف للتمييز).
   - الأدمن: ترس (gear) خوخي على خلفية غامقة.

   **`sw.js` — Service Worker مشترك** (يُسجَّل من الثلاث تطبيقات): استراتيجية network-first (يجيب النسخة الأحدث دايماً أثناء التطوير المستمر) مع fallback للكاش لو الاتصال انقطع لحظياً. بيتجاهل أي طلب مش GET أو مش من نفس الأصل (يعني ما يتدخل بطلبات Firestore/Google APIs إطلاقاً).

   **كل صفحة (10 ملفات: الزبون، السائق، وكل صفحات الأدمن الثمانية) صار فيها:**
   - `<link rel="manifest">` يشاور على المانيفست المناسب لدورها
   - `<meta name="theme-color">` مطابق لهوية كل تطبيق
   - وسوم iOS (`apple-mobile-web-app-capable`, `apple-touch-icon`, إلخ) لدعم "أضف للشاشة الرئيسية" على آيفون كمان
   - سكريبت تسجيل الـ Service Worker بنهاية `<head>`

   **⚠️ ملاحظة مهمة:** هاي خطوة تثبيت (installability) بس — **مش بناء APK حقيقي**. لو احتجنا لاحقاً توزيع Play Store رسمي أو APK مستقل، الخطوة الجاية هي Capacitor (يحتاج Android Studio/SDK محلياً عند مؤيد، ما ينبنى من هون).

   **للتجربة:** بعد ما يوصل التحديث لـ GitHub Pages (1-2 دقيقة)، افتح كل رابط من متصفح Chrome على أندرويد → قائمة المتصفح (⋮) → "إضافة إلى الشاشة الرئيسية" / أو بيظهر بانر تلقائي بالأسفل. على آيفون: زر المشاركة → "إضافة إلى الشاشة الرئيسية".

23. 🆕 **شاشة الإشعارات بلوحة التحكم (`akleto-admin-notifications.html`) — الجزء 1 من 3 (استقبال + إرسال):**

   **بنية Firestore الجديدة:**
   - `admin_notifications` — تنبيهات تدخل للأدمن (حقول: `type`, `title`, `message`, `created_at`, `read`). أنواع مدعومة حالياً بالواجهة: `new_order`, `order_ready`, `order_delivered`, `driver_issue` (fallback لأيقونة 🔔 لأي نوع آخر).
   - `user_notifications` — رسائل يبعتها الأدمن (حقول: `audience` [`all_customers`/`all_drivers`/`customer`/`driver`], `target_id`, `target_name`, `title`, `message`, `created_at`, `sent_by`).

   **الشاشة فيها تبويبين:**
   - **الوارد:** فيد حي (`onSnapshot`) لآخر 100 تنبيه، مرتبة الأحدث أولاً، مع نقطة حمراء للتنبيهات غير المقروءة وزر "تعليم الكل كمقروء" (batch write).
   - **إرسال:** فورم يختار الجمهور (الكل / زبون محدد / سائق محدد)، عنوان + نص، ويكتب لـ `user_notifications`. تحته سجل بآخر 30 رسالة مُرسلة.

   **helper مشترك جديد بـ `firebase-init.js`:** `watchUnreadNotifications(callback)` — يراقب عدد التنبيهات غير المقروءة بالوقت الحقيقي، مستخدم لعرض بادج بالسايدبار (كل صفحات الأدمن الثمانية) + نقطة حمراء على جرس 🔔 بالداشبورد الرئيسي.

   **⚠️ ما زال ناقص (الجزءان 2 و3 المتفق عليهم):**
   1. **ربط التنبيهات التلقائية** — حالياً `admin_notifications` فاضية إلا لو حدا كتب فيها يدوياً؛ لسا ما ضفنا الكود اللي يكتب تنبيه تلقائياً عند إنشاء طلب جديد (بتطبيق الزبون)، أو عند تحديث حالة الطلب لـ "جاهز"، أو عند التسليم (بتطبيق السائق/المتجر).
   2. **استقبال الإشعارات عند الزبون والسائق** — الرسائل المكتوبة بـ `user_notifications` حالياً **ما حدا شايفها** إلا من جدول السجل بلوحة التحكم؛ لازم نضيف جرس/قائمة إشعارات بتطبيق الزبون والسائق يقرأوا منها (فلترة حسب `audience` و`target_id`).

   **ملاحظة معمارية:** استخدمنا نفس لغة تصميم تبويبات "شاشة الطلبات" (`.ord-tabs`/`.ord-tab`) بنسخة محلية بهاي الصفحة (`.notif-tabs`/`.notif-tab`) للحفاظ على الاتساق البصري.

24. 🆕 **الاستقبال الفعلي للإشعارات (الجزء 2 من 3) — الزبون + السائق + المتجر:**

   **إضافة "المتجر" كجمهور بشاشة الإرسال بلوحة التحكم:** صار عندنا 6 خيارات جمهور: `all_customers`, `all_drivers`, `all_stores`, `customer`, `driver`, `store`. اختيار "متجر محدد" يعبّي قائمة من مجموعة `stores`.

   **جرس 🔔 حقيقي بكل تطبيق (زبون، متجر [داخل تطبيق الزبون بوضع المتجر]، سائق):**
   - كل تطبيق فيه شيت (`sheet-overlay`) يعرض قائمة الإشعارات الموجهة إله.
   - **آلية الفلترة:** لكل تطبيق مستمعين (`onSnapshot`) منفصلين على `user_notifications` — واحد للبث العام (`all_customers`/`all_drivers`/`all_stores`) وواحد للرسائل الموجهة تحديداً (`where('audience','==','customer'), where('target_id','==', customerId)` وهكذا). النتيجتين يندمجوا ويترتبوا محلياً حسب `created_at` تنازلي.
   - **عدّاد غير مقروء بدون تعديل مخطط البيانات:** بدل ما نضيف حقل "read" لكل مستلم (يحتاج بنية أعقد)، استخدمنا `localStorage` بمفتاح خاص لكل نوع مستخدم (`akleto_customer_notif_seen`, `akleto_store_notif_seen`, `akleto_driver_notif_seen`) يخزّن آخر وقت فتح فيه المستخدم شاشة الإشعارات؛ أي رسالة أحدث من هالوقت = غير مقروءة وبتفعّل نقطة حمراء (`.bell-dot`) على الجرس.
   - بتطبيق الزبون: الجرس موجود بشاشة الرئيسية **وبشاشة المتجر** (نفس الملف `akleto-customer.html`، وضع المتجر)، مع تفرقة كاملة بمفتاح localStorage لتجنب تعارض العدّاد بين هويتي الزبون والمتجر.
   - بتطبيق السائق: الجرس بأعلى الشاشة دايماً (topbar ثابت).

   **⚠️ ناقص لسا (الجزء 3 المتفق عليه):**
   ربط التنبيهات **التلقائية** بمجموعة `admin_notifications` (تنبيه أوتوماتيكي للأدمن عند طلب جديد / طلب جاهز / طلب موصّل) — لسا ما تربط، فـ"الوارد" بلوحة التحكم يضل فاضي إلا لو حدا كتب فيه يدوياً.

25. 🆕 **ربط التنبيهات التلقائية بالأدمن (الجزء 3 من 3 — اكتمل نظام الإشعارات كاملاً):**

   4 أحداث بدورة حياة الطلب تكتب تلقائياً بمجموعة `admin_notifications` (كل وحدة `read:false` عند الإنشاء):
   - **`new_order`** — بتطبيق الزبون، فور `placeOrder()` تنجح، مباشرة بعد ما يتحدد المتجر.
   - **`order_ready`** — بوضع المتجر (`akleto-customer.html`)، فور `markStoreOrderReady()`.
   - **`order_claimed`** 🆕 (نوع جديد ما كان بالتصميم الأولي، أضفناه لتغطية دورة الحياة كاملة) — بتطبيق السائق، فور نجاح `claimOrder()` (Transaction الاستلام).
   - **`order_delivered`** — بتطبيق السائق، فور `markDelivered()`.

   كل تنبيه فيه `title` (يتضمن كود الطلب بصيغة `#XXXX` عبر دالة `ticketCode()` الموجودة أصلاً بكل تطبيق) و`message` (بيذكر اسم الزبون/السائق/المتجر المعنيين) و`related_id` (معرّف الطلب، لاستخدام مستقبلي لو حبينا نربط النقر بالتنبيه لفتح تفاصيل الطلب مباشرة).

   **ملاحظة موثوقية:** كتابة `admin_notifications` ما بتوقف أو تفشّل العملية الأساسية لو صار فيها خطأ — مكتوبة بـ `.catch()` منفصل (fire-and-forget) بعد نجاح تحديث/إنشاء الطلب نفسه، عشان أي مشكلة بكتابة التنبيه ما تأثر على تجربة المستخدم الأساسية (تأكيد الطلب، تحديث الحالة...).

   **✅ نظام الإشعارات الآن مكتمل بالكامل بأجزائه الثلاثة:** شاشة أدمن (وارد+إرسال) ← استقبال فعلي (زبون/سائق/متجر) ← تنبيهات تلقائية بدورة حياة الطلب.

26. 🆕 **تطوير الشاشة الرئيسية وتطبيق الزبون بشكل عام (جلسة تحسينات UI/UX):**

   **أ) الشاشة الرئيسية (`akleto-customer.html`) أعيد تصميمها بالكامل:**
   - قسم hero علوي بتدرّج عنابي فيه صندوق بحث فوري بالوجبات (بالاسم).
   - تصنيفات (chips) قابلة للتمرير أفقياً — تُبنى ديناميكياً من حقل `category` الفعلي بالوجبات (مش قائمة ثابتة بالكود)، مع "الكل" كافتراضي.
   - بطاقات الوجبات أعيد تصميمها: صعوبة + وقت التحضير + عدد الأشخاص كـ tags، سعر تقديري (`base_price`)، زر "+" دائري للإضافة السريعة.
   - شريط سلة عائم (`cartFab`) يظهر فوق التنقل السفلي فقط بالشاشة الرئيسية لما يكون في وجبات مختارة — يفتح شاشة السلة مباشرة.

   **ب) القائمة الجانبية (drawer) أعيد تصميمها بالكامل:**
   - رأس القائمة بتدرّج عنابي (مطابق لهوية الـ hero)، أفاتار دائري، زر إغلاق (✕) صريح.
   - كل عنصر تنقّل بأيقونة داخل مربع ملوّن (`ic-box`) بدل إيموجي عادي + سهم (`chev`).
   - إصلاح جانبي: `.cs-overlay` كان يستخدم `inset:0` (نفس الباغ المعروف بأندرويد القديم) — استُبدل بـ `top/left/right/bottom` صريحة.
   - ✅ **حُذف تبويب "الدخول كتاجر" نهائياً من القائمة** (والدالة `menuGoStore`) — الدخول كتاجر لسا متاح من شاشة الترحيب الأساسية فقط.
   - 🆕 **أُضيفت 3 عناصر جديدة تحت قسم "الحساب":**
     1. **📍 عنواني** — شاشة كاملة جديدة (`addressScreen`)، **عنوان محفوظ فعلياً** بوثيقة الزبون (`customers/{customerId}`): حقول `address_area` (المنطقة/الحي)، `address_details` (تفاصيل الشارع/البناية)، `address_notes` (ملاحظات اختيارية)، `address_updated_at`. يُحمَّل عبر `getDoc` (استُورد حديثاً من `firebase-init.js` — كان موجود بالتصدير أصلاً بس غير مستخدم بهاد الملف) عند فتح الشاشة، ويُحفظ عبر `updateDoc`. **⚠️ ملاحظة مهمة:** هاد العنوان محفوظ للمرجعية فقط حالياً — **لسا ما انربط بمنطق "أقرب متجر"** (`findBestStoreForIngredients`) اللي لسا يعتمد كلياً على `navigator.geolocation` الحي وقت الطلب (نقطة 18 بالتاريخ القديم). ربطه بحساب المسافة خطوة مستقبلية منفصلة.
     2. **💳 معلومات الدفع** — شيت "قريباً" (بناءً على تأكيد مؤيد: الدفع الإلكتروني مستبعد من الـ MVP، فالتبويب فاضي مؤقتاً بانتظار جهوزية الدفع الإلكتروني).
     3. **⚙️ الإعدادات** — شيت "قريباً" فاضي مبدئياً (بناءً على تأكيد مؤيد).
   - شيت "قريباً" مشترك (`comingSoonSheet` / `COMING_SOON_CONTENT` بالـ JS) — قابل لإعادة الاستخدام لأي ميزة مستقبلية بس نضيف مفتاح جديد بـ `COMING_SOON_CONTENT` بدل بناء شيت جديد من الصفر.

   **ج) شاشة تفاصيل الوجبة (bottom sheet):** توضيح مهم بناءً على طلب مؤيد — كل مكوّن صار يعرض سطرين منفصلين: **"يحتاج: X {وحدة الوصفة}"** (كمية الوصفة بالضبط) و **"🛍️ رح تشتري: Y {اسم العبوة}"** (الكمية الفعلية اللي رح تُشترى بعد التقريب لعبوات كاملة) — لتفادي التباس الزبون (مثال حقيقي: "خبز برغر" وصفته تحتاج 2 حبة بس بيتباع بعبوة 6 حبات، فكان يخاف يفهم غلط إنه رح يشتري 2 كيس). **لا تضيف أي جملة شرح عامة/بانر توضيحي فوق القائمة** — تمت تجربتها وطُلب حذفها صراحة لأنها غير مفهومة/زايدة عن الحاجة؛ الوضوح المطلوب هو بس بالتسمية المباشرة "يحتاج" مقابل "رح تشتري" بدون أي نص إضافي.

   **ملفات متأثرة:** `akleto-customer.html` فقط بكل النقاط أعلاه.

27. 🆕 **شاشة تأكيد الطلب الجديدة (بين السلة وإرسال الطلب فعلياً):**

   **الفكرة:** زر "تأكيد الطلب" بشاشة السلة ما عاد يرسل الطلب مباشرة — صار يفتح شاشة مراجعة جديدة (`orderConfirmScreen`)، وهاي الشاشة فيها زر "تأكيد الطلب" الحقيقي اللي يستدعي `finalizeOrder()` (كانت اسمها `placeOrder()` وانحذفت بالكامل، استُبدلت).

   **محتوى الشاشة الجديدة:**
   - عناصر الطلب (اسم الوجبة + عدد الأشخاص) + زر "عرض التفاصيل الكاملة" يفتح قائمة المكونات المدمجة الكاملة (نفس بيانات `getFinalCartIngredients()` المستخدمة بالسلة).
   - تفاصيل التوصيل: اسم المستلم ورقم الهاتف (من الجلسة) + **عنوان التوصيل** (يُجلب حياً بـ `getDoc` من وثيقة الزبون `customers/{customerId}` كل مرة تُفتح الشاشة — مو من كاش). **لو ما في عنوان محفوظ (`address_area` فاضي)، يظهر تحذير واضح مع رابط "أضف عنوانك الآن" ويمنع تأكيد الطلب** (فحص إلزامي داخل `finalizeOrder` كمان، مو بس بالواجهة).
   - حقل **"ملاحظات إضافية للتاجر أو السائق"** (اختياري، `textarea` جديد `#oc-notes`) — يُحفظ بحقل `customer_notes` على وثيقة الطلب، ويظهر لكل من المتجر (بشيت طلبات المتجر بنفس ملف الزبون) والسائق (`akleto-driver.html`).
   - طريقة الدفع: صف ثابت غير تفاعلي "الدفع عند الاستلام 💵" (بما إنه الدفع الإلكتروني لسا مستبعد من الـ MVP — لا يوجد خيار بطاقة).
   - ملخص الفاتورة: إجمالي الطلب (subtotal من المكونات) + **رسوم التوصيل (ثابتة 1.00 د.أ — ثابت `DELIVERY_FEE`)** + **"بدل الخدمة" (⚠️ مش "الضريبة" — تسمية متعمّدة بناءً على طلب مؤيد)** = 4% من قيمة الطلب قبل أي خصومات، بحد أدنى 0.3 د.أ وحد أعلى 1.0 د.أ (`computeServiceFee()` بمنطق `Math.min(1.0, Math.max(0.3, subtotal*0.04))`) + المجموع النهائي.

   **حقول جديدة على وثيقة `orders`:**
   - `delivery_address` (نص مدمج: المنطقة + التفاصيل + ملاحظة العنوان بين قوسين إن وُجدت)
   - `customer_notes` (ملاحظة الطلب نفسها، منفصلة عن ملاحظة العنوان المحفوظة بحساب الزبون)
   - `subtotal_price`, `delivery_fee`, `service_fee` (تفصيل الفاتورة، بالإضافة لـ `total_estimated_price` الموجود سابقاً واللي صار الآن = subtotal + delivery_fee + service_fee)
   - `payment_method` (حالياً دايماً `'cash_on_delivery'` — مجرد قيمة ثابتة تمهيداً لدعم طرق دفع تانية بالمستقبل)

   **تطبيق السائق (`akleto-driver.html`):** أُضيف صف "📍 عنوان التوصيل" (`sheet-address`) وصندوق ملاحظة الزبون (`sheet-notes-wrap`) بشيت تفاصيل الطلب — **هاي أول مرة السائق يشوف فيها عنوان توصيل فعلي** (كان الفجوة المذكورة بالنقطة "على الأفق" بالتاريخ القديم؛ الحل هلق هو نص جاهز محفوظ بحساب الزبون، مش خوارزمية توجيه GPS حية).

   **شاشة طلبات المتجر (بنفس `akleto-customer.html`، وضع المتجر):** أُضيف عرض ملاحظة الزبون (`so-notes-wrap`) بشيت تفاصيل الطلب الوارد، لو موجودة.

   **ملفات متأثرة:** `akleto-customer.html` (الأكبر)، `akleto-driver.html`. **لم يُعدَّل** `akleto-admin-orders.html` بهاي الجلسة (لسا ما يعرض `delivery_address`/`customer_notes`/تفصيل الرسوم — ممكن حاجة مستقبلية لو الأدمن حب يشوفهم).

28. 🆕 **استكمال ربط بيانات شاشة تأكيد الطلب بلوحة أدمن الطلبات (`akleto-admin-orders.html`):**
   - شيت تذكرة الطلب صار يعرض قسم "📍 التوصيل" جديد (`sheetDeliveryWrap`) فوق قسم الوجبات مباشرة: يعرض `delivery_address` كنص، وملاحظة الزبون `customer_notes` بصندوق برتقالي مميز (`.receipt-note`، بلون `--orange`/`--orange-glow` من نظام الألوان الحالي). القسم كامل مخفي لو ما في عنوان ولا ملاحظة (طلبات قديمة قبل هالتحديث).
   - قبل "الإجمالي التقديري" صار في تفصيل فاتورة (`sheetFeesWrap`): إجمالي الطلب (`subtotal_price`) + رسوم التوصيل (`delivery_fee`) + بدل الخدمة (`service_fee`) — **يظهر فقط للطلبات الجديدة اللي فيها `subtotal_price`**؛ الطلبات الأقدم بتضل تعرض بس "الإجمالي التقديري" متل ما كانت (توافق خلفي، بدون كسر أي شي).
   - بطاقة التذكرة بقائمة الطلبات (kitchen ticket list) صار فيها مؤشر صغير "📝 ملاحظة" لو الطلب فيه `customer_notes` — عشان الأدمن يلقط بسرعة إنه في طلب محتاج انتباه خاص بدون ما يفتح التفاصيل.
   - ملف متأثر: `akleto-admin-orders.html` فقط (كود inline بالصفحة نفسها، ما لمسنا `admin-style.css` المشترك).

29. 🆕🔴 **تغيير جذري (breaking): نظام عناوين متعددة مع خرائط حقيقية (Leaflet/OpenStreetMap) — استبدل نظام "العنوان الواحد" القديم بالكامل.**

   **الخلفية:** بجلسة سابقة كان اتبنى عنوان واحد بس محفوظ مباشرة كحقول (`address_area`, `address_details`, `address_notes`) على وثيقة `customers/{customerId}`. هلق انبنى بدلاً منه نظام كامل متعدد العناوين مع تحديد موقع فعلي من خريطة — **الحقول القديمة اتشالت نهائياً ومافي أي كود بيرجع يقرأها أو يكتبها.**

   **أ) مكتبة الخرائط:** أُضيف Leaflet 1.9.4 عبر CDN (`unpkg.com`) بـ `<head>` — بلاطات OpenStreetMap (`tile.openstreetmap.org`)، **بدون أي مفتاح API** (ما في حساب Google Maps). هاي أول مكتبة خرائط بالمشروع كامل.

   **ب) بنية البيانات الجديدة:** subcollection جديدة `customers/{customerId}/addresses/{addressId}`:
   ```
   { label, street, building_number, floor, apartment_number, notes, lat, lng, created_at, updated_at }
   ```
   والعنوان الافتراضي يُحدَّد بحقل واحد `default_address_id` على وثيقة الزبون نفسها (`customers/{customerId}.default_address_id`) — **مش** بحقل `is_default` بكل وثيقة عنوان (تفادياً لكتابات متعددة عند كل تبديل).

   **ج) حاوية جديدة بالشاشة الرئيسية:** `.location-widget` تحت التوب بار مباشرة وفوق الـ hero — تعرض "📍 التوصيل إلى: {أيقونة} {اسم العنوان الافتراضي}" (أو "حدد عنوان التوصيل" لو ما في عنوان بعد). الضغط عليها يفتح شاشة "عناويني". تُحدَّث تلقائياً عند بدء التطبيق (`loadInitialData` صار يستدعي `loadAddressesData()` + `refreshHomeLocationWidget()`).

   **د) شاشة "عناويني" (`addressesScreen`):** تعرض كل العناوين كبطاقات (`addr-card`) — أيقونة حسب التسمية (🏠 المنزل / 🏢 العمل / 👨‍👩‍👧 بيت الأهل / 📍 غيره)، شارة "الافتراضي" على البطاقة الحالية، وزرَي "تعديل" و"اجعله افتراضي". زر "➕ إضافة عنوان جديد" بالأسفل.

   **هـ) شاشة إضافة/تعديل عنوان (`addressFormScreen`):**
   - خريطة Leaflet تفاعلية (`#addrMap`، ~230px ارتفاع) بدبوس قابل للسحب + الضغط على أي مكان بالخريطة ينقل الدبوس — `pickedLat`/`pickedLng` بمتغيرات JS عالمية تُحدَّث لحظياً.
   - زر "📍 موقعي الحالي" (`locateMeOnMap()`) يستخدم `navigator.geolocation` لتوسيط الخريطة على موقع الجهاز الفعلي.
   - عند فتح شاشة "عنوان جديد" (مش تعديل)، الخريطة تُهيَّأ افتراضياً على مركز عمّان (31.9539, 35.9106) ثم تحاول صامتاً تاخذ موقع الجهاز الحقيقي وتنقل الدبوس تلقائياً لو المستخدم وافق على صلاحية الموقع.
   - **⚠️ ملاحظة تقنية مهمة:** خريطة Leaflet لازم تتهيّأ بعد ما الشاشة تصير ظاهرة فعلياً (مو `display:none`)، فيه تأخير `setTimeout(..., 60)` قبل استدعاء `initAddrMap()` + `invalidateSize()` بعد 150ms إضافية — لو حصل مستقبلاً تقطيع/عرض غريب بالخريطة (خصوصاً بعد فتح الشاشة من مسارات تانية)، هاي أول نقطة تفحصها.
   - رقائق تسمية سريعة (chips): المنزل / العمل / بيت الأهل / عنوان آخر — تملأ حقل "اسم العنوان" تلقائياً (تستخدم نفس صنف `.cat-chip` الموجود بالشاشة الرئيسية).
   - حقول: اسم الشارع/المنطقة، رقم البناية، الطابق، رقم الشقة (بصف واحد أفقي)، ملاحظات اختيارية.
   - أول عنوان يُحفظه الزبون يصير الافتراضي تلقائياً (`default_address_id`).
   - زر "🗑️ حذف هذا العنوان" يظهر بوضع التعديل فقط؛ لو محذوف كان هو الافتراضي، يُعاد تعيين الافتراضي لأول عنوان متبقي (أو `null` لو ما ضل شي).

   **و) شاشة تأكيد الطلب:** `renderOcDeliveryDetails()` صارت تجيب **العنوان الافتراضي** من الـ subcollection (مش الحقول القديمة على وثيقة الزبون). لو ما في أي عنوان محفوظ، نفس تحذير "أضف عنوانك الآن" القديم لسا موجود (بس رابطه هلق يفتح شاشة "عناويني" مش الفورم القديم).

   **ز) الحقول الجديدة على وثيقة `orders`:** إضافة `delivery_lat` و `delivery_lng` (من العنوان المختار) — بالإضافة لـ `delivery_address` (نص مبني الآن من `label + street + building + floor + apartment + notes` بدل `area + details + notes` القديمة).

   **ح) تطبيق السائق (`akleto-driver.html`):** أُضيف رابط **"🗺️ فتح الموقع على الخريطة"** بشيت تفاصيل الطلب — يفتح OpenStreetMap بإحداثيات `delivery_lat`/`delivery_lng` بتاب جديد. يظهر بس لو الطلب فيه إحداثيات (الطلبات الأقدم بدون خرائط ما رح يظهر فيها الرابط، بدون كسر أي شي).

   **ط) الدوال/العناصر المحذوفة نهائياً (ما تحاول تستخدمها أو ترجعلها):** `addressScreen` (الـ id)، `menuOpenAddress` (بدون s)، `openAddressScreen`, `closeAddressScreen`, `saveAddress` (بدون Form)، حقول `addr-area`/`addr-details`/`addr-notes`، وحقول الوثيقة `address_area`/`address_details`/`address_notes`/`address_updated_at`. **البديل الدائم:** `menuOpenAddresses` (بـ s)، `openAddressesScreen`/`closeAddressesScreen`، `openAddressForm(id)`/`closeAddressForm`/`saveAddressForm`/`deleteAddressForm`.

   **ملفات متأثرة:** `akleto-customer.html` (الأكبر بكثير)، `akleto-driver.html`. **لم يُعدَّل** `akleto-admin-orders.html` بهاي الجلسة (يعرض `delivery_address` كنص بس متل ما هو من قبل — يقدر يستفيد من `delivery_lat`/`delivery_lng` لرابط خريطة مستقبلاً لو الأدمن حب).

30. 🆕 **شاشة تأكيد الطلب — دفعة ميزات كبيرة: تنفيذ الطلب بعدّاد تراجع، مدة توصيل ديناميكية، فيزا (قريباً)، إكرامية السائق، ونظام قسائم خصم فعلي كامل.**

   **أ) توضيح تسمية الأزرار (كان يسبب لبس):**
   - زر السلة يبقى **"تأكيد الطلب"** ويفتح شاشة المراجعة متل ما هو.
   - زر شاشة المراجعة صار اسمه **"تنفيذ الطلب"** (بدل "تأكيد الطلب" المكرر). عند الضغط عليه:
     - يبدأ **عدّاد تراجع 3 ثواني داخل الزر نفسه** ("⏳ جاري التنفيذ خلال 3... إلغاء") — لو ضغط الزبون "إلغاء" بينلغي الإرسال فوراً ويرجع الزر لطبيعته. لو خلص العدّاد بدون إلغاء، بينفّذ الإرسال الفعلي.
     - الدوال: `startOrderCountdown()` (يبدأ العدّاد، يتحقق العنوان قبل ما يبلش)، `cancelOrderCountdown()`، و`executeOrderSubmit()` (الإرسال الفعلي — هاي كانت اسمها `finalizeOrder` وانحذفت، الاسم الجديد داخلي فقط مش على `window`).

   **ب) مدة التوصيل الديناميكية:** بانر جديد أعلى شاشة المراجعة (`#oc-eta-banner`) يعرض "⏱️ مدة التوصيل المتوقعة: X-Y دقيقة"، محسوبة من `computeEstimatedDelivery()`:
   - وقت التحضير = أطول `prep_time_min` بين الوجبات المطلوبة (مش مجموعها — افتراض إنه المطبخ يحضّرهم بالتوازي).
   - وقت الطريق = المسافة (Haversine بين موقع الزبون ومتجر الترشيح المبدئي) ÷ سرعة افتراضية 25 كم/ساعة.
   - + 10 دقائق بفر ثابت للاستلام والتسليم. النطاق المعروض = الرقم الناتج إلى +10 دقائق إضافية.
   - **يستخدم استدعاء تمهيدي لـ `findBestStoreForIngredients()` فقط للمعاينة والتقدير** — الاستدعاء الحقيقي يصير من جديد وقت `executeOrderSubmit()` (احتمال ضئيل يتغير المتجر المرشّح بين المعاينة والتنفيذ الفعلي، مقبول لأنه ما في طلب انحفظ بعد).
   - يُحفظ بالطلب كـ `estimated_delivery_min` / `estimated_delivery_max`، وتُعرض بلوحة أدمن الطلبات كإضافة على سطر الوقت بالشيت.

   **ج) طريقة الدفع:** أُضيف صف ثاني **"بطاقة فيزا / ماستركارد"** تحت "الدفع عند الاستلام" — شكله باهت (`opacity:.5`)، `pointer-events:none` (مش قابل للنقر إطلاقاً)، بشارة "قريباً" صغيرة. لسا ما في أي منطق دفع إلكتروني حقيقي — مجرد عرض بصري تحضيري.

   **د) إكرامية السائق (حاوية جديدة):** رقائق مبالغ مقترحة (بدون / 0.25 / 0.5 / 1 د.أ) + رقاقة "مبلغ آخر" تفتح حقل رقمي حر. القيمة المختارة (`selectedTip`) تنضاف كسطر منفصل بملخص الفاتورة ("إكرامية السائق") وتُحسب ضمن `total_estimated_price`، وتُحفظ بالطلب كـ `driver_tip`. **تُعرض لتطبيق السائق بوضوح** كسطر مميز أخضر "🎁 منها إكرامية لك: X د.أ" قبل "المبلغ المطلوب تحصيله" (حتى يعرف قد إيش من المبلغ المُحصَّل كاش هو إكراميته الشخصية). تُعرض أيضاً بلوحة أدمن الطلبات ضمن تفصيل الفاتورة.

   **هـ) قسيمة الشراء — نظام قسائم فعلي كامل (مو مجرد واجهة):**
   - **مجموعة Firestore جديدة `coupons`** — **مُعرّف الوثيقة نفسه هو كود الخصم بحروف كبيرة** (مثال: `RAMADAN25`)، مش حقل داخلي — هيك التحقق بجانب الزبون بيصير بقراءة وثيقة وحدة مباشرة (`getDoc`) بدل query.
     الحقول: `discount_type` ('percentage'|'fixed'), `discount_value`, `max_discount_amount` (اختياري، سقف للخصم إذا النوع نسبة مئوية), `min_order_amount` (اختياري), `usage_limit` (اختياري، فاضي = بلا حد), `used_count` (يبدأ 0، بينزاد تلقائياً), `is_active`, `expires_at` (Timestamp اختياري), `created_at`/`updated_at`.
   - **صفحة أدمن جديدة `akleto-admin-coupons.html`** — قائمة/إضافة/تعديل/حذف الأكواد بنفس نمط باقي صفحات الأدمن (modal + card list). **أُضيف قسم جانبي جديد "التسويق" مع رابط "🎟️ قسائم الخصم" بالقائمة الجانبية لكل صفحات الأدمن التسع الأخرى** (تم بسكربت Python بحث عن anchor فريد `href="akleto-admin-stores.html"` وإدراج القسم الجديد بعده — نفس أسلوب "Bulk sidebar insertions" الموثّق بالتاريخ القديم).
   - **بجانب الزبون:** حاوية "🎟️ قسيمة الشراء" بشاشة تأكيد الطلب — حقل كود + زر "تطبيق". `applyCoupon()` يتحقق: الكود موجود ومفعّل، ما انتهت صلاحيته، ما وصل الحد الأقصى للاستخدام، الطلب يحقق الحد الأدنى المطلوب (إن وُجد). يحسب الخصم ويطبّق سقف `max_discount_amount` وسقف "ما يتجاوز قيمة الطلب نفسها". عند القبول، يظهر شريط أخضر بالكود المطبّق مع زر "إزالة".
   - **⚠️ قرار مهم مؤكد من مؤيد:** بدل الخدمة (4%) **يُحسب دايماً من قيمة الطلب قبل خصم القسيمة** — مش بعده. الخصم يُطرح لحاله بالمجموع النهائي: `total = max(0, subtotal - discount) + delivery_fee + service_fee + tip`.
   - **أمان التزامن عند الإرسال الفعلي:** بدل ما نكتفي بالتحقق وقت `applyCoupon` (ممكن يصير Race Condition لو كذا زبون استخدموا نفس الكود بنفس اللحظة وقارب على نفاذ `usage_limit`)، `executeOrderSubmit()` يستخدم **`runTransaction`** (استُورد حديثاً بملف الزبون من `firebase-init.js` — كان مُصدَّر أصلاً بس ما كان مستورد بهاد الملف) لقراءة `used_count` من جديد وزيادته بأمان قبل إنشاء الطلب. لو فشلت المعاملة (الكود انحذف أو خلص حده الأقصى بين لحظة التطبيق والتنفيذ)، الطلب ما يُرسل وتظهر رسالة توضيحية للزبون إنه يشيل الكود ويجرب بدونه.
   - الحقول المحفوظة بالطلب: `coupon_code` (أو `null`)، `coupon_discount`. تُعرض بلوحة أدمن الطلبات كسطر أخضر "خصم قسيمة (الكود)" بتفصيل الفاتورة.

   **ملفات متأثرة:** `akleto-customer.html` (الأكبر بكثير)، `akleto-driver.html`، `akleto-admin-orders.html`، **ملف جديد** `akleto-admin-coupons.html`، + تعديل بسيط (رابط قائمة جانبية فقط) على باقي صفحات الأدمن الثمانية (`akleto-admin.html`, `-meals`, `-ingredients`, `-units`, `-customers`, `-drivers`, `-stores`, `-notifications`).

31. 🆕 **صور المكونات + شاشة المنتج الجديدة (`akleto-customer.html` فقط):**
   - كل مكان يظهر فيه مكوّن للزبون صار له صورة مصغّرة (`.ing-thumb-sm`، 38×38، دائرية الزوايا) بدل الاعتماد على الاسم فقط، مع إيموجي 📦 كـ fallback لو ما في `image_url`: شاشة تفاصيل الوجبة، شاشة السلة (المكونات المدمجة)، شيت اختيار مكوّن يدوي، والتفاصيل الكاملة بشاشة تأكيد الطلب.
   - **شاشة منتج جديدة (`productSheet`)** — bottom sheet بنفس نمط شيت الوجبة (`sheet-hero`/`sheet-title`/`sheet-desc`): تفتح عند الضغط على صورة أو اسم أي مكوّن بأي مكان بالتطبيق، وتعرض: صورة كبيرة، الاسم، التصنيف (`category` — نص حر متل ما هو مخزّن بلوحة الأدمن، ما في map ترجمة)، سعر العبوة، وحجم العبوة (`package_label` = `package_qty` بوحدة الأساس). فيها زر "➕ ضيف للسلة" يستدعي `addIngredientToCart()` الموجودة أصلاً (نفس المنطق يلي بشيت اختيار مكوّن يدوي بالسلة).
   - دالة مساعدة جديدة `ingImageFor(ingredientId)` تبحث بـ `ingredientsCache` المحمّل أصلاً بالتطبيق (ما احتجنا استعلام Firestore إضافي).
   - `getMealIngredients()` صار يرجّع حقل `imageUrl` إضافي على كل عنصر.
   - ملف متأثر: `akleto-customer.html` فقط — لا تغيير على مخطط بيانات `ingredients` (الحقل `image_url` كان موجود أصلاً من لوحة الأدمن، بس ما كان مستخدم بجانب الزبون).

32. 🆕🔴 **تغيير معماري جذري: فصل تطبيق المتجر عن تطبيق الزبون لملفين منفصلين + تبويب "المنتجات" الجديد + إعادة تنظيم السلة.**

   **أ) الفصل المعماري (`akleto-customer.html` ↔ `akleto-store.html` الجديد):**
   - من الآن، تطبيق المتجر (تسجيل دخول بموظف/PIN، الشاشة الرئيسية للمتجر، "منتجاتي"، "طلبات المتجر") **صار ملف HTML مستقل بالكامل**: `akleto-store.html`. **لم يعد جزءاً من `akleto-customer.html`.**
   - **⚠️ تأثير عملي على موظفي المتاجر:** ما عاد في زر "🏪 الدخول كتاجر" جوا تطبيق الزبون (اتحذف نهائياً من شاشة الترحيب). **موظفو المتاجر لازم يستخدموا رابط منفصل الآن:** `moayyad44.github.io/akalito/akleto-store.html` — لازم تبعتلهم هذا الرابط الجديد (بوكماركة/PWA تثبيت) بدل ما كانوا يفتحوا رابط الزبون العادي ويضغطوا الزر.
   - شاشة دخول المتجر (`storeLoginScreen`) صارت هي **الشاشة الافتراضية** لملف `akleto-store.html` (تُعرض مباشرة، بدون `class="hidden"`)، وزر "رجوع" اتحذف منها (ما في شاشة ترحيب زبون نرجعله).
   - **قرار تبسيط متعمّد:** الـ `<style>` الكامل (كل كلاسات CSS، للزبون وللمتجر سوا) **اتنسخ حرفياً بنفس الشكل بالملفين الاثنين** — ما تم فصله/تقليمه لتقليل حجم كل ملف. هذا معناه كل ملف فيه كام كيلوبايت CSS زيادة غير مستخدمة (كلاسات الزبون داخل ملف المتجر والعكس)، بس هذا اختيار آمن ومتعمّد لتقليل مخاطر كسر أي شي أثناء الفصل — مو خطأ. لو حابين تحسين الحجم مستقبلاً ممكن نرجع نقلّم CSS لكل ملف لحاله.
   - **مكتبة Leaflet (الخرائط) اتشالت بالكامل من `akleto-store.html`** — مو مستخدمة هناك إطلاقاً (فقط بشاشة عناوين الزبون)، فما في داعي يحمّلها متصفح موظف المتجر.
   - **استيراد Firebase مُخصَّص لكل ملف** (مو نفس القائمة الكاملة بالاثنين): `akleto-customer.html` يستورد (`getDoc`, `deleteDoc`, `MEASURE_TYPES`, `convertToBase`, `loadMeasureTypes`, `runTransaction`) وما يستورد `setDoc`. `akleto-store.html` يستورد `setDoc` بس وما يستورد `getDoc`/`deleteDoc`/`MEASURE_TYPES`/`convertToBase`/`loadMeasureTypes`/`runTransaction` (مو مستخدمين إطلاقاً بمنطق المتجر).
   - **نظام الإشعارات (`user_notifications`) بقي مشترك بالمنطق** (كان أصلاً مبني ليدعم الاثنين عبر فحص `storeId`/`customerId`) — بس كل ملف هلق عنده **نسخته المبسّطة الخاصة فقط** (`notifSeenKey()`/`startNotifListeners()` بكل ملف يفحص فقط الحالة يلي تخصه، بدون فرع الطرف التاني).
   - **`ingredientsCache`** لازم يبقى موجود بالملفين (متغيّر عالمي منفصل بكل ملف) — الزبون يستخدمه لعرض الوجبات/شاشة المنتجات، والمتجر يستخدمه (يحمّله بنفسه عند فتح "منتجاتي" لو كان فاضي) لعرض أسماء/تفاصيل المكونات المتاحة للتسعير.
   - **مانيفست/أيقونات المتجر:** ما في ملف `manifest-store.json` أو أيقونات مخصصة له بالمستودع بعد — `akleto-store.html` حالياً بيستخدم نفس `manifest-customer.json` وأيقونات الزبون مؤقتاً (تفاصيل الـ `<title>` بالتاب مختلفة "أكليتو — المتجر" بس، مو أيقونة التطبيق لو تثبيته PWA). **نقطة تحسين مستقبلية** لو حابين هوية بصرية منفصلة كاملة لموظفي المتجر.

   **ب) تبويب "🧺 المنتجات" الجديد (`akleto-customer.html`):**
   - تبويب رابع بشريط التنقل السفلي: **الرئيسية، المنتجات، سلتي، حسابي** (بهذا الترتيب — قرار UX: التصفح الحر بعد الرئيسية مباشرة، قبل السلة).
   - شاشة `productsScreen` جديدة: بحث فوري + تجميع كل المكونات (`ingredientsCache`) حسب حقل `category` الحر (نفس نمط تصنيفات الوجبات بالرئيسية)، كل قسم بعنوان "🏷️ {التصنيف}" وشبكة بطاقات (نفس تصميم `.meal-tile` بالضبط، معاد استخدامه حرفياً). الضغط على البطاقة يفتح `openProductSheet()`، وزر "+" يضيف مباشرة عبر `addIngredientToCart()`.
   - **الفكرة الجوهرية:** الزبون يقدر يشتري مكونات مباشرة بدون أي علاقة بوجبة — سلة "منتجات فقط" بدون أي وجبة مختارة، بالكامل مدعومة.

   **ج) إصلاحات حرجة مرتبطة (اكتُشفت وأُصلحت أثناء بناء الميزة أعلاه):**
   - **باغ:** شاشة السلة كانت تعرض "سلتك فاضية" لو ما في وجبات مختارة، **حتى لو كان فيها منتجات مضافة يدوياً فعلياً**. تم إصلاحه: الفحص هلق `!hasMeals && !hasIngredients` مش `!hasMeals` بس.
   - **باغ:** شارة عدد السلة (`cartBadge`) وزر الشريط العائم (`cartFab`) كانوا يحسبوا بس `selectedMeals.length`، فسلة فيها منتجات بس بدون وجبات كانت تظهر بعداد صفر. تم إصلاحه بحساب `selectedMeals.length || عدد المكونات النهائية`.
   - **باغ:** فتح شاشة تأكيد الطلب (`openOrderConfirm`) وبدء تنفيذ الطلب (`startOrderCountdown`) كانا يرفضان المتابعة كلياً لو `!selectedMeals.length`، حتى لو في منتجات. تم توسيع الشرط ليشمل حالة "منتجات بدون وجبات".
   - `renderOcItems()` بشاشة تأكيد الطلب صار يتكيّف: لو في وجبات يعرضها متل ما كان، ولو **مافي وجبات إطلاقاً** يعرض قائمة المنتجات المضافة مباشرة كـ"عناصر الطلب" بدلاً منها.
   - شاشة السلة صار فيها عنوان واضح "🍽️ الوجبات المختارة (N)" فوق قسم الوجبات (كان بدون أي عنوان)، وعنوان "🧠 قائمة المكونات" (بدل "المدمجة" الثابتة) يتكيّف حسب وجود وجبات من عدمه.

   **ملفات متأثرة:** `akleto-customer.html` (تعديل كبير + حذف كل منطق المتجر منه)، **ملف جديد بالكامل** `akleto-store.html`. لم يُلمس أي ملف أدمن أو `akleto-driver.html` بهاي النقطة.

33. 🆕 **صفحتان قانونيتان جديدتان: شروط الاستخدام وسياسة الخصوصية.**
   - ملفان مستقلان جديدان: `akleto-terms.html` و`akleto-privacy.html` — صفحات محتوى ثابت (مش SPA)، بنفس الهوية البصرية (كريمي/عنابي، خطوط Cairo/Tajawal)، بدون أي اعتماد على firebase-init.js.
   - **معلومات التواصل المستخدمة:** الاسم "أكليتو" (بدون شركة مسجّلة رسمياً حسب معلومات مؤيد)، البريد `moaiad.app92@gmail.com`، الهاتف `0797584124`.
   - **⚠️ بناءً على طلب مؤيد صراحة: لا توجد سياسة إلغاء/استرجاع بهاي الصفحات ولا بأي مكان بالتطبيق.**
   - **⚠️ تنويه مهم لأي جلسة قادمة:** هاي المسودة كُتبت من Claude كصياغة أولية معقولة، **مش مراجعة قانونية فعلية من محامي**. مؤيد على علم بهيك وقرر يبنيها هلق (مو آخر شي بالمشروع) لأنها منخفضة التكلفة تقنياً وضرورية من أول يوم بما إنه التطبيق يجمع بيانات شخصية حقيقية (اسم، هاتف، عنوان، إحداثيات GPS) من البداية. قبل أي إطلاق فعلي للجمهور لازم مراجعة محامي أردني مختص (خصوصاً قانون حماية البيانات الشخصية الأردني).
   - **الربط داخل `akleto-customer.html`:**
     - نص "بالضغط على تنفيذ الطلب..." بشاشة تأكيد الطلب صار فيه رابطين فعليين (`target="_blank"`) للشروط والخصوصية بدل نص عادي.
     - تذييل القائمة الجانبية (drawer footer) صار فيه رابطين نفس الشيء تحت "أكليتو 🍽️".
   - **ملفات متأثرة:** ملفان جديدان (`akleto-terms.html`, `akleto-privacy.html`) + تعديل بسيط (روابط فقط) على `akleto-customer.html`. لم يُلمس أي ملف تاني.

34. 🆕 **إصلاح: دالة `ticketCode()` كانت مفقودة من `akleto-customer.html`** — كانت مستخدمة عند كتابة إشعار `new_order` للأدمن (`executeOrderSubmit`) بس غير معرّفة إطلاقاً بهاد الملف (موجودة أصلاً بـ`akleto-driver.html` فقط)، فكل محاولة إرسال طلب كانت تنهار بخطأ `ticketCode is not defined` بعد ما الطلب نفسه يكون انكتب فعلاً بـ`orders` — يعني الزبون كان يشوف رسالة فشل عامة رغم إنه الطلب انحفظ بقاعدة البيانات بدون إشعار للأدمن ولا شاشة نجاح. تم نسخ نفس الدالة (`function ticketCode(id) { return '#' + (id||'----').slice(-4).toUpperCase(); }`) من `akleto-driver.html` لأول الـ`<script type="module">` بملف الزبون. **⚠️ تنويه:** أي طلبات فشلت بهاي الرسالة قبل الإصلاح يحتمل تكون موجودة فعلياً بـ`orders` بلوحة الأدمن بدون إشعار — تفقّد تكرارات محتملة.

35. 🆕 **ربط العنوان المحفوظ للزبون بخوارزمية «أقرب متجر» مباشرة (بدل الاعتماد الحصري على الموقع الحي):**
   - دالة جديدة `getDefaultAddressCoords()` — تجيب `lat`/`lng` من العنوان الافتراضي المحفوظ بـ`addressesCache` (أو أول عنوان لو ما في افتراضي محدَّد) لو موجودة إحداثيات صالحة.
   - دالة جديدة `getRoutingCoords()` — **تُفضّل دايماً إحداثيات العنوان المحفوظ** (ثابتة وموثوقة، محدَّدة يدوياً من الزبون بالخريطة)، وترجع لـ`getCustomerCoords()` (الموقع الحي عبر `navigator.geolocation`) بس لو ما في عنوان محفوظ بإحداثيات إطلاقاً.
   - استُبدل استخدام `getCustomerCoords()` بـ`getRoutingCoords()` بمكانين: `findBestStoreForIngredients()` (اختيار أقرب متجر مؤهّل) و`computeEstimatedDelivery()` (تقدير مدة التوصيل).
   - **الأثر العملي:** الزبون يلي حفظ عنوان بلوحة العناوين ما عاد يحتاج يوافق على صلاحية GPS الحية وقت كل طلب — النظام يحسب المسافة مباشرة من العنوان المحفوظ. صلاحية الموقع الحي صارت بديل احتياطي بس لزبون ما ضاف عنوان بعد.
   - **ملاحظة:** لسا لازم `addressesCache` يكون محمّل (يصير تلقائياً بـ`loadInitialData` عند فتح التطبيق) قبل ما تتحسب المسافة بدقة — لو انفتحت شاشة السلة بسرعة قبل ما يخلص التحميل، بيرجع مؤقتاً لمنطق الموقع الحي كما كان (سلوك fallback موجود أصلاً).
   - ملف متأثر: `akleto-customer.html` فقط.

36. 🆕 **تفعيل شاشة "الإعدادات" بتطبيق الزبون + قوائم جانبية (drawer) جديدة كليًا لتطبيقي المتجر والسائق:**

   **أ) شاشة "الإعدادات" الحقيقية (`akleto-customer.html`):** كانت شيت "قريباً" فاضي — استُبدلت بشاشة كاملة (`settingsScreen`) بنفس نمط شاشة "عناويني" (store-sub-topbar + زر رجوع):
   - **تعديل الاسم الظاهر:** حقل نص معبّى من `customerName` الحالي + زر "حفظ الاسم" (`saveSettingsName()`) يحدّث `customers/{customerId}.name` عبر `updateDoc` + يحدّث `localStorage`/المتغيرات المحلية/كل أماكن عرض الاسم (الترحيب، حسابي، القائمة الجانبية) فوراً بدون Refresh.
   - رابط "🔔 الإشعارات" يفتح شيت الإشعارات الموجود أصلاً (`openNotifSheet()`).
   - رابطي "📄 الشروط والأحكام" و"🔒 سياسة الخصوصية" (نفس الروابط الموجودة بتذييل القائمة الجانبية، مكرّرة هون للوصول الأسهل).
   - قسم "منطقة الخطر" لحذف الحساب: بما إنه ما في نظام حذف حساب آلي حالياً، الحل المؤقت هو صندوق تواصل مباشر (اتصال/إيميل) — الزبون يتواصل يدوياً وتتم مساعدته، بدل بناء منطق حذف كامل غير مطلوب حالياً.
   - سطر ثابت بأسفل الشاشة: "أكليتو — الإصدار 1.0".
   - رابط القائمة الجانبية تغيّر من `menuOpenComingSoon('settings')` إلى `menuOpenSettings()`.

   **ب) قائمة جانبية (drawer) جديدة لتطبيق المتجر (`akleto-store.html`):** زر ☰ جديد بأعلى يسار شاشة المتجر الرئيسية (بجانب اسم المتجر) يفتح قائمة منسدلة من اليمين (نفس تصميم/CSS درج الزبون، كانت موجودة بالملف من نسخ الـ CSS الكامل وقت الفصل المعماري بس بدون أي HTML/JS يستخدمها):
   - عناصر: الرئيسية، طلبات المتجر، منتجاتي، الإشعارات، بوابة المتجر (قريباً)، خروج من حساب المتجر (بلون تحذيري بالأسفل).
   - دوال جديدة: `toggleStoreMenu()`, `closeStoreMenu()`, `goStoreHome()` (يخفي شاشتي المنتجات/الطلبات ويرجع للرئيسية)، و`menuStoreGoHome/Orders/Products()` كـ wrappers تغلق القائمة وتنقل للشاشة المطلوبة (تستخدم `openStoreOrders()`/`openStoreProducts()` الموجودتين أصلاً).

   **ج) قائمة جانبية (drawer) جديدة لتطبيق السائق (`akleto-driver.html`) — من الصفر بالكامل:** الملف ما كان فيه أي CSS لقائمة جانبية إطلاقاً (خلافاً للزبون والمتجر)، فتم نسخ نفس كتلة CSS (`.cs-overlay`/`.cs-drawer`/`.cs-nav-item`...) وتخصيصها بإيموجي 🛵 بخلفية الهيدر بدل 🍽️/🏪. زر ☰ جديد بجانب شعار "أكليتو 🛵" بأعلى التطبيق يفتح القائمة:
   - عناصر: الطلبات المتاحة، توصيلاتي، حسابي (نفس تبويبات شريط التنقل السفلي الثلاثة، بس بالقائمة الجانبية كمان لسهولة الوصول من أي مكان)، الإشعارات، تسجيل الخروج.
   - دوال جديدة: `toggleDriverMenu()` (يعرض حرف اسم السائق كأفاتار + الاسم/الهاتف)، `closeDriverMenu()`، `menuDriverSwitch(name)` (يغلق القائمة وينادي `switchScreen()` الموجودة أصلاً).

   **ملاحظة معمارية:** القائمة الجانبية بكل تطبيق (زبون/متجر/سائق) مستقلة تمامًا عن الأخرى — كل ملف عنده نسخته الخاصة من الـ CSS/HTML/JS (بدون أي استيراد مشترك)، بما يتوافق مع قرار الفصل المعماري السابق (نقطة 32) اللي خلّى كل تطبيق ملف HTML مستقل بالكامل.

   **ملفات متأثرة:** `akleto-customer.html`، `akleto-store.html`، `akleto-driver.html`. لم يُلمس أي ملف أدمن.

37. 🆕 **حذف الحساب صار فعلي مباشر (زبون + سائق) بدل التواصل مع الإدارة:**
   - **الزبون (`akleto-customer.html`):** قسم "منطقة الخطر" بشاشة الإعدادات صار فيه زر حقيقي "🗑️ حذف حسابي نهائياً" بدل صندوق تواصل هاتف/إيميل. `deleteMyAccount()`: يعرض `confirm()` تحذيري واضح (نفس نمط تأكيد حذف العنوان/تسجيل الخروج الموجود أصلاً بالملف)، وعند التأكيد يحذف أولاً كل عناوين الزبون المحفوظة (subcollection `customers/{id}/addresses`، best-effort)، ثم يحذف وثيقة الزبون نفسها (`customers/{id}`) عبر `deleteDoc`، وأخيراً ينظّف `localStorage` ويعيد تحميل الصفحة (يرجع لشاشة الترحيب/التسجيل من جديد).
   - **السائق (`akleto-driver.html`):** زر مماثل "🗑️ حذف حسابي نهائياً" أُضيف بشاشة "حسابي" تحت زر "تسجيل خروج". `deleteDriverAccount()`: نفس نمط التأكيد، يحذف وثيقة `drivers/{driverId}` عبر `deleteDoc`، يفصل مستمعي `onSnapshot` النشطين (`unsubAvailable`/`unsubMine`) قبل التنظيف، وينظّف `localStorage` ويعيد التحميل. **`deleteDoc` أُضيف حديثاً لقائمة استيراد `firebase-init.js` بهاد الملف** (كان غير مستورد سابقاً بتطبيق السائق).
   - **ملاحظة:** الحذف نهائي وفوري بدون فترة سماح أو أرشفة — يتوافق مع حجم المستخدمين التجريبي الحالي القليل. لو احتجنا لاحقاً "استرجاع الحساب خلال X يوم" هاي نقطة تحسين مستقبلية منفصلة.
   - **ملفات متأثرة:** `akleto-customer.html`، `akleto-driver.html`. لم يُلمس تطبيق المتجر أو أي ملف أدمن بهاي النقطة.

38. 🆕 **مشروع Capacitor حقيقي لتطبيق الزبون — أول خطوة فعلية نحو APK/Google Play (مجلد جديد `mobile-customer/`):**
   - بعد نقاش مع مؤيد حول أفضل مسار مستقبلي (PWABuilder مقابل Capacitor مقابل الاستمرار بـ PWA فقط)، تم اختيار **Capacitor** كالمسار الرسمي — لأنه بيولّد مشروع Android حقيقي (مش مجرد غلاف WebView خفيف)، يدعم النشر الرسمي على Google Play مستقبلاً، ويسمح بإضافة أي SDK أندرويد أصلي لاحقاً (إشعارات push حقيقية، GPS خلفية) بدون سقف.
   - **مجلد جديد بالمستودع: `mobile-customer/`** — يحتوي: `package.json` (تبعيات `@capacitor/core`, `@capacitor/android`, `@capacitor/cli`, `@capacitor/assets`)، `capacitor.config.json`، `www/index.html` (placeholder بسيط، مش مستخدم فعلياً)، `resources/icon.png` (نسخة من `icons/customer-512.png`)، ومجلد `android/` الكامل (مشروع Android Studio حقيقي جاهز للفتح والبناء مباشرة).
   - **قرار معماري أساسي:** `capacitor.config.json` مضبوط بـ`server.url` يشاور مباشرة على `https://moayyad44.github.io/akalito/akleto-customer.html` (`androidScheme: https`) — يعني التطبيق المُثبَّت (APK) **يحمّل المحتوى حياً من GitHub Pages بدل تضمينه محلياً بالـ APK**. الأثر العملي المهم: أي تحديث مستقبلي نسويه على `akleto-customer.html` وندفعه لـ GitHub بينعكس تلقائياً عند كل مستخدم مثبّت التطبيق، **بدون أي حاجة لبناء APK جديد أو توزيعه من جديد**. البديل (تضمين الملفات محلياً بـ`www/` لدعم عمل بدون إنترنت) لسا ما تم اعتماده — خطوة مستقبلية منفصلة إذا احتجناها.
   - **الأيقونات وشاشة البداية (splash):** وُلِّدت تلقائياً بكل المقاسات المطلوبة (mipmap-ldpi حتى xxxhdpi + splash بأوضاع portrait/landscape/dark) عبر أداة `@capacitor/assets` من نفس أيقونة الزبون الموجودة أصلاً (`icons/customer-512.png`) — نفس الهوية البصرية (عنابي/كريمي) متل نسخة الـ PWA بالمتصفح.
   - **صلاحيات إضافية بـ `AndroidManifest.xml`:** أُضيف `ACCESS_FINE_LOCATION` و`ACCESS_COARSE_LOCATION` (بجانب `INTERNET` الموجودة بالقالب الافتراضي) — ضروريتين عشان `navigator.geolocation` (المستخدمة بشاشة العناوين/تحديد أقرب متجر) تشتغل صح جوا WebView الخاص بـ Capacitor، مش بس بمتصفح عادي.
   - **`README.md` داخل `mobile-customer/`** فيه دليل كامل بالعربي لمؤيد: تثبيت Android Studio، فتح مجلد `android/` بالذات (مش `mobile-customer/` نفسه)، خطوات "Build APK(s)"، مكان استخراج ملف الـ APK النهائي، وشرح مبسّط لآلية الـ`server.url` وليش ما يحتاج إعادة بناء لكل تحديث محتوى.
   - **⚠️ لم يتم بناء APK فعلي بعد من هالجلسة** — البيئة الحالية (sandbox الخاص بـ Claude) ما فيها Android SDK/Gradle الكاملة لتشغيل بناء فعلي والتحقق منه، فالخطوة التالية (البناء الفعلي بالضغط على "Build APK(s)" بـ Android Studio) لازم تصير من جهاز مؤيد شخصياً حسب الدليل بـ`README.md`.
   - **⚠️ الـ APK الحالي (لما يُبنى) هو نسخة "debug" غير موقّعة (unsigned/debug-signed)** — صالحة تماماً للتثبيت والتجربة الداخلية المباشرة (تثبيت يدوي على جهاز)، لكن **لا تصلح للرفع على Google Play** — هاي تحتاج خطوة توقيع "release" منفصلة (مفتاح keystore + إعداد `signingConfig` بـ Gradle) لسا ما اتخذت، وستكون خطوة مستقبلية منفصلة عند الاقتراب من إطلاق رسمي.
   - **نطاق هالخطوة:** تطبيق الزبون فقط (`akleto-customer.html`). لو احتجنا نفس الشي لتطبيقي السائق أو المتجر لاحقاً، نكرر نفس الخطوات بمجلدين منفصلين (`mobile-driver/`, `mobile-store/`) — الـ`README.md` يذكر هذا صراحة.
   - **ملفات متأثرة:** مجلد جديد بالكامل `mobile-customer/` (لم يُلمس أي ملف ويب موجود — `akleto-customer.html` وباقي التطبيق ضلوا زي ما هم بدون أي تعديل).

39. 🆕 **إشعارات الدفع (Push Notifications) لتطبيق الزبون — أول ميزة "أصلية حقيقية" لتلبية سياسة Google Play "Minimum Functionality":**
   - بعد ما اتفهم مع مؤيد إنه Google Play بسنة 2026 بيرفض تطبيقات الـ WebView اللي "بس بتعرض موقع" بدون قيمة مضافة حقيقية (سياسة 4.3)، تقرر البلش فوراً بإضافة ميزة native حقيقية بدل تأجيلها لآخر لحظة قبل الرفع.
   - **بمجلد `mobile-customer/`:** تركيب `@capacitor/push-notifications` وربطه بمشروع أندرويد عبر `npx cap sync android` — هالخطوة عدّلت تلقائياً `android/build.gradle` (أضافت `classpath 'com.google.gms:google-services:4.4.4'`) و`android/app/build.gradle` (أضافت منطق شرطي: لو ملف `google-services.json` موجود، يطبّق google-services plugin — وإلا يطبع تحذير بدون ما يوقف البناء).
   - **صلاحية جديدة بـ `AndroidManifest.xml`:** `POST_NOTIFICATIONS` (مطلوبة إجبارياً لأندرويد 13/API 33 فما فوق عشان يظهر الإذن للمستخدم).
   - **كود جديد بـ `akleto-customer.html`:** دالة `initPushNotifications()` تُستدعى تلقائياً من داخل `enterApp()` (بعد كل تسجيل دخول ناجح أو استرجاع جلسة محفوظة). الدالة تتحقق أول شي من `window.Capacitor?.isNativePlatform?.()` — لو التطبيق مفتوح من متصفح عادي (PWA)، الدالة تطلع فوراً بهدوء بدون أي أثر؛ الكود بيشتغل فقط جوا الـ APK. لو جوا التطبيق، تطلب إذن الإشعارات، تسجّل الجهاز (`Push.register()`)، وتحفظ "توكن الجهاز" (fcm_token) + `fcm_platform: 'android'` + `fcm_updated_at` داخل `customers/{customerId}` بـ Firestore — بدون استيراد أي حزمة JS خارجية، الاستدعاء صار مباشرة عبر `Capacitor.Plugins.PushNotifications` (الـ proxy العام لأي plugin مسجّل)، لأن الصفحة تُحمَّل من رابط خارجي (`server.url`) لا من ملفات مبنية محلياً بالتطبيق، فما كان ممكن نستورد حزمة npm الخاصة بالـ plugin مباشرة داخل `<script type="module">` — الحل هو استخدام الـ proxy العام اللي يحقنه Capacitor بأي صفحة (محلية أو خارجية) بغض النظر عن مصدرها.
   - **ما تم اختباره فعلياً:** تم التحقق من صحة syntax لكود JS المعدّل (`node --check`) فقط. ما تم بناء APK فعلي ولا اختبار حقيقي على جهاز — البيئة الحالية ما فيها Android SDK. الاختبار الفعلي بده يصير من جهاز مؤيد بعد أول بناء APK جديد.
   - **⚠️ ناقص من هالميزة (خطوة تالية محددة، لسا ما اتعملت):** حفظ التوكن بـ Firestore هو نص واحد بس من "استقبال الإشعار" — **الإرسال الفعلي** (مثلاً إشعار تلقائي لما تتغيّر حالة الطلب لـ"جاهز" أو "بالطريق") بده يحتاج **Cloud Function** خلفية تراقب تغييرات مستند الطلب وترسل عبر Firebase Cloud Messaging للتوكن المحفوظ. هذا يتطلب تفعيل خطة Firebase "Blaze" (استخدام مجاني كافي لحجم المشروع الحالي عادة). لم يتم البدء بهذا الجزء بعد — مقرر لجلسة قادمة.
   - **⚠️ متطلب يدوي من مؤيد قبل أول بناء ناجح لإشعارات فعّالة:** لازم يروح لـ Firebase Console → مشروع `akleto-prod` → Project settings → يضيف تطبيق أندرويد باسم الحزمة **`com.akalito.customer`** بالضبط → يحمّل `google-services.json` → يحطه بمسار `mobile-customer/android/app/google-services.json` (هذا الملف حساس/سري نسبياً فما تم توليده أو رفعه من هون — لازم يوصل من جهاز مؤيد مباشرة). بدونه التطبيق لسا بيبنى ويشتغل عادي، بس بدون إشعارات.
   - **ملفات متأثرة:** `akleto-customer.html` (إضافة فقط، ما تم لمس أي منطق موجود)، `mobile-customer/android/app/src/main/AndroidManifest.xml`، `mobile-customer/android/build.gradle`، `mobile-customer/android/app/build.gradle`، `mobile-customer/android/app/capacitor.build.gradle`، `mobile-customer/android/capacitor.settings.gradle`، `mobile-customer/package.json` + `package-lock.json`، و`mobile-customer/README.md` (توثيق الخطوات اليدوية لمؤيد).

40. 🆕🔴 **تحويل نظام الخرائط بالكامل من Leaflet/OpenStreetMap إلى Google Maps Platform:**

   **السبب:** مؤيد قرر إنه الخرائط المستخدمة بالمشروع تكون Google Maps (مش OpenStreetMap المجاني) — قرار مرتبط كمان بخطة تحويل التطبيق لـ APK/AAB ورفعه على Google Play (تأكيد: التطبيق رح يبقى "الطريقة 1" — الـ APK يحمّل الصفحات لايف من GitHub Pages، مش نسخة offline مبنية داخل التطبيق؛ هذا متوافق تماماً مع مشروع Capacitor الموجود أصلاً بـ `mobile-customer/capacitor.config.json` اللي فيه `server.url` يشاور مباشرة على `https://moayyad44.github.io/akalito/akleto-customer.html`).

   **مفتاح Google Maps API:** مؤيد جهّز مفتاح من Google Cloud Console (Maps JavaScript API + Places API + Geocoding API مفعّلين، Billing مفعّل). **⚠️ لازم يتأكد مؤيد إنه قيّده فعلياً بـ HTTP referrer على `moayyad44.github.io/*` من Google Cloud Console (Credentials → تعديل المفتاح → Application restrictions → Websites)** — المفتاح ظاهر الآن بكود المصدر بكل الملفات الثلاثة تحت (عادي لمفاتيح خرائط الويب، بس التقييد ضروري لمنع سرقة الاستخدام).

   **`akleto-customer.html` — شاشة إضافة/تعديل العنوان (`addressFormScreen`):**
   - أُزيلت مكتبة Leaflet بالكامل (`<link>`/`<script>` بالـ `<head>`).
   - أُضيف `loadGoogleMapsApi()` — دالة تحميل كسول (lazy load) تحقن `<script src="https://maps.googleapis.com/maps/api/js?key=...&libraries=places">` بأول مرة تحتاج الخريطة بس (مو من أول ما تفتح الصفحة) وتخزن الـ Promise (`googleMapsLoadPromise`) لتفادي تكرار التحميل.
   - `initAddrMap()` صارت `async` وتستخدم `google.maps.Map` + `google.maps.Marker` (draggable) بدل `L.map`/`L.marker`/`L.tileLayer` — نفس السلوك تماماً (دبوس قابل للسحب، ضغطة على الخريطة تنقل الدبوس، `pickedLat`/`pickedLng` تتحدث بنفس الطريقة).
   - `locateMeOnMap()` و`destroyAddrMap()` وهاندلر الموقع التلقائي عند فتح "عنوان جديد" اتحدّثوا لتستخدم Google Maps API (`setCenter`/`setZoom`/`setPosition` بدل `setView`/`setLatLng`).
   - **لا تغيير على بنية البيانات** (`lat`/`lng` بنفس الشكل بـ `customers/{id}/addresses/{addressId}`) ولا على أي منطق حساب مسافة/أقرب متجر — التغيير محصور بالعرض المرئي للخريطة نفسها فقط.

   **`akleto-driver.html`:** رابط "🗺️ فتح الموقع على الخريطة" بشيت تفاصيل الطلب صار يفتح `https://www.google.com/maps/search/?api=1&query={lat},{lng}` بدل رابط OpenStreetMap (بدون حاجة لمفتاح API — رابط خرائط جوجل العادي يشتغل بدون مفتاح لفتح موقع بس).

   **`akleto-admin-stores.html` — إضافة ميزة جديدة لم تكن موجودة سابقاً:** نموذج إضافة/تعديل متجر ما كان فيه أي خريطة أصلاً (بس حقلي رقم lat/lng + زر "استخدم موقعي الحالي"). أُضيفت خريطة Google Maps تفاعلية (`#sfMap`، 200px ارتفاع) تحت حقلي الإحداثيات:
   - دبوس قابل للسحب + ضغط على الخريطة ينقل الدبوس ويحدّث حقلي `sf-lat`/`sf-lng` تلقائياً.
   - الكتابة اليدوية بحقلي lat/lng (`oninput="syncStoreMapFromInputs()"`) تحرّك الدبوس بالخريطة كمان (اتجاه عكسي).
   - "استخدم موقعي الحالي" صار يحرّك الدبوس بالخريطة بالإضافة لتعبئة الحقول.
   - تُهيَّأ الخريطة على مركز عمّان (31.9539, 35.9106) بمتجر جديد، أو على إحداثيات المتجر الحالية بوضع التعديل، وتُدمَّر (`destroyStoreMap()`) عند إغلاق المودال.

   **⚠️ نطاق لم يُلمس بهاي الجلسة:** شاشة "عناويني" (قائمة العناوين) نفسها ما فيها خريطة إطلاقاً أصلاً (بس بطاقات نصية) — الخريطة فقط بشاشة إضافة/تعديل عنوان واحد. ما تم أيضاً إضافة Places Autocomplete (بحث عن عنوان بالاسم) رغم تفعيل Places API — المفتاح جاهز لهاي الميزة لو طُلبت مستقبلاً.

   **ملفات متأثرة:** `akleto-customer.html`، `akleto-driver.html`، `akleto-admin-stores.html`.

41. 🆕 **تصحيح: خطأ حرف واحد بمفتاح Google Maps API كان يسبب `InvalidKeyMapError`:**
   المفتاح المكتوب أول مرة بالكود انتهى بـ `...qoSEMY` (E كبيرة)، بينما المفتاح الفعلي بـ Google Cloud Console ينتهي بـ `...qoSeMY` (e صغيرة) — API keys حساسة لحالة الأحرف (case-sensitive). تم تصحيحه بالملفين `akleto-customer.html` و`akleto-admin-stores.html`. **الدرس المستفاد:** عند نسخ/لصق مفتاح API من مصدر خارجي (خصوصاً عبر الموبايل)، قارنه حرف بحرف مع المصدر الأصلي قبل استخدامه بالكود — التشابه البصري بين بعض الأحرف (زي E/e) سهل يفوت.

42. 🆕 **ملخص شامل لإعدادات Google Maps الحالية وحمايتها (للرجوع له عند أي مشكلة مستقبلية):**

   **الإعدادات النهائية المستقرة:**
   - المفتاح الصحيح ينتهي بـ `...qoSeMY` (e صغيرة بآخر حرف قبل MY — **ليس** E كبيرة، هاي الغلطة اللي صارت سابقاً).
   - **Application restrictions:** Websites، مقيّد على `moayyad44.github.io/*` بس.
   - **API restrictions:** Restrict key → Maps JavaScript API + Places API + Geocoding API فقط.
   - **Billing:** مربوط بـ "My Billing Account"، حالته Active.
   - **حماية إضافية:** Quota محدّد بـ **500 طلب/يوم** (Maps JavaScript API) من صفحة Quotas بمشروع akalito، لمنع أي استخدام غير طبيعي بدون ما يوقف الاستخدام الطبيعي.
   - **Budget Alert:** ميزانية تنبيه بقيمة **$2/شهر** بحساب الفوترة — بترسل إيميل تنبيه فقط (ما توقف الخدمة تلقائياً؛ الحماية الفعلية للتوقف هي الـ Quota أعلاه).

   **🔧 دليل تشخيص سريع لو رجعت مشكلة "الخريطة ما تظهر" مستقبلاً:**
   1. افتح Developer Console بالمتصفح (F12 → Console) وشوف السطر الأحمر يلي ينتهي بـ `...MapError` — هذا بيحدد السبب مباشرة (أو لو عالموبايل، الكود مبرمج يعرض alert تلقائي بنفس الرسالة بفضل `window.gm_authFailure`).
   2. `InvalidKeyMapError` → قارن المفتاح بالكود مع المفتاح الفعلي بـ Google Cloud Console حرف بحرف (احتمال تغيّر المفتاح أو انعمل Regenerate بالغلط).
   3. `RefererNotAllowedMapError` → تأكد الصيغة المكتوبة بـ Website restrictions هي بالضبط `moayyad44.github.io/*` (بدون https://، بدون www، بدون مسافات).
   4. `ApiNotActivatedMapError` → تأكد الثلاث APIs (Maps JavaScript, Places, Geocoding) مفعّلة من APIs & Services → Library.
   5. `BillingNotEnabledMapError` أو رفض عام رغم الإعدادات صحيحة → تأكد حساب الفوترة "Active" وليس بوضع "Free Trial" غير مفعّل بالكامل (زر "Activate full account").
   6. لو تجاوزت الـ 500 طلب/يوم بالخطأ (مثلاً اختبار مكثف)، الخريطة بتتوقف مؤقتاً لحد اليوم التالي — ارفع الـ Quota مؤقتاً من نفس صفحة Quotas لو محتاج.

40. ✅ **`google-services.json` تمت إضافته فعلياً — إشعارات الدفع لتطبيق الزبون صارت جاهزة تقنياً بالكامل من جهة الاستقبال:**
   - مؤيد سجّل تطبيق أندرويد بمشروع `akleto-prod` على Firebase Console باسم الحزمة `com.akalito.customer` وحمّل ملف `google-services.json`، ثم رفعه هون فوق المحادثة.
   - تم التحقق من صحة الملف (JSON صالح + `package_name` مطابق تماماً لـ`com.akalito.customer`) ووضعه بالمسار الصحيح: `mobile-customer/android/app/google-services.json`، ثم دفعه للمستودع.
   - هذا يعني إنه بأول بناء APK جاي من جهاز مؤيد، خطوة `google-services` بـ Gradle رح تنفّذ تلقائياً (كانت مشروطة بوجود هذا الملف — راجع البند 39)، وميزة استقبال إشعارات الدفع (تسجيل توكن الجهاز بمستند الزبون) رح تشتغل فعلياً لأول مرة.
   - **لسا ناقص (نفس الملاحظة من البند 39، ما تغيّرت):** الإرسال الفعلي للإشعارات (Cloud Function عند تغيّر حالة الطلب) لسا ما تم البدء فيه — مقرر لجلسة قادمة.
   - **ملاحظة أمنية بسيطة:** ملف `google-services.json` تمت إضافته للمستودع (commit) بشكل طبيعي ومقصود — هذا الملف ليس سرياً بنفس معنى مفاتيح API الحساسة (الحماية الفعلية تعتمد على قيود اسم الحزمة + قواعد أمان Firestore/Firebase، مش على إخفاء الملف)، وهذه ممارسة قياسية بمشاريع Firebase/Android.

## 25 يوليو 2026 — إصلاح: خطأ تسجيل دخول الزبون على أجهزة جديدة (permission-denied)
- **العرض:** تسجيل زبون جديد من جهاز ثاني (لم يسجّل عليه من قبل) كان يفشل برسالة عامة "صار خطأ — جرب مرة ثانية"، بينما الجهاز الأول يعمل بشكل طبيعي (لأن عنده جلسة `customerId` محفوظة مسبقاً بـ localStorage وما بيمر بعملية `addDoc` من جديد).
- **السبب الحقيقي:** قواعد أمان Firestore (Security Rules) كانت تفرض `allow read, write: if request.auth != null` على كل المجموعات تقريباً — بينما تطبيقات الزبون/السائق/المتجر مصممة أصلاً **بدون Firebase Auth حقيقي** (تسجيل بالاسم+الهاتف يُخزّن بـ localStorage فقط، حسب القرار الموثّق مسبقاً). أي `addDoc`/`updateDoc` بدون جلسة Auth كان يُرفض فوراً بخطأ `permission-denied`.
- **خطوة التشخيص:** عُدّل مؤقتاً `errBox.textContent` بدالة `startCustomerSession` (وملفات مشابهة) ليعرض `e.code` و`e.message` الفعليين بدل الرسالة العامة، وهذا كشف الخطأ `permission-denied — Missing or insufficient permissions` مباشرة على شاشة الجهاز الثاني. بعد التأكد من الحل أُرجعت الرسالة العامة "صار خطأ — جرب مرة ثانية" (commit منفصل).
- **الحل المُطبَّق:** إعادة كتابة قواعد Firestore بالكامل من Firebase Console (Firestore Database → Rules → Publish) بحيث:
  - المجموعات المفتوحة على الجميع بدون Auth (بما يطابق تصميم التطبيقات الحالي): `customers` (+ subcollection `addresses`)، `drivers`، `orders`، `notifications`، `user_notifications`، وقراءة (+ إنشاء موظف) على `stores`/`store_employees`.
  - المجموعات التي تحتاج Auth للكتابة فقط (لوحة الأدمن): `meals`, `ingredients`, `meal_ingredients`, `measure_config` (قراءة عامة `if true`، كتابة `if request.auth != null`)، وكذلك تعديل/حذف `stores`.
  - أي مجموعة مستقبلية غير محددة صراحة: تبقى محمية افتراضياً (`if request.auth != null`) كإجراء أمان احترازي.
- **⚠️ نقطة أمان معروفة ومقبولة مؤقتاً:** فتح `customers`/`orders`/`drivers` بالكامل (`if true`) يعني أي حد يعرف بنية الـ collection نظرياً يقدر يقرأ/يكتب بيانات أي زبون آخر — نفس نقطة الضعف الموثقة مسبقاً تحت "بناء نظام دخول حقيقي قبل الإطلاق الفعلي" (لا زالت أولوية قبل أي إطلاق جماهيري). القواعد الحالية أولوية = "خلي التطبيق يشتغل بمرحلة الاختبار"، مش قواعد إنتاج نهائية.
- **درس عام لأي جلسة قادمة:** لو صار `permission-denied` مفاجئ بعد ما كان التطبيق شغّال، السبب الأكثر شيوعاً هو تغيّر/انتهاء قواعد Firestore (خصوصاً Test Mode الافتراضي يلي عنده تاريخ انتهاء تلقائي ~30 يوم) — أول خطوة تشخيص يجب أن تكون: (1) عرض `e.code`/`e.message` الفعلي مؤقتاً بمكان رسائل الخطأ العامة، (2) مراجعة Firestore Rules مباشرة من Console قبل افتراض أي سبب تقني آخر (شبكة، كود، إلخ).

## 25 يوليو 2026 — ميزة: فصل شاشة "حساب جديد" عن "تسجيل الدخول" بتطبيق الزبون
- **شاشة الترحيب (`welcomeScreen`)** صار فيها تبويبين بالأعلى: **"حساب جديد"** (نشط افتراضياً) و**"تسجيل الدخول"**.
  - **حساب جديد:** نفس الحقول القديمة (اسم + هاتف) — قبل الإنشاء صار فيه تحقق مسبق (`getDocs` بـ `where('phone','==',phone)`) إنه ما في حساب موجود مسبقاً بنفس الرقم؛ لو موجود، تظهر رسالة توجّه المستخدم لتبويب "تسجيل الدخول" بدل إنشاء حساب مكرر.
  - **تسجيل الدخول:** حقل الهاتف فقط (حقل الاسم يختفي بهذا الوضع). يبحث عن الزبون بـ `where('phone','==',phone)`، لو موجود وغير محظور (`is_blocked`) يسجّل دخول مباشرة بنفس `customerId` المحفوظ بـ Firestore، لو غير موجود يوجّه المستخدم لتبويب "حساب جديد".
- **دوال جديدة/معدّلة:** `switchAuthMode(mode)` (يبدّل الواجهة بين الوضعين)، `handleAuthSubmit()` (نقطة الدخول الموحدة لزر "ابدأ/دخول" حسب `authMode` الحالي)، `signInCustomer()` (تسجيل الدخول بالهاتف فقط). `startCustomerSession()` بقيت لإنشاء الحساب الجديد بس أضيف لها تحقق تكرار الهاتف. `logoutCustomer()` صار يرجّع الشاشة لوضع "حساب جديد" الافتراضي عند الخروج.
- **⚠️ ملاحظة مهمة:** هذا **ليس** نظام Auth حقيقي (لا كلمة مرور ولا OTP ولا Firebase Auth) — أي حد يعرف رقم هاتف زبون آخر يقدر "يسجّل دخول" كأنه هو (بما إن رقم الهاتف هو المعرّف الوحيد). هذا تحسين لتجربة الاستخدام (منع تكرار الحسابات، السماح بالدخول من جهاز جديد بنفس الحساب) **وليس** حل لمشكلة الأمان الأساسية الموثقة سابقاً تحت "بناء نظام دخول حقيقي قبل الإطلاق الفعلي" — لازالت هذه أولوية قبل أي إطلاق جماهيري واسع.

## 26 يوليو 2026 — مراجعة: نقطتين من قائمة "عيوب تطبيق الزبون"
1. **✅ تبيّن إنه محلولة مسبقاً:** "أقرب متجر يعتمد على GPS الحي بدل العنوان المحفوظ" — بالمراجعة لقيت `getRoutingCoords()` (مضافة بجلسة سابقة) أصلاً **تُفضّل دايماً إحداثيات العنوان الافتراضي المحفوظ**، وترجع للموقع الحي (`navigator.geolocation`) بس كخطة بديلة لو ما في عنوان محفوظ بإحداثيات إطلاقاً. مستخدمة فعلياً بـ `findBestStoreForIngredients()` و`computeEstimatedDelivery()`. القيد المتبقي الوحيد: لو الزبون ما ضاف أي عنوان أصلاً ورفض صلاحية GPS، التوزيع لسا بيتجاهل المسافة (يختار أول متجر مؤهّل). هذا سيناريو حافة (edge case) مقبول حالياً — الحل الفعلي هو تشجيع/إجبار إضافة عنوان قبل أول طلب، قرار مستقبلي منفصل.

2. **🆕 أُضيفت خريطة نظرة عامة بشاشة "عناويني"** (القائمة، مو بس شاشة إضافة/تعديل عنوان واحد):
   - عنصر جديد `#addrOverviewMap` أعلى قائمة البطاقات، يظهر بس لو في عنوان واحد ع الأقل بإحداثيات صالحة (وإلا مخفي تماماً).
   - يعرض نقطة (marker) لكل عنوان محفوظ بأيقونة تطابق نوعه (نفس `addressIconFor()` المستخدمة بالبطاقات)، والعنوان الافتراضي بأولوية عرض أعلى (`zIndex`).
   - الضغط على أي نقطة بالخريطة يفتح مباشرة شاشة تعديل ذاك العنوان (`openAddressForm(id)`).
   - لو أكثر من عنوان: الخريطة تتوسّع تلقائياً (`fitBounds`) لتشمل كل النقاط دفعة وحدة.
   - تتحدّث تلقائياً بعد أي حفظ/حذف/تعيين عنوان افتراضي جديد (نفس الأماكن يلي بتستدعي `renderAddressesList()`).
   - دوال جديدة: `renderAddressesOverviewMap()`, `destroyAddrOverviewMap()`. متغيرات جديدة: `addrOverviewMapInstance`, `addrOverviewMarkers`.

## 26 يوليو 2026 — تراجع: إزالة خريطة نظرة عامة من شاشة "عناويني"
تمت إضافة خريطة نظرة عامة (`#addrOverviewMap`) بشاشة "عناويني" بجلسة سابقة بنفس اليوم، لكن مؤيد جرّبها وما عجبته شكلها بصرياً — تم **حذفها بالكامل** (HTML + CSS + JS: `renderAddressesOverviewMap`, `destroyAddrOverviewMap`, المتغيرات `addrOverviewMapInstance`/`addrOverviewMarkers`، وكل الاستدعاءات). شاشة "عناويني" رجعت لشكلها القديم: قائمة بطاقات فقط بدون خريطة، والخريطة تبقى موجودة بس بشاشة إضافة/تعديل عنوان واحد (`#addrMap`) كما كانت.
**درس:** ميزات بصرية/تصميمية (خصوصاً إضافة خرائط أو عناصر جديدة على شاشات موجودة) يفضّل اقتراحها أو وصفها لمؤيد قبل التنفيذ الكامل، أو تنفيذها بشكل بسيط قابل للتراجع السريع.

## 26 يوليو 2026 — بداية هوية بصرية: استبدال أيقونات الإيموجي بـ SVG مخصصة (تطبيق الزبون)
بعد نقاش عن هوية التطبيق البصرية، اتفقنا نبدأ بأسرع تحسين تأثيراً: استبدال الإيموجي (يختلف شكله حسب نوع الجهاز/الموبايل) بأيقونات SVG موحّدة بلون العلامة التجارية. أول خطوتين منفّذتين بـ `akleto-customer.html`:
- **الشريط السفلي (bottom-nav):** الأيقونات الأربعة (🏠🧺🛒👤) استُبدلت بأيقونات SVG خطية (`stroke="currentColor"`) — بتاخذ لون النص تلقائياً فتنسجم مع حالة "نشط/غير نشط" الموجودة أصلاً بدون أي تعديل إضافي بالمنطق.
- **شعار شاشة الترحيب:** استُبدل إيموجي 🍽️ بشعار SVG بسيط مصمَّم خصيصاً (دائرة بلون العلامة التجارية `#A23E48` فيها رسمة وعاء طعام + بخار)، ضمن `<div class="logo-mark">`.
**المتبقي من نفس المسار (لجلسة قادمة لو حبينا نكمل):** إيموجي القائمة الجانبية (📍💳⚙️⏻)، أيقونات شاشة المنتجات/بطاقات الوجبات، أيقونة `cart-fab`، وربما تطبيق نفس شعار SVG بتطبيقات السائق/المتجر/الأدمن لتوحيد الهوية عبر كل المنصة.

## 26 يوليو 2026 — استكمال هوية بصرية: أيقونات القائمة الجانبية والإعدادات
استكمالاً لاستبدال الإيموجي بأيقونات SVG (بدأ بالشريط السفلي وشعار الترحيب بجلسة سابقة نفس اليوم):
- **القائمة الجانبية (Drawer):** كل عناصر `.ic-box` (🏠 الرئيسية، 🛒 سلتي، 📦 طلباتي وحسابي، 📍 عنواني، 💳 معلومات الدفع، ⚙️ الإعدادات، ⏻ تسجيل الخروج) استُبدلت بأيقونات SVG خطية `stroke="currentColor"` — بتاخذ لون العلامة التجارية بدل ما تبقى إيموجي رمادي/ملوّن بشكل ثابت.
- **شاشة الإعدادات:** نفس المعالجة لعناصرها (🔔 الإشعارات، 📄 الشروط والأحكام، 🔒 سياسة الخصوصية).
- **شعار النص:** تبسيط "أكليتو 🍽️" إلى "أكليتو" فقط بشريط العنوان العلوي (`topbar-logo`) وتذييل القائمة الجانبية — الإيموجي كان زيادة غير ضرورية جنب النص.
- CSS: أضيف `.cs-nav-item .ic-box svg { width:18px; height:18px; }` لضبط حجم الأيقونات الجديدة داخل الدائرة الملوّنة.
**المتبقي من نفس المسار (لو حبينا نكمل بجلسة قادمة):** أيقونات شاشة المنتجات/بطاقات الوجبات، أيقونة `cart-fab`، إيموجي زخرفية أصغر (🔔 بعنوان الشيت، 👋 بالترحيب، ✅ بالأزرار — أولوية أقل لأنها مدموجة بالنص مش أيقونات مستقلة)، وتطبيق نفس شعار SVG بتطبيقات السائق والمتجر ولوحة الأدمن لتوحيد الهوية عبر كل المنصة.

## 26 يوليو 2026 — تصحيح: مشكلة Referrer الخاصة بخرائط Google انحلّت
مؤيد أكّد إن الخريطة شغّالة تمام حالياً وقيد الـ Referrer على مفتاح Google Maps API اتظبط وانحلّت المشكلة الموثقة سابقاً (كانت مسجّلة كـ"نقطة معلّقة"). لا حاجة لأي متابعة إضافية بهذا الخصوص إلا لو صار عطل جديد لاحقاً.
**تفعيل الإشعارات الفعلي (Cloud Function + google-services.json):** مؤجَّل مؤقتاً بطلب مؤيد — مش أولوية حالياً.
**الأولوية الحالية:** الاستمرار بضبط/تحسين الهوية البصرية للتطبيق (مسار مستمر من نفس اليوم).

## 1 أغسطس 2026 — 🆕🔴 تغيير معماري جذري: فصل تطبيق السائق لملفات منفصلة + واجهة رئيسية جديدة بخريطة كاملة الشاشة

**السبب:** بطلب مؤيد صريح — بداية تطوير موسّعة لتطبيق السائق، وأول خطوة هي فصل كل شاشة لملف HTML مستقل (نفس نمط فصل المتجر عن الزبون بجلسة سابقة)، بالإضافة لواجهة رئيسية جديدة كلياً مبنية على خريطة بدل قائمة نصية.

**أ) الفصل المعماري — 5 ملفات بدل ملف واحد:**
- `akleto-driver.html` — صار **شاشة الدخول/التسجيل فقط** (اسم + هاتف، نفس المنطق القديم بدون تغيير في `drivers` — أُضيف حقل `is_available:false` افتراضياً عند الإنشاء). بعد نجاح الدخول أو وجود جلسة محفوظة، يحوّل تلقائياً لـ `akleto-driver-home.html`.
- `akleto-driver-home.html` — **جديد كلياً، الواجهة الرئيسية الجديدة** (تفاصيل بالفقرة ب أدناه).
- `akleto-driver-orders.html` — شاشة "الطلبات المتاحة" (كانت `homeScreen` بالملف القديم) — نفس منطق `claimOrder` (Transaction) وورقة تفاصيل الطلب.
- `akleto-driver-deliveries.html` — شاشة "توصيلاتي" (قيد التوصيل + سجل سابق) — نفس منطق `markDelivered`.
- `akleto-driver-account.html` — شاشة "حسابي" (إحصائيات + تسجيل خروج + حذف الحساب).

**ملف جديد مشترك `driver-shared.js` (ES Module):** يجمع كل المنطق المتكرر بين الملفات الخمسة بدل تكراره يدوياً بكل ملف (خلافاً لنمط "تكرار الشل يدوياً" المتّبع بصفحات الأدمن) — يصدّر: إدارة الجلسة (`getDriverSession`, `requireDriverSession`, `saveDriverSession`, `logoutDriver`), حذف الحساب (`deleteDriverAccountById`), أدوات عرض (`ticketCode`, `timeAgo`, `timeAgoNotif`, `showToast`), التوفر والموقع الحي (`setDriverAvailability`, `updateDriverLocation`), الإشعارات (`watchDriverNotifs`, `updateNotifBadge`, `renderNotifList`), ومستمعي الطلبات/السائقين (`watchAvailableOrders`, `watchMyOrders`, `watchHotspotOrders`, `watchActiveDrivers`, `setNavBadge`). كل صفحة تستورد منه ما تحتاجه.

**⚠️ الشريط السفلي (bottom-nav) والقائمة الجانبية (drawer) وشيت الإشعارات لا زالت مُكرَّرة كـ HTML يدوياً بكل ملف** (نفس نمط الأدمن الموثّق سابقاً) — التنقّل بينها صار عبر روابط `<a href>`/`location.href` عادية (تحميل صفحة كاملة) بدل `switchScreen()` الداخلية القديمة، لأنها ملفات منفصلة فعلياً الآن.

**ب) الواجهة الرئيسية الجديدة (`akleto-driver-home.html`) — خريطة كاملة الشاشة:**
- **خريطة Google Maps تملأ كامل الشاشة** (`#map`, position:fixed كامل الشاشة خلف كل العناصر) — نفس مفتاح ونمط التحميل الكسول (`loadGoogleMapsApi`) المستخدم أصلاً بـ `akleto-customer.html`/`akleto-admin-stores.html`، بالإضافة لمعالج `window.gm_authFailure` (توست تحذيري بدل console فقط) لتشخيص أخطاء المفتاح مباشرة على الشاشة.
- تُهيَّأ افتراضياً على مركز عمّان، وتحاول صامتاً توسيط نفسها على موقع السائق الفعلي عند فتح الصفحة (`centerOnMyLocation(true)`).
- **الأزرار العائمة فوق الخريطة (بالضبط كما طُلب):**
  1. **☰ زر القائمة الجانبية** — بأعلى يمين الشاشة (داخل شريط علوي شفاف متدرّج `.map-topbar` يطفو فوق الخريطة بدون ما يحجبها بالكامل).
  2. **🔴/🟢 زر متاح/غير متاح** (`.avail-toggle`) — كبسولة بارزة أسفل الشريط العلوي مباشرة، تتحول لونها (رمادي↔زيتوني) حسب الحالة. الضغط عليها وهي "غير متاح": يطلب صلاحية الموقع، يحدّث `drivers/{id}` بـ `is_available:true` + `lat`/`lng` + `availability_updated_at`، يبدأ `navigator.geolocation.watchPosition` (تحديث موقع السائق بـ Firestore كل ~12 ثانية كحد أقصى، تفادي كتابات زائدة). الضغط عليها وهي "متاح": يوقف التتبّع ويحدّث `is_available:false`. الحالة تُحفظ محلياً (`akleto_driver_available`) فتُستأنف تلقائياً عند إعادة فتح الصفحة.
  3. **🎯 زر موقعي الحالي** — يوسّط الخريطة على موقع الجهاز الحي (زوم 15) بدون علاقة بحالة التوفر.
  4. **🏪 زر مواقع تكاثر الطلبات** — زر تبديل (toggle): يجيب كل الطلبات النشطة (`status in ['pending','preparing','ready']`) لحظياً عبر `watchHotspotOrders`، يجمعها حسب `store_id`، ويعرض نقطة بنفسجية بحجم متدرّج (10-22px) وعدد الطلبات كـ label فوق موقع كل متجر (من كاش `stores` المحمّل مرة وحدة). المتاجر بدون إحداثيات (`lat`/`lng`) تُستبعد تلقائياً (نفس القيد الموثّق سابقاً بخوارزمية "أقرب متجر").
  5. **🛵 زر حركة السائقين لايف** — زر تبديل: يراقب `drivers` where `is_available==true` لحظياً عبر `watchActiveDrivers`، ويعرض أيقونة دراجة نارية خوخية اللون لكل سائق متاح غير السائق الحالي نفسه (السائقين بدون `lat`/`lng` مُستبعدين لأنهم لسا ما فعّلوا التوفر أو ما أعطوا صلاحية الموقع).
  - كل زر Toggle يعرض عداد صغير (`.fab-count`) فوقه بعدد النقاط المعروضة حالياً، ويتلوّن (`active`) لما يكون مفعّل. صندوق توضيحي صغير (`.legend-box`) يظهر أسفل يمين الشاشة فقط لما يكون أي من الطبقتين مفعّلة، يشرح لون كل نقطة.
  - **لتوفير القراءات من Firestore:** المستمعين (`onSnapshot`) لطبقتي "المتاجر الساخنة" و"السائقين اللايف" يبدآن فقط عند الضغط على الزر المعني (lazy)، ويُفصلان (`unsubscribe`) فوراً عند إلغاء التفعيل — ما يشتغلان تلقائياً بالخلفية بمجرد فتح الصفحة.
- الشريط السفلي (bottom-nav) على هالصفحة فيه 4 تبويبات: **الرئيسية (نشط)، المتاحة، توصيلاتي، حسابي** — بشارات عدد حيّة (`availBadge`/`myBadge`) بنفس منطق الملفات القديمة.

**ج) حقول جديدة على وثيقة `drivers`:** `is_available` (bool، افتراضي `false` عند التسجيل)، `lat`/`lng` (آخر موقع معروف)، `location_updated_at`، `availability_updated_at`.

**⚠️ نقاط ناقصة/معروفة لأي جلسة قادمة:**
- **قواعد Firestore الحالية تسمح بالفعل بالقراءة/الكتابة المفتوحة على `drivers`** (موثّق بجلسة "25 يوليو 2026") فما احتجنا أي تعديل على الـ Rules عشان ميزة الموقع الحي تشتغل — بس هاي نفس نقطة الضعف الأمنية العامة الموثّقة (لا يوجد Auth حقيقي).
- **لوحة الأدمن (`akleto-admin-drivers.html`) ما زالت لا تعرض `is_available` ولا آخر موقع** — ممكن تحسين مستقبلي (مثلاً نقطة خضراء "متصل الآن" على بطاقة كل سائق).
- **لا يوجد حالياً Cloud Function أو تنظيف تلقائي** لسائق ترك المتصفح مفتوح بدون إغلاق التطبيق (`is_available` ممكن تظل `true` لو أغلق التبويب فجأة بدل الضغط "غير متاح") — نقطة تحسين مستقبلية (مثلاً اعتماد `location_updated_at` لإخفاء أي سائق "لايف" ما تحدّث موقعه من أكتر من X دقيقة بدل الاعتماد فقط على `is_available`).
- **`akleto-admin-stores.html` هو المصدر الوحيد لإحداثيات المتاجر** المستخدمة بطبقة "مواقع التكاثر" — أي متجر ما زالت إحداثياته ناقصة (نقطة معروفة سابقاً) ما رح يظهر كنقطة ساخنة أبداً حتى لو عنده طلبات فعلية.
- **لم يُلمس أي ملف أدمن أو تطبيقي الزبون/المتجر بهاي الجلسة** — التغيير محصور بالكامل بملفات السائق الخمسة + `driver-shared.js` الجديد.

**الخطوة التالية المقترحة (بانتظار توجيه مؤيد):** توسيع شاشات "الطلبات المتاحة"/"توصيلاتي" لتستفيد من نفس الخريطة (مثلاً عرض الطلب المطلوب توصيله كخط مسار على الخريطة)، أو الاستمرار بتطوير شاشات إضافية للسائق حسب ما يحدده مؤيد.

## 1 أغسطس 2026 — إصلاح: اختفاء الخريطة كاملة الشاشة بـ `akleto-driver-home.html` على الموبايل + إزالة الشريط السفلي من تطبيق السائق

- **العرض:** بعد بناء الواجهة الرئيسية الجديدة لتطبيق السائق (خريطة كاملة الشاشة)، الخريطة كانت تظهر لجزء من الثانية بالضبط ثم تختفي بالكامل (كل الأزرار العائمة فوقها كانت تضل ظاهرة بشكل طبيعي) — بس على الموبايل تحديداً؛ خرائط باقي المشروع (`akleto-admin-stores.html`, `akleto-customer.html`) ضلّت شغّالة بشكل طبيعي طول الوقت، وهذا أكّد إنه المشكلة مش بمفتاح Google Maps API نفسه ولا بالإعدادات (referrer/billing/quota) بل بشي خاص بهاي الصفحة تحديداً.
- **السبب الجذري:** `akleto-driver-home.html` هي **أول خريطة بكامل المشروع تاخذ الشاشة كاملة** (`position:fixed` بارتفاع `100vh`/`bottom:0`) — بينما كل خرائط المشروع السابقة (المتاجر، العناوين) بأحجام ثابتة صغيرة (200-230px) جوا حاويات عادية. على متصفحات الموبايل (كروم أندرويد بالذات)، شريط عنوان المتصفح المتحرك (بيختفي عند التمرير) بيسبب مشكلة معروفة: قيمة وحدة `vh` بتتغيّر فعلياً لحظة تحميل الصفحة (قبل ما يستقر ارتفاع نافذة العرض الحقيقي)، فخريطة Google Maps بترسم بحجم/موضع غلط لحظة إنشائها الأولى وتختفي عملياً — نفس مشكلة "خريطة بحجم صفر" الموثّقة سابقاً لكن بصيغة أشد لأنها كاملة الشاشة.
- **الحل المُطبَّق (بـ `akleto-driver-home.html` فقط):**
  1. تحديد ارتفاع حاوية الخريطة **بالبكسل مباشرة عبر جافاسكربت** (`mapEl.style.height = window.innerHeight + 'px'`) بدل الاعتماد فقط على CSS (`vh`/`fixed`) — يُستدعى قبل إنشاء الخريطة وبعدها 3 مرات (100/300/800 مِلّي ثانية) لالتقاط أي تغيّر متأخر بارتفاع شريط المتصفح.
  2. إضافة وحدة `height:100dvh` (Dynamic Viewport Height — مصممة تحديداً لهاي المشكلة، مدعومة على المتصفحات الحديثة) كطبقة حماية إضافية فوق `100vh` بالـ CSS.
  3. `google.maps.event.trigger(map, 'resize')` بعد كل إعادة حساب للارتفاع، بالإضافة لمستمعين على `resize`/`orientationchange` لأي تغيّر لاحق بحجم الشاشة.
  4. **معالج تشخيص دائم على الشاشة (`showMapError`):** بدل توست يختفي بعد ثوانٍ، أي خطأ بتحميل/إنشاء الخريطة (`gm_authFailure`, فشل تحميل السكربت, استثناء JS) الآن يعرض شريط أحمر ثابت أعلى الشاشة لحد ما يزول تلقائياً (لو الخريطة اشتغلت فعلياً عبر حدث `idle`) — بقي مفيد كخطوة تشخيص أولى حتى لو ما كان هو سبب هاي المشكلة تحديداً.
  5. حماية إضافية بـ `loadGoogleMapsApi()`: تحقق من وجود `<script src*="maps.googleapis.com">` بالصفحة أصلاً قبل إضافة نسخة ثانية (تفادي خطأ "included the Google Maps JavaScript API multiple times" لو تكرر التحميل بالغلط).
- **درس مهم لأي خريطة كاملة الشاشة مستقبلية** (لو بنينا خريطة full-screen بتطبيق الزبون أو المتجر لاحقاً): **لازم من أول يوم** نحدد ارتفاع الحاوية بالبكسل عبر JS (`window.innerHeight`) + `100dvh` بالـ CSS، مش نعتمد على `100vh`/`position:fixed` وحدها — خصوصاً على الموبايل.

**بنفس الجلسة — إزالة الشريط السفلي (bottom-nav) من تطبيق السائق بالكامل (الأربع صفحات):**
- بطلب مؤيد، اتشال شريط التنقّل السفلي (الرئيسية/المتاحة/توصيلاتي/حسابي) من `akleto-driver-home.html`, `akleto-driver-orders.html`, `akleto-driver-deliveries.html`, `akleto-driver-account.html` — التنقّل صار **حصراً عبر القائمة الجانبية (☰)**.
- شارات العدد الحيّة (عدد الطلبات المتاحة، عدد التوصيلات النشطة) اللي كانت على أزرار الشريط السفلي، انتقلت لعناصر القائمة الجانبية المقابلة (`availBadge`/`myBadge`) — نفس منطق `setNavBadge()` بـ `driver-shared.js` بدون أي تعديل، بس العناصر المستهدفة تغيّرت مكانها بالـ DOM.
- `body { padding-bottom }` بالـ CSS المشترك (داخل توليد الصفحات) رجع لقيمة صغيرة عادية (20px) بدل 76px (اللي كانت مخصصة لإفساح المجال للشريط السفلي المحذوف).

## 1 أغسطس 2026 — استبدال كل الإيموجي الجاهز بتطبيق السائق بأيقونات SVG مصممة + حالة تحميل لزر التوفر

**بطلب مؤيد صريح** ("ما بدي ايقونات جاهزة بدي ايقونات مصممة لتطبيق أكليتو مثل ما عملنا بتطبيق الزبون") — نفس المبدأ اللي طُبّق سابقاً على تطبيق الزبون (إزالة الإيموجي الجاهز من العناصر التفاعلية/الأيقونية واستبدالها بأيقونات خطية SVG مرسومة يدوياً بنفس أسلوب المشروع).

**أ) مكتبة أيقونات مشتركة جديدة (`ICON_PATHS` + دالة `icon()` بـ `common.py` — أداة بناء داخلية، مو ملف مرفوع على GitHub):**
- كل الأيقونات مرسومة يدوياً بأسلوب موحّد: `stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"`, `viewBox="0 0 24 24"` — نفس أسلوب أيقونات القائمة الجانبية الأصلية (home/orders/bike/person/bell/logout) اللي كانت موجودة من البداية.
- أيقونات جديدة أُضيفت: `locate` (موقعي الحالي — دائرة + خطوط تقاطع)، `store` (متجر — واجهة محل بمظلة)، `phone`, `pin` (دبوس موقع)، `meal` (وعاء طعام + بخار — نفس موتيف شعار التطبيق الأساسي بالضبط)، `gift` (إكرامية)، `note` (ملاحظة)، `map` (خريطة مطوية — رابط فتح خرائط جوجل)، `check` (تم التوصيل)، `trash` (حذف الحساب)، `history` (ساعة — توصيلات سابقة)، `spinner` (دائرة تحميل دوّارة، تُدار عبر كلاس `.spin` بأنيميشن CSS).
- استُخدمت هاي المكتبة بكل الملفات الخمسة: القائمة الجانبية (أيقونة شعار خلفية درج القائمة صارت SVG حقيقي بدل إيموجي بخلفية شفافة، بدل `::after{content:"🛵"}`)، ذيل الدرج ("أكليتو — السائق" بدون إيموجي، نفس قرار تطبيق الزبون بحذف الإيموجي من النص بدل استبداله)، شعار الهيدر العلوي (أيقونة دراجة صغيرة بخلفية دائرية ملوّنة جنب "أكليتو")، شيت تفاصيل الطلب (كل صف: متجر/زبون/هاتف/عنوان/ملاحظات/وجبات/إكرامية/رابط خرائط)، عناوين الأقسام (📦→box، 🛵→bike، 📜→history)، بطاقات الطلبات (متجر/عدد وجبات/زبون)، أزرار الإجراءات (استلام الطلب/تم التوصيل/تسجيل خروج/حذف الحساب)، وأزرار الخريطة العائمة الثلاثة بالواجهة الرئيسية (🎯→locate، 🏪→store، 🛵→bike).
- **إيموجي متبقٍّ عن قصد (منخفض الأولوية):** رسائل التوست القصيرة (`✅ تم استلام الطلب`, `⚠️ سائق ثاني سبقك`, إلخ) — إيموجي مدموج بنص رسالة عابرة مو أيقونة مستقلة، نفس التصنيف اللي اتفقنا عليه بتطبيق الزبون.

**ب) حالة تحميل (Loading) لزر متاح/غير متاح بالواجهة الرئيسية:**
- قبل هالتحديث، الزر كان يتغيّر شكله فوراً بعد نجاح الكتابة بـ Firestore بس بدون أي مؤشر أثناء فترة الانتظار (طلب صلاحية الموقع + `getCurrentPosition` + كتابة Firestore، ممكن تاخذ ثانية لثواني).
- الآن: أول ما يضغط السائق الزر، فوراً (`setAvailLoading(true, ...)`) — الزر يتعطّل عن الضغط المتكرر (`disabled`)، النقطة الملوّنة (`.dot`) تختفي وتظهر مكانها أيقونة `spinner` دوّارة (أنيميشن CSS `.spin` — دوران متواصل 0.8 ثانية)، والنص يتغيّر لرسالة توضيحية مرحلية: "جاري تحديد موقعك..." ثم "جاري التفعيل..." (أو "جاري الإيقاف..." بحالة إلغاء التوفر).
- عند انتهاء العملية (نجاح أو فشل)، `finally { setAvailLoading(false); }` يعيد الزر لحالته الطبيعية ويستدعي `renderAvailBtn()` لعرض الحالة الفعلية (متاح/غير متاح) أو رسالة خطأ توست إذا فشلت.
- هاي أول حالة Loading حقيقية (Spinner) تُضاف لتطبيق السائق — نمط قابل لإعادة الاستخدام لأي عملية تانية فيها انتظار محسوس (نفس فكرة `.spin` class + دالة `setXLoading` ممكن تتكرر بمكان تاني لو احتجنا).

**ج) لا تغيير على أي منطق بيانات أو Firestore بهالجلسة** — التعديل بصري بالكامل (أيقونات + حالة تحميل)، كل حقول `drivers`/`orders` وكل الدوال بـ `driver-shared.js` ضلّت بدون تغيير.

## 1 أغسطس 2026 — 🆕 تقارير العمل + تسديد الرصيد المستحق (قريباً) + بطاقات الائتمان (قريباً)

**بطلب مؤيد:** إضافة 3 تبويبات جديدة بالقائمة الجانبية لتطبيق السائق تحت قسم "المالية" جديد: **تقارير العمل**، **تسديد الرصيد المستحق**، **بطاقات الائتمان**. ملفان جديدان + منطق مالي جديد بـ `driver-shared.js`.

**⚠️ نموذج العمل المالي المُتفق عليه مع مؤيد (مهم لأي جلسة قادمة تلمس هالموضوع):**
- الكابتن **يستلم كامل مبلغ الطلب كاش (COD)** من الزبون وقت التسليم.
- الكابتن **يحتفظ فقط** بـ `delivery_fee` (رسوم التوصيل، حالياً ثابتة 1 دينار بتطبيق الزبون) + `driver_tip` (الإكرامية إن وُجدت) — الباقي (سعر الوجبات + `service_fee`) **مستحق لأكليتو**.
- **عمولة أكليتو** تُخصم من نصيب الكابتن نفسه (`delivery_fee + driver_tip`)، مو من كامل مبلغ الطلب.
- ⚠️⚠️ **نسبة العمولة `COMMISSION_RATE = 0.10` (10%) بـ `driver-shared.js` قيمة افتراضية مؤقتة فقط** — مؤيد صرّح إنه "لسا ما حددنا رقم" وطلب نبني الشاشات بقيمة افتراضية قابلة للتعديل. **لازم تأكيد الرقم النهائي مع مؤيد قبل أي إطلاق فعلي** — تغييره سهل: سطر واحد بـ `driver-shared.js` (`export const COMMISSION_RATE = 0.10;`) وكل الحسابات بكل الشاشات بتتحدث تلقائياً معه (كلها بتستدعي `computeOrderFinancials()`/`aggregateOrderFinancials()` المركزيتين، ما في حسابات مكررة يدوياً بأي مكان).
- **معادلة كل طلب** (بدالة `computeOrderFinancials(order)` بـ `driver-shared.js`):
  - `collected` = `total_estimated_price` (الكاش المُستلم)
  - `grossEarning` = `delivery_fee + driver_tip` (نصيب الكابتن قبل العمولة)
  - `commission` = `grossEarning × COMMISSION_RATE`
  - `netProfit` = `grossEarning − commission` (صافي ربح الكابتن الفعلي)
  - `owed` = `collected − netProfit` (المستحق لأكليتو — هاد اللي لازم الكابتن يسدده)

**أ) تقارير العمل (`akleto-driver-reports.html`) — بيانات حقيقية من Firestore، مو Mock:**
- **بطاقة "الرصيد الحالي" بأعلى الصفحة** (دايماً على كل الطلبات المكتملة من بداية الحساب، بدون تصفية بفترة): "المبالغ اللي بجيبك" (= مجموع `collected`) و"الرصيد المستحق لأكليتو" (= مجموع `owed`)، مع زر "تسديد الرصيد الآن" يودّي لصفحة التسديد.
- **مُبدّل فترة** (اليوم / هذا الأسبوع / الكل، افتراضياً "الكل") بيتحكم بالإحصائيات والقائمة تحته بس، مو ببطاقة الرصيد الحالي.
- **شبكة إحصائيات:** ساعات العمل، الكيلومترات المقطوعة (كلاهما من ورديات مقفولة فقط — بالفقرة ب أدناه)، عدد الطلبات المكتملة، صافي الأرباح — كلها محسوبة من الطلبات/الورديات المفلترة بالفترة المختارة.
- **قائمة تفاصيل الطلبات:** كل طلب مكتمل (`status:'done'`) للسائق الحالي بالفترة المختارة، مع تفكيك كامل (الكاش المحصّل / ربحك الصافي / العمولة ونسبتها / المستحق) لكل طلب على حدة.

**ب) نظام ورديات عمل جديد (لحساب ساعات العمل والمسافة المقطوعة) — Collection جديد `driver_shifts`:**
- كل وردية = `{{ driver_id, start_at, end_at, distance_km }}`.
- **تُنشأ تلقائياً** (`startShift()`) كل ما يفعّل الكابتن زر "متاح" بالواجهة الرئيسية، ويُحفظ `shift_id` بـ `localStorage` (`akleto_driver_shift_id`) عشان تنعرف الوردية المفتوحة حتى لو انسكّرت الصفحة وانفتحت من جديد وهو لسا "متاح".
- **تُقفل تلقائياً** (`endShift()`) لما يوقف التوفر، وتُحفظ المسافة المتراكمة (`distance_km`) وقتها.
- **حساب المسافة:** أثناء `watchPosition`، كل نقطة جديدة تُقاس بمعادلة Haversine (`haversineKm()`) ضد آخر نقطة، وتُجمع بمتغيّر محلي بالمتصفح (`currentShiftDistanceKm`) — **ما بتنكتب لـ Firestore إلا لحظة إغلاق الوردية فقط** (توفير كتابات). قفزات GPS غير منطقية (أقل من 5 متر أو أكبر من 2 كم دفعة وحدة) تُتجاهل تلقائياً لتفادي تضخيم المسافة بأخطاء GPS لحظية.
- **⚠️ قيد معروف (نفس مشكلة `is_available` الموثّقة سابقاً):** لو الكابتن سكّر المتصفح فجأة وهو "متاح" بدون ما يضغط "غير متاح"، الوردية بتضل مفتوحة (`end_at: null`) للأبد، والمسافة المتراكمة بهالجلسة **بتضيع كاملة** (لأنها كانت بس بذاكرة المتصفح، ما انكتبت لـ Firestore). تقارير الساعات/المسافة بتتجاهل تلقائياً أي وردية `end_at: null` (ما بتدخل بالمجموع)، فالرقم المعروض للكابتن بيكون **أقل من الواقع** بهاي الحالة، مو أكتر. تحسين مستقبلي محتمل: كتابة `distance_km` لـ Firestore بشكل دوري أثناء الوردية (كل دقيقة مثلاً) بدل بس عند الإغلاق، أو استخدام `beforeunload`/`visibilitychange` لمحاولة إغلاق الوردية بشكل أفضل.

**ج) تسديد الرصيد المستحق (`akleto-driver-settle.html`) — واجهة "قريباً" فقط، بدون دفع فعلي:**
- بطلب مؤيد صراحة (**لسا ما اخترنا بوابة دفع**) — الصفحة تعرض الرصيد المستحق الفعلي (حي من Firestore، نفس حساب صفحة التقارير) + شريط توضيحي واضح إنه الدفع الإلكتروني عبر فيزا قريباً، وصف "طرق الدفع" بحالة معطّلة (`opacity:.55`) مع شارة "قريباً" — **بدون أي زر دفع فعلي يشتغل**.

**د) بطاقات الائتمان (`akleto-driver-cards.html`) — واجهة "قريباً" فقط، بدون أي تخزين لبيانات بطاقات فعلية:**
- **قرار أمني متعمّد:** رفضت تخزين رقم بطاقة ائتمان كامل (PAN) مباشرة بـ Firestore حتى لو "بس للاستخدام لاحقاً" — قواعد Firestore الحالية مفتوحة قراءة/كتابة بالكامل (موثّق بجلسة 25 يوليو 2026)، فتخزين أرقام بطاقات فعلية فيها = خرق أمني حقيقي وربما مخالفة قانونية (PCI-DSS)، بغض النظر عن نية الاستخدام. أي نظام بطاقات فعلي **لازم** يمر عبر بوابة دفع (Tokenization) — هي اللي بتخزن بيانات البطاقة الحساسة بشكل آمن، مو تطبيقنا مباشرة.
- الصفحة حالياً: بطاقة بصرية (Mock UI فاضية بأرقام `••••`) + زر "إضافة بطاقة جديدة" **معطّل فعلياً** (`disabled`) + شريط توضيحي يشرح السبب (بانتظار HyperPay أو PayTabs أو مشابه).

**هـ) لا تغيير على منطق الاستلام/التوصيل/التوفر الأساسي** — كل الإضافات هاي طبقة جديدة فوق البنية الموجودة، الحقول والمنطق القديم بـ `orders`/`drivers` ما تأثر.

**الخطوة التالية المعلّقة (بانتظار مؤيد):** 1) تأكيد نسبة العمولة النهائية. 2) اختيار بوابة الدفع (HyperPay/PayTabs/Stripe أو غيرها) وربطها فعلياً بصفحتي التسديد والبطاقات. 3) قرار بخصوص تسوية "الرصيد المستحق" التاريخي — حالياً الحساب تراكمي منذ إنشاء كل طلب مكتمل ولا يوجد أي آلية "تصفير" الرصيد بعد أي تسديد فعلي (لأنه ما في تسديد فعلي أصلاً بعد) — لما تُربط بوابة الدفع، لازم Collection جديد (`driver_settlements` مثلاً) يسجّل كل عملية تسديد ناجحة، وتصير معادلة "الرصيد المستحق" = `مجموع owed − مجموع التسديدات الناجحة`.

## 1 أغسطس 2026 — عمولة قابلة للتحكم من الأدمن (نسبة/مبلغ ثابت) + دعم الدفع الأونلاين بحساب الرصيد المستحق

**بطلب مؤيد صريح:** بعد جلسة بناء "تقارير العمل" (نفس اليوم)، طلب مؤيد تعديلين على النموذج المالي:
1. "الزبون ممكن يدفع عن طريق الفيزا وبالحالة راح ينخفض الرصيد المستحق على الكابتن" — دعم منطقي للدفع الأونلاين بحساب المستحق.
2. "حط تبويب جديد بتطبيق الأدمن يخليني أتحكم بكل العمولات (زي عمولة السائق، نسبة أو قيمة) بدل الخدمة الموجودة بتطبيق الزبون" — نقل التحكم بالعمولة من كود مبرمج ثابت لإعداد يتحكم فيه الأدمن مباشرة.

**أ) تبويب جديد بلوحة الأدمن: `akleto-admin-settings.html` ("العمولات والإعدادات") — Collection جديد `app_settings/commissions`:**
- رابط جديد بقسم "الإعدادات" بالقائمة الجانبية لكل صفحات الأدمن (كان مجرد `<div>` ثابت غير فعّال من البداية باسم "الإعدادات" — تحول لرابط فعلي باسم "العمولات والإعدادات"). **⚠️ لأنه القائمة الجانبية مُكرّرة يدوياً بكل ملف أدمن (نفس النمط الموثّق من قبل)، اضطريت أعدّل الرابط بـ 10 ملفات أدمن دفعة وحدة عبر سكربت بايثون (regex بديل عن نسخ يدوي لكل ملف) — أي تعديل مستقبلي على القائمة الجانبية لازم يتكرر بنفس الـ 10 ملفات + `akleto-admin-settings.html` نفسها (11 ملف بالمجموع).**
- الصفحة نفسها: نموذج بسيط — اختيار نوع العمولة (نسبة مئوية % / مبلغ ثابت لكل طلب) + قيمة رقمية + معاينة حيّة (مثال حسابي فوري قبل الحفظ) + زر حفظ يكتب لمستند `app_settings/commissions`: `{{ driver_commission_type: 'percentage'|'fixed', driver_commission_value, updated_at }}`.
- **⚠️ ملاحظة تخزين مهمة:** النسبة المئوية تُخزَّن بالداتابيز ككسر عشري (10% ↔ `0.10`) — لكن حقل الإدخال بالواجهة يعرض ويقبل رقم من 0-100 (يعني "10" مش "0.10") ويحوّلها الكود تلقائياً وقت الحفظ/التحميل. أي تعديل مباشر على المستند من Firebase Console لازم ينتبه لهاي النقطة.

**ب) `driver-shared.js` — استبدال ثابت العمولة الجامد بمستمع حي:**
- حُذف `export const COMMISSION_RATE = 0.10` القديم، وحل محله:
  - `watchCommissionSettings(cb)` — يفتح `onSnapshot` حي على `app_settings/commissions`، ويحدّث متغيّر مخبّأ داخلي (`_commissionConfig`) بكل تغيير (بدون الحاجة لإعادة تحميل الصفحة — أي تعديل يحفظه الأدمن بينعكس **فوراً** على أي صفحة سائق مفتوحة حالياً).
  - `getCommissionConfig()` — قراءة القيمة المخبّأة الحالية بشكل متزامن (تُستخدم داخلياً بـ `computeOrderFinancials`).
  - قيمة احتياطية افتراضية (`{{type:'percentage', value:0.10}}`) لحد ما توصل أول قراءة فعلية من Firestore، أو لو الأدمن لسا ما حفظ أي إعداد أبداً.
  - كل صفحة تحسب أرباح/عمولة (`akleto-driver-reports.html`, `akleto-driver-settle.html`) **لازم تستدعي `watchCommissionSettings()` مرة عالأقل عند التحميل** وإلا بتضل تستخدم القيمة الاحتياطية فقط.
- `computeOrderFinancials(order)` تحدّثت لتدعم نوع العمولة الديناميكي (`cfg.type === 'fixed' ? ... : ...`)، مع سقف أمان: عمولة ثابتة ما بتتجاوز نصيب الكابتن نفسه (`Math.min(cfg.value, grossEarning)`) حتى لو الأدمن حط رقم كبير بالغلط.

**ج) دعم الدفع الأونلاين — تأثيره على "المستحق" و"الكاش بالجيب":**
- حقل `order.payment_method` (موجود أصلاً بتطبيق الزبون، حالياً دايماً `'cash_on_delivery'` لأنه خيار "بطاقة فيزا/ماستركارد" لسا معطّل بواجهة تأكيد الطلب بعلامة "قريباً" — **لم يُلمس تطبيق الزبون بهالجلسة، الواجهة هناك جاهزة أصلاً من قبل**).
- `computeOrderFinancials()`: لو `payment_method === 'card'` أو `'online'` → `isOnlinePaid: true` → **`owed` (المستحق لأكليتو) = صفر** لهاد الطلب تحديداً، لأنه الزبون دفع لأكليتو مباشرة أونلاين والكابتن أصلاً ما قبض أي كاش بيده. العمولة وصافي الربح بيضلوا يُحسبوا عادي (متعلقين بشغل التوصيل نفسه، مو بطريقة الدفع).
- `aggregateOrderFinancials()`: تجميع "الكاش بالجيب" (`collected`) **يستثني تلقائياً** أي طلب `isOnlinePaid` — بما إنه هاد الكاش أصلاً ما مرّ بإيد الكابتن.
- **⚠️ قيد معروف/تبسيط متعمّد:** لو صار الدفع أونلاين فعلياً بالمستقبل (بعد ربط بوابة دفع)، منطقياً أكليتو بتصير **مديونة للكابتن** بصافي ربحه (`netProfit`) عن هيك طلبات (لأنه هو أدّى شغل التوصيل بس ما قبض شي كاش)، مو بس "لا مستحق ولا مديونية". النظام الحالي بيسجّل `owed: 0` بس (لا يحسب مديونية عكسية لصالح الكابتن) — تحسين مستقبلي لازم لما تتفعّل ميزة الدفع الأونلاين فعلياً، غالباً بحاجة Collection جديد لتتبع "مستحقات أكليتو للكباتن" بشكل منفصل عن "مستحقات الكباتن لأكليتو".

**د) لا تغيير على تطبيق الزبون بهالجلسة** — واجهة اختيار طريقة الدفع (كاش/فيزا قريباً) بشاشة تأكيد الطلب كانت موجودة ومبنية بشكل صحيح من قبل، ما احتجنا نلمسها.

## 1 أغسطس 2026 — رصيد صافي ثنائي الاتجاه (مستحق منك/لك) + نظام طلبات تسديد/تحويل فعلي + صفحة أدمن للتأكيد

**بطلب مؤيد** (متابعة مباشرة لجلسة العمولة/الدفع الأونلاين بنفس اليوم): طلب مؤيد يتأكد إنه لو الزبون دفع فيزا، أكليتو تصير **مديونة للكابتن** برسوم التوصيل + الإكرامية (مو بس "لا شي مستحق") — وطلب نظام كامل: بنفس تبويب "تسديد الرصيد المستحق"، الرصيد يُحسب صافي مباشرة، ولون الحاوية يتغيّر حسب لمين الرصيد (أخضر = مستحق للكابتن مع زر "تحويل لحسابه البنكي"، أحمر/قرميدي = مستحق لأكليتو مع إمكانية تحديد المبلغ وتحويله).

**⚠️ هاي الجلسة استبدلت وبنت فوق جلسة "تقارير العمل + عمولة الأدمن" اللي قبلها بنفس اليوم — أي وصف سابق بـ context.md لـ "الرصيد المستحق" كرقم أحادي الاتجاه بس (owed فقط) صار قديم، اقرأ هالفقرة كمرجع نهائي.**

**أ) Collection جديد `driver_balance_transactions` — سجل كل طلبات التسديد/التحويل الفعلية:**
- كل معاملة: `{{ driver_id, driver_name, type: 'settlement'|'payout', amount, status: 'pending'|'completed'|'rejected', created_at, resolved_at }}`.
- `'settlement'` = الكابتن قدّم طلب إنه بده يسدد مبلغ لأكليتو. `'payout'` = الكابتن قدّم طلب إنه بده أكليتو تحوّله مبلغ (لأنها مدينة له).
- **⚠️ حرج:** بس المعاملات `status:'completed'` تُحتسب فعلياً بالرصيد الصافي — `'pending'` **ما بتأثر على الرصيد أبداً** لحد ما تتأكد يدوياً من لوحة الأدمن. هيك تفادينا إنه الكابتن يقدر "يصفّر" رصيده بمجرد إرسال طلب بدون ما يصير فيه تسديد/تحويل حقيقي فعلياً (بما إنه ما في بوابة دفع تتحقق أوتوماتيكياً).

**ب) `driver-shared.js` — دوال جديدة:**
- `watchDriverBalanceTransactions(driverId, cb)` — مراقبة حية لكل معاملات سائق معيّن.
- `createSettlementRequest(driverId, driverName, amount)` / `createPayoutRequest(driverId, driverName, amount)` — إنشاء طلب `pending` جديد.
- `computeNetBalance(orderTotals, transactions)` — **الدالة المركزية الجديدة** اللي بتجمع كل شي: تاخذ ناتج `aggregateOrderFinancials()` (يحتوي الآن حقل إضافي `owedToDriver` = مجموع صافي أرباح الطلبات المدفوعة أونلاين) + قائمة المعاملات، وبترجّع:
  - `owedByDriver` = (owed من الطلبات) − (مجموع تسديدات `completed`) — لا تقل عن صفر.
  - `owedToDriver` = (owedToDriver من الطلبات) − (مجموع تحويلات `completed`) — لا تقل عن صفر.
  - `net` = `owedByDriver − owedToDriver`. **موجب = الكابتن مدين لأكليتو. سالب = أكليتو مدينة للكابتن. صفر = متوازن.**
- `aggregateOrderFinancials()` تحدّثت لتحسب `owedToDriver` (صافي ربح كل الطلبات المدفوعة أونلاين، بغض النظر عن أي تحويل فعلي — هاي مسؤولية `computeNetBalance` بعدين).

**ج) `akleto-driver-settle.html` — الآن تفاعلية فعلياً، مو "قريباً" فاضية:**
- بطاقة رصيد ديناميكية اللون (CSS classes: `.owe-akleto` أحمر/قرميدي، `.owe-driver` أخضر، `.neutral` رمادي) حسب إشارة `net`.
- **لو `net > 0` (الكابتن مدين لأكليتو):** يظهر مربع إدخال مبلغ (معبّى مسبقاً بكامل المبلغ المستحق، قابل للتعديل لأي مبلغ أقل أو يساوي) + زر "إرسال طلب التسديد" → `createSettlementRequest()`.
- **لو `net < 0` (أكليتو مدينة للكابتن):** زر واحد "طلب تحويل الرصيد كامل لحسابي" (بدون إدخال مبلغ — التحويل دايماً بكامل `owedToDriver` المتبقي) → `createPayoutRequest()`.
- **لو `net = 0`:** رسالة "رصيدك متوازن 🎉" بدون أي زر إجراء.
- شريط تحذيري ثابت يوضّح إنه ما في بوابة دفع/تحويل بنكي فعلية — أي طلب بيضل `pending` لحد ما الإدارة تراجعه وتنفّذه يدوياً (تحويل بنكي حقيقي أو تسديد كاش حسب الاتفاق) وتأكّده من لوحة الأدمن.
- **سجل الطلبات** (`txList`) — يعرض كل طلبات السائق (تسديد/تحويل) مع حالتها (قيد الانتظار/تم/مرفوض) بالترتيب الزمني.
- `akleto-driver-reports.html` تحدّثت بنفس منطق `computeNetBalance()` بالضبط (نفس الألوان، بطاقة الرصيد بأعلى الصفحة) — زر "إدارة الرصيد" يودّي لصفحة التسديد.

**د) صفحة أدمن جديدة: `akleto-admin-settlements.html` ("طلبات التسديد والتحويل") — رابط جديد بالقائمة الجانبية لكل ملفات الأدمن الـ11 (بعد رابط "العمولات والإعدادات" مباشرة، بنفس أسلوب التعديل بـ regex عبر Python دفعة وحدة، **مو نسخ يدوي** — أي تعديل مستقبلي على القائمة الجانبية لازم يشمل هالملف الجديد كمان، صار المجموع 12 ملف فيهم نفس القائمة المكرّرة):**
- تبويبين: "قيد الانتظار" (افتراضي) و"السجل السابق".
- كل طلب `pending` فيه زرّين: **"تأكيد ✓"** (بيحوّل الحالة `completed` + `resolved_at`، بعد `confirm()` توضيحي يفرّق بين "استلمت المبلغ من الكابتن" أو "حوّلت المبلغ للكابتن" حسب النوع) و**"رفض ✕"** (`rejected`).
- شارة عدد حية على رابط القائمة الجانبية (`settleNavBadge`) تعرض عدد الطلبات `pending` بكل الوقت — بتساعد الأدمن يلاحظ فوراً لما يوصل طلب جديد بدون ما يفتح الصفحة.
- ⚠️ **مافي تحقق تلقائي إنه الأدمن فعلاً نفّذ التحويل/التسديد الحقيقي قبل الضغط "تأكيد"** — هاد إجراء ثقة يدوي بالكامل (لأنه ما في بوابة دفع)، لازم انتباه الأدمن الفعلي إنه ما يأكد إلا بعد التنفيذ الحقيقي فعلاً.

**هـ) قيد معروف/تبسيط متعمّد لسا قائم:** لو صار عندنا لاحقاً كتير طلبات معلّقة بنفس الوقت لنفس الكابتن (مثلاً طلب تسديد جزئي + طلب تسديد جزئي تاني)، حالياً كل طلب مستقل تماماً عن الباقي — ما في قفل (lock) يمنع الكابتن من إرسال أكتر من طلب لنفس المبلغ المتاح بنفس الوقت (مثلاً يرسل طلبين تسديد بنفس القيمة قبل ما الأدمن يراجع أي وحدة منهم). التحقق الوحيد الحالي هو حد أقصى ثابت وقت الإرسال (`amount > latestBalance.owedByDriver`) مبني على آخر رصيد معروف بالمتصفح لحظة الإرسال، مو قفل معاملاتي فعلي بقاعدة البيانات. تحسين مستقبلي ممكن لو صار الموضوع مصدر التباس عملي.

## 1 أغسطس 2026 — 📋 خطة مستقبلية (غير منفّذة بعد): ربط بوابة الدفع + آلية التسديد/التحويل النهائية

**⚠️ هاي فقرة تخطيط/نقاش فقط — لا شي منها منفّذ بالكود حالياً.** جاءت من نقاش مباشر مع مؤيد بعد بناء "طلبات التسديد والتحويل" (نفس اليوم، الفقرة اللي قبل هاي) — أكّد مؤيد إنه هالتبويب **حل مؤقت** لحد ما تتفعّل بوابة دفع حقيقية، ونقاش شكل الحل النهائي. **مؤيد قرر صراحة تأجيل تنفيذ خانتي الآيبان والبطاقة الائتمانية بالكامل حالياً** — هاي الفقرة توثيق للخطة فقط عشان الرجوع لها لما نبلش التنفيذ الفعلي.

**أ) جهة "الكابتن يدفع لأكليتو" (تسديد الرصيد المستحق منه) — بعد ربط بوابة الدفع:**
- الكابتن يدفع مباشرة ببطاقته الائتمانية عبر بوابة الدفع (Visa/Mastercard).
- بمجرد ما البوابة ترجّع تأكيد نجاح العملية، معاملة `driver_balance_transactions` (نوع `settlement`) تتسجّل بحالة `completed` **مباشرة من الكود** (عبر webhook أو استجابة API مباشرة من البوابة) — **بدون الحاجة لمراجعة/تأكيد يدوي من الأدمن** لهالنوع تحديداً. خطوة "تأكيد الأدمن" الحالية بصفحة `akleto-admin-settlements.html` بتصير غير ضرورية لمعاملات `settlement` فقط (تضل ضرورية لـ `payout` — انظر الفقرة ب).
- **⚠️ بخصوص بيانات البطاقة:** لن نخزّن رقم البطاقة الكامل (PAN) ولا CVV بـ Firestore أبداً تحت أي ظرف — القرار الأمني هذا (الموثّق بجلسة سابقة نفس اليوم) ثابت. عند ربط بوابة الدفع فعلياً، إدخال بيانات البطاقة (الرقم الكامل) بيصير **مباشرة بواجهة/iframe بوابة الدفع نفسها** (Tokenization)، مو بحقول تطبيقنا. أقصى شي ممكن نخزّنه محلياً (اختياري، لتحسين تجربة المستخدم فقط): آخر 4 أرقام + اسم حامل البطاقة + تاريخ الانتهاء — **بدون الرقم الكامل ولا CVV إطلاقاً** — كـ"مرجع بصري" بس (مثلاً "بطاقتك المحفوظة تنتهي بـ 4242")، مش بيانات فعلية تُستخدم للسحب.

**ب) جهة "أكليتو تدفع للكابتن" (تحويل رصيده له) — الحل المتفق عليه: كشف بنكي جامع مطبوع (بديل عملي بدل انتظار Payout API من بوابة الدفع):**
- **حقل الآيبان (IBAN):** يُطلب من الكابتن أثناء **تسجيل الحساب** (مع بيانات البطاقة أيضاً — الفقرة أ)، **اختياري بإمكانية تخطّي ("تخطي، بقدر أدخلهم بعدين")**. لو تخطّاه وقتها، بيصير إلزامي **بس لما يحاول يطلب سحب رصيده تحديداً** — يعني: أول ما يضغط "طلب تحويل الرصيد" وما عنده آيبان مسجّل، النظام يوقفه ويطلب منه يدخل الآيبان أول (بشاشة "حسابي" مثلاً) قبل ما يقدر يكمل الطلب. **نفس المنطق بالضبط ينطبق على بيانات البطاقة (آخر 4 أرقام + اسم + تاريخ انتهاء) لما يحاول يسدد رصيد مستحق منه — إلزامي عند محاولة الدفع، اختياري وقت التسجيل.**
- **آلية التحويل الفعلي:** بدل انتظار Payout API (اللي مو كل بوابات الدفع فيها أصلاً، نقطة أثرناها بالنقاش)، الحل: صفحة `akleto-admin-settlements.html` (الموجودة أصلاً حالياً) بتتوسّع لتشمل **"كشف تحويلات جامع"** — جدول واحد (مو ورقة منفصلة لكل كابتن) فيه: اسم كل كابتن طالب سحب + رقم آيبانه + المبلغ المستحق له، **قابل للطباعة** (زر "طباعة الكشف" → `window.print()` مع تنسيق CSS مخصص للطباعة `@media print`).
- **⚠️ سلوك مهم اتفقنا عليه صراحة:** بمجرد ما الموظف يضغط زر الطباعة، **التطبيق يعتبر تلقائياً إنه كل الأرصدة المذكورة بالكشف تم تسديدها للكباتن** (يعني فعل الطباعة نفسه = تأكيد ضمني لكل معاملات `payout` المدرجة بالكشف، تتحول لـ `completed` تلقائياً بنفس لحظة الطباعة، **بدون** الحاجة لزر "تأكيد" منفصل لكل كابتن على حدة زي الوضع الحالي). المنطق: الموظف ياخذ الورقة المطبوعة فعلياً للبنك بعدها، فالطباعة نفسها هي نقطة الالتزام بالنظام. **هاد يختلف عن الوضع الحالي المبني** (اللي فيه تأكيد يدوي منفصل بزر لكل معاملة) — لما ننفّذ هالميزة، لازم نستبدل/نضيف مسار الطباعة كبديل لأزرار "تأكيد" الفردية لمعاملات `payout` تحديداً (تسديدات `settlement` تضل إما يدوية زي الوضع الحالي، أو أوتوماتيكية لو بوابة الدفع مفعّلة — بالفقرة أ).

**ج) ملخص قرار مؤيد النهائي بهالجلسة:** **تأجيل تنفيذ حقلي الآيبان وبيانات البطاقة بالكامل حالياً** — لا إضافة بحقول التسجيل ولا بصفحة الحساب ولا أي تحقق إلزامي بعد. هاي الفقرة كلها **توثيق خطة فقط** للرجوع لها لما يُقرَّر البدء الفعلي (على الأرجح مرتبط بتوقيت اختيار بوابة الدفع نفسها).

## 1 أغسطس 2026 — تفعيل التمرير (Scroll) بالقائمة الجانبية لتطبيق السائق ولوحة الأدمن

**السبب:** بعد إضافة كل التبويبات الجديدة نفس اليوم (تقارير العمل، تسديد الرصيد، بطاقات الائتمان بتطبيق السائق؛ العمولات والإعدادات، طلبات التسديد والتحويل بلوحة الأدمن)، القوائم الجانبية صارت تحتوي عناصر أكتر من ارتفاع الشاشة بالأجهزة الصغيرة، وما كان فيها تمرير — العناصر الأخيرة كانت تنقطع/تختفي بدون طريقة توصلها.

**الحل — بنفس النمط بكل من تطبيق السائق ولوحة الأدمن: تقسيم الدرج لـ 3 مناطق (هيدر ثابت / منطقة تمرير / تذييل ثابت) بدل عمود واحد كامل:**

**أ) تطبيق السائق (`common.py` → `sidebar_drawer()`):**
- عناصر التنقّل (من "التنقّل" لحد زر "تسجيل الخروج") صارت ملفوفة بـ `<div class="cs-drawer-body">` جديد — `flex:1; overflow-y:auto; -webkit-overflow-scrolling:touch; min-height:0;`.
- `.cs-drawer-header` (الصورة + الاسم + الهاتف) و`.cs-drawer-footer` ("أكليتو — السائق") ضلّوا ثابتين خارج منطقة التمرير.
- `.cs-nav-item.danger` (تسجيل الخروج) كان معتمد على `margin-top:auto` لدفعه لأسفل الدرج القديم — اتشالت لأنه صار داخل `.cs-drawer-body` (`flex:1`) اللي أصلاً بياخذ المساحة المتبقية؛ زر الخروج هلق آخر عنصر بالقائمة القابلة للتمرير (طبيعي ومتوقّع بما إنه صار عندنا عناصر كتير).

**ب) لوحة الأدمن (`admin-style.css` + كل ملفات الأدمن الـ12):**
- كل أقسام `.nav-section` (من "الرئيسية" لحد "الإعدادات") صارت ملفوفة بـ `<div class="sidebar-scroll">` جديد بنفس خصائص CSS (`flex:1; overflow-y:auto; min-height:0;`).
- `.logo` (الشعار بالأعلى) و`.sidebar-footer` (بطاقة الأدمن + زر الخروج بالأسفل) ضلّوا ثابتين خارج منطقة التمرير — بما إنه `.sidebar` أصلاً `display:flex; flex-direction:column;`.
- **⚠️ التطبيق تم عبر سكربت Python (regex) على كل الـ12 ملف أدمن دفعة وحدة** (نفس أسلوب تعديلات القائمة الجانبية السابقة بنفس اليوم) — مو نسخ يدوي لكل ملف.

**⚠️ تذكير مهم لأي تعديل مستقبلي على القائمة الجانبية (بالتطبيقين):** بما إنه القوائم الجانبية **مُكرَّرة يدوياً بكل ملف HTML** (نمط ثابت بكامل المشروع، موثّق من قبل)، أي إضافة/تعديل/حذف لعنصر تنقّل واحد لازم يتكرر يدوياً (أو عبر سكربت) بكل الملفات المعنية: **7 ملفات سائق** + **12 ملف أدمن** = **19 ملف بالمجموع** لازم تبقى متطابقة ببنية القائمة الجانبية.

## 1 أغسطس 2026 — تعديل زرّي الخريطة العائمين: مواقع المتاجر بدون عدد الطلبات + سائقين لايف بنطاق 500م بإيموجي حقيقي

**بطلب مؤيد** — تعديلين على `akleto-driver-home.html`:

**أ) 🏪 زر مواقع تكاثر الطلبات — حذف عرض عدد الطلبات:**
- قبل: كل نقطة متجر كانت تعرض حجم متدرّج (10-22px) + رقم نصي فوقها (`label`) يمثّل عدد الطلبات المنتظرة بهاد المتجر تحديداً.
- الآن: **كل نقطة متجر بحجم موحّد ثابت (13px)، بدون أي رقم أو تدرّج بالحجم** — بس تدل على "في متجر عنده طلب واحد ع الأقل بانتظار الاستلام"، بدون كشف العدد الدقيق.
- شارة العدد الصغيرة على الزر نفسه (`fabHotspots`) ضلّت موجودة، بس **معناها تغيّر**: بدل "مجموع الطلبات المنتظرة بكل المتاجر" (قيمة قديمة غير دقيقة أصلاً كانت خليط)، صارت **عدد المتاجر المختلفة** اللي عندها طلبات حالياً (`storeIdsWithOrders.size`) — رقم متاجر، مش رقم طلبات.

**ب) 🛵 زر حركة السائقين — نطاق 500 متر + إيموجي دراجة حقيقي بدل الأيقونة المرسومة:**
- **النطاق:** بدل عرض كل السائقين المتاحين بأي مكان بالمدينة، الزر هلق يعرض بس السائقين اللي **ضمن نصف قطر 0.5 كم (500 متر) عن موقعي الحالي** — فلترة عبر `haversineKm()` (نفس الدالة المستخدمة لحساب مسافة الورديات، بـ `driver-shared.js`) مقارنة بمتغيّر `myCurrentPosition` (محدَّث بكل مرة نحصل موقع جديد عبر `placeMyMarker()`).
- **⚠️ سلوك حي مهم:** الفلترة **تُعاد تلقائياً كل ما يتحرك موقعي أنا نفسي** (مو بس لما تتغيّر بيانات السائقين بـ Firestore) — عبر تخزين آخر قائمة سائقين مستلمة بمتغيّر `lastDriversList`، واستدعاء `renderLiveDrivers(lastDriversList)` مجدداً من جوا `placeMyMarker()` نفسها كل مرة يوصل موقع جديد (سواء من `centerOnMyLocation` أو من `watchPosition` أثناء التوفر). هيك القائمة المعروضة تبقى "لايف" فعلياً حسب موقعي الحالي، مو بس حسب موقع باقي السائقين.
- **الأيقونة:** بدل SVG المرسوم يدوياً (نفس شكل شعار "توصيلاتي")، صار Marker شفاف تماماً (`fillOpacity:0, strokeOpacity:0`) مع `label` نصّي = إيموجي 🛵 فعلي (`fontSize:'24px'`) — **استثناء متعمّد من قرار "لا إيموجي، بس أيقونات مصممة" الموثّق سابقاً بنفس اليوم**، لأنه هون السياق مختلف: علامة على خريطة تمثّل كيان حي متحرك بالواقع، مو عنصر واجهة (زر/هيدر/قائمة) — مؤيد طلبها صراحة بهاد الشكل تحديداً.
- زر "موقعي الحالي" (🎯) وزر "متاح/غير متاح" **ما تأثروا إطلاقاً** بهالتعديل — لسا نفس الأيقونات المصممة (SVG) زي ما هي.

## 1 أغسطس 2026 — تعديلات إضافية سريعة على زرّي الخريطة (متابعة مباشرة لنفس اليوم)

- **علامة موقعي أنا (self marker):** صارت تستخدم نفس إيموجي 🛵 الحقيقي المستخدم لعلامات الكباتن الآخرين اللايف (بدل SVG المرسوم `motoIcon` القديم) — نفس دالة `emojiMarkerIcon()` + `label` بحجم أكبر شوي (26px بدل 24px) للتمييز البصري البسيط بين نفسي والكباتن الآخرين.
- **زر 🏪 مواقع تكاثر الطلبات:** حُذفت شارة العدد الصغيرة فوق الزر نهائياً (كانت تعرض عدد المتاجر النشطة) — الزر هلق بدون أي رقم إطلاقاً بأي حالة (مفعّل أو لأ). زر 🛵 السائقين ضلّ محتفظ بشارة عدده (لم يُطلب تغييره).
- **نص التوضيح (legend):** "سائق ضمن 500 م منك" ← "**كابتن** ضمن 500 م منك" — المصطلح المفضّل بالتطبيق هو "كابتن" مش "سائق" (نفس المصطلح المستخدم أصلاً بعناوين صفحات "تقارير العمل"/"طلبات التسديد" بلوحة الأدمن). ⚠️ باقي الإشارات لكلمة "سائق" بالكود (تعليقات داخلية، fallback اسم `d.name || 'سائق'` بالـ tooltip) **لم تُغيَّر** — التعديل اقتصر على نص التوضيح المرئي المطلوب تحديداً. لو ظهرت رغبة لاحقاً بتوحيد المصطلح بكل مكان بالواجهة، محتاج مراجعة شاملة أوسع.

## 3 أغسطس 2026 — تبسيط نص legend الكباتن + إضافة شاشة الطلب النشط الكاملة (6 مراحل)

- **نص legend الرئيسية:** "🛵 كابتن ضمن 500 م منك" ← "🛵 كابتن" فقط (بقي الإيموجي، حُذف الجزء اللي بيحدد المسافة).
- **شارة عدد زر 🛵 السائقين اللايف:** حُذفت نهائياً (كانت وحيدة المتبقية من بين شارتي الأزرار — زر 🏪 كان انحذفت شارته سابقاً بنفس اليوم). الزر هلق بدون رقم بأي حالة.
- **طُلب إيموجي/أيقونة ثلاثية الأبعاد لراكب فوق الدراجة بدل 🛵:** ما في إيموجي يونيكود قياسي (ولا حتى بنسخ Fluent 3D المفتوحة المصدر) بيعرض راكب فوق سكوتر/دراجة — كلها نسخ ثلاثية الأبعاد من نفس 🛵 بدون راكب. اقترحت بديل SVG مرسوم يدوياً (نفس أسلوب أيقونات التطبيق) ثم بديل صورة/أيقونة 3D جاهزة من مكتبات خارجية (Flaticon/Icons8، بترخيص). مؤيد رفض الاثنين وقرر عدم المتابعة بهالتعديل — **لم يتغيّر أي شي بخصوص إيموجي 🛵 بأي مكان.**

### ميزة جديدة كبيرة: شاشة الطلب النشط الكاملة (Active Order Guard)

لما الكابتن يستلم طلب جديد (`claimOrder` بصفحة "الطلبات المتاحة")، بدل ما تظهر قائمة منسدلة من الأسفل (`orderSheet` القديم)، هلق تظهر **شاشة كاملة تغطي الشاشة بالكامل** (`#activeOrderGuard`, z-index: 999999) وتمنع الكابتن من التفاعل مع أي شي تاني بالتطبيق لحد ما يخلّص الطلب بالكامل — حتى لو سكّر التطبيق وفتحه من جديد، لأنها مبنية بالكامل على حالة الطلب بـ Firestore (مش localStorage)، فبترجع تظهر تلقائياً بأي صفحة كابتن يفتحها بعد الدخول.

**المكان:** كل المنطق (CSS + HTML injection + JS) موجود بملف واحد مشترك `driver-shared.js` (دالة مُصدَّرة وحيدة: `mountActiveOrderGuard(session)`) — كل صفحات السائق السبع بعد تسجيل الدخول (`home`, `orders`, `deliveries`, `account`, `reports`, `settle`, `cards`) استوردتها وأضافت سطر واحد بس: `if (session) mountActiveOrderGuard(session);` مباشرة بعد سطر `requireDriverSession()`. هيك تفادينا تكرار الكود بسبع ملفات وما احتجنا نظام Python للبناء (مش موجود بهالجلسة أصلاً).

**حقول Firestore جديدة على مستند `orders`:**
- `driver_flow_stage`: نص — `'to_store'` (افتراضي عند الاستلام) → `'at_store'` → `'to_customer'` → `'at_customer'`. يتحدث بـ `updateDoc` مع كل زر انتقال.
- `driver_rating`: رقم 1-5 (يُحفظ بالمرحلة السادسة — تقييم الكابتن لتجربة التوصيل، مش تقييم الزبون. لو احتجنا لاحقاً تقييم من نوع تاني، الاسم قابل لإعادة تسمية).
- `driver_rating_at`: timestamp وقت التقييم.

**شرط ظهور الشاشة (بالكود، `mountActiveOrderGuard`):** أول طلب (أقدم بالـ `created_at`) من طلبات الكابتن (`watchMyOrders`) يحقق: `status === 'delivering'` **أو** (`status === 'done'` و`driver_rating` غير موجود). أول ما يصير عنده تقييم، الطلب ما بيطابق الشرط تلقائياً والشاشة بتختفي.

**المراحل الست بالتفصيل:**
1. **توجه للمتجر** — اسم/عنوان/هاتف المتجر (يُجلب حي من مستند `stores/{store_id}` عبر `store_id` المحفوظ بالطلب) + زر "فتح الموقع بخرائط جوجل" (بالإحداثيات لو موجودة، وإلا بالعنوان نصياً) + زر "وصلت للمتجر" → يحدّث `driver_flow_stage: 'at_store'`.
2. **استلام من المتجر** — قائمة العناصر (`order.items` + أسماء الوجبات من `meals` collection، مُخزّنة مؤقتاً بذاكرة الصفحة) + زر "استلمت الطلب. توجه للعميل" → `driver_flow_stage: 'to_customer'`.
3. **توجه للعميل** — اسم/هاتف الزبون (زر اتصال `tel:`)، طريقة الدفع (نقداً/بطاقة)، العنوان والملاحظات، زر خرائط جوجل لعنوان التوصيل + زر "لقد وصلت" → `driver_flow_stage: 'at_customer'`.
4. **تسليم الطلب** — ملخص سريع (متجر + زبون + عدد الوجبات) + زر اتصال بالزبون + زر "الإبلاغ عن مشكلة" (يفتح قائمة مشاكل جاهزة، الضغط على أي وحدة منها يسجّل إشعار بـ `admin_notifications` type=`driver_issue_report` وبعدها يفتح `tel:` لرقم الدعم) + المبلغ المطلوب تحصيله بخط كبير واضح (لو الدفع نقدي) أو رسالة "تم الدفع إلكترونياً" (لو بطاقة) + زر "تم التسليم بنجاح" → يحدّث `status: 'done'` و`delivered_at`.
5. **إيصال/ملخص مالي** (تلقائي بمجرد `status === 'done'`) — نفس المبلغ (أو رسالة الدفع الإلكتروني) بخط كبير، وتحته بخط رمادي خفيف تفصيل: رسوم التوصيل + الإكرامية، عمولة أكليتو، صافي ربح الكابتن، والمستحق منه لأكليتو (نفس حسابات `computeOrderFinancials` الموجودة أصلاً بالتقارير). فيها زر "متابعة" ينقل محلياً (client-side فقط، بدون حقل Firestore إضافي) للمرحلة السادسة.
6. **تقييم التجربة** — 5 نجوم + زر "شكراً لك" يحفظ `driver_rating` و`driver_rating_at` ويقفل الشاشة نهائياً لهاد الطلب.

**⚠️ نقاط تحتاج انتباه/قرار لاحق:**
- **رقم الدعم:** ثابت مؤقت بالكود (`SUPPORT_PHONE` بـ `driver-shared.js`) = `+962790000000` — **رقم وهمي**، لازم يتحدث برقم دعم أكليتو الحقيقي قبل الإطلاق.
- **الانتقال من المرحلة 5 للمرحلة 6:** ما في بالطلب الأصلي تفاصيل دقيقة لهالانتقال، فافترضت زر "متابعة" بشكل منطقي — قابل للتغيير لو مؤيد حاب آلية تانية (مثلاً تأخير تلقائي بالثواني).
- **صفحة `akleto-driver-deliveries.html`:** لسا فيها الكود القديم (`orderSheet`, `markDelivered`) لعرض/إنهاء الطلبات النشطة يدوياً — ما انحذف، بس هلق عملياً ما رح يظهر/يُستخدم إطلاقاً لأي طلب نشط لأن الشاشة الكاملة الجديدة بتغطيه وتمنع التفاعل معه من أول ما يستلم الكابتن الطلب. هذا تكرار غير ضار (dead code) بس ممكن يُنضّف لاحقاً لو حبينا نبسّط الملف.
- **طلب واحد نشط بنفس الوقت:** ما في منع صريح بالكود من `claimOrder` يمنع كابتن يستلم أكثر من طلب، بس عملياً الشاشة الكاملة بتمنعه من الوصول لصفحة "الطلبات المتاحة" أصلاً طول ما عنده طلب نشط — فبيصير عندنا "طلب واحد بكل مرة" بشكل فعلي بدون داعي لتعديل منطق `claimOrder`.
- تم التحقق من صحة الكود بـ `node --check` لكل الملفات السبعة + `driver-shared.js` قبل الرفع — ما في أخطاء syntax.

### تصحيح سريع (بنفس اليوم): المرحلة 2 وما بعدها بتعرض مكونات مش وجبات

مؤيد نبّه إنه الكابتن فعلياً بيستلم **مكونات** من المتجر مش وجبات جاهزة (موديل أكليتو أصلاً شراء مكونات، مش وجبات مطبوخة). التعديل:
- **المرحلة 2:** كانت تعرض `order.items` (وجبات + عدد حصص، عبر `meals` collection) ← صارت تعرض `order.merged_ingredients` (نفس الحقل المستخدم أصلاً بشاشة تفاصيل الطلب بلوحة الأدمن): اسم المكوّن + الكمية + وحدة التعبئة (`package_label`)، مع ملاحظة صغيرة اختيارية عن الكمية اللي تحتاجها الوصفة (`base_qty_needed`/`base_unit_label`) لو موجودة. العنوان تغيّر لـ"استلم المكونات من المتجر" والزر لـ"استلمت المكونات. توجه للعميل".
- **المرحلة 4:** سطر "عدد الوجبات" (كان من `order.items.length`) ← صار "عدد المكونات" من `order.merged_ingredients.length`.
- حُذفت دالة `aogGetMealsCache()` و`getDocs` من الاستيراد بـ `driver-shared.js` لأنها ما عادت مستخدمة بعد إزالة الاعتماد على `meals` collection بهالشاشة.

### تحسينات إضافية (بنفس اليوم) على شاشة الطلب النشط

- **المرحلة 4 — "عدد المكونات" صار قابل للطي/الفتح:** الضغط على السطر بيفتح قائمة منسدلة (`#aogIngredientsList`) تعرض تفاصيل كل مكوّن (الاسم + الكمية + وحدة التعبئة)، بنفس التنسيق المستخدم بالمرحلة 2 — استُخرجت دالة مشتركة `aogIngredientsHtml(order)` يستخدمها كل من المرحلة 2 (العرض دائماً ظاهر) والمرحلة 4 (العرض قابل للطي عبر `window.__aogToggleIngredients()`).
- **زر "الإبلاغ عن مشكلة" صار أيقونة ثابتة (⚠️) بزاوية الشاشة (يسار/أسفل)، ظاهرة بكل المراحل الست** بدل ما كانت زر نصي داخل محتوى المرحلة 4 فقط. الأيقونة والقائمة المنبثقة (`#aogProblemPanel`) صارت جزء من الحاوية الثابتة (`aogEnsureContainer`) نفسها، مش جزء من محتوى أي مرحلة — فبتبقى موجودة وقابلة للضغط بأي وقت طول ما الشاشة الكاملة ظاهرة (من استلام الطلب لحد التقييم).

## ملاحظة مستقبلية (لسا ما اتنفذت) — مذكورة بتاريخ 3 أغسطس 2026

مؤيد طلب تسجيل فكرة تعديل قادم بدون تنفيذها الآن:

- **حذف تبويب "الطلبات المتاحة"** بالكامل من القائمة الجانبية بتطبيق السائق (يعني حذف/إخفاء `akleto-driver-orders.html` كصفحة منفصلة بالتنقل).
- **البطاقات (الحاويات) اللي كانت تظهر جوه هاي الصفحة وبتمثل طلب جديد متاح** — بدل ما تظهر بصفحة منفصلة، رح تظهر مباشرة **بالصفحة الرئيسية (`akleto-driver-home.html`) فوق الخريطة**.
- كل بطاقة/طلب جديد رح تظهر لمدة **5 ثواني** بس، وبعدها (إذا ما تم قبولها) تختفي وتنتقل الفرصة **لكابتن تاني** (يعني آلية توزيع طلبات بالدور/بالتايم-آوت بدل ما يشوف كل الكباتن كل الطلبات المتاحة بنفس الوقت لأجل غير مسمى).

هاي نقطة تحتاج تصميم أعمق قبل التنفيذ (آلية "الدور" بين الكباتن، شو بيصير لو محدش قبل خلال 5 ثواني، تزامن الوقت عبر أجهزة متعددة، إلخ) — بننتظر تأكيد مؤيد للبدء فيها بجلسة قادمة.

### تعديلين إضافيين على شاشة الطلب النشط (بنفس اليوم)

- **أيقونة "الإبلاغ عن مشكلة":** انتقلت من أعلى/يسار الأسفل (فوق الفوتر) إلى **أعلى الشاشة، بالزاوية اليسرى** (تحت الهيدر مباشرة)، وصغّرت من 50px إلى 38px قطر. القائمة المنبثقة (`#aogProblemPanel`) تبعتها بنفس الاتجاه (تفتح لتحت من مكان الأيقونة).
- **المرحلة 3 (توجه للعميل):** أضيف سطر يوضح **المبلغ اللي راح يستلمه الكابتن من العميل** — لو الدفع نقدي بيظهر المبلغ بالأرقام، ولو بطاقة ائتمانية بيظهر "مدفوع إلكترونياً ✅". هيك الكابتن يعرف المبلغ من أول ما يتوجه للعميل مش لما يوصل بس (المرحلة 4 نفسها لسا فيها نفس المعلومة بخط أكبر وأوضح كملخص نهائي).

### تعديل الهيدر — إزالة الحاوية البرتقالية ودمج أيقونة الإبلاغ مع رقم الطلب

- **الحاوية البرتقالية (شارة "المرحلة X من 6"، `#aogStageBadge`):** انحذفت نهائياً من الهيدر — الدالة `aogSetStage()` بقيت بالكود بس أصبحت بدون تأثير (تتحقق إذا العنصر موجود قبل ما تحدّثه)، ما في داعي نحذفها كنص لتفادي تكسير باقي الاستدعاءات.
- **أيقونة الإبلاغ عن مشكلة (⚠️):** انتقلت من كونها زر دائري عائم (floating) بزاوية الشاشة لتصير **أيقونة مسطحة داخل صف الهيدر نفسه، جنب رقم الطلب (#...) مباشرة** — بدون ظل أو خلفية دائرية ملوّنة، يعني ما بتبين مرتفعة عن باقي الواجهة. القائمة المنبثقة (`#aogProblemPanel`) صارت تفتح تحت الهيدر مباشرة بدل ما كانت أبعد لتحت.


## 3 أغسطس 2026 — تعديلات إضافية على هيدر شاشة الطلب النشط + تسجيل متطلبات مستقبلية (إشعارات/صوت/Overlay)

### تعديلات مُنفَّذة على الهيدر (`driver-shared.js`)

- **إعادة شارة "المرحلة X من 6" (`#aogStageBadge`):** كانت انحذفت بجلسة سابقة (الدالة `aogSetStage()` بقيت موجودة بس بدون تأثير فعلي). رجعت الآن كحبة برتقالية صغيرة مسطحة (`#F5A65B`، بدون ظل).
- **فصل تخطيط الهيدر:** `.aog-header` صار `justify-content:space-between` بدل `flex-start` — أيقونة ⚠️ (الإبلاغ عن مشكلة) لحالها بطرف الشاشة، ومجموعة (رقم الطلب `#aogCode` + شارة المرحلة `#aogStageBadge`) ملفوفين سوا بـ`.aog-header-code-group` بالطرف التاني، فصار في مسافة واضحة بينهم.
- **حذف كلمة "المرحلة" من نص الشارة:** `aogSetStage()` هلق بس بتحط `${n} من 6` (مثلاً "3 من 6") بدل "المرحلة 3 من 6".

### ⚠️ متطلبات مستقبلية مسجّلة بطلب مؤيد (لسا ما اتنفذت — تحتاج جلسة عمل جدّية على مستوى Capacitor/Android الأصلي)

مؤيد طلب تسجيل ثلاث ميزات جديدة متعلقة بتوفّر الكابتن والإشعارات، كلها تتطلب كود أصلي (native) بمشروع Android عبر Capacitor — مو مجرد تعديل HTML/JS بالويب PWA:

1. **إشعار ثابت "جاري العمل..." أثناء التوفّر:**
   - لما الكابتن يفعّل "متاح"، لازم يظهر إشعار (notification) من تطبيق أكليتو نصه "جاري العمل...".
   - **الإشعار لازم يكون ثابت (persistent/ongoing)** — يعني الكابتن ما يقدر يمسحه/يشيله يدوياً من قائمة الإشعارات (لا بالسحب ولا بأي طريقة)، طول ما هو متاح.
   - **الإشعار بختفي تلقائياً فقط لما الكابتن يرجع "غير متاح".**
   - **ملاحظة تقنية (لتنفيذ لاحق):** هذا النمط بيتطلب على أندرويد **Foreground Service** حقيقي (مو مجرد Local Notification عادية عبر `@capacitor/local-notifications`)، لأنه الإشعارات العادية بالنظام قابلة للمسح دايماً من المستخدم إلا إذا كانت مرتبطة بخدمة foreground شغالة — هذا يحتاج بلَغِن Capacitor مخصص أو كود Java/Kotlin أصلي إضافي بمشروع Android (مش موجود بالمشروع حالياً).

2. **إظهار التطبيق فوق تطبيقات أخرى — تم التوضيح:** مؤيد أكّد إنه المقصود هو **الخيار (ب): رجوع التطبيق تلقائياً للمقدمة (foreground)** عند وصول طلب جديد وهو بالخلفية أو الشاشة مقفولة — **مش** نافذة عائمة (Overlay/`SYSTEM_ALERT_WINDOW`).
   - **⚠️ قيد تقني مهم بأندرويد:** بدءاً من أندرويد 10، **ما مسموح لتطبيق بالخلفية إنه "يقفز" تلقائياً ويفتح نفسه بالمقدمة من تلقاء نفسه** (قيد أمني من جوجل لمنع التطبيقات من مقاطعة المستخدم بأي وقت). الطريقة المتاحة فعلياً هي عبر **Full-Screen Intent Notification** (نفس الآلية المستخدمة بتطبيقات المكالمات مثل واتساب/المنبهات): إشعار خاص بصلاحية `USE_FULL_SCREEN_INTENT` بيقدر يفتح Activity/شاشة كاملة فوق شاشة القفل حتى لو التطبيق مقفول بالكامل — هاي أقرب آلية موجودة لتحقيق "التطبيق يفتح نفسه" المطلوب، وبتحتاج كود أندرويد أصلي (مش جزء افتراضي من Capacitor).

3. **رنة صوت عند وصول طلب جديد:**
   - لما يوصل أوردر جديد للكابتن، لازم يصير صوت رنة تنبيه (مو بس اهتزاز أو إشعار صامت).
   - يحتاج غالباً `@capacitor/local-notifications` (مع صوت مخصص) أو ربطها مع نظام الـPush Notifications الأصلي (`@capacitor/push-notifications`) المستخدم أصلاً بتطبيق الزبون — بما إنه تطبيق السائق APK لسا ما اتبنى أصلاً (لسا PWA فقط بدون Capacitor project منفصل خاص فيه)، هاي الميزة الثلاث كلها بشكل عملي بتنتظر إنشاء مشروع Capacitor لتطبيق السائق (نفس اللي صار للزبون بـ`mobile-customer/`) قبل ما تصير قابلة للتنفيذ الفعلي.

**الخلاصة:** الميزات الثلاث مرتبطة ببعض ومصيرها التنفيذ الفعلي مرتبط بخطوة أوسع لسا ما صارت: **بناء مشروع Capacitor لتطبيق السائق** (مثل ما صار للزبون)، لأنه بدونها ما في وصول لـForeground Services ولا صلاحيات Overlay ولا صوت إشعارات أصلي على أندرويد. بانتظار تأكيد مؤيد للبدء بهاي الخطوة بجلسة قادمة.

## 3 أغسطس 2026 — إنشاء مشروع Capacitor لتطبيق السائق (`mobile-driver/`)

**بطلب مؤيد** — أول خطوة أساسية قبل تنفيذ الميزات المستقبلية المسجّلة أعلاه (إشعار ثابت أثناء التوفر، رجوع تلقائي للمقدمة، رنة طلب جديد)، تم إنشاء مشروع Capacitor حقيقي لتطبيق السائق، بنفس نمط `mobile-customer/` الموجود مسبقاً لتطبيق الزبون.

**كيف تم الإنشاء:** استُخدم فعلياً `npx cap add android` (مو تصميم يدوي للملفات) داخل بيئة الجلسة، فالمشروع مبني بنفس أدوات Capacitor الرسمية تماماً مثل الزبون — مش تقليد يدوي.

**الإعدادات:**
- **App ID:** `com.akalito.driver`
- **اسم التطبيق:** أكليتو كابتن
- **`server.url`:** `https://moayyad44.github.io/akalito/akleto-driver.html` — يعني نفس أسلوب الزبون تماماً (تحميل حي من GitHub Pages، فأي تحديث لاحق على أي صفحة سائق ينعكس تلقائياً بدون بناء APK جديد).
- **الأيقونة:** استُخدمت `icons/driver-512.png` الموجودة أصلاً بالمشروع (نفس أيقونة PWA manifest) لتوليد كل أحجام أيقونات أندرويد (mipmap) وشاشات splash (`npx capacitor-assets generate --android`).
- **الحزم المثبّتة:** نفس حزم الزبون (`@capacitor/core`, `@capacitor/android`, `@capacitor/push-notifications`, بالإضافة لـ`@capacitor/assets` و`@capacitor/cli` كـdevDependencies).
- **صلاحية `POST_NOTIFICATIONS`** أُضيفت بـ`AndroidManifest.xml` (لأندرويد 13+)، تحضيراً لإشعارات الدفع لاحقاً.

**الملفات المرفوعة على GitHub (`mobile-driver/`):** 84 ملف — كامل هيكل مشروع Android (Gradle wrapper، `MainActivity.java` بالحزمة `com.akalito.driver`، كل موارد الأيقونات وشاشات splash، `AndroidManifest.xml`، إلخ) + `README.md` مخصص بنفس أسلوب README الزبون، يشرح لمؤيد خطوات فتحه ببناء الـ APK بـAndroid Studio.

**⚠️ ملفات مستثناة عمداً من الرفع (مطابقة لسلوك `.gitignore` بمشروع الزبون):**
- `android/app/build/` (مخرجات بناء مؤقتة)
- `android/app/src/main/assets/public/` + ملفات config المولّدة تلقائياً (`capacitor.config.json`, `capacitor.plugins.json`, `res/xml/config.xml`) — تُعاد توليدها تلقائياً بأمر `npx cap sync android`
- `android/capacitor-cordova-android-plugins/` — مولّد تلقائياً، غير موجود أصلاً بمستودع الزبون

**⚠️ ناقص لسا (بانتظار مؤيد، نفس حالة الزبون بالضبط):**
- ملف **`google-services.json`** — لازم يُنشئ مؤيد تطبيق أندرويد جديد بمشروع Firebase `akleto-prod` بـpackage name **`com.akalito.driver`** بالضبط، ويحمّل الملف، ويحطه بـ`mobile-driver/android/app/google-services.json`. بدونه التطبيق يبنى ويشتغل عادي، بس إشعارات الدفع فقط ما رح تشتغل.
- ربط استقبال FCM token وحفظه بمستند الكابتن بـFirestore (`drivers/{driverId}`) — لسا ما تم، نفس الحالة الناقصة بتطبيق الزبون (الجزء المتبقي "الإرسال الفعلي" عبر Cloud Function).
- الكود الأصلي (Java/Kotlin) للميزات الثلاث المستقبلية (Foreground Service، Full-Screen Intent، رنة مخصصة) — لسا ما بدأ، بانتظار تأكيد مؤيد إن هذا المشروع يبني ويشتغل عنده أول مرة بـAndroid Studio قبل ما نضيف كود أعقد فوقه.

**الخطوة التالية المقترحة:** مؤيد يجرب يفتح `mobile-driver/android` بـAndroid Studio عنده ويبني أول APK تجريبي (debug) للتأكد إن كل شي يشتغل قبل ما نكمل بإضافة ميزات الإشعارات الأصلية.

## 4 أغسطس 2026 — تطبيق المتجر: شاشة "طلبات المتجر" صارت الشاشة الرئيسية

**بطلب مؤيد** — تعديل على `akleto-store.html`:

- **حُذفت الشاشة الرئيسية القديمة نهائياً** (`storeScreen`) — كانت تعرض 3 بطاقات (📦 طلبات المتجر، 🧺 منتجاتي، 📊 بوابة المتجر) + زر خروج.
- **شاشة "طلبات المتجر" (`storeOrdersScreen`) صارت هي الشاشة الرئيسية** — أول شي يظهر مباشرة بعد تسجيل الدخول (`enterStoreMode()`).
- كانت هاي الشاشة سابقاً صفحة فرعية بـ`store-sub-topbar` (زر رجوع ← + عنوان فقط). هلق صار فيها نفس الـ`topbar` الكامل اللي كان بالشاشة الرئيسية القديمة: زر القائمة الجانبية (☰)، اسم المتجر + اسم الموظف المسجّل دخول، وجرس الإشعارات (مع نقطة `bellDotStore`).
- **مسارات التنقل المعدَّلة:**
  - `goStoreHome()`: هلق تخفي `storeProductsScreen` وتظهر `storeOrdersScreen` (بدل الشاشة المحذوفة).
  - `openStoreProducts()` / `closeStoreProducts()`: يتنقلوا بين `storeOrdersScreen` (الرئيسية الجديدة) و`storeProductsScreen`.
  - `openStoreOrders()` / `closeStoreOrders()`: أصبحتا مجرد استدعاء لـ`goStoreHome()` (لأنه الطلبات هلق هي الشاشة الرئيسية نفسها، مو شاشة منفصلة تُفتح/تُقفل).
  - `menuStoreGoOrders()` (من القائمة الجانبية): صار بس `goStoreHome()`.
  - `logoutStore()`: عُدّلت لتخفي `storeOrdersScreen` و`storeProductsScreen` (بدل الشاشة القديمة المحذوفة).
- **شارة عدد الطلبات القديمة (`storeOrdersBadge`)** اللي كانت فوق بطاقة "طلبات المتجر" بالشاشة الرئيسية القديمة — انحذف عنصر الـHTML تبعها (ما عاد إلها مكان منطقي، لأنه عدد الطلبات الواردة أصلاً ظاهر بتبويب "📥 واردة (N)" داخل شاشة الطلبات نفسها). دالة `updateStoreOrdersBadge()` بقيت بالكود بدون تأثير (فيها تحقق `if (!badge) return;` أصلاً فما بتسبب خطأ).
- **الوصول لـ"منتجاتي" و"خروج من حساب المتجر"** لسا متوفر بشكل طبيعي من القائمة الجانبية (☰) زي ما كان تماماً — لم يتأثر.
- تم التحقق من صحة الكود (كلا الـ`<script>` — العادي والـ`module`) بـ`node --check` قبل الرفع — ما في أخطاء syntax.

### تعديل سريع بنفس اليوم — حذف تبويب "طلبات المتجر" من القائمة الجانبية

بما إنه شاشة "طلبات المتجر" صارت هي الشاشة الرئيسية (التعديل أعلاه بنفس اليوم)، حُذف عنصر التنقّل "طلبات المتجر" من القائمة الجانبية (`csDrawerStore`) — صار زائد عن الحاجة. باقي القائمة (الرئيسية، منتجاتي، الإشعارات، بوابة المتجر، خروج) بدون تغيير. دالة `menuStoreGoOrders()` بقيت بالكود بدون استخدام فعلي (dead code غير ضار، بنفس نمط تُوثّق سابقاً بمشروع السائق).

## 4 أغسطس 2026 — تطبيق المتجر: رقم طلب رقمي 5 خانات + تعديل محتوى حاوية الطلب بالقائمة

**بطلب مؤيد** — تعديلات على `akleto-store.html` فقط (باقي التطبيقات — الأدمن/السائق/الزبون — لسا تستخدم `ticketCode` القديمة المشتقة من آخر 4 محارف بمعرّف Firestore، لم تتأثر):

- **`ticketCode(id)` بتطبيق المتجر فقط:** بدل ما كانت تاخذ آخر 4 محارف من معرّف الطلب بـFirestore (ممكن تحتوي حروف)، هلق بتولّد رقم من 5 خانات أرقام فقط عبر دالة hash بسيطة (`hash = hash*31 + charCode`) ثم `% 100000` مع `padStart(5,'0')` — مثال: `#00014`. **ملاحظة مهمة:** هذا رقم مشتق (deterministic) من معرّف الطلب نفسه لعرضه بشكل رقمي مقروء بس مش رقم تسلسلي حقيقي (زي "أول طلب، تاني طلب..") — احتمال تصادم نادر جداً بين طلبين مختلفين وارد نظرياً (1 من 100000) بس غير محسوس عملياً. لو احتجنا لاحقاً رقم تسلسلي حقيقي 100% بدون تصادم، هذا يتطلب تعديل أوسع (عداد مركزي بـFirestore يُحدَّث بـtransaction وقت إنشاء كل طلب من تطبيق الزبون) — لم يُطلب هذا حالياً.
- **حاوية الطلب بقائمة "طلبات المتجر" (`renderStoreOrdersList`):** حُذف سطر "🍽️ X وجبة" (كان يعرض `o.items.length`، مو منطقي لأنه موديل أكليتو شراء مكونات مو وجبات جاهزة). أُضيف بدلاً منه:
  - 👤 اسم الزبون (`o.customer_name`، نفس الحقل المستخدم بلوحة الأدمن).
  - 💰 قيمة الفاتورة (`o.total_estimated_price`، نفس الحقل المستخدم أصلاً بشيت تفاصيل الطلب وبلوحة الأدمن كـ"الإجمالي التقديري").
  - عدد المكونات (`merged_ingredients.length`) بقي موجود، بس بجانب اسم الزبون بدل عدد الوجبات.
- تم التحقق من صحة الكود (كلا الـ`<script>`) بـ`node --check` قبل الرفع.

## 4 أغسطس 2026 — عداد تسلسلي حقيقي لرقم الطلب (order_number) بدل الرقم المشتق

**بطلب مؤيد** — بعد ما تبيّن إنه الرقم المشتق (hash من معرّف الطلب، % 100000) معرّض لتصادم حقيقي بعد ~370 طلب تقريباً (مفارقة أعياد الميلاد)، تم استبداله برقم تسلسلي حقيقي بدون احتمال تصادم إطلاقاً.

### آلية العداد (`akleto-customer.html` — مكان إنشاء الطلب فقط)
- مجموعة جديدة بـFirestore: `counters` — مستند واحد `orders` فيه حقل `last_number`.
- عند تنفيذ أي طلب من تطبيق الزبون: بدل `addDoc` المباشر، صار في **Firestore transaction** واحدة تسوي الاثنين سوا بشكل ذرّي (atomic):
  1. تقرأ `counters/orders`، تاخذ `last_number` الحالي (أو 0 لو أول مرة).
  2. تزيده بواحد → هذا هو `order_number` الجديد.
  3. تكتب القيمة الجديدة بمستند العداد.
  4. تكتب مستند الطلب نفسه (بمعرّف مُولَّد مسبقاً عبر `doc(collection(db,'orders'))`) وتضيف له حقل `order_number` بالإضافة لكل الحقول القديمة.
- بما إنها transaction حقيقية، Firestore بيضمن عدم تكرار الرقم حتى لو صار طلبين بنفس اللحظة تماماً من جهازين مختلفين.
- أول طلب بعد هالتحديث ياخذ `order_number: 1`.

### دالة العرض الموحّدة `orderDisplayCode(order)`
أُضيفت بكل مكان كان فيه `ticketCode`، بنفس الفكرة بكل مكان: تفضّل `order.order_number` (تنسيقه 5 خانات بأصفار بادئة، مثلاً `00001`) لو موجود، وإلا ترجع لـ`ticketCode(order.id)` القديمة (fallback للطلبات القديمة قبل هالتحديث اللي ما إلها `order_number`):
- `akleto-store.html` — قائمة الطلبات + شيت التفاصيل + إشعار "طلب جاهز"
- `akleto-admin-orders.html` — قائمة الطلبات + شيت التفاصيل (هون الدالة ترجع الرقم بدون `#` للتوافق مع أسلوب هالملف اللي يضيف `#` بمكان الاستخدام)
- `driver-shared.js` (مُصدَّرة `export`) — تستخدمها 3 صفحات سائق (`akleto-driver-reports.html`, `akleto-driver-deliveries.html`, `akleto-driver-orders.html`) بعد إضافتها لقائمة الاستيراد بكل ملف، بالإضافة لشاشة الطلب النشط (`mountActiveOrderGuard`) وإشعار "مشكلة أثناء التوصيل"
- `akleto-customer.html` — إشعار الأدمن "طلب جديد" وقت الإنشاء

**⚠️ نقطة تحتاج تأكيد مؤيد:** لو عنده قواعد أمان (Security Rules) مخصصة بمشروع Firebase `akleto-prod` (مش قواعد مفتوحة عامة)، لازم يتأكد إنه مجموعة `counters` الجديدة مسموح فيها القراءة/الكتابة (نفس صلاحيات `orders`/`coupons` تقريباً) — وإلا الـtransaction بتفشل وقت تنفيذ أول طلب. لسا ما انعمل اختبار حي فعلي بمتصفح حقيقي (بيئة الجلسة ما فيها متصفح)، بس الكود اتفحص syntax-wise (`node --check`) على كل الملفات المعدَّلة السبعة ولا في أخطاء.

**تم التحقق من صحة الكود بـ`node --check`** على: `akleto-customer.html`, `akleto-store.html`, `akleto-admin-orders.html`, `driver-shared.js`, `akleto-driver-reports.html`, `akleto-driver-deliveries.html`, `akleto-driver-orders.html` — كلها بدون أخطاء syntax.

## 4 أغسطس 2026 — نظام تحميل الأوردر بتطبيق المتجر (شيت تفاصيل الطلب)

**بطلب مؤيد** — لما الموظف يضغط على طلب من شاشة "طلبات المتجر"، شيت التفاصيل صار فيه نظام تفاعلي كامل لمساعدته يحمّل البضاعة صح ويتتبع تقدمه:

### `akleto-store.html`
- **Checklist تفاعلي لكل مكوّن** (`store-load-row`): كل صف فيه صورة المنتج (`ing.image_url`، وبديل أيقونة 📦 لو ما في صورة)، الاسم، الكمية المطلوبة، ودائرة تأشير. الموظف يضغط الصف كامل يعلّمه "تم تحميله" (شطب على الاسم + تلوين أخضر للدائرة).
- **شريط تقدّم حي** (`so-progress-wrap`): يعرض "تحميل السلة: X من Y" ونسبة مئوية، ويتحدّث لحظياً مع كل ضغطة.
- **زر "جهّزنا المكونات" أصبح مقفول (disabled) لحد ما يخلص الموظف تحميل كل المنتجات** — لما يكون في منتجات ناقصة، الزر بيعرض "حمّل باقي المنتجات (N) أولاً" بدل النص العادي. هذا القيد المقصود منه يمنع تعليم الطلب "جاهز" غلط قبل ما تكتمل السلة فعلياً.
- **مؤقّت زمني حي** (`so-load-timer`, أعلى الشيت، تنسيق ⏱ mm:ss): يبدأ العد من لحظة فتح شيت الطلب (`storeLoadStartedAt = Date.now()`)، ويتحدّث كل ثانية عبر `setInterval`. لما الموظف يضغط "جاهز للسائق"، الوقت المستغرق (بالثواني) يُحفظ بحقل جديد `loading_duration_seconds` على مستند الطلب بـFirestore — قابل لاستخدامه مستقبلاً بتقارير الأداء (أسرع/أبطأ متجر بالتحميل، الخ).
- **رقم الزبون يظهر تلقائياً لما يكون في ملاحظة على الطلب** — زر "📞 اتصل بالزبون — [الرقم]" (رابط `tel:`) يظهر جنب ملاحظة الزبون مباشرة، عشان الموظف يقدر يتواصل معه بسهولة لو الملاحظة تحتاج توضيح.
- حالة الـcheck-list وبار التقدّم والمؤقّت **تتصفّر تلقائياً** كل ما يفتح الموظف طلب جديد (`openStoreOrderSheet`)، وتتوقف/تتنظف لما يقفل الشيت (`closeStoreOrderSheet`) — عشان ما يصير تداخل بين طلبين مفتوحين بالتتابع.
- حُذفت الكلاس القديمة الميتة `.store-ing-row` (كانت للعرض الثابت القديم بدون تفاعل، ما عاد إلها استخدام).

### `akleto-customer.html`
- عند إنشاء الطلب، صار كل عنصر بـ`merged_ingredients` يحمل معه `image_url` (مأخوذ من `ingredientsCache` وقت إنشاء الطلب) — قبل هيك كان الحقل مو محفوظ بمستند الطلب، فتطبيق المتجر ما كان يقدر يعرض صور المنتجات إلا لو يعمل استعلام إضافي. هلق الصورة موجودة جاهزة بنفس مستند الطلب.
- **ملاحظة:** الطلبات القديمة (قبل هالتحديث) ما رح يكون فيها `image_url` بمكوناتها — تطبيق المتجر بيتعامل معها تلقائياً بعرض أيقونة 📦 بديلة، ما في خطأ أو كسر بالواجهة.

تم التحقق من صحة الكود (`node --check`) على الملفين قبل الرفع — بدون أخطاء.

### تعديل بنفس اليوم — حفظ تقدّم "نظام تحميل الأوردر" لو المتصفح تسكّر بالغلط

بطلب مؤيد بعد ملاحظته إنه التصميم الأساسي بسيط — أُضيفت طبقة استمرارية (persistence) حقيقية لنظام تحميل الأوردر بـ`akleto-store.html`:

- **حفظ فوري بـ`localStorage`** (مفتاح `akalito_store_load_<orderId>`) لكل تغيير: كل ما الموظف يعلّم منتج (`toggleStoreLoadItem`)، الحالة الكاملة (المنتجات المعلّمة + وقت بداية التحميل) تُكتب فوراً بـlocalStorage، مو بس بالذاكرة.
- **الاسترجاع التلقائي:** لما يفتح `openStoreOrderSheet(orderId)` أي طلب، أول شي يتحقق إذا في تقدّم محفوظ سابقاً لنفس الطلب — لو موجود يسترجعه بالكامل (نفس المنتجات المعلّمة ونفس وقت بداية المؤقّت الأصلي، مو يبدأ من صفر)، ولو مو موجود يبدأ حالة جديدة ويحفظها فوراً.
- **قفل الطلب لا يمسح التقدّم:** الضغط على "✕" لقفل الشيت (`closeStoreOrderSheet`) بيصفّر بس المتغيرات المحلية بالذاكرة (عشان طلب تاني يبدأ نظيف)، بس **ما بيحذف الحفظ بـlocalStorage** — يعني الموظف يقدر يقفل الشيت، يفتح طلب تاني، يرجع يفتح نفس الطلب الأول ولاقي كل شي زي ما تركه بالضبط.
- **الحذف يصير بس بحالتين:**
  1. لما يضغط الموظف "جاهز للسائق" بنجاح (`markStoreOrderReady`) — التقدّم يُحذف فوراً بعد نجاح التحديث.
  2. تنظيف تلقائي دوري (`cleanupStaleStoreLoadProgress`) — يشتغل تلقائياً كل ما يوصل تحديث جديد لقائمة طلبات المتجر (`onSnapshot` بـ`startStoreOrdersListener`)، وبيمسح أي تقدّم محفوظ لطلب صار حالته مو "وارد" (pending/preparing) — يعني لو الطلب اتلغى أو انسحب من متجر تاني أو تم تجهيزه من جهاز/موظف تاني، التقدّم القديم يتنضف تلقائياً وما يضل عالق بـlocalStorage.

هيك النظام صار مقاوم فعلياً لإغلاق المتصفح أو تحديث الصفحة أو حتى تبديل التبويبات بمنتصف عملية التحميل — بدون احتياج المستخدم يعمل أي شي إضافي.

تم التحقق من صحة الكود بـ`node --check` قبل الرفع.

### تعديل بنفس اليوم — تصليح مشكلة عدم ظهور صور المنتجات + تكبير الصورة بالضغط

**السبب الجذري لعدم ظهور الصور:** الطلبات القديمة (اللي اتسجلت قبل تحديث حفظ `image_url` بـ`merged_ingredients`) ببساطة ما فيها الحقل أصلاً — مو خطأ بالعرض، الحقل غير موجود بالبيانات نفسها. الطلبات الجديدة (بعد التحديث السابق) لازم يظهر فيها الصور عادي.

**الحل المُضاف (`akleto-store.html`) — بيغطي الحالتين (قديمة وجديدة):**
- `resolveIngredientImage(ing)`: دالة بتفضّل `ing.image_url` المحفوظة بالطلب نفسه، وإذا مو موجودة (طلب قديم، أو الصورة تغيّرت بعدين) بترجع تدور عليها بـ`ingredientsCache` (بيانات المكونات الأساسية) حسب `ingredient_id` — نفس المصدر اللي شاشة "منتجاتي" أصلاً بتستخدمه.
- `ensureIngredientsCacheLoaded()`: دالة جديدة تضمن تحميل `ingredientsCache` مرة وحدة بس (مع حماية من طلبات متكررة متزامنة عبر `ingredientsCacheLoadingPromise`)، وتُستدعى تلقائياً بالخلفية لما يدخل الموظف على التطبيق (`enterStoreMode()`) — يعني الصور تكون جاهزة قبل ما يفتح أي طلب أصلاً. `loadStoreProductsData()` (شاشة منتجاتي) صارت تستخدم نفس الدالة الموحّدة بدل التكرار.
- `openStoreOrderSheet`: يرسم الـchecklist فوراً (بأيقونة 📦 مؤقتة لو الصورة لسا ما وصلت)، وبعدين لما يخلص تحميل `ingredientsCache` يعيد رسم القائمة تلقائياً بالصور الحقيقية — بدون ما يحس الموظف بأي تأخير محسوس بالتطبيق العادي (الكاش محمّل مسبقاً من enterStoreMode).

**ميزة جديدة — تكبير الصورة (Lightbox):**
- الضغط على صورة أي منتج بقائمة تحميل الأوردر (مو الصف كامل، الصورة نفسها بس) بيفتح معاينة كاملة الشاشة للصورة (`imgLightbox`)، بخلفية داكنة شفافة، والضغط بأي مكان بيسكّرها.
- استُخدم `event.stopPropagation()` عشان الضغط على الصورة ما "يسرّب" الحدث لصف الـcheckbox ويعلّم/يلغي تعليم المنتج بالغلط بنفس الضغطة.

تم التحقق من صحة الكود (كلا الـ`<script>`) بـ`node --check` قبل الرفع.

## 4 أغسطس 2026 — تصليح scrolling القائمة الجانبية بلوحة الأدمن

**المشكلة:** القائمة الجانبية (`.sidebar`) بلوحة الأدمن ما كانت تعمل scroll لما تكبر عناصر التنقّل عن ارتفاع الشاشة.

**السبب:** `.sidebar` بـ`admin-style.css` كانت `min-height: 100vh` بدل `height: 100vh`. بما إنها `position: fixed` و`display:flex` وجواها `.sidebar-scroll` (اللي المفروض هو يعمل `overflow-y:auto` ويتحمّل السكرول لحاله)، استخدام `min-height` بدل `height` كان بخلّي الـ`.sidebar` نفسها تكبر وتطول عن الشاشة بدل ما تتقيّد بارتفاع الشاشة — فبالتالي الفليكس تشايلد (`.sidebar-scroll`) ما كان يضطر يعمل scroll أبداً، والمحتوى الزايد كان يطلع برا حدود الشاشة وما فيه طريقة توصله (لأنه العنصر position:fixed أصلاً).

**الحل:** تغيير سطر وحيد: `min-height: 100vh` → `height: 100vh` بكلاس `.sidebar`. بما إنه `admin-style.css` ملف مشترك بين كل صفحات الأدمن (orders, meals, ingredients, drivers, stores, customers, coupons, settlements, notifications, settings, units)، هالتصليح انطبق عليهم كلهم بتعديل واحد بدون الحاجة نعدّل كل صفحة لحالها.

## 4 أغسطس 2026 — توحيد الشروط والأحكام وسياسة الخصوصية عبر التطبيقات الثلاث

**بطلب مؤيد** — كان `akleto-terms.html` و`akleto-privacy.html` موجودين مسبقاً بس مكتوبين من منظور الزبون فقط (رابط الرجوع ثابت لـ`akleto-customer.html`، والمحتوى كله عن الطلبات والزبون بدون ذكر السائق أو المتجر). تم توحيدهم ليغطوا الأدوار الثلاث، وربطهم بكل التطبيقات:

### `akleto-terms.html` (شروط الاستخدام) — أعيدت كتابته بالكامل
- قسم عام (طبيعة الخدمة + الحساب) ينطبق على الكل.
- قسم مخصص "3️⃣ للزبائن" (نفس محتوى الطلبات/الأسعار القديم).
- قسم جديد "4️⃣ للسائقين" — طبيعة العمل كشريك مستقل، مسؤولية تسديد المستحق نقداً، احتساب العمولة، حق أكليتو بتعليق الحساب.
- قسم جديد "5️⃣ للمتاجر الشريكة" — مسؤولية دقة/جودة المنتجات، تحديث التوفر والأسعار بدقة، تجهيز الطلبات بوقت معقول.
- باقي الأقسام (حدود المسؤولية، التعديلات، القانون الحاكم) عُدّلت لتشير للتطبيقات الثلاث سوا.

### `akleto-privacy.html` (سياسة الخصوصية) — أعيدت كتابته بالكامل
- جدول البيانات المُجمّعة وُسّع ليشمل: موقع السائق الحي أثناء التوصيل، اسم المتجر واسم الموظف المسجّل دخول، بيانات الرصيد والتسويات المالية للسائقين — بالإضافة للبيانات القديمة الخاصة بالزبون.
- قسم "مشاركة البيانات" وُضّح بالتفصيل شو بيشوف كل طرف (المتجر يشوف بيانات الزبون المطلوبة للتجهيز، السائق يشوف اسم/رقم/عنوان الزبون للتوصيل، الزبون ما بيشوف بيانات شخصية زايدة عن اللازم عن السائق/الموظف).

### رابط الرجوع الديناميكي (كلا الملفين)
- زر الرجوع (`#backBtn`) صار يقرأ باراميتر `?from=` بالرابط (`customer` | `driver` | `store`) عبر JS بسيط بنهاية الصفحة، ويوجّه المستخدم رجوع لتطبيقه الصحيح (`akleto-customer.html` / `akleto-driver-home.html` / `akleto-store.html`) — بدل ما كان دايماً يرجّع لتطبيق الزبون بغض النظر مين فتح الصفحة. لو الباراميتر غير موجود أو غير معروف، الافتراضي "زبون" (نفس السلوك القديم، متوافق مع أي روابط قديمة بدون الباراميتر).
- أضيف أيضاً شريط تبويبات بصري صغير أعلى الصفحة (🧑 الزبون / 🛵 السائق / 🏪 المتجر الشريك) يبيّن أي تبويب "نشط" حسب الباراميتر — تنويه بصري بس المحتوى نفسه ظاهر للكل بصفحة وحدة (مش صفحات منفصلة لكل دور).

### إضافة الروابط بالتطبيقات
- **تطبيق الزبون** (`akleto-customer.html`): كانت الروابط موجودة أصلاً بـ3 أماكن (القائمة الجانبية، فوتر بسيط، وملاحظة تأكيد الطلب) — أُضيف لها بس `?from=customer` توضيحاً (سلوكها ما تغيّر، كانت أصلاً تشتغل صح).
- **تطبيق المتجر** (`akleto-store.html`): أُضيفت روابط جديدة بالقائمة الجانبية (قبل زر "خروج من حساب المتجر" مباشرة) بـ`?from=store`.
- **تطبيق السائق**: أُضيفت الروابط بالقائمة الجانبية على **كل الصفحات السبع** اللي فيها الدرور (`akleto-driver-home.html`, `akleto-driver-orders.html`, `akleto-driver-deliveries.html`, `akleto-driver-reports.html`, `akleto-driver-settle.html`, `akleto-driver-cards.html`, `akleto-driver-account.html`) بـ`?from=driver`، قبل زر "تسجيل الخروج" مباشرة بكل صفحة — بما إنه الدرور markup مو مُولَّد من مكان واحد مشترك (مو زي `driver-shared.js` للمنطق)، تم تعديل الصفحات السبع مباشرة بنفس المقتطف بالضبط. صفحة `akleto-driver.html` (تسجيل الدخول، قبل ما يصير في مستخدم مسجّل) ما فيها درور أصلاً، فما احتاجت تعديل.

تم التحقق من صحة كل الملفات المعدَّلة (JS syntax بـ`node --check`) قبل الرفع — 11 ملف بالمجموع.

## 4 أغسطس 2026 — شاشة "المالية" بتطبيق المتجر (إعادة تسمية "بوابة المتجر" + عمولات وساعات عمل)

**بطلب مؤيد** — تبويب "بوابة المتجر" (اللي كان placeholder فاضي بيقول "قريباً") تحوّل لشاشة حقيقية اسمها **"المالية"**، بتعرض للموظف المسجّل دخول حالياً: الطلبات اللي حمّلها، ساعات عمله، وعمولته المستحقة، مع إمكانية طلب تحصيلها. تعديل يمس 3 ملفات: `akleto-admin-settings.html`، `akleto-store.html`، وبنية بيانات Firestore (مجموعتين جديدتين).

### آلية العمولة (حسب طلب مؤيد بالضبط)
- عمولة الموظف: **1 دينار ثابت لكل طلب يحمّله** (افتراضياً) — **قابلة للتحكم من لوحة الأدمن**، إما كمبلغ ثابت لكل طلب أو كنسبة مئوية من قيمة الطلب.
- عمولة أكليتو من عمولة الموظف: **10% افتراضياً** من إجمالي عمولة الموظف (مو من قيمة الطلب) — **أُضيفت قابلة للتحكم من الأدمن أيضاً** (نفس منطق تحكم عمولة السائق، بس رقم منفصل).
- مثال: موظف حمّل 20 طلب بعمولة ثابتة 1 د.أ → إجمالي عمولته 20 د.أ → عمولة أكليتو منها (10%) = 2 د.أ → الصافي المستحق للموظف = 18 د.أ.

### `akleto-admin-settings.html` — إعدادات جديدة
- بطاقة جديدة "عمولة موظف المتجر" (🏪) بجانب بطاقة عمولة السائق الموجودة أصلاً، بنفس النمط (نوع العمولة: ثابت/نسبة + معاينة حية).
- حقل إضافي: "نسبة عمولة أكليتو من عمولة الموظف (%)".
- كل هذا يُخزَّن بنفس مستند `app_settings/commissions` الموجود أصلاً (حقول جديدة: `store_commission_type`, `store_commission_value`, `store_akleto_cut_percent`, `store_updated_at`) — بدون التأثير على إعدادات عمولة السائق الموجودة بنفس المستند.

### `akleto-store.html` — شاشة المالية
- **إعادة التسمية:** "بوابة المتجر" بالقائمة الجانبية صارت "المالية"، وتفتح شاشة حقيقية (`storeFinanceScreen`) بدل رسالة "قريباً".
- **ربط الموظف بالطلب:** لما يعلّم الموظف الطلب "جاهز للسائق" (`markStoreOrderReady`)، صار الطلب يُحفظ معه `loaded_by_emp_name`، `loaded_by_emp_phone`، و`ready_at` (وقت التجهيز الفعلي، منفصل عن `created_at` وقت إنشاء الزبون للطلب — مهم لحساب العمولة حسب تاريخ التحميل الفعلي مو تاريخ الطلب).
- **فلترة بالتاريخ:** أزرار سريعة (اليوم / هاي الأسبوع / هاي الشهر / الكل) + حقلين تاريخ مخصصين (من/إلى) لأي مدى مخصص.
- **حاويات إحصائية:** عدد الطلبات المحمّلة + ساعات العمل (محسوبة من `store_work_sessions`)، كلاهما يتغيّر تلقائياً حسب المدى الزمني المختار.
- **بطاقة العمولة:** سعر العمولة الحالي (يُقرأ حي من إعدادات الأدمن)، إجمالي عمولة الموظف، عمولة أكليتو المخصومة منها، والصافي المستحق.
- **زر "تحصيل العمولة":** يرسل طلب تحصيل (مجموعة جديدة `store_commission_requests`، حالة `pending`) + إشعار للأدمن (`admin_notifications`). لو في طلب معلّق أصلاً، الزر يختفي ويظهر بدله شريط "⏳ طلب قيد المراجعة" بدل السماح بطلبات مكررة.
- **سجل الطلبات:** قائمة كل طلب حمّله الموظف بالمدى المختار، مع الكود ووقت التجهيز وقيمة العمولة عنه بالتحديد.

### تتبع ساعات العمل — مجموعة جديدة `store_work_sessions`
- عند كل تسجيل دخول (`enterStoreMode`)، يُنشأ مستند جلسة عمل جديدة `{store_id, emp_name, emp_phone, login_at, logout_at: null}` — إلا لو في جلسة مفتوحة أصلاً محفوظ معرّفها بـ`localStorage` (يعني لو الموظف سكّر المتصفح بالغلط بدون تسجيل خروج رسمي وبعدين رجع فتح، ما بينشئ جلسة جديدة زيادة، بيكمل نفس الجلسة المفتوحة).
- عند تسجيل الخروج (`logoutStore`)، الجلسة المفتوحة تُقفل (`logout_at`) ويُمسح معرّفها من `localStorage`.
- حساب "ساعات العمل" لأي مدى زمني مختار بيقتصّ (clip) كل جلسة على حدود المدى المطلوب بدقة — يعني لو جلسة بدأت الساعة 8 صباح وانتهت 4 عصر، وفلترت على "الساعة 12 لـ2"، بياخذ بس الـ2 ساعة المتقاطعة، مو الجلسة كاملة.

### حساب العمولة نفسها
- بيعتمد على `storeOrdersCache` الموجودة أصلاً (real-time listener لكل طلبات المتجر) — فلترة client-side حسب `loaded_by_emp_phone` وحالة الطلب (ready/delivering/done) والمدى الزمني، بدون أي استعلام Firestore إضافي لهاي الجزئية تحديداً (كفاءة أفضل، صفر تكلفة قراءة زيادة).

**⚠️ ملاحظتين مهمتين لمؤيد:**
1. **مجموعتين جديدتين بـFirestore** (`store_work_sessions`, `store_commission_requests`) — لو في قواعد أمان مخصصة (Security Rules) غير مفتوحة بشكل عام، لازم تُضاف لهم صلاحيات قراءة/كتابة مشابهة للمجموعات الموجودة (`orders`, `store_ingredients`)، وإلا الميزة رح تفشل بصمت (يطلع خطأ بالـconsole فقط).
2. **طلب "تحصيل العمولة" حالياً بيسجّل الطلب بس ويبلّغ الأدمن — ما في لسا شاشة إدارية بلوحة الأدمن لمراجعة/الموافقة على هالطلبات وتسديدها** (زي `akleto-admin-settlements.html` الموجودة لتسوية السائقين). هاي خطوة تالية منطقية لو حاب نكملها — بس ما كانت مطلوبة صراحة بهالطلب.

تم التحقق من صحة الكود (كلا الملفين، وكلا الـ`<script>` بـ`akleto-store.html`) بـ`node --check` قبل الرفع.

### تعديل بنفس اليوم — شاشة إدارية لمراجعة طلبات تحصيل عمولات موظفي المتاجر

**بطلب مؤيد** — أُضيف الجزء الناقص من ميزة "المالية": شاشة بلوحة الأدمن لمراجعة والموافقة/رفض طلبات "تحصيل العمولة" اللي يرسلها موظفو المتاجر.

بدل إنشاء صفحة أدمن جديدة، تم توسيع الصفحة الموجودة أصلاً `akleto-admin-settlements.html` (طلبات التسديد والتحويل الخاصة بالسائقين) لتشمل مصدرين:
- **🛵 طلبات السائقين** (كانت موجودة أصلاً — بدون أي تغيير بسلوكها).
- **🏪 طلبات المتاجر (عمولات الموظفين)** — جديد، من مجموعة `store_commission_requests`.

مفتاح تبديل ("Source switch") فوق تبويبات "قيد الانتظار / السجل السابق" الموجودة أصلاً، بحيث نفس الصفحة تخدم الاثنين بدون تكرار الكود:
- بطاقة كل طلب تحصيل من متجر تعرض: اسم الموظف + اسم المتجر، المبلغ، المدى الزمني المطلوب (`range_from` → `range_to`، أو "كل الفترة" لو مفتوح)، ورقم هاتف الموظف.
- زرين "تأكيد ✓" / "رفض ✕" لكل طلب معلّق — التأكيد يعني "أكليتو حوّلت هالمبلغ فعلياً للموظف" (بيحدّث حالة الطلب لـ`completed`)، والرفض يحدّثها لـ`rejected`.
- عداد الطلبات المعلّقة بالقائمة الجانبية (`settleNavBadge`) صار يجمع عدد الطلبات المعلّقة من المصدرين سوا (سائقين + متاجر).

تم التحقق من صحة الكود بـ`node --check` قبل الرفع. الميزة الآن متكاملة من طرف لطرف: الموظف يطلب من تطبيق المتجر → الأدمن يراجع ويوافق/يرفض من لوحة الأدمن → حالة الطلب تنعكس تلقائياً بتطبيق المتجر بمجرد ما يفتح شاشة المالية مرة ثانية.

## 4 أغسطس 2026 — زر "متاح/غير متاح" بتطبيق المتجر (نفس فكرة تطبيق السائق)

**بطلب مؤيد** — أُضيف زر متاح/غير متاح بأعلى شاشة "طلبات المتجر" (الشاشة الرئيسية)، بنفس الفكرة والتصميم البصري لزر التوفر بتطبيق السائق (نقطة + نص + حالة تحميل بأيقونة دوّارة CSS `.spin`)، بس بدون الحاجة لصلاحية الموقع (المتجر ثابت المكان، عكس السائق).

### الفرق عن `is_active` الموجود أصلاً
- `is_active` (بمستند المتجر بـFirestore، يتحكم فيه الأدمن فقط من `akleto-admin-stores.html`): يحدد إذا المتجر **مفعّل كشريك بالنظام أصلاً** — لو `false`، الموظف ما يقدر حتى يسجّل دخول ("هذا المتجر معطّل حالياً — تواصل مع الأدمن").
- `is_open` (**جديد**، يتحكم فيه الموظف مباشرة من تطبيق المتجر، لحظياً): يحدد إذا المتجر **متاح الآن تحديداً لاستقبال طلبات جديدة** — زي لافتة "مفتوح/مغلق" فعلية، للاستراحات أو نهاية الدوام أو أي سبب مؤقت، بدون الحاجة يتواصل مع الأدمن كل مرة.

### `akleto-store.html`
- `watchStoreOpenState()`: مراقبة حية (`onSnapshot`) على مستند `stores/{storeId}` — أي تغيير (حتى لو صار من جهاز تاني لنفس المتجر) ينعكس فوراً بالزر.
- الحقل `is_open` غير الموجود بالمستند (متجر لسا ما استخدم الميزة) يُعامَل كـ`true` (متاح) افتراضياً — ما في كسر لأي سلوك قديم.
- `toggleStoreOpen()`: يحدّث `is_open` بـFirestore مباشرة، مع حالة تحميل واضحة ("جاري التفعيل..." / "جاري الإيقاف...") وتنظيف بـ`finally`.

### `akleto-customer.html` — احترام حالة الإغلاق بالتوجيه
- `loadActiveStoresForPicker()` (قائمة اختيار المتجر يدوياً) و`loadStoresAndAvailability()` (التوجيه التلقائي لأقرب متجر عنده كل المكونات) — كلاهما صار يستثني أي متجر `is_open === false` من النتائج، بالضبط زي `is_active`.
- **تحقق أمان إضافي عند إرسال الطلب فعلياً** (مو بس بالتصفح): قبل إنشاء الطلب مباشرة، بيتم جلب مستند المتجر المُقفل بالسلة (`cartLockedStore`) من جديد والتأكد إنه لسا `is_active !== false` و`is_open !== false` — يمنع سيناريو نادر: الزبون فتح صفحة الطلب والمتجر كان مفتوح، بعدين الموظف قفل المتجر أثناء ما الزبون قاعد يعبّي السلة، فلو حاول يأكد الطلب بعد هيك بيوقف برسالة واضحة بدل ما يوصل طلب لمتجر مقفول.

تم التحقق من صحة الكود (كلا الملفين) بـ`node --check` قبل الرفع.

**ملاحظة توكن GitHub:** التوكن الأول المستخدم بالجلسة انتهت صلاحيته أثناء الشغل، مؤيد زوّد توكن جديد وكمّلنا الرفع عادي.


## 6 أغسطس 2026 — تنظيف CSS المكرر بين akleto-customer.html وakleto-store.html

بطلب مؤيد — تنفيذ نقطة التحسين المؤجلة الموثّقة بنقطة 32 ("لو حابين تحسين الحجم مستقبلاً ممكن نرجع نقلّم CSS لكل ملف لحاله").

**الطريقة:** سكربت Python تحقق آلياً لكل قاعدة CSS (selector كامل من كلاسات/IDs بس، بدون عناصر HTML عامة زي h1/div عشان ما نكسر تنسيق عام) هل أي كلاس/ID فيها موجود فعلياً بأي مكان تاني بالملف (HTML أو JS) — لو ولا وحدة من كلاسات القاعدة مستخدمة، القاعدة تُحذف كاملة. تم التحقق تلقائياً بعدها إنه **كل محتوى الملف عدا وسم `<style>` نفسه بقي مطابق 100% بدون أي تغيير** (لا HTML ولا JS اتلمس إطلاقاً).

**النتيجة:**
- `akleto-customer.html`: حذف 20 كلاس خاصة بتطبيق المتجر القديم (`.store-tabs`, `.store-tab`, `.product-tile*`, `.store-order-card*`, `.store-ing-row`, `.brand-spinner`) — كانت متبقية من قبل الفصل المعماري (نقطة 32) وما عاد أي HTML/JS بالملف يستخدمها. حجم CSS: 31KB → 28KB.
- `akleto-store.html`: حذف 107 كلاس خاصة بتطبيق الزبون (الشاشة الرئيسية `#welcomeScreen`/`.home-hero`/`.cat-chip`، بطاقات الوجبات `.meal-tile*`، السلة `.cart-*`/`.mini-stepper`، شاشة تأكيد الطلب `.confirm-*`، شاشة العناوين `.addr-*`، الشريط السفلي `.bottom-nav`/`.nav-btn`، وغيرها) — حجم CSS: 33KB → 18KB (تقريباً نص الحجم).
- تم التحقق من صحة كود JS (`node --check`) على كلا الملفين (script عادي + module) بعد التعديل — بدون أخطاء.

**⚠️ نطاق محافظ متعمّد:** أي قاعدة CSS فيها selector يشمل عنصر HTML عام (زي `h1,h2,h3,.font-title`) اتركت كاملة بدون لمس حتى لو جزء منها (كلاس واحد) غير مستخدم — تفادياً لأي احتمال كسر تنسيق عناصر عامة. نفس الشي لقواعد `@media`/`@keyframes` (ما كان فيها أي حالة أصلاً بالملفين، فما احتجنا نلمسها). التعديل **لم يمس** أي ملف تاني غير هالاثنين، ولا أي منطق Firebase/JS.


## 6 أغسطس 2026 — إشعارات الدفع (Push Notifications) لتطبيق السائق

بالفحص تبيّن إنه `mobile-driver/android/app/google-services.json` أصلاً كان صحيح ومحدّث (يحوي client entries للاثنين `com.akalito.customer` و`com.akalito.driver` بنفس مشروع `akleto-prod`) — مش ناقص زي ما كنا فاكرين. الفجوة الحقيقية كانت: **كود تسجيل الإشعارات نفسه ما كان مكتوب أبداً بصفحات السائق** (بعكس الزبون اللي عنده `initPushNotifications` بـ`akleto-customer.html`).

**التنفيذ:**
- أضفت دالة `initDriverPushNotifications(driverId)` بـ `driver-shared.js` — نفس نمط الزبون بالضبط: تتحقق إنها جوا تطبيق أندرويد فعلي (`Capacitor.isNativePlatform()`) قبل أي شي (ما بتشتغل إطلاقاً بمتصفح عادي/PWA)، تطلب صلاحية الإشعارات، تسجّل، وتحفظ الـ FCM token بمستند `drivers/{driverId}` (`fcm_token`, `fcm_platform`, `fcm_updated_at`). لما السائق يضغط على إشعار وصل، بيوديه مباشرة لصفحة `akleto-driver-orders.html`.
- ربطتها بـ `akleto-driver-home.html` (أول صفحة بعد تسجيل الدخول) — تُستدعى مرة وحدة مع `session.id` بعد `requireDriverSession()`.
- تحقق syntax على الملفين (`node --check`) قبل الرفع — بدون أخطاء.

**⚠️ لسا ناقص عشان الإشعارات توصل فعلياً (نفس النقطة المعلّقة أساساً بالزبون):**
- تفعيل خطة Firebase **Blaze**
- بناء **Cloud Function** ترسل الإشعار الفعلي (عبر FCM) لما تتغيّر حالة الطلب — لسا ما بتفرق بين توكن الزبون وتوكن السائق، لازم تُبنى بحيث تبعت لكل طرف بالسيناريو المناسب له (زبون: تحديث حالة طلبه، سائق: طلب جديد متاح بمنطقته).


## 6 أغسطس 2026 — Cloud Function لإرسال إشعارات الدفع الفعلية (FCM)

بُني مشروع Cloud Functions كامل بجذر الريبو (`functions/index.js` + `functions/package.json` + `firebase.json` + `.firebaserc` مربوط بمشروع `akleto-prod`)، جاهز للنشر فور تفعيل خطة Blaze.

**3 دوال (كلهم بمنطقة `europe-west1`):**
1. `onOrderUpdated` — عند أي تحديث بمستند `orders/{orderId}`:
   - لو تغيّرت `status` → يبعت إشعار FCM للزبون (`customers/{customer_id}.fcm_token`) بنص عربي مخصص لكل حالة (`preparing`/`ready`/`delivering`/`done`/`cancelled`).
   - لو انعيّن `driver_id` لأول مرة (كان فاضي وصار فيه قيمة) → يبعت إشعار "طلب جديد إلك" للسائق (`drivers/{driver_id}.fcm_token`).
2. `onOrderReady` — لو الطلب صار `ready` وما زال بدون سائق (`driver_id` فاضي) → إشعار جماعي (broadcast) لكل السائقين اللي `is_available == true`، حتى ينتبهوا لطلب متاح بأسرع وقت. ملاحظة موثّقة بالكود: لو عدد السائقين المتاحين كبر مستقبلاً، الأفضل نحوّلها لـ FCM Topic بدل حلقة `send` فردية لكل توكن.

كل الدوال بتتعامل بهدوء مع توكن غير صالح/منتهي (`try/catch` حول كل `send`) بدون ما توقف باقي الإشعارات.

**⚠️ خطوات النشر المتبقية (يدوية، من جهاز مؤيد):**
1. تفعيل خطة **Blaze** على مشروع `akleto-prod` من Firebase Console (Cloud Functions ما بتشتغل بدونها حتى لو الاستخدام ضمن الحد المجاني).
2. `npm install` جوا مجلد `functions/` (أول مرة بس).
3. `firebase login` ثم `firebase deploy --only functions` من جذر الريبو.
4. بعد أول نشر ناجح، أي تحديث لـ `functions/index.js` بالمستقبل يحتاج نفس أمر النشر بس (بدون أي تعديل بتطبيقات الويب).


## 6 أغسطس 2026 — ✅ Cloud Function منشورة فعلياً وشغّالة (Deploy complete)

مؤيد نفّذ خطوات النشر من جهازه (تثبيت Node.js + Git + firebase-tools، تفعيل خطة Blaze، `firebase login`، `git clone`، `npm install`، `firebase deploy --only functions`). أول محاولة فشلت برسالة متوقعة لأول استخدام لـ Cloud Functions Gen 2 على المشروع ("Permission denied while using the Eventarc Service Agent... retry in a few minutes") — بعد إعادة المحاولة بعد كم دقيقة نجح النشر بالكامل.

**الحالة الآن:**
- `onOrderUpdated` و`onOrderReady` منشورتين وشغّالتين فعلياً بمنطقة `europe-west1` على مشروع `akleto-prod`.
- **تطبيق الزبون (APK الحالي على Google Play/المُوزَّع)**: رح يستقبل الإشعارات **فوراً بدون أي تحديث أو إعادة بناء** — لأن كود `initPushNotifications` كان أصلاً موجود بالتطبيق من قبل.
- **تطبيق السائق**: الكود جاهز (`initDriverPushNotifications`)، بس **لسا ما تم بناء APK له أبداً** — الإشعارات رح توصل للسائق بس بعد ما يُبنى ويُثبّت APK إله (نفس النقطة المعلّقة سابقاً بخانة "على الأفق").
- ملاحظة مهمة موثّقة: الإشعارات **ما بتوصل إطلاقاً** لو المستخدم فاتح الصفحة من متصفح عادي (PWA) بدل تطبيق APK مثبّت — هذا مقصود بالكود (`Capacitor.isNativePlatform()` check).
- تم قبول سياسة تنظيف تلقائي لصور الحاويات المؤقتة (container images) بـ Artifact Registry (تُحذف الصور الأقدم من يوم واحد تلقائياً، لتفادي أي فوترة صغيرة متراكمة).


## 6 أغسطس 2026 — مشروعا Capacitor Android لتطبيقي المتجر والإدارة (mobile-store / mobile-admin)

بناءً على قالب `mobile-driver` (اللي جُرّب ونجح بالبناء فعلياً)، تم استنساخ نفس البنية الكاملة لمشروعين جديدين:

- **`mobile-store/`** → يغلّف `akleto-store.html`، App ID: `com.akalito.store`، الاسم الظاهر: "أكليتو المتجر"
- **`mobile-admin/`** → يغلّف `akleto-admin.html`، App ID: `com.akalito.admin`، الاسم الظاهر: "أكليتو الإدارة"

**الفرق المتعمّد عن قالب السائق:**
- **بدون إشعارات دفع** (`@capacitor/push-notifications` والـ `google-services.json` مو مضافين) — لأن `akleto-store.html` و`akleto-admin.html` ما فيهم كود استقبال إشعارات أصلاً حالياً. `build.gradle` مكتوب أصلاً بحيث يتجاهل `google-services` plugin بهدوء لو الملف مو موجود (نفس آلية موثّقة بمشروع السائق)، فالبناء رح يشتغل عادي بدونها.
- **صلاحية `INTERNET` بس** بـ`AndroidManifest.xml` — تحققت إنه لا `akleto-store.html` ولا `akleto-admin.html` يستخدموا `geolocation` إطلاقاً، فما في داعي لصلاحية الموقع.
- `package-lock.json` ما انسخ (رح يتولّد تلقائياً أول `npm install`).
- كل الأيقونات/splash screens/gradle wrapper نُسخت **بنفس الملفات الثنائية بالضبط** من مشروع السائق (أيقونة مؤقتة مشتركة) — لسا محتاجة تُستبدل لاحقاً بأيقونة خاصة بكل تطبيق (موثّق بـ README كل مشروع).

**⚠️ تم بالكامل عبر GitHub API (بدون لمس أي ملف تاني بالريبو، تحقق آلي بعد الرفع)** — لسا ما جُرّب فعلياً بـ Android Studio ولا اتبنى APK منهم. **الخطوات المتبقية على مؤيد** (نفس أسلوب تطبيق السائق بالضبط):
1. `git pull origin main`
2. من جوا `mobile-store/` (وبعدها بشكل منفصل `mobile-admin/`): `npm install` ثم `npx cap sync android`
3. فتح مجلد `android` بكل مشروع بـ Android Studio، استنى Gradle Sync
4. `Build → Build Bundle(s)/APK(s) → Build APK(s)`


## 6 أغسطس 2026 — إشعارات الدفع لتطبيق المتجر

- أضفت `initStorePushNotifications(storeId)` بـ `akleto-store.html` (نفس نمط الزبون/السائق بالضبط) — تُستدعى من `enterStoreMode()` (تشتغل بعد تسجيل الدخول وبعد استرجاع الجلسة المحفوظة). التوكن يُحفظ بـ `stores/{storeId}.fcm_token`.
- حدّثت مشروع `mobile-store/` ليتضمن `@capacitor/push-notifications` (package.json، capacitor.settings.gradle، capacitor.build.gradle) وصلاحية `POST_NOTIFICATIONS` بـ AndroidManifest.
- أضفت دالة رابعة بـ Cloud Function: **`onOrderCreated`** — بترسل إشعار "طلب جديد 🛎️" للمتجر فور ما الزبون ينهي طلب (`orders/{orderId}` جديد). تم نشرها فوراً ضمن نفس النشر السابق (الدوال التانية موجودة أصلاً على السيرفر، هاي إضافة عليهم، بس **لسا لازم إعادة نشر يدوي** بنفس أمر `firebase deploy --only functions` حتى تنعكس فعلياً).

**⚠️ خطوة ضرورية ناقصة قبل ما تشتغل فعلياً (المتجر تحديداً، بعكس الزبون والسائق):**
تطبيق المتجر **مو مسجّل بمشروع Firebase بعد** كتطبيق أندرويد مستقل. لازم مؤيد:
1. يروح لـ [Firebase Console](https://console.firebase.google.com) → مشروع `akleto-prod` → ⚙️ Project settings → General → Add app → أيقونة أندرويد.
2. Android package name **بالضبط**: `com.akalito.store`
3. يحمّل `google-services.json` الناتج ويحطه بالمسار: `mobile-store/android/app/google-services.json`
4. بدون هالملف، الـ APK رح يبنى ويشتغل عادي (Gradle بيتجاهل الميزة بهدوء)، بس تسجيل الإشعارات (`Push.register()`) رح يفشل صامتاً لأنه ما في إعداد Firebase صحيح مربوط بـ package الاسم هذا.

**متبقي بعدها:** إعادة نشر الـ Cloud Function (`firebase deploy --only functions` من جذر المشروع على جهاز مؤيد) حتى تنعكس دالة `onOrderCreated` الجديدة على السيرفر.


## 6 أغسطس 2026 — ✅ google-services.json مرفوع لتطبيق المتجر

مؤيد سجّل `com.akalito.store` بـ Firebase Console ورفع لي `google-services.json` الناتج — تحقق منه ومطابق (يحوي client entry صحيح لـ `com.akalito.store` ضمن مشروع `akleto-prod`). تم رفعه لمسار `mobile-store/android/app/google-services.json`.

**تطبيق المتجر صار جاهز بالكامل** لإشعارات الدفع (نفس مستوى الزبون والسائق) — الباقي فقط: بناء APK جديد من جهاز مؤيد (`npm install` → `npx cap sync android` → بناء APK بـ Android Studio)، وإعادة نشر الـ Cloud Function (`firebase deploy --only functions`) حتى تنعكس دالة `onOrderCreated` الجديدة على السيرفر.


## 6 أغسطس 2026 — إصلاح زر الرجوع الفيزيائي (Android hardware back) + إصلاح خطأ "التحقق من توفر الوجبة"

**السبب الجذري لمشكلة زر الرجوع:** تطبيقات الزبون/المتجر/الإدارة تطبيقات صفحة-وحيدة (SPA) — كل الشاشات والنوافذ المنبثقة بتتحكم عبر إظهار/إخفاء CSS (`classList`)، مش عبر تنقّل حقيقي بالمتصفح. سلوك Capacitor الافتراضي لزر الرجوع الفيزيائي: لو WebView تقدر ترجع بالتاريخ (history) بترجع، وإلا بتقفل التطبيق مباشرة. بما إنه ما في تاريخ تصفّح حقيقي بهالتطبيقات (كل شي بنفس الصفحة)، كان دايماً بيسكّر التطبيق فوراً — بغض النظر عن أي شاشة/نافذة مفتوحة.

**الحل:** إضافة مكتبة `@capacitor/app` للأربع تطبيقات، مع معالج مخصص لحدث `backButton` بكل تطبيق يتحقق (بترتيب "الأعمق أولاً") من أي شاشة/نافذة مفتوحة حالياً ويستدعي **نفس دالة الإغلاق** المستخدمة أصلاً بزر الرجوع الداخلي (‹/✕) — وبس لو ما في شي مفتوح ونحن فعلاً بالشاشة الرئيسية، يطلع من التطبيق.

- **`akleto-customer.html`** (`setupHardwareBackButton`): يتحقق بالترتيب من نموذج عنوان جديد → شاشة العناوين → الإعدادات → تأكيد الطلب → النوافذ المنبثقة (وجبة/منتج/اختيار مكوّن/إشعارات/قريباً) → قائمة الحساب الجانبية → شاشة اختيار متجر بتبويب المنتجات → التبويب الحالي (لو مو الرئيسية يرجّعها للرئيسية) → أخيراً يطلع من التطبيق.
- **`akleto-store.html`** (`setupStoreHardwareBackButton`): نفس المبدأ (lightbox → نافذة الطلب → نافذة المنتج → الإشعارات → شاشة المنتجات → شاشة المالية → القائمة الجانبية → خروج).
- **`akleto-admin.html`** (`setupAdminHardwareBackButton`): أبسط (بس نافذتين منبثقتين: إضافة وجبة/مكوّن) — باقي صفحات الإدارة صفحات منفصلة حقيقية (`location.href`) فزر الرجوع الافتراضي بينها شغّال صح أصلاً بدون كود إضافي.
- **`driver-shared.js`**: تطبيق السائق أصلاً مبني من صفحات منفصلة حقيقية، فالتنقّل بينها بزر الرجوع شغّال صح افتراضياً. الإضافة الوحيدة: لو المستخدم على الصفحة الرئيسية (`akleto-driver-home.html`) تحديداً، زر الرجوع يطلع من التطبيق فوراً (بدل ما يحاول يرجع لصفحة الدخول). الكود شغّال تلقائياً بمجرد تحميل `driver-shared.js` بأي صفحة سائق (self-invoking function)، بدون حاجة لاستدعاء يدوي من كل صفحة.
- تحديث `package.json` + `capacitor.settings.gradle` + `capacitor.build.gradle` بالأربع مشاريع (`mobile-customer`, `mobile-driver`, `mobile-store`, `mobile-admin`) لربط module الـ`@capacitor/app` بمشروع أندرويد.

**إصلاح إضافي — خطأ "صار خطأ بالتحقق من توفر الوجبة" (تطبيق الزبون):** السبب كان دالة `loadStoreProductsMap` تستخدم استعلام Firestore بشرطين مع بعض (`where('store_id', ...) + where('is_available', ...)`) وهالنوع يحتاج **فهرس مركّب (composite index)** ما تم إنشاؤه أبداً بقاعدة البيانات — فكان يفشل بثبات. الإصلاح: الاستعلام صار بشرط واحد بس (`store_id`)، والفلترة على `is_available` انتقلت للكود نفسه (JS) — بدون أي حاجة لإنشاء فهرس جديد بـ Firebase Console.

**⚠️ متبقٍ على مؤيد لتفعيل إصلاح زر الرجوع فعلياً:**
1. `git pull origin main`
2. لكل من `mobile-customer` و`mobile-driver` و`mobile-store` و`mobile-admin`: `npm install` ثم `npx cap sync android`
3. إعادة بناء APK بـ Android Studio لكل تطبيق (نفس خطوات السائق بالضبط)

**إصلاح "التحقق من توفر الوجبة" ينعكس فوراً** بدون أي خطوة إضافية (لأنه التطبيق يحمّل من الرابط الحي على GitHub Pages مباشرة).

**⏳ لسا قيد التشخيص:** مشكلة زر "متاح/غير متاح" بتطبيق المتجر — بيرجع "صار خطأ" عند الضغط، غالباً بسبب قواعد صلاحيات Firestore (Rules) على مجموعة `stores`. بانتظار مؤيد يبعت محتوى القواعد الحالية من Firebase Console.


## 6 أغسطس 2026 — ✅ إصلاح صلاحيات Firestore لتطبيق المتجر (Missing or insufficient permissions)

**تشخيص دقيق عبر Chrome Remote Debugging** (chrome://inspect بعد تفعيل USB debugging على الموبايل ووصله بكيبل) — كشف الخطأ الحقيقي: `FirebaseError: Missing or insufficient permissions`.

**السبب الجذري:** قواعد Firestore الأصلية كانت:
```
match /stores/{storeId} {
  allow read: if true;
  allow write: if request.auth != null;
}
```
تطبيق المتجر ما بيستخدم Firebase Auth حقيقي (نظام دخول بسيط بـ PIN فقط)، فـ `request.auth` دايماً `null` من جهته → أي كتابة (تفعيل/إيقاف المتجر، طلب تحصيل عمولة) بترفض دايماً. اشتغل مصادفة بالمتصفح لأنه كان في جلسة Auth قديمة من لوحة الإدارة بنفس المتصفح (Firebase Auth بينحفظ بمستوى الـ origin/الدومين، فبينطبق على كل صفحات نفس الموقع) — أما بالـ APK (WebView معزول تماماً) ما في أي جلسة Auth إطلاقاً، فيفشل دايماً وبثبات.

**اكتشاف إضافي أثناء المراجعة:** 3 مجموعات ثانية بيستخدمها تطبيق المتجر (`store_ingredients` — تعديل سعر/توفر منتج، `store_work_sessions` — تسجيل دوام الموظف، `store_commission_requests` — طلب تحصيل العمولة) ما كانت معرّفة صراحة بالقواعد أصلاً، فكانت واقعة تحت قاعدة catch-all الأخيرة (`allow read, write: if request.auth != null`) — يعني نفس مشكلة الصلاحيات موجودة فيهم كمان (حتى لو ما تم الإبلاغ عنها بعد).

**الإصلاح** (نشره مؤيد يدوياً من Firebase Console → Firestore → Rules → Publish):
- `stores`: صار `allow read, write: if true` — نفس نمط `customers`/`drivers` (كلهم أنظمة دخول بدون Auth حقيقي حالياً).
- أُضيفت صراحة: `store_ingredients`، `store_work_sessions`، `store_commission_requests`، `admin_notifications` — كلهم `allow read, write: if true`.
- باقي القواعد **بدون أي تغيير**.

**✅ تم التأكيد من مؤيد إنه اشتغل تمام بعد النشر** — زر "متاح/غير متاح" وطلب تحصيل العمولة صاروا يشتغلوا من الـ APK مباشرة.

**⚠️ ملاحظة أمنية موثّقة (مو عاجلة، بس مهمة للمستقبل):** فتح `allow write: if true` على مجموعات زي `stores` معناه من الناحية النظرية أي حدا يعرف مسار الوثيقة (حتى بدون تطبيقنا) يقدر يعدّل بيانات أي متجر مباشرة عبر Firestore REST API. هاي بالضبط نفس فئة الخطر الموثّقة أصلاً بخصوص `customers`/`drivers` — كلها بتتحل نهائياً ببناء نظام Auth حقيقي (نقطة "قبل الإطلاق" الموثّقة سابقاً بالخطة).


## 7 أغسطس 2026 — ✅ نظام تسجيل حساب ودخول كامل للسائقين (تسجيل موثّق + مراجعة أدمن)

استُبدل نموذج التسجيل البسيط القديم (اسم + هاتف فقط) بنظام تسجيل كامل موثّق، بدون Firebase Auth حقيقي (نفس نمط باقي التطبيق — جلسة محلية بـ `localStorage` بعد التحقق من مجموعة `drivers` مباشرة).

**ملفات جديدة:**
- **`akleto-driver-signup.html`** 🆕 — معالج تسجيل بـ 3 خطوات (wizard):
  1. البيانات الشخصية والمركبة: الاسم الكامل (يتحقق إنه 3 مقاطع على الأقل)، رقم الهاتف (يتحقق إنه مش مسجّل مسبقاً عبر `findDriverByPhone`)، نوع المركبة (سيارة/دراجة نارية/سكوتر) عبر قائمة منسدلة، حقل بحث حر (combobox مبني يدوياً، مافي `<select>` بحث أصلي بالـ HTML) لاختيار الماركة والموديل من قائمة `JORDAN_CAR_MODELS`، ورقم لوحة المركبة.
  2. رفع 6 صور إلزامية (كل صورة تُرفع فوراً عند الاختيار عبر `uploadImage()` من `firebase-init.js` لمجلد `driver_documents/` بـ Storage، مع معاينة مصغّرة وعلامة ✓): الصورة الشخصية، الهوية (وجهين منفصلين)، رخصة القيادة، رخصة المركبة (وجهين منفصلين). ما بيقدر يكمّل للخطوة التالية إلا لو الصور الست مرفوعة.
  3. الآيبان (اختياري بالكامل) — زرّين: "حفظ وإرسال الطلب" أو "تخطي هالخطوة، رح أضيفه لاحقاً". بأي الحالتين يُستدعى `createDriverAccount()` وينشئ سجل بـ `drivers` بحالة `approval_status: 'pending'`.
  - شاشة نجاح أخيرة توضح إن الحساب "قيد المراجعة" وترجّع لصفحة الدخول — **ما بينشئ جلسة دخول تلقائية** (السائق لازم ينتظر موافقة الأدمن).
- **`jordan-vehicles.js`** 🆕 — قائمة ثابتة (`JORDAN_CAR_MODELS`) بأكثر من 100 موديل سيارة (ماركة + موديل) شائعة بسوق الأردن + بعض موديلات الدراجات النارية الشائعة، تُستورد بصفحة التسجيل لتغذية قائمة البحث. سهل التوسعة لاحقاً (مصفوفة نصوص عربية بسيطة).

**ملفات مُعدّلة:**
- **`akleto-driver.html`** — صار شاشة **دخول برقم الهاتف فقط** (بدون تسجيل بيانات جديدة): يبحث عن السائق بـ `findDriverByPhone()` ويتصرف حسب حالته:
  - ما لقاه → رسالة + رابط للتسجيل من `akleto-driver-signup.html`
  - `approval_status === 'pending'` → شاشة "حسابك قيد المراجعة"
  - `approval_status === 'rejected'` → شاشة "طلبك ما تم قبوله"
  - `is_active === false` (محظور) → شاشة "حسابك موقوف حالياً"
  - غير هيك (مقبول وفعّال) → يُنشئ الجلسة المحلية العادية (`saveDriverSession`) ويوديه للرئيسية — **بدون كلمة مرور، تماماً متل ما طلب مؤيد**.
- **`driver-shared.js`** — أُضيفت دوال جديدة مشتركة:
  - `findDriverByPhone(phone)` — استعلام `where('phone','==',phone)` يرجع أول نتيجة أو `null`.
  - `createDriverAccount(data)` — ينشئ سجل `drivers` جديد بكل الحقول (بيانات شخصية، روابط الصور الست، الآيبان) وبحالة `approval_status: APPROVAL_PENDING` تلقائياً.
  - `updateDriverIban(driverId, iban)` — تحديث الآيبان لسائق موجود أصلاً (تُستخدم من صفحة الحساب لو تخطى الخطوة بالتسجيل).
  - ثوابت الحالة: `APPROVAL_PENDING` / `APPROVAL_APPROVED` / `APPROVAL_REJECTED`.
- **`akleto-driver-account.html`** — أُضيفت بطاقة "🏦 الآيبان / الحساب البنكي" بشاشة الحساب (تعرض الحالة الحالية: القيمة المحفوظة أو "ما أضفته بعد")، مع نافذة سفلية (sheet) بسيطة لإضافته أو تعديله بأي وقت عبر `updateDriverIban`.
- **`akleto-admin-drivers.html`** — أُضيف قسم جديد كامل "⏳ طلبات قيد المراجعة" أعلى قائمة السائقين المقبولين، يظهر تلقائياً بس في طلبات معلّقة. الضغط على أي سائق (قيد المراجعة أو مقبول) يفتح **مودال تفاصيل كامل**: كل البيانات (الهاتف، نوع المركبة، الماركة/الموديل، رقم اللوحة، الآيبان)، وشبكة الوثائق الست (كل وحدة رابط يفتح الصورة كاملة بتبويب جديد). لو الحساب `pending`، يظهر بالمودال زرّين "قبول الطلب ✅" و"رفض الطلب ❌" يحدّثان `approval_status` مباشرة بـ Firestore. لو مرفوض، تظهر إشارة "(مرفوض)" بقائمة السائقين العادية بدل زر الحظر.

**ملاحظات مهمة لمؤيد:**
- **لا حاجة لإعادة نشر أي شيء** — كل التعديلات ملفات HTML/JS خام تُقرأ مباشرة من GitHub Pages، تنعكس فوراً بمجرد `git pull` أو حتى بدون أي خطوة إضافية لو بتفتح الرابط الحي.
- **السائقين القدامى** (اللي اتسجلوا قبل هالتحديث) ما عندهم حقل `approval_status` أصلاً — تم التعامل معهم بمنطق "لو مش `pending` ومش `rejected` → يعتبر مقبول"، فما راح ينحظروا أو يتعطلوا تلقائياً. ما تم لمس بياناتهم.
- **صلاحيات Firestore:** ما احتجنا أي تعديل على القواعد — مجموعة `drivers` أصلاً `allow read, write: if true` (نفس نمط `customers`/`stores`، موثّق سابقاً كدين أمني للمستقبل).
- **الصور** تُرفع لنفس Firebase Storage الحالي (`akleto-prod.firebasestorage.app`) تحت مسار `driver_documents/` — ما في حاجة لأي إعداد إضافي، `uploadImage()` جاهزة أصلاً بـ `firebase-init.js`.
- **متبقٍ اختياري مستقبلاً:** لسا ما في تنبيه/إشعار تلقائي للأدمن لما يوصل طلب تسجيل سائق جديد (بعكس الطلبات والمتاجر اللي عندها Cloud Function تنبيه) — لو حبينا نضيفها لاحقاً، نفس نمط `onOrderCreated` بالضبط بس على مجموعة `drivers` (`onDriverSignupCreated`).

## 8 أغسطس 2026 — 🔧 إصلاح خطأ رفع الصور + تقسيم شاشة التوثيق لـ4 خطوات إرشادية

مؤيد جرّب صفحة التسجيل الجديدة وأبلغ عن مشكلتين:

1. **حقل الماركة/الموديل** — كان أصلاً حقل نص حر (مو قائمة مقفلة)، بس ما كان واضح للمستخدم إنه يقدر يكتب يدوياً لو مركبته مش موجودة بقائمة `JORDAN_CAR_MODELS`. أُضيف تلميح صريح تحت الحقل يوضح هالإمكانية. (نوع المركبة العام سيارة/دراجة نارية/سكوتر تُرك كما هو بناءً على طلب مؤيد.)

2. **"خطأ، حاول مرة أخرى" عند رفع أي صورة** — السبب الأرجح: صور الكاميرا الحقيقية على الموبايل (capture="environment") بتطلع بحجم كبير (عدة ميجابايت)، وكانت `uploadImage()` بمهلة ثابتة 15 ثانية بس — ع نت موبايل عادي هاد مش كافي، فبيطلع خطأ "انتهت المهلة" بشكل شبه دائم. تم إصلاحها بخطوتين:
   - **`firebase-init.js`**: `uploadImage(file, folder, timeoutMs=15000)` — صار يقبل مهلة مخصصة اختيارية (باقي الاستدعاءات القديمة بباقي صفحات الأدمن ما تأثرت، لسا افتراضيها 15 ثانية).
   - **`akleto-driver-signup.html`**: أُضيفت دالة `compressImage()` تضغط أي صورة عبر `<canvas>` (تصغير لأقصى بعد 1500px + جودة JPEG 72%) **قبل** الرفع مباشرة — بتقلل حجم صور الكاميرا من عدة ميجابايت لعشرات الكيلوبايت، وبتسرّع الرفع كثير. مع هيك رُفعت المهلة لصفحة التسجيل تحديداً لـ 45 ثانية كطبقة أمان إضافية. الرسائل صارت توضح سبب الخطأ (انتهاء مهلة / صلاحيات / عام) بدل رسالة عامة واحدة.

**تعديل إضافي طلبه مؤيد:** بدل ما تكون الصور الست برفعة واحدة بشاشة واحدة، صارت مقسّمة لـ **4 شاشات فرعية متتالية** جوا الخطوة الثانية (كل وحدة إلها زر "التالي" خاص ولازم يكمّل صورها قبل ما يعدي):
   - **2أ. الصورة الشخصية** (صورة وحدة) — مع بطاقة إرشادات: خلفية بيضاء، اخلع النظارات، صورة واضحة بدون غباش، الوجه كامل وناظر للكاميرا.
   - **2ب. الهوية الشخصية** (وجهين) — إرشادات: البطاقة كاملة بدون قص، الكتابة واضحة، بدون انعكاس ضوء.
   - **2ج. رخصة القيادة** (وجه أمامي فقط، حسب طلب مؤيد) — إرشادات: سارية المفعول، البيانات واضحة.
   - **2د. رخصة المركبة** (وجهين) — إرشادات: رقم اللوحة مطابق للمكتوب بالخطوة الأولى، سارية المفعول.
   - كل شاشة فيها **رسمة SVG إرشادية بسيطة** (مرسومة يدوياً بالكود، مو صور حقيقية لوثائق — تفادياً لأي إشكال حقوق نشر أو خصوصية) توضح شكل التوثيق المطلوب (بطاقة بصورة شخصية، بطاقة بخطوط نص، رخصة مركبة بشكل سيارة...الخ).
   - شريط التقدم بأعلى الصفحة صار ديناميكي (`STEP_ORDER` array) يعكس الخطوات الست الفعلية (بيانات → صورة شخصية → هوية → رخصة قيادة → رخصة مركبة → آيبان) بدل 3 نقاط ثابتة.

**ما تغيّر:** بنية بيانات `drivers` بـ Firestore (نفس الحقول الست + الآيبان)، منطق `driver-shared.js` (`createDriverAccount`, `findDriverByPhone`)، ولوحة الأدمن — ولا شي منهم احتاج تعديل، التغيير كله بواجهة التسجيل وملف الرفع المشترك فقط.

**لسا ما تأكدنا منه:** مؤيد لسا ما جرّب باقي خطوات التسجيل (الهوية، الرخصة، الآيبان، التقديم النهائي، مراجعة الأدمن) ولا فحص وصول إشعارات الدفع فعلياً على الموبايل — بانتظار نتيجة تجربته القادمة.

## 8 أغسطس 2026 (جلسة ثانية) — مهمة 4 (تدقيق إشعارات الدفع) + مهمة 5 (تنبيه أدمن لطلب تسجيل سائق)

**مهمة 4 — تدقيق كود إشعارات الدفع (السائق + المتجر):**
تم فحص شامل للتأكد إنه ما في أخطاء برمجية تمنع وصول الإشعارات بعد ما صار عنا APK حقيقي (مبني بجلسة سابقة):
- `driver-shared.js` (`initDriverPushNotifications`) و`akleto-store.html` (`initStorePushNotifications`) — كلاهما مستدعى فعلياً بصفحات الرئيسية (`akleto-driver-home.html`, `akleto-store.html`) بعد تسجيل الدخول، ويسجّلان `fcm_token` بـ Firestore بشكل سليم.
- `google-services.json` موجود بمشروعي `mobile-driver` و`mobile-store` (أندرويد).
- صلاحية `POST_NOTIFICATIONS` موجودة بـ `AndroidManifest.xml` لكلا المشروعين (مطلوبة لأندرويد 13+).
- Cloud Functions (`functions/index.js`) سليمة: `onOrderUpdated` (حالة الطلب + تعيين سائق)، `onOrderCreated` (تنبيه المتجر)، `onOrderReady` (تنبيه جماعي للسائقين المتاحين).
- **الخلاصة:** الكود سليم من الناحية البرمجية بالكامل. المتبقي هو التجربة الفعلية على جهاز حقيقي (مؤيد بيجرب حالياً ورح يخبرنا بالنتيجة). لو ما وصلت رغم هيك، السبب الأرجح وقتها يكون بإعدادات Firebase Console (تفعيل Cloud Messaging API) أو مشكلة بمفتاح `google-services.json` نفسه — مو بالكود.
- **ملاحظة:** تطبيق الإدارة (`mobile-admin`) ما عنده أي بنية تحتية لإشعارات الدفع (لا `google-services.json` ولا مكتبة `@capacitor/push-notifications`) — هاي نقطة مستقبلية منفصلة موثّقة أصلاً (بند "احتمال الحاجة لإضافة إشعارات دفع لتطبيق الإدارة").

**مهمة 5 — ✅ تنبيه أدمن تلقائي عند طلب تسجيل سائق جديد:**
بدل ما نبني Cloud Function جديدة (زي `onOrderCreated`)، استُخدم نظام أبسط وموجود أصلاً بالتطبيق: مجموعة `admin_notifications` بـ Firestore (تنبيهات داخل تطبيق الإدارة نفسه، تُقرأ بصفحة `akleto-admin-notifications.html` وتظهر كـ badge برقم غير مقروء بكل صفحات الأدمن عبر `watchUnreadNotifications`). هاي نفس الآلية المستخدمة لتنبيهات الطلب الجديد، الطلب الجاهز، تسليم الطلب...الخ — **بدون حاجة لبناء APK جديد لتطبيق الإدارة ولا نشر Cloud Function.**
- **`driver-shared.js`** — دالة `createDriverAccount()` صارت تكتب تلقائياً (بعد إنشاء سجل السائق) مستند جديد بـ `admin_notifications` بنوع `driver_signup`، فيه اسم المتقدّم ورقم هاتفه.
- **`akleto-admin-notifications.html`** — أُضيف النوع `driver_signup` لجدول `TYPE_META` (أيقونة 🪪 وتسمية "طلب تسجيل سائق") عشان يظهر بشكل مقروء بقائمة الإشعارات بدل الأيقونة الافتراضية العامة.
- **النتيجة:** أي أدمن فاتح أي صفحة من صفحات لوحة التحكم (حتى لو مو صفحة السائقين تحديداً) رح يشوف رقم التنبيهات غير المقروءة يزيد فوراً لحظة ما حدا يقدّم طلب تسجيل كسائق، ويقدر يفتح "الإشعارات" أو "السائقين" مباشرة يراجع الطلب.

## 8 أغسطس 2026 (جلسة ثالثة) — ✅ شارة رقمية لتبويب "السائقين" بالقائمة الجانبية

طلب مؤيد إضافة شارة (badge) رقمية بجانب تبويب "السائقين" بالقائمة الجانبية — تشتغل بنفس منطق شارة "الإشعارات" و"طلبات التسديد" الموجودة أصلاً، بس بتعرض تحديداً **عدد طلبات تسجيل السائقين الجدد "قيد المراجعة"** (مو كل التنبيهات العامة).

- **`firebase-init.js`** — دالة جديدة `watchPendingDriversCount(callback)`: تستمع لحظياً (`onSnapshot`) على `drivers` حيث `approval_status == 'pending'` وترجع العدد.
- **كل صفحات لوحة الأدمن الـ12** (`akleto-admin.html` والـ11 صفحة الفرعية) — أُضيف عنصر `<span id="driversNavBadge">` بجانب "السائقين" بالقائمة الجانبية، واستدعاء `watchPendingDriversCount()` بنفس مكان استدعاء `watchUnreadNotifications()` (داخل `onAuthStateChanged`)، فتحديث الشارة صار لحظي وشغّال من أي صفحة، مو بس صفحة السائقين.
- **طريقة التنفيذ:** سكربت Python واحد طبّق نفس التعديل (الشارة + الاستيراد + الاستدعاء) على الـ12 ملف دفعة وحدة بالاعتماد على نمط `watchUnreadNotifications(count => {...})` المتكرر بنفس الصيغة بكل صفحة، مع تصحيح يدوي لملفين خرجا عن النمط القياسي شوي (`akleto-admin-notifications.html` باستيراد بصيغة مختلفة، و`akleto-admin.html` بمنطق إضافي لنقطة إشعار بالـ topbar). تم التحقق من سلامة الـ JS بكل ملف (`node --check`) بعد التعديل.
- **ملاحظة:** الشارة بتختفي تلقائياً (`class="hidden"`) لما العدد يصير صفر — نفس سلوك باقي الشارات بالتطبيق.

## 8 أغسطس 2026 (جلسة رابعة) — ✅ سبب رفض السائق + إمكانية تصحيح الطلب وإعادة الإرسال

طلب مؤيد: لما الأدمن يرفض طلب سائق، يقدر يكتب سبب الرفض (مثلاً "الصور غير واضحة")، ويظهر هالسبب للسائق عشان يصلّحه.

**لوحة الأدمن (`akleto-admin-drivers.html`):**
- زر "رفض الطلب ❌" بمودال تفاصيل السائق صار يفتح `prompt()` يطلب من الأدمن يكتب سبب الرفض إلزامياً (ما بيكمّل لو تركه فاضي). السبب يُحفظ بحقل `rejection_reason` بمستند السائق مع `approval_status: 'rejected'`.
- لو فتحت تفاصيل سائق مرفوض ومعه سبب محفوظ، يظهر بأسفل المودال بصندوق مميّز "سبب الرفض: ...".

**صفحة دخول السائق (`akleto-driver.html`):**
- شاشة "طلبك ما تم قبوله" صارت تعرض السبب المكتوب من الأدمن مباشرة (`driver.rejection_reason`).
- لو في سبب، يظهر زر إضافي "صلّح البيانات وأعد الإرسال ✏️" يوديه لصفحة التسجيل بوضع "تصحيح" (`akleto-driver-signup.html?resubmit={driverId}`).

**صفحة التسجيل (`akleto-driver-signup.html`) — وضع "إعادة التقديم":**
- لما تُفتح برابط فيه `?resubmit=driverId`، بتتجاوز فحص "الرقم مسجّل مسبقاً" (لأنه نفس صاحب الحساب)، وبتعبّي تلقائياً بيانات الخطوة الأولى (الاسم، الهاتف، نوع المركبة، الموديل، اللوحة) من السجل القديم، وبتعرض سبب الرفض بصندوق تنبيه أعلى الخطوة الأولى كتذكير بشو المطلوب تصحيحه.
- السائق لازم يعيد رفع الصور الست من جديد (ما تُعبّى تلقائياً — منطقي لأنه غالباً السبب يكون متعلق بالصور نفسها).
- عند الإرسال، بدل ما يُنشئ سجل جديد (`createDriverAccount`)، تُستخدم دالة جديدة **`resubmitDriverAccount(driverId, data)`** بـ `driver-shared.js` تحدّث نفس السجل القديم، ترجّعه لحالة `pending`، وتفرّغ `rejection_reason`، وتضيف تنبيه أدمن جديد (نوع `driver_signup`، رسالة "إعادة تقديم طلب سائق") — فبيظهر بشارة "السائقين" وقائمة "قيد المراجعة" تلقائياً زي أي طلب جديد.
- شاشة النجاح بوضع التصحيح تختلف نصياً عن التسجيل الجديد ("تم إرسال طلبك المعدّل" بدل "تم إرسال طلبك بنجاح").

## 8 أغسطس 2026 (جلسة خامسة) — ✅ إضافة "شهادة عدم محكومية" كوثيقة سابعة مطلوبة

طلب مؤيد إضافة صورة شهادة عدم محكومية حديثة كوثيقة إضافية أثناء تسجيل حساب السائق.

- **خطوة فرعية جديدة `2هـ`** بصفحة `akleto-driver-signup.html`، بين رخصة المركبة (2د) والآيبان (خطوة 3): صورة وحدة (مش وجهين)، مع إرشادات: صادرة من جهة رسمية، حديثة (خلال آخر 3 أشهر)، والختم الرسمي والبيانات واضحة. `STEP_ORDER` وشريط التقدم تحدّثا تلقائياً ليعكسوا 7 خطوات بدل 6.
- **حقل جديد بقاعدة البيانات:** `clearance_certificate_url` — أُضيف لكل من `createDriverAccount()` و`resubmitDriverAccount()` بـ `driver-shared.js`، ولمودال تفاصيل السائق بلوحة الأدمن (`akleto-admin-drivers.html`) ضمن شبكة الوثائق.
- **الصورة صارت إلزامية** — نفس منطق التحقق المستخدم بباقي الوثائق (ما يقدر يكمّل للآيبان بدون رفعها).
- التوثيق يشمل الآن **7 صور** بدل 6: شخصية، هوية (وجهين)، رخصة قيادة، رخصة مركبة (وجهين)، شهادة عدم محكومية.

## 8 أغسطس 2026 (جلسة سادسة) — ⚠️ مهمة 8: تنبيه "Full-Screen Intent" لطلب جديد بتطبيق السائق (يحتاج بناء واختبار حقيقي بجهاز)

**الهدف:** إشعار "طلب جديد" (تعيين طلب على سائق، أو طلب صار متاح للجميع) يفتح تطبيق السائق **تلقائياً فوق شاشة القفل حتى لو التطبيق مسكّر تماماً** — نفس تجربة أوبر/كريم عند وصول طلب.

**⚠️ هاي أول مرة نلمس كود أندرويد أصلي (Java) بالمشروع — غير باقي الجلسات اللي كانت كلها HTML/JS خام. لازم بناء + اختبار حقيقي بجهاز، وما قدرت أتحقق من نجاح الـ compile فعلياً (ما في Android SDK بالبيئة اللي اشتغلت فيها) — بس تحققت من صحة الـ XML وتوازن الأقواس بالجافا يدوياً.**

### شو تغيّر بالضبط:

**أ) `functions/index.js` — تغيير طريقة إرسال إشعارات السائق تحديداً:**
- دالة `sendPush()` صارت تقبل باراميتر خامس اختياري `fullScreenAlert` (افتراضياً `false`).
- لما `true`: الرسالة تُرسل **data-only بدون `notification` block** (العنوان والنص يترحّلوا جوا `data` بدل). هاي ضرورية تقنياً — رسائل FCM يلي فيها `notification` block بتتعرض تلقائياً من نظام أندرويد نفسه لما التطبيق مسكّر/بالخلفية **بدون ما يشتغل كود التطبيق أصلاً**، فما فيه طريقة نبني إشعار مخصص (Full-Screen). رسائل `data-only` بالمقابل **دايماً** بتوصل لكود `onMessageReceived()` بالتطبيق (حتى لو مسكّر)، وهيك منقدر نبني الإشعار المخصص إحنا.
- طُبّق `fullScreenAlert: true` على حالتين بس (تنبيهات السائق): `onOrderUpdated` (تعيين طلب على سائق) و`onOrderReady` (طلب صار متاح للجميع). باقي الإشعارات (الزبون، المتجر) **ما تغيّرت إطلاقاً** — لسا notification+data عادي، تظهر بشريط الإشعارات القياسي.

**ب) `mobile-driver/android/app/src/main/java/com/akalito/driver/OrderMessagingService.java` 🆕 (ملف جديد):**
- كلاس جديد **يرث من** `com.capacitorjs.plugins.pushnotifications.MessagingService` (خدمة Capacitor الافتراضية لاستقبال FCM) — مش بديل مستقل من الصفر، عشان **نحافظ على كل سلوك Capacitor الحالي** (تسجيل التوكن، تمرير الإشعارات لجافاسكربت وقت التطبيق مفتوح...الخ) لأي رسالة غير رسائل تنبيه الطلب.
- بيتحقق من `data.type`: لو `order_assigned` أو `order_available` → يبني إشعار `NotificationCompat` بقناة `IMPORTANCE_HIGH` جديدة (`order_alerts`)، مع `setFullScreenIntent(pendingIntent, true)` يفتح `MainActivity` مباشرة (مع `order_id` كـ extra). غير هيك → بيستدعي `super.onMessageReceived()` (نفس السلوك القديم بالضبط، بدون أي تغيير).

**ج) `AndroidManifest.xml` (مشروع `mobile-driver` فقط):**
- `<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />` + `VIBRATE` — صلاحيات جديدة مطلوبة.
- **استبدال خدمة Capacitor الافتراضية بخدمتنا:** `tools:node="remove"` على `com.capacitorjs.plugins.pushnotifications.MessagingService` (يشيلها من الـ manifest النهائي بعد الدمج)، وتسجيل `.OrderMessagingService` بدلها بنفس الـ intent-filter (`com.google.firebase.MESSAGING_EVENT`). هاي الطريقة الرسمية الموثّقة لاستبدال خدمة مكتبة بخدمة فرعية مخصصة بدون تعارض بالـ manifest merger.
- `MainActivity` صارت `android:showWhenLocked="true"` و`android:turnScreenOn="true"` (مطلوبين API 27+ عشان النشاط يقدر يفتح فوق شاشة القفل فعلياً، مش بس الإشعار).

**د) `res/drawable/ic_stat_order.xml` 🆕:** أيقونة بسيطة (جرس) لشريط الإشعار بدل استخدام أيقونة التطبيق الكاملة (يلي بتطلع مربّع أبيض قبيح كإشعار حسب توصيات أندرويد).

### ⚠️ نقاط حرجة لازم تصير قبل الاعتماد على الميزة:

1. **لازم `npx cap sync android` + بناء APK جديد لمشروع `mobile-driver` تحديداً** — التعديلات كلها ملفات أندرويد أصلية، ما بتنعكس بمجرد `git pull` متل باقي التطبيقات.
2. **لازم `firebase deploy --only functions`** — عشان يتفعّل الـ data-only payload الجديد بالسيرفر.
3. **اختبار حقيقي بجهاز حقيقي إلزامي:** جرّب طلب يتعيّن على سائق والتطبيق مسكّر تماماً (Force Stop مو بس Home)، وشوف هل بيفتح فوق شاشة القفل فعلاً.
4. **أندرويد 14+ (API 34) قيد خاص:** أجهزة بنسخة 14 فما فوق ممكن تحتاج المستخدم يفعّل صلاحية "Full Screen Intent" يدوياً من إعدادات التطبيق بالنظام (مو زر داخل تطبيقنا) — النظام هون بيتراجع تلقائياً لإشعار عادي (heads-up) بدل ما يكسر أو يطلع خطأ، بس التجربة الكاملة (الفتح التلقائي فوق القفل) ممكن ما تصير 100% إلا لو المستخدم فعّلها. ما بنيت واجهة لطلب هالصلاحية بهالجلسة — ممكن نضيفها لاحقاً لو صارت مشكلة فعلية بالتجربة.
5. **تحسين اختياري مستقبلي:** أيقونة الإشعار (`ic_stat_order.xml`) بسيطة جداً (جرس عام) — لو حاب نصمم أيقونة رسمية لأكليتو بدلها، ملف واحد بس (`res/drawable/ic_stat_order.xml`).

## 📋 لوحة المهام المُجمَّعة والمُنقَّحة — بتاريخ 9 أغسطس 2026 (مرجع سريع لأي جلسة قادمة)

> هاي فقرة تلخيصية بس — التفاصيل الكاملة لكل بند موجودة بمكانها الأصلي بالتاريخ المذكور أعلاه بنفس هالملف. الهدف هون إعطاء صورة سريعة "شو خلص، شو باقي" بدون قراءة الملف كامل من الأول.

### ✅ خلصت (جلسات 7-8 أغسطس 2026)
- نظام تسجيل حساب ودخول كامل للسائقين (7 وثائق + آيبان اختياري + مراجعة أدمن)
- سبب رفض السائق + إمكانية تصحيح الطلب وإعادة الإرسال
- شارة "السائقين" بالقائمة الجانبية (عدد الطلبات قيد المراجعة، لحظي، بكل صفحات الأدمن)
- تنبيه أدمن تلقائي داخل التطبيق عند أي طلب تسجيل سائق جديد (`admin_notifications`)
- شاشة مراجعة "طلبات تحصيل عمولة المتجر" — تأكدنا إنها مبنية أصلاً وشغّالة (`akleto-admin-settlements.html`)
- تدقيق شامل لكود إشعارات الدفع (السائق + المتجر) — سليم برمجياً، بانتظار تأكيد مؤيد من تجربة حقيقية
- تنبيه "Full-Screen Intent" لطلب جديد بتطبيق السائق (كود Java + Cloud Function) — **بانتظار مؤيد يبني APK جديد ويجرب على جهاز حقيقي، ما تأكدنا لسا**

### 🔴 قرارات لازم من مؤيد (مو شغل برمجي — الكود جاهز بانتظار الرقم/القرار)
1. تأكيد نسبة العمولة النهائية (`COMMISSION_RATE` بـ`driver-shared.js`، حالياً 10% افتراضي مؤقت)
2. اختيار بوابة دفع إلكتروني (HyperPay / PayTabs / Stripe أو غيرها)
3. تأكيد إذا في قواعد أمان Firestore مخصصة (Security Rules) أو لسا مفتوحة بالكامل

### 🟡 شغل تقني معلّق (نقدر نبدأ فيه أي وقت، بعضه مربوط بالقرارات فوق)
4. Collection جديد (`driver_settlements`) لتتبع تسديدات الكباتن الفعلية بعد ربط بوابة الدفع
5. حساب "مديونية أكليتو للكابتن" (الحالة المعكوسة) — مؤجّل لحد تفعيل الدفع الأونلاين
6. ميزة "الطباعة = تأكيد تلقائي" لكشوفات تسديد الكباتن — تخطيط بس، ما تنفذت
7. بنية تحتية لإشعارات الدفع بتطبيق الإدارة (`mobile-admin`) — ما عندها `google-services.json` ولا مكتبة Push إطلاقاً

### ⚪ ديون تقنية موثّقة (مو مستعجلة، لازم تتحل قبل الإطلاق الفعلي)
8. `allow write: if true` على `drivers`/`customers`/`stores` بـFirestore — يحتاج نظام Firebase Auth حقيقي
9. تنظيف dead code بصفحة `akleto-driver-deliveries.html`

### ⏳ بانتظار نتيجة تجربة مؤيد الحالية
- تسجيل حساب سائق كامل بالـAPK الجديد (بيانات + 7 صور + آيبان)
- وصول إشعارات الدفع فعلياً (سائق/متجر)
- بناء + تجربة ميزة Full-Screen Intent (مهمة 8) على جهاز حقيقي

## 10 أغسطس 2026 (جلسة سابعة) — ✅ إصلاح خطأ Compile منع بناء APK السائق (سبب مهمة 8)

**المشكلة يلي أبلغ عنها مؤيد:** بعد آخر تحديث (مهمة 8 — Full-Screen Intent)، صار Android Studio ما يقدر يبني APK لمشروع `mobile-driver`، وما فيه إمكانية معالجة الأخطاء من الاستديو مباشرة.

**السبب الجذري (تأكدت 100% بفحص ملفات الـ Gradle الفعلية من الريبو + محتوى حزمة `@capacitor/push-notifications` من npm):**
- `OrderMessagingService.java` (يلي أضيف بجلسة "مهمة 8") بيستورد ويستخدم `com.google.firebase.messaging.RemoteMessage` مباشرة.
- مكتبة `@capacitor/push-notifications` (نسخة 8.1.2 المستخدمة بالمشروع) بتضيف `com.google.firebase:firebase-messaging:25.0.1` بملفها الداخلي `android/build.gradle` **بنوع `implementation`**.
- بقواعد Gradle: اعتماديات نوع `implementation` **ما بتنعدّي (transitive)** لأي مشروع (module) تاني يستخدم هالمكتبة — يعني `app` module (مشروع السائق نفسه) ما كان إله رؤية على كلاس `RemoteMessage` وقت الترجمة (compile)، فبيطلع خطأ `cannot find symbol: class RemoteMessage`.
- هاي بالضبط النقطة يلي كانت محذّرة عنها بجلسة "مهمة 8" ("ما قدرت أتحقق من نجاح الـ compile فعلياً، ما في Android SDK بالبيئة") — تأكدت فعلياً إنها كانت المشكلة.

**الإصلاح المطبّق (على `main` مباشرة):**
1. `mobile-driver/android/variables.gradle`: أُضيف `firebaseMessagingVersion = '25.0.1'` (نفس النسخة الافتراضية يلي بتستخدمها مكتبة push-notifications، لتفادي تعارض نسخ/كلاسات مكررة).
2. `mobile-driver/android/app/build.gradle`: أُضيف سطر `implementation "com.google.firebase:firebase-messaging:$firebaseMessagingVersion"` صراحة بقسم `dependencies`.

**المطلوب من مؤيد الآن:**
- `git pull` على مشروع `mobile-driver` (تعديلات Gradle فقط، ما في تغيير كود جافا).
- Sync Gradle بـ Android Studio (أو حذف `.gradle` cache لو ظهرت مشاكل cache قديمة) ثم إعادة محاولة بناء الـ APK.
- لو ظهر خطأ compile جديد مختلف، يرسل نص الخطأ كامل من Android Studio (Build Output) بدل وصف عام — بيسرّع التشخيص كثير.
- بعد نجاح البناء، لسا المطلوب اختبار Full-Screen Intent فعلياً على جهاز حقيقي (نفس نقاط الاختبار المذكورة بجلسة "مهمة 8" أعلاه: طلب يتعيّن والتطبيق Force Stop، والتأكد من فتح الشاشة فوق القفل).

## 9 أغسطس 2026 (جلسة ثانية) — 🔧 حل: خطأ "ما في صلاحية للرفع" برفع صور تسجيل السائق

مؤيد أبلغ عن رسالة "ما في صلاحية للرفع — تواصل معنا" عند رفع أي صورة بصفحة تسجيل حساب السائق (`akleto-driver-signup.html`).

**السبب:** قواعد أمان Firebase Storage كانت تتطلب `request.auth != null` للكتابة (نفس النمط الافتراضي). تطبيق السائق ما بيستخدم Firebase Auth حقيقي (نفس فجوة الـ Auth الموثّقة سابقاً بتطبيق المتجر) — فرفع صور التوثيق بمرحلة التسجيل (قبل ما يصير عنده أي جلسة) كان مرفوض دايماً. باقي الرفعات بالتطبيق (صور الوجبات/المكونات من لوحة الأدمن) كانت شغّالة لأنها بتصير بعد تسجيل دخول أدمن حقيقي بـ Firebase Auth.

**الحل:** مؤيد عدّل قواعد Storage يدوياً من Firebase Console (تبويب Rules) وأضاف استثناء لمسار `driver_documents/` يسمح بالقراءة والكتابة بدون شرط مصادقة (`allow read, write: if true`)، بنفس نمط باقي الاستثناءات الموثّقة لمجموعات `drivers`/`customers`/`stores` بـ Firestore.

**✅ تأكيد مؤيد: انحلّت المشكلة، رفع الصور شغّال تمام الآن.**

**ملاحظة للمستقبل:** لو ظهرت نفس رسالة "ما في صلاحية للرفع" بأي مسار Storage جديد يُستخدم بدون Firebase Auth (مثلاً لو أضفنا رفع صور من تطبيق الزبون أو المتجر بمرحلة قبل تسجيل الدخول)، الحل نفسه: إضافة استثناء `allow read, write: if true` لمسار المجلد المحدد بقواعد Storage. القواعد مُدارة يدوياً بالكونسول (مو ملف بالمستودع) — حاولنا هالجلسة نحوّلها لملف `storage.rules` منشور عبر `firebase deploy --only storage` بس مؤيد فضّل يعدّلها مباشرة بالكونسول واشتغلت.

## 9 أغسطس 2026 (جلسة ثالثة) — ✅✅ تأكيد مؤيد: تسجيل السائق كامل + إشعارات الدفع شغّالين 100%

مؤيد أكّد صراحة نجاح بندين كانا معلّقين من جلسات سابقة:
- **تسجيل حساب سائق كامل من الألف للياء** (بيانات + 7 صور توثيق + آيبان + مراجعة أدمن) — تم واشتغل تمام على جهاز حقيقي.
- **وصول إشعارات الدفع فعلياً** (السائق والمتجر) — تم، الإشعارات وصلت للموبايل الحقيقي.

**الوحيد المتبقي من مهام هالمرحلة:** بناء واختبار ميزة Full-Screen Intent (مهمة 8) — مؤيد لسا ما فهم شو السيناريو المتوقع بالظبط عشان يجربها صح، تم توضيحه له بالمحادثة.

## 9 أغسطس 2026 (جلسة رابعة) — 🔧 حل: خطأ "صار خطأ حاول مرة ثانية" عند إنشاء طلب جديد (قواعد Firestore)

مؤيد أبلغ عن فشل إنشاء طلب جديد من تطبيق الزبون برسالة عامة "صار خطأ — جرب مرة ثانية".

**السبب:** نفس فئة مشكلة الـ Storage السابقة، بس بقواعد **Firestore** هالمرة. إنشاء طلب بيحتاج transaction يكتب على مجموعتين: `orders` و**`counters`** (لتوليد رقم طلب تسلسلي). مجموعة `counters` ما كانت موجودة إطلاقاً بقواعد Firestore، فكانت بتقع تحت القاعدة الافتراضية الأخيرة (`match /{document=**} { allow write: if request.auth != null; }`) يلي بترفض أي كتابة بدون Firebase Auth حقيقي — وتطبيق الزبون ما بيستخدم Auth حقيقي (نفس فجوة السائق/المتجر).

**بالفحص، اكتُشفت مشكلة إضافية كامنة بنفس القواعد:** مجموعة `coupons` (أكواد الخصم) كانت كمان غير موجودة بالقواعد — كانت رح تفشل بنفس الطريقة بالظبط أول ما زبون يجرب يطبّق كود خصم على طلبه (حتى لو ما كانت هي سبب هالبلاغ تحديداً).

**الحل:** مؤيد عدّل قواعد Firestore بالكامل من Firebase Console وأضاف:
```
match /counters/{counterId} { allow read, write: if true; }
match /coupons/{couponId} { allow read, write: if true; }
```
**✅ تأكيد مؤيد: انحلّت المشكلة، الطلب صار يترسل بنجاح.**

**ملاحظة توثيقية مهمة:** هاي ثاني مرة بنفس اليوم نلاقي مجموعة/مسار ناقص من قواعد الأمان (Storage مرة، Firestore مرة). القاعدة العامة للمستقبل: **أي مجموعة Firestore أو مسار Storage جديد يُستخدم من تطبيق زبون/سائق/متجر (بدون Firebase Auth حقيقي) لازم يُضاف له صراحة `allow read, write: if true`**، وإلا بيقع تحت القاعدة الافتراضية المقيّدة ويفشل بصمت مع رسالة خطأ عامة غير واضحة السبب. لما نضيف أي Collection جديد بالكود مستقبلاً، لازم نفحص فوراً هل هو مضاف بقواعد الأمان قبل ما نعتبر الميزة خلصت.

## 9 أغسطس 2026 (جلسة رابعة، تكملة) — ✅ تأكيد إضافي: ميزة Full-Screen Intent شغّالة (بنتيجة جيدة جداً، مو كاملة 100%)

مؤيد جرّب ميزة "طلب جديد يفتح تطبيق السائق تلقائياً" (مهمة 8) بعد ما فعّل صلاحية "إشعارات الشاشة الكاملة" وأزال قيود البطارية عن التطبيق بجهاز Infinix (أندرويد 14):

**النتيجة:** ✅ **الإشعار وصل بنجاح وبأولوية عالية حتى والتطبيق مسكّر تماماً وشاشة الموبايل مقفولة.** الاستثناء الوحيد: **الشاشة ما بتضوي لحالها تلقائياً** (يعني التطبيق ما بيفتح فوق شاشة القفل بشكل كامل تلقائي زي أوبر بالظبط)، بس الإشعار نفسه (صوت/اهتزاز/ظهور) شغّال ممتاز.

**الخلاصة:** الميزة حققت **95% من الهدف** — التنبيه الفوري عالي الأولوية شغّال 100% حتى بأسوأ الظروف (تطبيق مسكّر + شاشة مقفولة)، وبس "الفتح الأوتوماتيكي الكامل فوق القفل" (الـ 5% الأخيرة) ما تحقق بالكامل. هالسلوك متوقع ومقبول لأنه أندرويد 14+ فيه قيود إضافية معقّدة على الفتح التلقائي الكامل حتى مع صلاحية "Full Screen Intent" مفعّلة (تختلف حسب الشركة المصنّعة وإصدار XOS/OneUI/...الخ). **مؤيد موافق على هالنتيجة كافية ومقبولة — لا داعي لمزيد من العمل على هالميزة حالياً** إلا لو صار مشكلة فعلية بالاستخدام اليومي لاحقاً.

## 9 أغسطس 2026 (جلسة خامسة) — ✅ كشف التحويلات الجامع (بالطريقة الآمنة المعدّلة، مو "الطباعة = تأكيد تلقائي")

مؤيد اعترض بحق على الفكرة الأصلية الموثّقة سابقاً ("بمجرد الطباعة تتأكد كل المعاملات تلقائياً") — خطرة لأنها بتربط فعل ميكانيكي بسيط (طباعة) بتأكيد صرف فلوس فعلي، بدون حماية من طباعة مزدوجة/بالغلط. تم الاتفاق على بديل أأمن ونُفّذ فوراً بـ`akleto-admin-settlements.html`:

**الفكرة النهائية المنفّذة:**
- زر جديد **"📋 كشف التحويلات الجامع"** يظهر بس بتبويب "قيد الانتظار" لمصدر السائقين، وبس لو في تحويلات (`type: 'payout'`) معلّقة فعلاً.
- بيفتح مودال فيه جدول: اسم الكابتن + **آيبانه** (يُجلب حياً من مجموعة `drivers` لكل معاملة) + المبلغ، مع checkbox لكل صف (مُحدّد افتراضياً بالكامل) + "تحديد الكل".
- **زر "طباعة"** — `window.print()` بس، **بدون أي تأثير على قاعدة البيانات إطلاقاً** — قابل للتكرار براحة الأدمن عدد لا نهائي من المرات للمراجعة قبل الذهاب للبنك (CSS مخصص `@media print` يخفي كل الصفحة عدا الجدول نفسه).
- **زر منفصل "تأكيد التحويلات المحددة ✓"** — هاد بس اللي بيغيّر الحالة لـ`completed` فعلياً، وبس للصفوف المحدّدة (checkbox)، عبر `writeBatch` واحدة (عملية ذرية — إما كل التحديدات تنجح مع بعض أو ولا وحدة). فيه `confirm()` توضيحي يعرض عدد الكباتن والمبلغ الإجمالي قبل التنفيذ.
- **الفايدة الأمنية:** الأدمن يقدر يطبع الكشف، يروح البنك، يرجع، وبعدين **يشيل التحديد (☑) عن أي كابتن ما تحوّل له فعلياً** (مثلاً تحويل تأخر أو فشل) قبل ما يضغط تأكيد — فما في احتمال "تصفير غلط" لرصيد ما تحول أصلاً.
- ⚠️ **قيد معروف:** لو كابتن ما سجّل آيبان أصلاً (تسجيله كان اختياري)، الخانة بتظهر بتحذير "⚠️ ما سجّل آيبان" — الأدمن لازم ينتبه ويتواصل معه يدوياً قبل التحويل، ما في تحقق آلي يمنع تحديده بالجدول.


## 11 أغسطس 2026 — قرارات مؤيد بخصوص لوحة المهام المفتوحة (جلسة متابعة)

مؤيد راجع قائمة المهام المفتوحة وحدد التالي:

1. **نسبة العمولة (10%):** ✅ موافق عليها نهائياً كنسبة افتراضية — وأكّد إنه يقدر يعدّلها بنفسه في أي وقت من صفحة `akleto-admin-settings.html` (موجودة أصلاً، تتحكم بـ`app_settings/commissions` وتنعكس لحظياً بكل صفحات السائق عبر `watchCommissionSettings()`). لا حاجة لأي تعديل برمجي إضافي هون.
2. **بوابة الدفع الإلكتروني:** ⏸️ لسا قيد البحث من طرف مؤيد — تقرر تأجيلها مؤقتاً لحد ما التطبيق يترفع على Google Play أولاً. أي شغل مرتبط ببوابة الدفع (`driver_settlements`, ربط الدفع الفعلي بشاشتي التسديد/البطاقات) يضل مؤجّل لحد اختيار البوابة.
3. **نظام Auth حقيقي (Firebase Auth):** 🔜 مؤيد أكّد إنه رح نبلش فيه **قريباً** (أولوية قادمة، مش بعيدة) — لسا ما تحدد تاريخ/جلسة معينة.
6. **بنية إشعارات الدفع لتطبيق الإدارة (`mobile-admin`):** ⏸️ تأجيل — "خليها اشوي".
7. **بناء APK لـ `mobile-store` و`mobile-admin`:** ✅ تم من طرف مؤيد فعلياً — بناهم وجرّبهم بنجاح على جهاز حقيقي.
8. **قواعد Firestore المفتوحة (`allow write: if true`):** 🔜 مؤيد أكّد رح نعالجها **قريباً جداً** — أولوية عالية نسبياً بين المهام المتبقية (مرتبطة مباشرة بنقطة #3 أعلاه، نظام الـAuth الحقيقي).

**الخلاصة:** أولويتين قادمتين واضحتين حسب توجيه مؤيد: (أ) نظام Auth حقيقي + إصلاح قواعد الأمان المفتوحة سوا (مرتبطتين ببعض)، (ب) الاستمرار بالتحضير لرفع تطبيق الزبون (على الأقل) على Google Play بينما بوابة الدفع لسا معلّقة. بوابة الدفع وإشعارات تطبيق الإدارة مؤجّلتين حالياً بدون تاريخ محدد.


## 11 أغسطس 2026 (تكملة) — ✅ مهمة 9: تنظيف الكود الميت بصفحة `akleto-driver-deliveries.html`

بما إن باقي المهام المفتوحة (بوابة الدفع، Auth، إشعارات الأدمن) صارت بانتظار قرار/توقيت من مؤيد، تم البدء بالبند الوحيد الجاهز فوراً بدون أي اعتماديات: **مهمة 9 (تنظيف dead code)**.

**اللي انحذف:**
- الـ HTML: `<div class="sheet-overlay" id="orderSheet">` بالكامل (مودال تفاصيل الطلب القديم بحقوله الفرعية `sheet-code`, `sheet-store-name`, `sheet-address`... إلخ).
- الـ JS: دوال `window.markDelivered`, `window.openOrderSheet`, `window.closeOrderSheet`.
- زري "التفاصيل" و"تم التوصيل" من بطاقة الطلب النشط بـ`renderMyOrders()` (كانا بينادوا على الدوال المحذوفة).
- استيرادات صارت غير مستخدمة: `addDoc`, `doc`, `updateDoc`, `serverTimestamp`, `withTimeout` من `firebase-init.js`، و`ticketCode` من `driver-shared.js`.

**ليش كان آمن الحذف:** الشاشة الكاملة الجديدة (full-screen delivery flow) صارت تتولى كامل تفاعل الكابتن مع الطلب من لحظة الاستلام (`claimOrder`) لحد التسليم، وتمنع الوصول لصفحة "توصيلاتي" أصلاً طول ما في طلب نشط — فهالمودال والدوال المرتبطة فيه ما كانوا رح يظهروا أو يُستخدموا بأي سيناريو طبيعي.

**اللي ضلّ زي ما هو:** عرض قائمة الطلبات النشطة/المكتملة نفسه (`renderMyOrders`) — بس صار بدون أزرار تفاعل، عرض فقط (read-only)، وهيك أصبح متّسق فعلياً مع الواقع (ما في تفاعل ممكن يصير من هالصفحة أصلاً).

**التحقق:** `node --check` على كامل كود الـ JS بالصفحة بعد التعديل — ما في أخطاء syntax. تم البحث عن أي مرجع يتيم (orphaned reference) لأي من العناصر/الدوال المحذوفة بكامل الملف — ولا وحدة تبقّت.

**الحالة:** ✅ خلصت مهمة 9 بالكامل. المتبقي من لوحة المهام: بوابة الدفع (مؤجّلة)، Auth حقيقي + قواعد Firestore المفتوحة (قريباً حسب مؤيد)، إشعارات push لتطبيق الإدارة (مؤجّلة).


## 11 أغسطس 2026 (تكملة ثانية) — 🆕 توزيع الطلبات بالدور + حاوية "طلب جديد" على شاشة الخريطة

مؤيد طلب تنفيذ الفكرة المسجّلة سابقاً بـ3 أغسطس (كانت "تصميم بس، بدون تنفيذ"): نقل حاوية "طلب جديد" من صفحة "الطلبات المتاحة" المنفصلة إلى الصفحة الرئيسية (شاشة الخريطة)، مع عداد 5 ثواني وتوزيع بالدور بين الكباتن (أقرب سائق يشوف الطلب أولاً، ولو ما قبِل خلال 5 ثواني ينتقل تلقائياً لسائق تاني).

### القرار المعماري (بعد سؤال توضيحي لمؤيد)
مؤيد أكّد: **توزيع بالدور حقيقي** — الطلب يُعرض لكابتن واحد بس بكل لحظة (الأقرب لموقع المتجر)، ولو ما قبله خلال 5 ثواني يُنقل تلقائياً (سيرفر-سايد، مو بس اختفاء بصري) لكابتن تاني بالدور. **مو** مجرد عرض جماعي بمهلة بصرية.

### التنفيذ الكامل

**1) `functions/index.js` — إعادة بناء `onOrderReady` بالكامل:**
- ما عاد يبعت إشعار جماعي لكل السائقين المتاحين. بدل هيك:
  - `pickNextDriver()`: يحسب أقرب سائق **متاح** (`is_available == true`) **وغير مشغول** (ما عنده طلب `status: delivering` حالياً — فحص جديد ما كان موجود قبل) لموقع المتجر (`stores/{store_id}.lat/lng`)، عبر معادلة Haversine، مستثنياً أي سائق جُرّب قبل بنفس الطلب.
  - `offerOrderToNextDriver()`: يحدّث الطلب بـ`offered_driver_id` (مين معروض عليه حالياً) و`offer_expires_at` (وقت + 5 ثواني) و`offered_driver_ids` (قائمة تراكمية لمين جُرّب)، ويبعت له push مستهدف (data-only، نفس نمط Full-Screen Intent السابق)، وبيجدول **Cloud Task** (`advanceOrderOffer`) يشتغل تلقائياً بعد 5 ثواني بالضبط.
  - **`advanceOrderOffer`** (دالة جديدة، `onTaskDispatched` من `firebase-functions/v2/tasks`): تتحقق أول شي إذا لسا العرض ساري (`status === 'ready'` و`offered_driver_id` لسا نفسه) — لو السائق قبِل الطلب قبل ما توصل المهمة، ما بتعمل شي (منطقياً ملغاة تلقائياً). غير هيك، بتنادي `offerOrderToNextDriver` من جديد لتدوير الطلب لأقرب سائق تالي.
  - **خط أمان (fallback):** لو خلصت قائمة السائقين المتاحين بدون قبول (كلهم جُرّبوا)، الطلب يصير `offer_broadcast: true` (مفتوح للجميع، بدون عداد) + إشعار جماعي زي النظام القديم — عشان الطلب ما يعلق بدون سائق نهائياً لو عدد الكباتن أونلاين قليل.
  - **متطلب نشر مهم:** أول `firebase deploy --only functions` بعد هالتحديث لازم يفعّل/ينشئ Cloud Tasks Queue تلقائياً (Firebase Functions v2 بتتكفل بهالخطوة بنفسها وقت النشر — نفس نمط أول نشر لأي Gen2 function، ممكن ياخد كم دقيقة أول مرة). ما في حاجة لأي إعداد يدوي إضافي بـ Google Cloud Console.
  - لا تغيير على `package.json` — `firebase-admin@^12.1.0` و`firebase-functions@^5.0.0` (النسخ الحالية بالمشروع) بيدعموا `firebase-admin/functions` و`v2/tasks` مباشرة (تأكدت من كذا بفحص محتوى الحزمتين من npm registry مباشرة).

**2) `driver-shared.js` — دوال جديدة/معدّلة:**
- `watchMyOrderOffer(driverId, cb)` 🆕: يراقب الطلب المعروض شخصياً على هالسائق حالياً (`offered_driver_id == driverId && status == 'ready'`) — يُستخدم حصرياً لحاوية "طلب جديد" بشاشة الخريطة (فيها عداد).
- `watchAvailableOrders(driverId, cb)` (تغيّر التوقيع — صار يطلب `driverId`): بدل ما يجيب كل الطلبات `status == 'ready'` بشكل عام، صار يجمع بين طلبين حيّين: المعروضة شخصياً على هالسائق + المفتوحة للجميع (`offer_broadcast == true`) — لصفحة "الطلبات المتاحة" وشارة العداد بالقائمة الجانبية، بحيث تضل متّسقة مع منطق التوزيع بالدور الجديد (سائق ما بيشوف طلب معروض على غيره).
- `claimOrderTransaction(orderId, session)` 🆕 (دالة مشتركة قابلة لإعادة الاستخدام): نفس منطق `claimOrder` الموجود أصلاً بصفحة الطلبات المتاحة (transaction ذرية تتحقق `status === 'ready'` + تسجيل إشعار أدمن) — استُخرجت كدالة مشتركة عشان تُستخدم من الحاوية الجديدة بشاشة الخريطة بدون تكرار الكود. **صفحة `akleto-driver-orders.html` القديمة ضلّت تستخدم نسخة `claimOrder` المحلية الخاصة فيها بدون تغيير (شغالة صح أصلاً، ما لمسناها لتقليل المخاطرة)**.

**3) `akleto-driver-home.html` (شاشة الخريطة) — الإضافة الأساسية:**
- حاوية جديدة (`#nocWrap`) تظهر فوق الخريطة (نفس ارتفاع أزرار الـFAB تقريباً)، بتصميم بطاقة فيها:
  - شريط تقدّم (progress bar) بلون برتقالي بيتقلّص من 100% لـ0% بالضبط خلال المدة المتبقية الفعلية (محسوبة من `offer_expires_at` القادم من السيرفر، مش 5 ثواني ثابتة بالعميل — عشان تفادي اختلاف التوقيت/تأخير الشبكة).
  - **اسم المتجر** (`order.store_name`).
  - **المسافة للعميل**: محسوبة لحظياً بالعميل (Haversine، بنفس دالة `haversineKm` المستخدمة أصلاً لحساب مسافة الوردية) بين موقع السائق الحي (`myCurrentPosition`، نفس المتغيّر المستخدم لعلامة السائق بالخريطة) وعنوان التوصيل (`order.delivery_lat/lng`). لو الموقع الحي لسا ما تحدد، تظهر "—".
  - **قيمة الطلب** (`order.total_estimated_price`).
  - **نوع الدفع**: شارة "💵 كاش" أو "💳 فيزا" حسب `order.payment_method` (نفس الحقل والمنطق المستخدم أصلاً بشاشة الطلب النشط — `payment_method === 'card' || 'online'`). ⚠️ **ملاحظة مهمة لمؤيد**: حالياً كل الطلبات `cash_on_delivery` دايماً لأن بوابة الدفع الإلكتروني لسا مؤجّلة (زي ما تحدد بجلسة اليوم الأولى) — فعملياً هالشارة رح تظهر "كاش" فقط لحد ما يتفعّل الدفع الإلكتروني، بس الكود جاهز ومربوط صح من الآن.
  - زر "✓ قبول الطلب" وحيد — بينادي `claimOrderTransaction` المشتركة، ولو نجح يوجّه لصفحة "توصيلاتي" (نفس سلوك الاستلام القديم بالضبط).
- الحاوية بتختفي تلقائياً (`watchMyOrderOffer` بيرجّع `null`) بمجرد ما الطلب يتغيّر حالته (قُبل من نفس السائق، أو انسحب منه بالسيرفر بعد انتهاء الـ5 ثواني وتحوّل لسائق تاني) — تحديث لحظي، بدون أي تايمر إضافي بالعميل يتحكم بالإخفاء (السيرفر هو مصدر الحقيقة).

**4) `akleto-driver-orders.html`:** تعديل سطر واحد بس — تمرير `session.id` لـ`watchAvailableOrders` بعد تغيير توقيعها (باقي الصفحة بدون تغيير، شغالة كخط احتياطي/تفصيلي لو حاب السائق يشوف كل شي بصفحة منفصلة).

### التحقق قبل الرفع
- `node --check` على الأربع ملفات (functions/index.js, driver-shared.js, akleto-driver-home.html, akleto-driver-orders.html) — ولا خطأ syntax.
- تأكدت من دعم `firebase-admin@12.1.0` لـ`firebase-admin/functions`، و`firebase-functions@5.0.0` لـ`v2/tasks` مباشرة من ملفات الحزم على npm registry (بدون تحميل كامل الحزمة).

### ⚠️ المطلوب من مؤيد للنشر والاختبار
1. **نشر الدوال:** `firebase deploy --only functions` — أول نشر بعد هالتحديث ممكن ياخد وقت أطول شوي من العادة (تفعيل Cloud Tasks Queue تلقائياً لأول مرة). لو ظهر خطأ صلاحيات بأول نشر (شبيه بمشكلة Eventarc السابقة)، جرب تعيد المحاولة بعد كم دقيقة.
2. **بناء APK جديد لمشروع `mobile-driver`** (تعديلات على صفحات HTML/JS ويب، لازم `npx cap sync android` + بناء جديد بـAndroid Studio زي أي تحديث ويب سابق).
3. **سيناريو الاختبار المقترح:**
   - فعّل التوفر (`متاح`) على جهازين/حسابي سائق مختلفين (أو جهاز وحساب تجريبي).
   - أنشئ طلب من تطبيق الزبون لحد ما يوصل لحالة `ready` (المتجر يأكد التجهيز).
   - راقب: هل الحاوية بتظهر بس عالسائق الأقرب؟ هل شريط العداد بيتحرك صح ويختفي بعد ~5 ثواني بالضبط لو محدش قبِل؟ هل الطلب بيظهر بعدها عالسائق التاني تلقائياً بدون أي تدخل يدوي؟
   - جرب اضغط "قبول الطلب" وتأكد إنه بينتقل لصفحة "توصيلاتي" وتبدأ الشاشة الكاملة المعروفة (aog) بالمرحلة الأولى بشكل طبيعي.
4. **حالة نادرة يستاهل تجربتها لو أمكن:** لو ما في إلا سائق واحد أونلاين وما قبل خلال 5 ثواني، تأكد إنه الطلب يتحول لـ"مفتوح للجميع" (broadcast) بدل ما يعلق بدون سائق نهائياً (خط الأمان المذكور فوق).

**الحالة:** ✅ الكود مكتمل ومرفوع (commit `0418c41`). ⏳ بانتظار نشر Cloud Functions + بناء APK + اختبار فعلي من مؤيد قبل ما نعتبرها خلصت 100%.


## 11 أغسطس 2026 (تكملة ثالثة) — 🆕 بدء مهمة 3+8: Auth حقيقي + قواعد Firestore (تجربة أولى على drivers)

بعد موافقة مؤيد ("اوك") على الخطة المرحلية (البدء بمجموعة بسيطة كتجربة بدل تغيير كل شي دفعة وحدة)، تم تنفيذ أول مرحلة فعلياً على مجموعة `drivers`.

### اكتشاف مهم قبل البدء
`firestore.rules` **ما كانت موجودة أبداً بالريبو** — القواعد كانت تُدار يدوياً 100% من Firebase Console بدون أي تتبّع Git. تم إصلاح هالوضع: أضفنا `firestore.rules` للريبو وربطناها بـ`firebase.json` (قسم `"firestore": {"rules": "firestore.rules"}`) — من الآن فصاعداً القواعد جزء من الكود ولها تاريخ commits.

### التنفيذ

**1) `firebase-init.js`:** إضافة `signInAnonymously` للاستيراد والتصدير (كانت موجودة بس `getAuth, onAuthStateChanged, signInWithEmailAndPassword, signOut` — ناقصة الدالة المجهولة).

**2) `driver-shared.js` — نظام Auth جديد بالكامل:**
- `ensureDriverAuth()`: تضمن وجود جلسة Firebase Auth (تسجّل دخول Anonymous تلقائياً لو ما في)، ترجع الـ`uid` الثابت للجهاز.
- `linkDriverAuthUid(driverId)`: تربط الـ`uid` الحالي بمستند السائق (`auth_uid` field).
- `createDriverAccount()`: صار يستدعي `ensureDriverAuth()` ويحفظ `auth_uid` وقت التسجيل من أول لحظة.
- `mountActiveOrderGuard(session)` (تُستدعى بكل صفحات السائق تقريباً): صارت تستدعي `linkDriverAuthUid(session.id)` تلقائياً بالخلفية — **هاد المهم**: بيغطي كل السائقين اللي مسجّلين دخول أصلاً من قبل هالتحديث (localStorage session قديمة) بدون ما يحتاجوا يسجّلوا خروج/دخول من جديد؛ أول فتح للتطبيق بعد التحديث بيربط حسابهم تلقائياً بهدوء.

**3) `akleto-driver.html` (شاشة تسجيل الدخول):** بعد نجاح `findDriverByPhone` وقبل `saveDriverSession`، صار يستدعي `await linkDriverAuthUid(driver.id)` — تأكيد إضافي للمسار الطبيعي (تسجيل دخول جديد).

**4) `firestore.rules` (ملف جديد بالكامل) — أول قاعدة مقيّدة فعلياً:**
- دالة مساعدة `isAdmin()`: بتتحقق من وجود مستند بمجموعة جديدة `admins/{uid}` — الـ`uid` هون هو نفس uid حساب Firebase Auth (إيميل/باسورد) تبع الأدمن.
- مجموعة `admins`: قراءة لأي مسجّل دخول (لازمة عشان `isAdmin()` تشتغل)، كتابة ممنوعة بالكامل من التطبيق (تُدار يدوياً بالكونسول حالياً).
- مجموعة `drivers`:
  - قراءة: عامة (تستخدمها ميزة "السائقين القريبين مني" بالخريطة + لوحة الأدمن).
  - إنشاء: لازم Auth (حتى Anonymous) + `auth_uid` بالمستند الجديد يطابق هوية المُنشئ.
  - تعديل: مسموح للأدمن دايماً، أو لصاحب المستند (`auth_uid` يطابق، أو المستند قديم وبعده ما انربط — نافذة انتقالية مؤقتة). **استثناءات مهمة اكتشفناها أثناء الكتابة**: لازم نسمح للسائق يرجّع `approval_status` لـ`pending` (ميزة "إعادة التقديم" بعد الرفض موجودة أصلاً وبتحتاج هيك)، بس ممنوع يحطها `approved` بنفسه. وممنوع يلمس `is_active` إطلاقاً (تفعيل/إيقاف الحساب قرار أدمن حصري) — لاحظوا إنه `is_active` (حساب مفعّل/موقوف من الأدمن) مختلف تماماً عن `is_available` (متاح/غير متاح — بيبدّلها السائق نفسه بزر التوفر، ما لمسناها).
  - حذف: أدمن، أو السائق نفسه (ميزة "حذف حسابي" موجودة أصلاً بصفحة `akleto-driver-account.html` عبر `deleteDriverAccountById` — لازم تضل شغالة).
- باقي كل المجموعات (customers, stores, orders, meals... إلخ): **ضلّت بدون تغيير** (`allow read, write: if true`) — بانتظار دورها بعد ما نتأكد إنه نمط drivers شغال صح فعلياً بالتجربة.

### ⚠️ خطوات لازم تعملها إنت يدوياً قبل/أثناء النشر (بالترتيب بالضبط)

1. **فعّل تسجيل الدخول المجهول:** Firebase Console → مشروع akleto-prod → Authentication → Sign-in method → فعّل "Anonymous" (احتمال يكون مطفي افتراضياً — لو ما فعّلته، كل محاولات `signInAnonymously()` رح تفشل بصمت وكل شي رح يتوقف).

2. **أنشئ مستند الأدمن يدوياً** (خطوة لازمة وإلا `isAdmin()` رح ترجع false دايماً حتى إلك إنت):
   - افتح تطبيق الأدمن وسجّل دخولك العادي (إيميل/باسورد).
   - من Firebase Console → Authentication → Users، دوّر على حسابك ولاحظ عمود "User UID" (سلسلة طويلة من حروف وأرقام).
   - من Firebase Console → Firestore Database → أنشئ مجموعة جديدة اسمها بالضبط `admins`.
   - أنشئ مستند جديد جواها، ID المستند = نفس الـUser UID اللي نسخته (بالضبط، حرف حرف). محتوى المستند مش مهم، ممكن تحط حقل بسيط زي `name: "مؤيد"` بس تسجيل.
   - كرر لأي حساب أدمن ثاني لو في.

3. **انشر القواعد:** من مجلد المشروع (نفس المكان اللي فيه `firebase.json`) شغّل:
   ```
   firebase deploy --only firestore:rules
   ```
   لو طلع خطأ syntax بالقواعد (نادر، بس ممكن)، ابعتلي نص الخطأ بالضبط وبصلحها فوراً.

4. **ابني APK جديد** لمشروع `mobile-driver` (تعديلات JS/HTML جوهرية هالمرة، لازم `npx cap sync android` + بناء جديد بـAndroid Studio).

5. **سيناريوهات اختبار مقترحة (رتّبها بالضبط بهالترتيب):**
   - **أ) سائق قديم مسجّل دخول أصلاً (جلسة موجودة بالتطبيق من قبل):** افتح التطبيق عادي، تأكد الصفحة الرئيسية تفتح طبيعي بدون أخطاء (يعني `linkDriverAuthUid` اشتغلت بهدوء بالخلفية). بعدها جرب تبدّل حالة "متاح/غير متاح" وتأكد شغالة.
   - **ب) سائق جديد يسجّل من الصفر:** جرب التسجيل الكامل من `akleto-driver-signup.html` وتأكد الحساب يتحفظ صح وبيظهر عند الأدمن للموافقة.
   - **ج) تسجيل دخول برقم هاتف من جديد (بعد تسجيل خروج):** تأكد شغال عادي.
   - **د) إعادة تقديم بعد رفض:** لو عندك حساب تجريبي مرفوض، جرب زر "أعد التقديم" وتأكد بيرجع لـpending بنجاح (هاد كان أكبر خطر كسر بهالتحديث — تأكد منه منيح).
   - **هـ) حذف الحساب الذاتي:** من صفحة "حسابي" جرب زر حذف الحساب على حساب تجريبي (مو حساب حقيقي!) وتأكد بينحذف بنجاح.
   - **و) صلاحيات الأدمن:** تأكد لوحة الأدمن لسا تقدر توافق/ترفض/توقف سائقين عادي زي ما كانت (هاد بيتأكد إنه مستند `admins` انعمل صح).

### الخطوة التالية (بعد ما تتأكد كل شي شغال)
لو كل السيناريوهات فوق نجحت، نكمّل بنفس النمط بالضبط على `customers`، وبعدها `stores`، وأخيراً `orders` (الأعقد لأنها فيها 3 أطراف: زبون/متجر/سائق كل وحدة إلها صلاحية مختلفة على نفس المستند).

**الحالة:** ✅ الكود مكتمل ومرفوع (commit `3fc2e74`). ⏳ بانتظار الخطوات اليدوية الخمسة فوق من مؤيد + الاختبار الفعلي قبل ما نعتبر تجربة `drivers` ناجحة وننتقل للمجموعة التالية.


## 11 أغسطس 2026 (تكملة رابعة) — 🐛 إصلاح عاجل: تطبيق السائق كان بيعلق بكل تبويب

مؤيد بلّغ إنه التطبيق بيعلق لما يفتح أي تبويب من القائمة الجانبية بعد تحديث توزيع الطلبات بالدور. اكتشفت خطأين حقيقيين (مو وهم):

### الخطأ 1 — السبب الأساسي للعلوق (خطأ JS حقيقي)
وقت تعديل `watchAvailableOrders(cb)` لتصير `watchAvailableOrders(driverId, cb)` (لدعم التوزيع بالدور)، حدّثت بس صفحتين (`akleto-driver-home.html` و`akleto-driver-orders.html`) ونسيت **6 صفحات تانية** لسا بتنادي الدالة بالشكل القديم (`watchAvailableOrders(list => ...)`):
`akleto-driver-account.html`, `akleto-driver-cards.html`, `akleto-driver-deliveries.html`, `akleto-driver-reports.html`, `akleto-driver-settle.html`.

بهالحالة، الـcallback نفسه كان بيتمرّر مكان `driverId`، فـFirestore كانت بترفض القيمة (function مش نوع بيانات مسموح بـ`where()`) وترمي استثناء JS فوري وقت تحميل الصفحة — وبما إنه هالسطر top-level بالسكربت، الاستثناء كان بيوقف باقي تنفيذ السكربت بالكامل (يعني كل أزرار القائمة والتفاعلات بعده ما كانت تشتغل = التعليق اللي وصفه مؤيد بالضبط). ✅ تم تصحيح كل الصفحات الست لتمرير `session.id`.

### الخطأ 2 — قاعدة Firestore جديدة كانت بتمنع كل تحديثات السائقين الموافق عليهم
بقاعدة `drivers` الجديدة، الشرط `request.resource.data.approval_status != 'approved'` كان بيتحقق من **القيمة النهائية** بعد التحديث، مو من هل في **تغيير فعلي** لهاي القيمة. النتيجة: أي تحديث (حتى لتحديث الموقع أو تبديل التوفر أو ربط auth_uid) على سائق أصلاً `approved` كان مرفوض تلقائياً من القاعدة (لأنه الناتج النهائي دايماً `'approved'` بما إنه ما تغيّر). ✅ صُحّح الشرط ليتحقق من **الانتقال الفعلي** لحالة `approved` بس (`request.resource.data.approval_status == 'approved' && resource.data.approval_status != 'approved'`) — الآن مسموح بأي تحديث عادي على سائق موافق عليه أصلاً، وممنوع بس إنه يحط approval_status = approved بنفسه لو ما كانت هيك أصلاً.

### الحالة
✅ الإصلاحان مرفوعان (commit `dd9b5d9`). **لازم من مؤيد:**
1. `git pull` (أو التأكد إنه آخر تحديث وصل).
2. `firebase deploy --only firestore:rules` من جديد (عشان يطلع الإصلاح الثاني — القاعدة).
3. `npx cap sync android` + بناء APK جديد (عشان يطلع إصلاح الصفحات الست).
4. إعادة تجربة كل السيناريوهات المذكورة بالتحديث السابق من الصفر.

**درس مستفاد:** أي تغيير على توقيع دالة مشتركة (زي `watchAvailableOrders`) لازم فحص شامل لكل نقاط الاستخدام بكل الملفات (`grep` على اسم الدالة بكامل المشروع)، مو بس الصفحات اللي كانت بالتركيز وقتها. هالخطأ تحديداً كان لازم يُكتشف وقت المراجعة قبل الرفع الأول — تسجيل الملاحظة عشان نراجع بنفس الطريقة أي تعديل مستقبلي على دوال مشتركة بـ`driver-shared.js`.


## 12-13 أغسطس 2026 — 🔧 استبدال Cloud Tasks بحل أبسط (تدوير الطلبات بالدور)

بعد الإبلاغ إنه الطلب بطل يوصل للكباتن، سلسلة تشخيص طويلة مع مؤيد عبر `firebase functions:log`:

### التشخيص
1. أول شي طلع "فشل إرسال إشعار FCM: NotRegistered" — منفصل تماماً، بس توكن إشعار قديم (يُحل بإعادة تسجيل دخول، ما أثّر على الحاوية نفسها لأنها realtime مش push-dependent).
2. الخطأ الأساسي: **"Queue does not exist"** — Cloud Tasks Queue ما كانت تنعمل. جرّبنا بالترتيب: تفعيل `cloudtasks.googleapis.com` يدوياً، انتظار، `firebase deploy --only functions` كامل (طلع "Skipped — no changes detected" لكل الدوال حتى بعد تعديل الكود!)، تعديل الكود يدوياً لإجبار التغيير (لسا Skipped — تبيّن كان في مشكلة تانية غير الكاش)، وأخيراً **حذف الدالة بالكامل وإعادة إنشائها من الصفر** (`firebase functions:delete advanceOrderOffer` ثم deploy من جديد، طلعت "Successful create operation") — **ومع هيك برضه "Queue does not exist" استمرت**. يعني المشكلة أعمق من مجرد كاش النشر — غالباً صلاحيات أو قيود على مستوى المشروع بـGoogle Cloud خاصة بـCloud Tasks تحديداً، ما قدرنا نشخّصها عن بعد.

### القرار: التخلي عن Cloud Tasks بالكامل
بدل ما نستمر بمطاردة مشكلة صلاحيات غامضة بـGoogle Cloud، أعدنا تصميم آلية "تدوير العرض بعد 5 ثواني" بدون أي بنية تحتية خارجية:

**قبل:** `onOrderReady` تعرض الطلب على أقرب سائق، وتجدول **Cloud Task منفصلة** (`advanceOrderOffer`) تشتغل بعد 5 ثواني لتتحقق وتدوّر.

**بعد:** `onOrderReady` نفسها صارت تحتوي **حلقة كاملة** (`for` loop حتى 10 محاولات كحد أقصى): تعرض الطلب على أقرب سائق → `await sleep(5000)` (تنتظر جوا نفس تنفيذ الدالة) → تتحقق هل السائق قبِل (status تغيّر) → لو لأ، تدوّر لسائق تالي وتكرر. لا حاجة لأي Cloud Tasks Queue، لا `firebase-admin/functions`، لا `onTaskDispatched` — الكل جوا استدعاء واحد لـ`onDocumentUpdated` مع `timeoutSeconds: 120` (كافية لـ10 محاولات × 5 ثواني = 50 ثانية بالحد الأقصى النظري).

**التنازل الوحيد:** الدالة تضل "حية" وتستهلك موارد (تكلفة ضئيلة جداً) طول فترة الانتظار بدل ما تنتهي فوراً وتترك Cloud Task تتكفل بالتوقيت. لحجم استخدام أكليتو الحالي هالفرق غير محسوس عملياً، ومقابل هيك حصلنا على نظام أبسط بكتير وما بيعتمد على أي إعداد إضافي بـGoogle Cloud كان بيفشل بدون سبب واضح.

**دالة `advanceOrderOffer` صارت غير مستخدمة بالكود** — انحذفت من `functions/index.js`. **مطلوب من مؤيد حذفها يدوياً من Firebase** كمان (تنظيف، مش إلزامي بس أفضل):
```
firebase functions:delete advanceOrderOffer --region europe-west1
```

### الحالة
✅ الكود الجديد مرفوع (commit `e5d40af`). **المطلوب من مؤيد:**
1. `git pull`
2. `firebase deploy --only functions` (هالمرة onOrderReady تغيّرت فعلياً فمش رح تكون Skipped)
3. (اختياري) حذف `advanceOrderOffer` القديمة زي فوق.
4. إعادة تجربة نفس السيناريو: طلب جديد → حاوية تطلع عالسائق → لو ما ضغط قبول، بعد ~5 ثواني لازم يتحول لـbroadcast (لو سائق واحد بس) أو يدور لسائق تاني (لو أكتر من وحد) **بدون أي "Queue does not exist" هالمرة** لأنه ما عاد في Cloud Tasks إطلاقاً بالنظام.


## 13 أغسطس 2026 — مهمة 3+8 (تكملة): Auth حقيقي + قواعد Firestore على customers

بعد استقرار ميزة توزيع الطلبات بالدور، رجعنا لمهمة 3+8 وطبّقنا نفس نمط `drivers` على `customers`.

### 🚨 اكتشاف عاجل تم إصلاحه فوراً قبل أي شي تاني
أثناء المراجعة، لقينا إنه المجموعة الفرعية `customers/{id}/addresses` (عناوين توصيل الزبون) **ما كانت مغطاة بأي قاعدة أمان إطلاقاً من يوم رفع `firestore.rules` الأول (11 أغسطس)** — لأنه Firestore ما بتورّث قواعد المستند الأب للمجموعات الفرعية تلقائياً، ونُسيت وقتها. يعني على الأغلب إدارة عناوين التوصيل كانت معطّلة بصمت لمدة يومين. تم إصلاحها فوراً بقاعدة مؤقتة مفتوحة (`allow read, write: if true`) لحد ما وصلنا لدور customers بالتصميم المدروس (بالتحديث التالي).

**درس مستفاد وثّقناه:** أي إضافة قاعدة أمان جديدة لازم تتضمن فحص شامل لكل المجموعات الفرعية (subcollections) المرتبطة، مو بس المجموعة الرئيسية.

### تطبيق Auth حقيقي على تطبيق الزبون (`akleto-customer.html`)
نفس نمط السائق بالضبط (الزبون تطبيق صفحة واحدة single-page، فكان أبسط من السائق):
- استيراد `signInAnonymously`, `onAuthStateChanged`, `auth` من `firebase-init.js`.
- `ensureCustomerAuth()` و`linkCustomerAuthUid(customerId)` — نفس منطق السائق تماماً.
- `startCustomerSession` (إنشاء حساب): صار يحفظ `auth_uid` من أول لحظة.
- `enterApp()` (نقطة الدخول الموحّدة — بتُستدعى من التسجيل، الدخول، **واستعادة الجلسة المحفوظة** بفتح التطبيق): صارت تستدعي `linkDriverAuthUid` تلقائياً — نقطة ربط وحيدة تغطي كل السيناريوهات الثلاثة (بعكس السائق يلي احتاج تعديل بأكتر من مكان لأنه تطبيق متعدد الصفحات).

### قواعد Firestore الجديدة على `customers`
نفس بنية `drivers` بالضبط:
- قراءة عامة (تستخدمها لوحة الأدمن).
- إنشاء: لازم Auth + `auth_uid` مطابق.
- تعديل: الأدمن دايماً، أو صاحب الحساب (`auth_uid` يطابق، أو نافذة انتقالية للحسابات القديمة). **ممنوع الزبون يلمس `is_blocked`** (حظر/فك حظر قرار أدمن حصري من `akleto-admin-customers.html`).
- حذف: أدمن، أو الزبون نفسه (ميزة حذف الحساب الذاتي موجودة أصلاً بالتطبيق).

**إضافة جديدة مقارنة بنمط drivers:** دالة مساعدة `isOwnerCustomer(customerId)` بالقواعد — تُستخدم لتأمين المجموعة الفرعية `customers/{id}/addresses/{addressId}` (اللي هي نفسها ما فيها حقل `auth_uid`، فلازم تتحقق من ملكية **المستند الأب** عبر `get()`). هاي أول مرة نستخدم فيها `get()` جوا قاعدة أمان بالمشروع — يستاهل انتباه لو صار أي تعديل مستقبلي على شكل مستند العميل.

### 🎁 إصلاح إضافي (غير مخطط له، اكتُشف بالمراجعة وقت هالشغل)
بنفس الجلسة، مؤيد بلّغ عن خلل بشاشة تأكيد الطلب: زر "تعديل" (لتغيير عنوان التوصيل من داخل شاشة الدفع) كان بيفتح شاشة العناوين **فوق** شاشة تأكيد الطلب بدل ما يحل محلها (تراكب شاشات)، وبعد اختيار عنوان جديد كان يرجّع الزبون للشاشة الرئيسية بدل ما يكمل تنفيذ الطلب. تم إصلاحها بإضافة "ذاكرة سياق" (`addressesScreenReturnTo`) تتذكر من وين انفتحت شاشة العناوين، وترجّع الزبون تلقائياً لتأكيد الطلب بعد اختيار/حفظ عنوان — **بدون** تصفير الإكرامية أو كود الخصم اللي كان مختارهم قبل (كانت هاي بتنصفّر لو استخدمنا نفس دالة الفتح العادية).

### الحالة والمطلوب من مؤيد
✅ الكود مرفوع بالكامل (commits: `d661c35` لتطبيق الزبون، `2e1e660` للقواعد). **المطلوب:**
1. `git pull`
2. `firebase deploy --only firestore:rules`
3. `npx cap sync android` + بناء APK جديد لـ`mobile-customer` (تعديلات JS/HTML فقط، ما في تعديل Java هالمرة).
4. اختبار بنفس نمط السائق: حساب زبون قديم مسجّل دخول أصلاً (يفتح عادي، auth_uid ينربط بهدوء) → تسجيل زبون جديد → تسجيل دخول من جديد → **إدارة العناوين (إضافة/تعديل/حذف/تعيين افتراضي)** بالذات أهم اختبار (كانت معطّلة يومين، ولازم نتأكد الإصلاح شغال) → حذف الحساب الذاتي (على حساب تجريبي) → صلاحيات الأدمن (حظر/فك حظر زبون) لسا شغالة.
5. اختبار إضافي لإصلاح شاشة تأكيد الطلب: اختر وجبة → وصل لآخر شاشة → دوس "تعديل" → غيّر العنوان → تأكد الرجوع صح مع بقاء الإكرامية/الكوبون.

### الخطوة التالية
بعد ما يتأكد كل شي فوق شغال، نكمّل بنفس النمط على `stores`، وأخيراً `orders` (الأعقد — 3 أطراف: زبون/متجر/سائق).


## 13 أغسطس 2026 (تكملة) — مهمة 3+8 (تكملة تانية): Auth حقيقي + قواعد Firestore على stores

كمّلنا "دون ما ننتظر نتيجة اختبار customers" (بطلب مؤيد المباشر "كمل") — طبّقنا نفس المسار على `stores`، لكن بنموذج مختلف شوي لأنه خصوصية تسجيل الدخول مختلفة عن السائق/الزبون.

### الفرق الجوهري عن نمط drivers/customers
تسجيل دخول المتجر مش "حساب شخصي واحد" — **أكتر من موظف ممكن يسجّلوا دخول لنفس المتجر بنفس رمز PIN**، كل واحد من جهازه الخاص. يعني ما نقدر نربط `auth_uid` **واحد** بمستند المتجر زي ما عملنا بالسائق/الزبون (أول موظف بيسجّل دخول كان رح "يقفل" المتجر على جهازه بس).

**الحل:** مصفوفة `authorized_uids` على مستند المتجر نفسه — كل جهاز/موظف يسجّل دخول صح (بالـPIN الصحيح)، الـuid تبعه ينضاف للمصفوفة (`arrayUnion`). القاعدة الجديدة بتتحقق: `request.auth.uid in resource.data.authorized_uids`.

### التنفيذ
**1) `firebase-init.js`:** إضافة `arrayUnion` للاستيراد والتصدير (ناقصة من قبل).

**2) `akleto-store.html`:**
- `ensureStoreAuth()` / `linkStoreAuthUid(storeId)` — نفس نمط السائق/الزبون، بس `linkStoreAuthUid` بتعمل `arrayUnion` عالمتجر بدل `updateDoc` مباشر (لأنه ممكن يكون في كذا موظف مسبقاً بالمصفوفة، ما بدنا نمسحهم).
- `loginAsStore()`: بعد إيجاد/إنشاء سجل الموظف بـ`store_employees`، صار يربط `auth_uid` فيه (سواء موظف جديد أو موجود من قبل عبر جهاز جديد)، **وكمان** يضيف الـuid لمصفوفة `authorized_uids` على مستند المتجر.
- استعادة الجلسة المحفوظة (`DOMContentLoaded`): صارت تستدعي `linkStoreAuthUid(storeId)` تلقائياً — يغطي الموظفين المسجّلين دخول أصلاً من قبل هالتحديث.

### قواعد Firestore الجديدة
**`stores/{storeId}`:**
- قراءة عامة (تصفّح المتاجر من تطبيق الزبون + البحث بالـPIN وقت الدخول قبل ما يصير أي ربط أصلاً).
- إنشاء: **أدمن بس** (المتاجر تُنشأ من لوحة التحكم حصراً، ما في تسجيل ذاتي لمتجر جديد).
- تعديل: الأدمن دايماً، أو أي موظف uid موجود بـ`authorized_uids`، أو نافذة انتقالية للمتاجر القديمة اللي لسا ما انربطت. **ممنوع غير الأدمن يلمس `is_active` أو `pin`**.
- حذف: أدمن بس.

⚠️ **ملاحظة أمان وثّقناها بالكود نفسه:** النافذة الانتقالية هون أوسع شوي من درايفرز/كستمرز — أي جهاز مسجّل Auth مجهول (حتى بدون ما يعرف الـPIN) نظرياً يقدر "يتبنى" متجر قديم ما اتلمس بعد عبر استدعاء مباشر لقاعدة البيانات (تجاوز واجهة التطبيق). التأثير العملي محدود (بس `is_open`/`fcm_token` قابلين للتعديل، مش القائمة أو الأسعار)، بس يستاهل انتباه — أول ما كل المتاجر النشطة تسجّل دخول مرة وحدة بعد النشر، بننصح نراجع نشيل هالنافذة الانتقالية بالكامل من القاعدة (تصير `authorized_uids` لازم تكون موجودة دايماً).

**`store_employees/{empId}`:** قراءة عامة (تدعم منطق "دوّر عن موظف موجود بغض النظر عن جهازه" + شاشة الأدمن لعرض كل الموظفين)، إنشاء لأي Auth، تعديل لصاحب السجل بس (أو نافذة انتقالية مشابهة).

### الحالة والمطلوب من مؤيد
✅ الكود مرفوع بالكامل (commit `2cc4f60`). **المطلوب:**
1. `git pull`
2. `firebase deploy --only firestore:rules`
3. `npx cap sync android` + بناء APK جديد لـ`mobile-store`.
4. اختبار: تسجيل دخول موظف جديد بمتجر (بالـPIN الصحيح) → تبديل حالة "مفتوح/مغلق" (`is_open`) وتأكد شغالة → تسجيل موظف تاني بنفس المتجر من جهاز/متصفح تاني وتأكد الاثنين يقدروا يشتغلوا عادي (مو بس أول وحد) → صلاحيات الأدمن (تعطيل متجر، تعديل بياناته) لسا شغالة.

### الخطوة التالية
بعد التأكد، `orders` هي آخر مجموعة بمهمة 3+8 — الأعقد لأنها فيها 3 أطراف (زبون يُنشئ، متجر يعدّل الحالة، سائق يستلم) كل وحد بصلاحية مختلفة على نفس المستند بأوقات مختلفة.



## 15 أغسطس 2026 — مهمة 3+8 (تكملة نهائية): Auth حقيقي + قواعد Firestore على orders — آخر مجموعة بالخطة

**أُنجزت آخر مرحلة من مهمة 3+8** — تطبيق Firebase Anonymous Auth + قواعد Firestore مقيّدة فعلياً على `orders`، أعقد مجموعة بالمشروع (3 أطراف: زبون/متجر/سائق) على نفس نمط `drivers`/`customers`/`stores` المُنجَز سابقاً.

### 🐛 باغ تم اكتشافه وإصلاحه بنفس المراجعة
`store_employees` كانت معرّفة **مرتين** بـ`firestore.rules`: مرة بقاعدة مقيّدة (بالنمط الصحيح من جلسة الأمس)، ومرة تانية بقاعدة مفتوحة `allow read, write: if true` بقائمة "باقي المجموعات" بالأسفل — نُسيت وقتها. بما إنه Firestore بيطبّق **OR** بين كل `match` تطابق نفس المسار، القاعدة المفتوحة كانت عملياً **تُلغي** القيود المفروضة بالكامل من يوم رفعها. تم حذف السطر المكرر المفتوح — القاعدة المقيّدة الوحيدة هي الفعّالة الآن.

### دوال مساعدة جديدة بـ`firestore.rules`
- `isOwnerDriver(driverId)`: تتحقق إنه Auth الحالي يطابق `auth_uid` بمستند `drivers/{driverId}`، مع نافذة انتقالية لسائقين قدامى ما انربطوا بعد — نفس نمط `isOwnerCustomer` الموجودة أصلاً.
- `isStoreEmployee(storeId)`: تتحقق إنه Auth الحالي موجود بمصفوفة `authorized_uids` بمستند `stores/{storeId}` — نفس منطق قاعدة `stores` نفسها.

### قواعد `orders` الجديدة
- **القراءة:** ضلّت عامة (`if true`) — نفس السبب الموثّق بباقي المجموعات: تقييدها لكل طرف لحاله يحتاج فلترة على مستوى الاستعلام (query-level)، ومو أولوية حالياً بما إنه ما فيها بيانات دفع حساسة.
- **الإنشاء:** حصراً من الزبون — `request.auth != null` + `customer_id` بالمستند الجديد يطابق حساب الزبون المسجّل دخول (`isOwnerCustomer`).
- **التعديل — 4 مسارات (OR):**
  1. **الأدمن:** أي تعديل بأي وقت.
  2. **المتجر:** بس لو `store_id` بالطلب يطابق متجره (`isStoreEmployee`)، وممنوع يغيّر `customer_id`/`store_id`/`driver_id`/`total_estimated_price` — عملياً بيقدر يحدّث لـ`ready` + حقول `loaded_by_emp_*`/`ready_at`/`loading_duration_seconds` بس (يطابق تماماً منطق `markStoreOrderReady` بـ`akleto-store.html`).
  3. **سائق يستلم طلب جاهز** (`ready → delivering`): `driver_id` الجديد لازم يطابق حسابه هو (`isOwnerDriver`)، ونفس قيود عدم تغيير `customer_id`/`store_id`/السعر — يطابق `claimOrderTransaction` بـ`driver-shared.js`.
  4. **سائق أصلاً مستلم الطلب:** يقدر يكمل مراحل التوصيل (`driver_flow_stage`، `status→done`، `driver_rating`...) بس ممنوع يغيّر `driver_id`/`customer_id`/`store_id`/السعر بعد الاستلام.
- **الحذف:** أدمن بس.
- **ملاحظة معمارية مهمة:** Cloud Functions (`onOrderReady`, `onOrderUpdated`, `onOrderCreated`) بتستخدم Firebase Admin SDK اللي أصلاً بيتجاوز كل قواعد الأمان — ما احتجنا أي استثناء صريح إلهم بالقواعد.

### التحقق قبل الرفع
- تتبّعت كل نقاط الكتابة الفعلية على `orders` بالكود (`akleto-customer.html` وقت الإنشاء، `akleto-store.html` بـ`markStoreOrderReady`، `driver-shared.js` بـ`claimOrderTransaction` وباقي تحديثات شاشة الطلب النشط) — كلها تستخدم `update`/`set` جزئي بدون لمس الحقول المحمية (`customer_id`/`store_id`/`driver_id`/`total_estimated_price` غير `driver_id` بحالة الاستلام تحديداً)، فمتوافقة مع القاعدة الجديدة بدون أي حاجة لتعديل كود إضافي بالتطبيقات الثلاثة.
- تأكدت إنه `linkCustomerAuthUid`/`linkDriverAuthUid`/`linkStoreAuthUid` كلها تُستدعى تلقائياً عند فتح كل تطبيق (`enterApp()` بالزبون، `mountActiveOrderGuard()` بكل صفحة سائق، استعادة الجلسة + تسجيل الدخول بالمتجر) — يعني `auth_uid` رح يكون مربوط دايماً قبل أي محاولة كتابة على `orders`، حتى للحسابات القديمة المسجّلة قبل هالتحديث.
- فحص توازن الأقواس بالملف كامل (بعد إزالة التعليقات العربية اللي فيها أقواس نصية) — متوازن 100%.

### ⚠️ المطلوب من مؤيد للتفعيل الفعلي
1. `git pull`
2. `firebase deploy --only firestore:rules`
3. **لا حاجة لبناء APK جديد لأي من التطبيقات الأربعة** — هالمرحلة لمست بس `firestore.rules` (ملف قواعد، مو كود تطبيق) — التفعيل فوري بمجرد النشر.
4. **سيناريوهات اختبار مقترحة (الأهم فعلياً — orders هي قلب دورة الاستخدام):**
   - زبون يُنشئ طلب جديد من الصفر → يتأكد يترسل بنجاح.
   - متجر يستلم الطلب (`preparing`) ويضغط "جهّزنا المكونات — جاهز للسائق" → يتأكد ينتقل لـ`ready` بنجاح.
   - سائق يشوف حاوية "طلب جديد" بشاشة الخريطة ويضغط "قبول الطلب" → يتأكد ينتقل لـ`delivering` وتفتح شاشة الطلب النشط الكاملة.
   - سائق يكمل كل مراحل شاشة الطلب النشط الست (توجه للمتجر → استلام → توجه للعميل → تسليم → إيصال → تقييم) → يتأكد كل انتقال حالة يشتغل بدون خطأ صلاحيات.
   - الأدمن يفتح `akleto-admin-orders.html` ويتأكد لسا يقدر يغيّر حالة أي طلب يدوياً (نفس صلاحياته الكاملة زي قبل).

### 🎉 خلاصة مهمة 3+8 (بعد إنجاز orders)
**المجموعات الأربعة المخطط لها كلها صارت مقيّدة فعلياً بـFirebase Anonymous Auth:** `drivers` → `customers` (+ `addresses`) → `stores` (+ `store_employees`) → `orders`. باقي المجموعات (تشغيلية/مالية داخلية، بدون تعرّض مباشر بواجهة مستخدم غير موثوق تكتب عليها مباشرة بدون منطق تطبيق وسيط) ضلّت على وضعها المفتوح — ديون تقنية موثّقة، مو أولوية حالياً. **بانتظار تأكيد مؤيد من الاختبار الفعلي بعد النشر قبل ما نعتبر المهمة كاملة 100%.**


## 15 أغسطس 2026 (تكملة) — مهمات جديدة (Backlog) بعد إنجاز مهمة 3+8

مؤيد طلب نضيف 5 مهمات جديدة للعمل عليها بالجلسات القادمة. **لسا ما بدأنا فيها — توثيق أولي بس عشان ما تضيع.**

1. **نغمة إشعار وصول طلب جديد للسائق** — مدتها 10 ثواني، تشتغل لما يوصل عرض طلب شخصي/broadcast للسائق (شاشة الخريطة، حاوية "طلب جديد").
2. **تعديل الهوية البصرية** — اللوغو، الألوان، الخط الحالي مش عاجب مؤيد 100%. يحتاج نقاش اتجاه تصميمي جديد قبل التنفيذ.
3. **باغ: زر "تنفيذ الطلب" بتطبيق الزبون** — الضغط على **النص/الكلمة نفسها** ما بيستجيب، بس الضغط على **الحاوية (container)** المحيطة فيها بيستجيب. يبدو منطقة الضغط (click/tap target) مش شاملة كامل العنصر — على الأغلب مشكلة CSS (pointer-events أو z-index على عنصر داخلي) أو event listener مربوط بالحاوية بس مش بالنص. يحتاج فحص أول بالكود.
4. **إشعار ثابت (persistent notification) لما السائق يصير "متاح"** — يظهر بشريط إشعارات الموبايل ويضل ثابت (زي إشعارات التطبيقات اللي شغالة بالخلفية — Ongoing/Foreground Service notification على أندرويد) لحد ما الكابتن يرجع يحوّل حالته لـ"غير متاح" يدوياً. يحتاج على الأغلب Foreground Service حقيقي على مستوى Android (Capacitor) مش مجرد إشعار FCM عادي، لأنه إشعار FCM عادي ممكن ينمسح/يختفي، والمطلوب هون إشعار **ثابت ما يقفل** طول ما السائق متاح.
5. **الزبون يحدد المتجر اللي بدو يشتري منه مكونات الوجبة** — حالياً (حسب الفهم من الكود السابق) توجيه الطلب للمتجر بيصير تلقائياً (`routingStoreId`/`routingStoreName`) بمنطق معيّن (على الأغلب أقرب متجر عنده كل المكونات). المطلوب الجديد: يعطى الزبون خيار **يختار هو بنفسه** المتجر بدل التوجيه التلقائي. يحتاج نقاش تصميم قبل التنفيذ: هل يختار متجر واحد لكل السلة كلها؟ شو بيصير لو المتجر المختار ناقصه مكوّن معيّن (نفس منطق "المتجر الجزئي" الموجود حالياً بالتوجيه التلقائي)؟ وين بالضبط بتصير الاختيار بمسار الطلب (صفحة اختيار الوجبات، أو شاشة التأكيد)؟

### الخطوة التالية
البدء بترتيب أولوية يحدده مؤيد، بدءاً غالباً بالمهمات الأوضح تقنياً (1، 3) قبل المهمات اللي تحتاج نقاش تصميم/منتج (2، 4، 5).


## 15 أغسطس 2026 (تكملة) — مهمة #1 من الـbacklog: إصلاح باغ زر "تنفيذ الطلب"

**السبب الجذري المكتشف:** عنصر `<div class="toast" id="toast">` (تنبيهات صغيرة زي "تمت الإضافة للسلة") موجود دايماً بالـDOM بموضع `position:fixed` ثابت بالمنتصف السفلي للشاشة، ويتحكم بظهوره فقط عبر `opacity: 0 → 1` (كلاس `.toast.show`). المشكلة: بحالته المخفية (`opacity:0`) **ما كان فيه `pointer-events: none`** — يعني رغم إنه غير مرئي، صندوقه (`z-index:300`) كان **يبلع الضغطات** لأي عنصر تحته يقع بنفس موقعه بالضبط. بتطبيق الزبون، هالموقع صادف يتراكب مع منطقة نص زر "تنفيذ الطلب" — فالضغط بالضبط على الكلمة كان يُبلع من الـtoast الخفي، بينما الضغط على أطراف الزر (خارج مربع الـtoast الضيق) كان يوصل للزر العادي.

**نطاق الإصلاح:** نفس الـpattern (نفس CSS تقريباً حرفياً) موجود بـ**9 ملفات** — فحصت الريبو كامل ولقيته بـ:
- `akleto-customer.html` (مصدر البلاغ الأصلي)
- `akleto-store.html`
- كل صفحات السائق: `akleto-driver-home.html`, `akleto-driver-orders.html`, `akleto-driver-account.html`, `akleto-driver-cards.html`, `akleto-driver-deliveries.html`, `akleto-driver-reports.html`, `akleto-driver-settle.html`

أضفت `pointer-events: none;` لقاعدة `.toast` بكل الملفات التسعة — إصلاح CSS بسيط بدون أي تغيير بالمنطق أو الـJS. هذا معناه المشكلة كانت (أو ممكن كانت) موجودة كمان بأي زر تاني بنفس منطقة الشاشة بكل تطبيقات المشروع، مو بس زر تأكيد الطلب — الإصلاح الوقائي هون بيمنع نفس الباغ يظهر بأماكن تانية بالمستقبل.

**لم يُعدَّل:** `akleto-driver.html` (شاشة تسجيل الدخول)، `akleto-driver-signup.html`، وملفات الأدمن — إما ما فيها نفس الـpattern أصلاً أو ما فيها toast بمنطقة حرجة (لم تُفحص جميعها بعمق، بس فحصت الملفات المرشّحة الأساسية).

### المطلوب من مؤيد
`git pull` بس — تعديل CSS خالص بالملفات، **لا حاجة لأي نشر Firebase أو بناء APK**. جرّب زر "تنفيذ الطلب" (اضغط بالضبط على النص هالمرة) وتأكد اشتغل، وخبرني إذا لاحظت نفس المشكلة بأي زر تاني بأي تطبيق (احتمال ضعيف انه لسا موجود بمكان تاني ما انتبهنا له).


## 15 أغسطس 2026 (تكملة) — مهمة #2 من الـbacklog: نغمة تنبيه "طلب جديد" (10 ثواني)

**الحل المُختار:** توليد الصوت مباشرة عبر **Web Audio API** (نغمتين متبادلتين "دينغ-دونغ" لمدة 10 ثواني بالضبط) بدل تحميل ملف mp3 خارجي — أسرع، بدون طلب شبكة، بدون استضافة أصل صوتي بالريبو، وبدون أي تعديل على إعدادات Capacitor/Android.

### التنفيذ
- **`driver-shared.js`** (دوال جديدة، مشتركة لباقي صفحات السائق مستقبلاً لو احتجناها):
  - `unlockNewOrderAlertAudio()`: تفتح/تستأنف `AudioContext` — لازم تُستدعى مرة وحدة داخل معالج ضغطة زر حقيقي (autoplay policy بمتصفحات الموبايل ما بتسمح بتشغيل صوت بدون تفاعل مستخدم مباشر أول مرة).
  - `playNewOrderAlertSound()`: تشغّل نمط "دينغ-دونغ" (988Hz ثم 740Hz) متكرر كل 0.9 ثانية لمدة 10 ثواني كاملة، وترجع دالة إيقاف فوري.
  - `stopNewOrderAlertSound()`: توقف أي نغمة شغالة فوراً (بتقفل الـAudioContext نفسه — أبسط طريقة موثوقة لقطع oscillators مجدولة مسبقاً بمنتصف تشغيلها).
- **`akleto-driver-home.html`** (شاشة الخريطة — المكان الوحيد اللي فيه حاوية "طلب جديد"):
  - `unlockNewOrderAlertAudio()` تُستدعى داخل `toggleAvailability()` لما السائق يضغط "متاح للطلبات" (تفاعل مستخدم حقيقي، بيفتح الطريق لتشغيل تلقائي لاحق بدون تفاعل مباشر).
  - `playNewOrderAlertSound()` تُستدعى داخل `renderOrderOffer()` بس أول مرة يظهر فيها كل طلب جديد بالذات (`order.id !== currentOfferId`) — نفس الشرط اللي يشغّل أنيميشن شريط العد التنازلي، ما بتتكرر مع كل تحديث لايف عادي لنفس الطلب.
  - `stopNewOrderAlertSound()` تُستدعى فوراً بحالتين: (1) الطلب اختفى قبل ما تخلص العشر ثواني (انقبل من كابتن تاني أو انتهت مهلته)، (2) الكابتن قبل الطلب بنفسه.

### التحقق قبل الرفع
فحصت صياغة الجافاسكربت بـ`node --check` (module mode) لكل من `driver-shared.js` وكتلة `<script type="module">` بـ`akleto-driver-home.html` — سليمة بالكامل.

### المطلوب من مؤيد
1. `git pull`
2. **لا حاجة لنشر Firebase** — بس لازم **إعادة بناء APK** للسائق (تعديل JS/HTML بيحتاج تحديث حزمة Capacitor).
3. اختبار: افتح تطبيق السائق، فعّل "متاح للطلبات"، وخلي طلب يوصلك (أو خلي شخص تاني يطلب) وتأكد النغمة تشتغل 10 ثواني كاملة عند وصول أول عرض. جرّب كمان تقبل الطلب بنص المدة وتأكد الصوت يوقف فوراً.
4. ⚠️ **ملاحظة مهمة:** لو السائق دخل التطبيق وفعّل "متاح" من جلسة سابقة (الجهاز اتقفل وانفتح بدون ما يضغط الزر من جديد)، الـAudioContext ممكن ما يكون انفتح بعد (autoplay policy) فما تشتغل النغمة أول مرة. هاد سيناريو نادر بس وارد — لو صار، الحل الفوري هو يضغط أي زر بالصفحة (حتى لو مو زر التوفر) قبل أول طلب يوصله. لو تكرر الموضوع فعلياً بعد الاختبار، لازم نضيف `unlockNewOrderAlertAudio()` على أول تفاعل عام بالصفحة (مثلاً أول ضغطة بأي مكان) بدل الاعتماد بس على زر التوفر.


## 15 أغسطس 2026 (تكملة) — ⚠️ تصحيح مهم: آلية تحميل التطبيقات فعلياً

اكتشفت أثناء تنفيذ مهمة #4 إني قلت معلومة غلط بجلسات سابقة (لمهمتَي #1 نغمة الطلب و#3 باغ الtoast) — قلت "لازم إعادة بناء APK"، وهاد **مو صحيح**. الحقيقة:

كل الأربع تطبيقات (`mobile-customer`, `mobile-store`, `mobile-driver`, `mobile-admin`) معدّة بـ`capacitor.config.json` بحقل `server.url` يشاور مباشرة على GitHub Pages (`https://moayyad44.github.io/akalito/akleto-*.html`) بدل تحميل الملفات من داخل الـAPK نفسه (`webDir: "www"` موجود بس **متجاوَز فعلياً** بوجود `server.url`). GitHub Pages مفعّل فعلاً على الريبو (`main` branch، الجذر).

**يعني عملياً:**
- **تعديلات HTML/CSS/JS** (أي ملف `.html` أو `.js` بجذر الريبو، بما فيها `driver-shared.js`) → **تنعكس فوراً** بمجرد الـpush (GitHub Pages بيعيد البناء تلقائياً خلال ثواني لدقيقة) — **بدون أي حاجة لإعادة بناء أو تثبيت APK جديد**. مؤيد بس يسكّر ويفتح التطبيق من جديد (أو حتى بدون هيك، حسب استراتيجية الكاش بالـWebView).
- **تعديلات كود Android أصلي حقيقي** (زي مهمة #4: ملفات `.java`/`.kt`، `AndroidManifest.xml`، `build.gradle`، إضافة/تعديل Native Plugins) → **هاي فقط** اللي فعلاً تحتاج `npx cap sync android` + إعادة بناء وتثبيت APK جديد من Android Studio.

**درس للمستقبل:** أي مهمة قادمة، لازم أحدّد بوضوح من البداية "JS/HTML بس" ولا "فيها كود Android أصلي" عشان أعطي تعليمات نشر صحيحة من أول مرة.

---

## 15 أغسطس 2026 (تكملة) — مهمة #4 من الـbacklog: إشعار ثابت (Foreground Service) لحالة "متاح للطلبات"

**هاي أول مهمة بالbacklog الجديد اللي فيها كود Android أصلي حقيقي فعلاً محتاج إعادة بناء APK.**

### القرار التقني
إشعار محلي عادي (Local Notification) ما بيكفي لأنه المستخدم يقدر يمسحه بالسحب بأي وقت — والمطلوب إشعار **ثابت فعلياً** (Ongoing) ما يروح إلا لما الكابتن يحوّل حالته يدوياً. هذا يتطلب **Foreground Service** حقيقي بمستوى نظام أندرويد (نفس آلية تطبيقات التوصيل التانية زي أوبر/كريم وقت الكابتن أونلاين).

### الملفات الجديدة/المعدّلة (`mobile-driver/android/...`)
- **`AvailabilityService.java`** (جديد): Foreground Service حقيقي. إشعار `setOngoing(true)` بقناة إشعارات منفصلة هادئة (`driver_availability_status`, `IMPORTANCE_LOW`, بدون صوت/اهتزاز) — مفصولة تماماً عن قناة `order_alerts` الموجودة أصلاً (تنبيه الطلب الفعلي) لتفادي أي تشويش. `foregroundServiceType="location"` (بالمانيفست) — تصنيف صادق ومطابق فعلياً لسلوك التطبيق (تتبّع موقع لايف أثناء التوفر عبر `startLocationWatch`)، أهم لمراجعة Google Play لاحقاً. `START_NOT_STICKY` عمداً — الـJS هو المتحكم الصريح بدورة حياة الخدمة، مو نظام أندرويد تلقائياً.
- **`DriverAvailabilityPlugin.java`** (جديد): Capacitor Plugin مخصص (أول plugin مخصص بالمشروع) يجسّر JS مع الخدمة — بيظهر تلقائياً بـ`window.Capacitor.Plugins.DriverAvailability.{start,stop}()` بمجرد تسجيله.
- **`MainActivity.java`**: `registerPlugin(DriverAvailabilityPlugin.class)` قبل `super.onCreate()`.
- **`AndroidManifest.xml`**: صلاحيتين جديدتين (`FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_LOCATION`) + تسجيل `<service>` بنوعها.

### الجانب JS (`driver-shared.js` + `akleto-driver-home.html`)
- `startAvailabilityForegroundNotification()` / `stopAvailabilityForegroundNotification()`: بوابة آمنة — بتتجاهل بهدوء لو التطبيق مش جوا أندرويد (متصفح/PWA) أو لو نسخة APK قديمة قبل إضافة الـplugin (نفس نمط `initDriverPushNotifications` الموجود أصلاً).
- تُستدعى `start` عند تفعيل التوفر (داخل `toggleAvailability`)، و`stop` عند إلغاؤه، وكمان `stop` احتياطياً داخل `logoutDriver()`.
- لو الصفحة فُتحت وهي أصلاً بحالة "متاح" من جلسة سابقة (`localStorage`) — نعيد تشغيل الخدمة صراحة عند التحميل، لأنه Foreground Service ما بتكمّل تلقائياً عبر إعادة فتح التطبيق (فلسفة `START_NOT_STICKY` نفسها).

### التحقق قبل الرفع
- فحصت صياغة JS (`node --check`) لكل من `driver-shared.js` وكتلة السكربت بـ`akleto-driver-home.html` — سليمة.
- فحصت توازن الأقواس بكل ملفات الجافا الثلاثة (الجديدة + المعدّلة) — متوازنة بالكامل.
- ⚠️ **لم يتم Compile فعلي أو اختبار على جهاز حقيقي** — البيئة الحالية ما فيها وصول لـGoogle Maven/Gradle (محدودة بقائمة نطاقات مسموحة ما فيها `dl.google.com` أو `maven.google.com`)، فما قدرت أبني الـAPK فعلياً وأتحقق منه. **الكود مبني على أنماط Android/Capacitor قياسية وموثّقة رسمياً، بس التحقق النهائي لازم يصير عند مؤيد بالبناء الفعلي.**

### المطلوب من مؤيد (هاي المرة فعلاً لازم إعادة بناء)
1. `git pull`
2. بمجلد `mobile-driver`: `npx cap sync android` (يضمن أي تغييرات مانيفست/بلجن تنعكس بمشروع Android Studio)
3. افتح `mobile-driver/android` بـAndroid Studio وابني APK جديد (Build → Generate Signed Bundle/APK حسب سير عملك المعتاد)
4. ثبّت النسخة الجديدة وجرّب:
   - فعّل "متاح للطلبات" → تأكد يظهر إشعار ثابت بشريط الإشعارات، وجرّب تسحبه للتأكد ما بينمسح
   - اضغط على الإشعار → تأكد يفتح التطبيق عادي
   - حوّل لـ"غير متاح" من داخل التطبيق → تأكد الإشعار يختفي فوراً
   - سكّر التطبيق بالكامل (اسحبه من الـrecent apps) وهو "متاح" → افتحه من جديد وتأكد الإشعار يرجع يظهر (اختبار سيناريو "النظام قتل العملية بالخلفية")
   - سجّل خروج وأنت "متاح" → تأكد الإشعار يختفي (الإجراء الاحتياطي بـ`logoutDriver`)
5. ⚠️ لو ظهرت أي مشكلة صلاحيات أو crash عند أول تشغيل بعد التحديث (خصوصاً على أندرويد 12 وما فوق بخصوص Foreground Service types)، خبرني فوراً بنص الخطأ من Logcat لو قدرت توصله.


## 15 أغسطس 2026 (تكملة) — مهمة #5: تصميم نظام بدائل المكونات (نوع + حجم) — التصميم النهائي المُقفل ✅

بعد نقاش طويل ونماذج تفاعلية (4 نسخ) وصلنا لتصميم نهائي موافق عليه من مؤيد. **هاي وثيقة المرجع الرسمية قبل التنفيذ.**

### الفكرة النهائية
قائمة مكونات الوجبة تضل **موحّدة الشكل بالكامل** — نفس الصف، نفس الارتفاع، نفس الخط لكل المكونات (عادية وبدائل). الفرق الوحيد: المكوّن اللي إله بدائل، صفّه نفسه يصير **قابل للسحب أفقياً (Carousel/Slider)** بدل نص ثابت، بحيث:
- البديل المختار حالياً يظهر بمنتصف الصف (بنفس تنسيق الصفوف العادية بالضبط: نقطة + اسم + سعر)
- **طرف** الخيار السابق/التالي يبيّن شوي من حواف الصف (يمين ويسار) كتلميح بصري إنه فيه أكتر من خيار — بدون ما يكسر شكل القائمة الموحّد
- أيقونة صغيرة (⇄) جنب الاسم كتلميح إضافي إنه قابل للسحب
- نقاط صغيرة (dots) تحت الصف تبيّن عدد الخيارات والموقع الحالي
- **ضغطة مطوّلة (long-press, ~480ms)** على الصف تفتح شيت (bottom sheet) فيه كل الخيارات مرتبة بشكل واضح — لو المكوّن عنده أكتر من "نوع"، بتظهر تبويبات (مثلاً سنيورة/نبيل) فوق قائمة "الأحجام" المرتبطة بكل نوع

### نظامين مختلفين بنفس الواجهة (حسب قرار مؤيد بالنقاش)
1. **بديل "نوع"** (مثلاً لحمة برغر سنيورة/نبيل): **الأدمن يحدد الافتراضي يدوياً** وقت إضافة الوجبة. الزبون يشوف الافتراضي جاهز (badge "افتراضي ⭐")، وبقدر يبدّله لأي وقت.
2. **بديل "حجم"** لنفس المنتج (كاتشاب كبير/صغير): **التطبيق يحسب الحجم الأنسب تلقائياً** حسب الكمية المطلوبة بالوصفة × عدد الأشخاص (badge "✨ الأنسب"، بيتحدّث ديناميكياً مع تغيّر عدد الأشخاص). الزبون برضه بقدر يبدّله يدوياً لو حاب (مثلاً حاب كاتشاب كبير يفضل عنده حتى لو الطلب وجبة وحدة).
3. **الحالة المركّبة** (نوع × حجم مع بعض — مثلاً كاتشاب سنيورة/نبيل × كبير/صغير = 4 تركيبات): بتظهر بالكاروسيل كقائمة مسطّحة (الأربع تركيبات)، وبشيت الاختيار الكامل منظّمة بتبويبات نوع فوق قائمة حجم — الأدمن يحدد النوع الافتراضي، والتطبيق يحسب الحجم الأنسب جوا هداك النوع.

### قرار مؤيد الحاسم بخصوص "الأرخص تلقائياً"
تراجعنا عن فكرة "الأرخص تلقائياً" للنوع (كانت الفكرة الأولى) — **مرفوضة**. الأدمن هو يتحكم بالنوع الافتراضي بالكامل، مو خوارزمية سعر. الحساب التلقائي (الأنسب اقتصادياً) يبقى محصور بـ"الحجم" بس، مو "النوع".

### مخطط قاعدة البيانات المقترح (جدول `meal_ingredients`) — لسا ما نُفّذ
حقول جديدة اختيارية (backward-compatible 100% — الصفوف الحالية بدون هالحقول تضل تشتغل زي ما هي بالضبط، إجبارية بدون بدائل):
- `alt_group` (نص، اختياري): معرّف تشاركه كل الصفوف اللي هي بدائل لبعض بنفس الوجبة. لو فاضي = مكوّن إجباري عادي (السلوك الحالي).
- `alt_brand` (نص، اختياري): اسم "النوع" (مثلاً "سنيورة"، "نبيل") — يُستخدم لتجميع صفوف الحجم المختلفة تحت نفس النوع بشيت الاختيار.
- `is_default_brand` (boolean، اختياري): علامة على كل الصفوف اللي تنتمي للنوع المحدد كافتراضي من الأدمن.
- بونص معماري: لو الأدمن استخدم نفس `alt_group` بأكتر من وجبة، خوارزمية حساب "الأنسب اقتصادياً" (لو رجّعناها لاحقاً لأي حالة) بتجمع الكمية المطلوبة من الوجبات كلها مع بعض تلقائياً.

### الملفات النموذجية (Prototypes) — للمرجعية بس، مش كود إنتاج
تم بناء 4 نسخ تفاعلية تجريبية بالمحادثة (HTML مستقل، بدون Firebase) لتجربة التصميم قبل التنفيذ الفعلي:
- v1: أزرار (pills) مكدّسة تحت كل مكوّن — **مرفوضة** (معقّدة بصرياً)
- v2: بطاقات كاروسيل منفصلة عن قائمة المكونات — **مرفوضة** (شكل مختلف عن باقي القائمة)
- v3: بطاقات كاروسيل كبيرة بنمط "peek slide" — **مرفوضة** (لسا شكلها مختلف عن الصفوف العادية)
- **v4: الشكل النهائي المُقفل** — صف موحّد بنفس شكل باقي القائمة، قابل للسحب، مع طرف الخيار المجاور بادي من الحافة + ضغطة مطوّلة لشيت الاختيار الكامل

### الخطوة التالية (لسا ما بدأنا التنفيذ الفعلي)
نطاق العمل (3 ملفات مترابطة):
1. `akleto-admin-meals.html`: واجهة لتحديد `alt_group`/`alt_brand`/`is_default_brand` لكل صف مكوّن وقت تحرير الوجبة
2. `akleto-customer.html`: 
   - تعديل شاشة الوجبة (`mealSheet`) لعرض القائمة الموحّدة بالتصميم v4 (كاروسيل داخل الصف + شيت اختيار بالضغطة المطوّلة)
   - خوارزمية حساب "الحجم الأنسب" تلقائياً حسب الكمية المطلوبة × عدد الأشخاص (تحتاج نستخدم نظام `convertToBase`/`measure_type` الموجود أصلاً)
   - تحديث حساب السعر الإجمالي بالوجبة ليتفاعل مع اختيار البدائل
   - تحديث `getFinalCartIngredients()`/`findBestStoreForIngredients()` ليتعاملوا مع البديل المختار فعلياً (مو كل الخيارات بالمجموعة) عند حساب التوجيه للمتجر
3. لسا معلّق: تفاصيل "خطة مونة شهرية" (الجزء التاني من backlog task #5 — اختيار المتجر أول بعدها الوجبات المتوافقة معه) — هاد جزء منفصل لسا ما بلّشنا فيه، بعد ما نخلّص نظام البدائل هاد.

⚠️ **ملاحظة مهمة:** لسا ما لمسنا أي كود فعلي أو بيانات إنتاج لهاي المهمة — كل اللي صار لهلق تصميم ونماذج تجريبية بس، بانتظار البدء بالتنفيذ الحقيقي بالجلسة القادمة.

## 16 أغسطس 2026 — مهمة #5: التنفيذ الفعلي لنظام بدائل المكونات ✅ (تعديل تصميم مهم عن الوثيقة السابقة)

بدأنا التنفيذ الحقيقي (بعد ما كان التصميم بس مُقفل بالجلسة السابقة). **تصميم الكاروسيل الأصلي (v4: سحب أفقي بحواف بادية) اتبدّل لتصميم أبسط بطلب مؤيد المباشر بمنتصف الجلسة** — بدل الكتابة اليدوية لاسم "مجموعة البديل"، صار الأدمن يحدد البدائل بالـclick من قائمة اختيار (picker) مباشرة، وبتطبيق الزبون صار التنقّل بأسهم ‹ › + نقاط (dots) بدل السحب الفعلي باللمس (touch drag) — أبسط تنفيذاً وأكثر موثوقية عبر المتصفحات، بنفس الفكرة الجوهرية (صف موحّد الشكل، بدون كسر تصميم القائمة).

### 1) `akleto-admin-meals.html` — واجهة تحديد البدائل (تحديثين متتاليين)
- **أولاً:** أضيف زر ⇄ لكل صف مكوّن يفتح لوحة فيها 3 حقول نصية (مجموعة البديل، اسم النوع، افتراضي) — يتطلب من الأدمن **كتابة نفس اسم المجموعة يدوياً** بكل الصفوف المرتبطة.
- **بعدها (تعديل بطلب مؤيد):** استُبدلت آلية الكتابة اليدوية بـ**قائمة اختيار (picker)** — الضغط على ⇄ يفتح قائمة كل المكونات (مع بحث)، وبالـ✔ يُحدَّد أي مكوّن بديل. لو المكوّن المختار مش موجود أصلاً بالوجبة، **ينضاف تلقائياً** كصف جديد. معرّف المجموعة (`alt_group`) يُولَّد تلقائياً بالخلفية (`grp_<timestamp>_<idx>`) — الأدمن ما بيشوفه ولا بيتعامل معه مباشرة. حقل "اسم النوع" (`alt_brand`) ضل موجود كخيار إضافي اختياري جنب كل عنصر مُحدَّد بالقائمة، مع ⭐ لتحديد الافتراضي (تبادلي — تحديد واحد بيلغي باقي الافتراضيات بنفس المجموعة تلقائياً).
- **حماية عند الحفظ:** مجموعة البديل ما بتتسجّل بقاعدة البيانات (`alt_group`/`alt_brand`/`is_default_brand`) إلا لو فيها **عضوين فعليين ع الأقل** (`groupCounts[key] > 1`) — أي مجموعة "يتيمة" (بسبب إلغاء تحديد لاحق) بتُحفظ تلقائياً كمكوّن عادي مستقل بدون أي حقل زيادة. الحقول القديمة الموجودة بأي صف بدون بديل تضل زي ما هي (توافق كامل للخلف).
- **ملفات CSS:** أُضيفت أنماط `<style>` مضمّنة داخل نفس الملف (`.ing-alt-toggle`, `.ing-alt-panel`, `.ing-alt-picker`, `.ing-alt-pick-item`, إلخ) — بدون لمس `admin-style.css` المشترك.

### 2) `akleto-customer.html` — التفعيل الفعلي بتطبيق الزبون
- **`getMealIngredients(mealId)`**: صار يرجّع `altGroup`/`altBrand`/`isDefaultBrand` مع كل صف (قراءة مباشرة من `meal_ingredients`).
- **`buildMealDisplayRows(ings)`** (جديدة): تبني ترتيب صفوف العرض — صف واحد فقط لكل مجموعة بديل (بدل تكرار كل عضو كصف منفصل)، بالإضافة للمكونات المستقلة العادية بنفس الترتيب الأصلي.
- **خوارزمية "الحجم الأنسب"** (`computeBestSizeOption`): لكل خيارات مجموعة معيّنة (أو خيارات نفس "النوع" لو فيه أكتر من نوع)، تحسب `ceil(الكمية_المطلوبة_بوحدة_الأساس / حجم_العبوة) × سعر_العبوة` لكل خيار وتختار **الأرخص إجمالاً** — بتتحدّث ديناميكياً كل ما تغيّر عدد الأشخاص (badge "✨ الأنسب").
- **الافتراضي عند فتح الوجبة أول مرة** (`computeGroupDefault`): لو المجموعة فيها أكتر من "نوع" واحد، يُختار النوع اللي حدده الأدمن (`is_default_brand`)؛ بعدها يُحسب الحجم الأنسب **داخل هداك النوع تحديداً**. القرار الحاسم (الجلسة السابقة): **لا يوجد اختيار تلقائي "أرخص" على مستوى النوع** — بس الأدمن يتحكم فيه.
- **حالتين "يدوي" منفصلتين** بكل اختيار مخزّن (`brandManual`/`sizeManual`): طالما الزبون ما بدّل شي يدوياً، "الحجم" بيعاد حسابه تلقائياً مع كل تغيير بعدد الأشخاص؛ أول ما الزبون يبدّل يدوياً (بالأسهم أو من شيت الاختيار)، يضل اختياره ثابت بغض النظر عن تغيّر عدد الأشخاص بعدها (زي ما مؤيد أكّد بنقاش التصميم).
- **واجهة الصف الموحّد** (`altRowHtml`): نفس شكل وارتفاع باقي صفوف المكونات بالضبط + أيقونة ⇄ + badge النوع (لو موجود) + badge "✨ الأنسب" (لو ينطبق) + سهمين ‹ › للتنقّل السريع بين كل الخيارات (قائمة مسطّحة تشمل كل التركيبات نوع×حجم) + نقاط (dots) تحت الصف تبيّن الموقع الحالي.
- **شيت اختيار كامل** (`altOptionsSheet` + `openAltOptionsSheet`/`renderAltOptionsSheet`): يفتح بالضغط على صورة/اسم المكوّن بالصف — تبويبات فوق لكل "نوع" (لو أكتر من واحد، مع ⭐ على تبويب الافتراضي)، وتحتها قائمة "الأحجام" المرتبطة بالنوع المفعّل حالياً، مع "✨ الأنسب" على الخيار الأرخص وعلامة ✓ على المختار حالياً.
- **حفظ الاختيار بالسلة:** `selectedMeals` (كل عنصر بالسلة) صار فيه حقل جديد `altSelections` (بالذاكرة بس، زي باقي حالة السلة — ما في تخزين محلي أو Firestore لهاي المرحلة، مطابق لنمط `cartOverrides`/`selectedMeals` الموجود أصلاً). يُحمَّل عند إعادة فتح وجبة موجودة أصلاً بالسلة، ويُحفظ عند `confirmAddMealToCart`.
- **`getEffectiveMealIngredients(sel)`** (جديدة): بتاخذ اختيار الزبون الفعلي (مو كل الخيارات) وترجّع لائحة المكونات "الحقيقية" لوجبة معيّنة بالسلة. **`computeMergedIngredients()` صارت تستخدمها بدل `getMealIngredients()` مباشرة** — يعني حساب السعر الإجمالي، دمج الكميات بين كل وجبات السلة، وتوجيه الطلب للمتجر (`findBestStoreForIngredients`) كلهم صاروا يتفاعلوا تلقائياً مع البديل المختار فعلياً بس، مو كل الخيارات بالمجموعة.
- **فحص التوفر عند الإضافة للسلة** (`confirmAddMealToCart`): صار يتحقق من توفر البدائل **المختارة حالياً بس** (`getEffectiveMealIngredients` بدل `getMealIngredients` الكاملة) عند فحص المتجر المناسب.

### التحقق قبل الرفع
- فحصت صياغة الجافاسكربت (`node --check`) لكل سكربتَي `akleto-admin-meals.html` (module + plain) و`akleto-customer.html` (module + plain) — سليمة بالكامل بكل التعديلات.
- ما تم اختبار فعلي على جهاز حقيقي أو بيانات إنتاج حقيقية — التحقق التالي المطلوب من مؤيد.

### المطلوب من مؤيد للاختبار
1. `git pull` — **تعديلات JS/HTML بس، بدون أي حاجة لنشر Firebase أو إعادة بناء APK** (كل الملفين معروضين مباشرة عبر GitHub Pages حسب الآلية المكتشفة بجلسة سابقة).
2. بلوحة الأدمن (`akleto-admin-meals.html`): افتح وجبة فيها مكوّن بديل محتمل (مثلاً لحمة برغر)، دوس ⇄، وجرّب تحديد بديل (بنوع أو بس بحجم) وحفظ.
3. بتطبيق الزبون: افتح نفس الوجبة، شوف الصف الموحّد الجديد، جرّب الأسهم ‹ ›، جرّب تفتح شيت الاختيار الكامل (بالضغط عالصورة/الاسم)، وغيّر عدد الأشخاص وتأكد "✨ الأنسب" بتتحدّث صح.
4. أضف الوجبة للسلة وتأكد السعر الإجمالي وتوجيه المتجر بيعكسوا البديل المختار فعلياً (مو كل الخيارات).
5. ⚠️ **لسا معلّق (خارج نطاق هاي الجلسة):** التصميم البصري النهائي (تدرّج/انسيابية الأسهم والنقاط) ممكن يحتاج ضبط بعد المعاينة الفعلية على الموبايل — هاد أول تنفيذ عملي وما تمت مراجعته بصرياً بعد. كمان جزء "خطة مونة شهرية" (النصف التاني من backlog task #5) لسا ما بدأنا فيه.

## 16 أغسطس 2026 (تكملة) — مهمة #5: تعديلين على تفاعل الصف بطلب مؤيد المباشر بعد أول معاينة

1. **حذف أسهم ‹ › التنقّل نهائياً، واستبدالها بسحب حقيقي بالإصبع (أو الماوس على المتصفح):**
   - استُخدمت **Pointer Events** (`pointerdown`/`pointermove`/`pointerup`/`pointerleave`/`pointercancel`) بدل touch events الخام — بتوحّد سلوك اللمس والماوس بنفس الكود (مفيد للتجربة والاختبار عبر المتصفح كمان مو بس الموبايل).
   - أثناء السحب، الصف بيتحرك أفقياً بشكل حي (`translateX`) متزامن تماماً مع حركة الإصبع (`transform:none` transition وقت السحب نفسه، وبيرجع بانتقال ناعم `.2s ease` لما يفلت الإصبع) — هيك الزبون بيشوف "الحركة الأفقية" فعلياً متل ما طلب مؤيد، مو مجرد قفزة فجائية.
   - تجاوز الحد (threshold: 40px) بيبدّل للخيار التالي/السابق (سحب لليسار = التالي، لليمين = السابق) من القائمة المسطّحة لكل خيارات المجموعة (نوع×حجم مع بعض).
   - `touch-action: pan-y` على عنصر السحب — يسمح للسحب الأفقي المخصص يشتغل بينما التمرير العمودي العادي للشيت يضل شغال طبيعي بدون تعارض.

2. **إعادة تعريف سلوك "التاب" (لمسة بسيطة بدون سحب) على الصف:**
   - قبل: كان بيفتح شيت الاختيار الكامل مباشرة.
   - **بعد التعديل:** لمسة بسيطة (بدون سحب فعلي) بتفتح **صفحة المنتج** (`openProductSheet`) لنفس الخيار المختار حالياً — سلوك موحّد مع باقي المكونات العادية بالقائمة (كلها بتفتح صفحة المنتج بالتاب).
   - **شيت الاختيار الكامل** (تبويبات النوع + قائمة الأحجام) هلق بيُفتح فقط عبر **ضغطة مطوّلة (long-press, ~480ms)** — بيتحقق بمؤقّت (`setTimeout`) بيتلغي أوتوماتيكياً لو الزبون بلّش يسحب قبل ما يخلص الـ480ms.
   - **إضافة صور المنتج داخل شيت الاختيار الكامل نفسه** (`renderAltOptionsSheet`) — كل خيار (نوع/حجم) هلق فيه صورة مصغّرة (`ing-thumb-sm`) جنب اسمه، بنفس نمط باقي الشاشات، بدل ما كان نص بس بدون صور. الضغط على الصورة تحديداً داخل الشيت بيفتح صفحة المنتج (`event.stopPropagation()` عشان ما يحدد الخيار بالغلط بمجرد ما يشوف صورته).

### الملفات المتأثرة
`akleto-customer.html` فقط — تعديلات CSS (`.alt-slide-track` بدل `.alt-nav-btn`/`.alt-row-controls` المحذوفة) + استبدال دالة `cycleAltOption` بمجموعة دوال `altRowPointerDown`/`altRowPointerMove`/`altRowPointerUp`/`altRowPointerLeave` (حالة سحب واحدة مشتركة `altDragState` بالذاكرة، مو لكل صف على حدة — يكفي لأنه بس صف واحد ممكن يُسحب بنفس اللحظة).

### التحقق قبل الرفع
فحصت صياغة الجافاسكربت (`node --check`) لكل من السكربتين (plain + module) — سليمة.

### المطلوب من مؤيد
`git pull` وجرّب السحب الفعلي بالإصبع عالموبايل (مو بس بالماوس بالمتصفح — التجربة الحقيقية باللمس ممكن تحتاج ضبط بسيط بالـthreshold لو حسّيته حساس زيادة أو قليل).

## 16 أغسطس 2026 (تكملة) — مهمة #4: تحديث نص الإشعار الثابت بطلب مؤيد

⚠️ **توضيح مهم:** هاي المهمة (Foreground Service الثابت لحالة "متاح للطلبات") كانت **مبنية بالكامل فعلياً من جلسة سابقة (15 أغسطس)** — الكود (`AvailabilityService.java`, `DriverAvailabilityPlugin.java`, تعديلات `MainActivity.java`/`AndroidManifest.xml`/`driver-shared.js`/`akleto-driver-home.html`) موجود بالمستودع، بس **لسا ما تم بناء APK فعلي واختباره على جهاز حقيقي** (موثّق بالجزء أعلاه). اليوم بس عدّلنا نص الإشعار بطلب مؤيد.

**التعديل:** بـ`AvailabilityService.java` — نص الإشعار الثابت صار:
- العنوان: **"جاري العمل 🟢"** (بدل "أنت متاح لاستلام الطلبات 🟢")
- النص الفرعي: "أنت متاح لاستلام طلبات جديدة — افتح تطبيق أكليتو كابتن لتغيير حالتك"

فحصت توازن الأقواس بالملف بعد التعديل — سليم. رفعته على GitHub.

### المطلوب من مؤيد (نفس خطوات البناء الموثّقة سابقاً، ما تغيّرت)
1. `git pull`
2. بمجلد `mobile-driver`: `npx cap sync android`
3. افتح `mobile-driver/android` بـAndroid Studio وابني APK جديد
4. ثبّت وفعّل "متاح للطلبات" وتأكد الإشعار الثابت يظهر بالنص الجديد "جاري العمل 🟢" وما بينمسح بالسحب.
5. باقي سيناريوهات الاختبار موثّقة أعلاه (تسجيل خروج، إغلاق التطبيق بالكامل، تحويل لـ"غير متاح"...).

## 16 أغسطس 2026 (تكملة) — مهمة #4: تصحيح النص + إصلاح باغ حقيقي "الإشعار بيروح عادي"

مؤيد بنى APK فعلياً وجرّبه — النص اتغيّر بطلبه، بس **الإشعار طلع قابل للسحب/الإخفاء بالعادي، مش ثابت فعلياً** زي المطلوب.

### النص (بسيط)
- العنوان: **"اكليتو"**
- النص الفرعي: **"جاي العمل..."**

### الباغ — السبب المرجّح والإصلاح
`build.gradle` بيّن `targetSdkVersion = 36` (أندرويد 15). بدءاً من أندرويد 14 (API 34)، نظام أندرويد **يرفض** تشغيل Foreground Service بنوع `location` إلا لو إذن الموقع (`ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION`) **ممنوح فعلياً على مستوى النظام** وقت الاستدعاء بالضبط.

المشكلة: التطبيق بيطلب الموقع عبر `navigator.geolocation.watchPosition` (JS عادي جوا الـWebView) — مش عبر Capacitor plugin أصلي بخطوة `requestPermissions()` صريحة ومضمونة التوقيت. فيه احتمال حقيقي إنه وقت أول تفعيل لـ"متاح"، إذن الموقع الأصلي (نظام أندرويد) لسا ما انضمن بعد لحظة استدعاء `startForeground` بنوع `location` — فالاستدعاء يفشل (استثناء أو رفض صامت)، ويضل الإشعار "يتيم" غير مرتبط فعلياً بخدمة أمامية شغّالة → قابل للسحب زي أي إشعار عادي.

**الإصلاح (`AvailabilityService.java` + `AndroidManifest.xml`):**
- أُضيف نوع `dataSync` كـ**fallback** بالمانيفست (`foregroundServiceType="location|dataSync"`) + صلاحية `FOREGROUND_SERVICE_DATA_SYNC` الجديدة.
- بالكود: قبل استدعاء `startForeground`، نفحص فعلياً (`ContextCompat.checkSelfPermission`) هل إذن الموقع ممنوح حالياً:
  - **ممنوح** → نستخدم `FOREGROUND_SERVICE_TYPE_LOCATION` الصادق (زي ما كان، لمراجعة Google Play مستقبلاً).
  - **مش ممنوح بعد** → نستخدم `FOREGROUND_SERVICE_TYPE_DATA_SYNC` بدل — بيضمن الإشعار الثابت (`setOngoing`) يشتغل بغض النظر عن صلاحية الموقع، بدون الاصطدام بقيد أندرويد 14+.
- طبقة حماية إضافية: `try/catch` مزدوجة — لو صار أي استثناء غير متوقع (حتى لو الفحص أعلاه قال إنه ممنوح بس فشل لسبب تاني)، نحاول `dataSync`، وإذا فشلت هي كمان، نرجع لـ`startForeground()` العادي بدون نوع محدد كملاذ أخير — الهدف ضمان الإشعار **دايماً** يصير ongoing فعلياً، ما يفشل بصمت أبداً.

### التحقق قبل الرفع
- فحصت توازن الأقواس بـ`AvailabilityService.java` — سليم.
- فحصت صحة XML بـ`AndroidManifest.xml` — سليم.
- ⚠️ **لسا ما تم اختبار فعلي بعد هالإصلاح** — نفس القيد المعروف (بيئة العمل ما فيها وصول لـGoogle Maven للـcompile الفعلي). الإصلاح مبني على قراءة دقيقة لمتطلبات Android 14+ الموثّقة رسمياً وسبب منطقي متوافق تماماً مع الأعراض الموصوفة، بس التأكيد النهائي لازم يصير بجهاز مؤيد.

### المطلوب من مؤيد
1. `git pull`
2. بمجلد `mobile-driver`: `npx cap sync android`
3. ابني APK جديد بـAndroid Studio
4. جرّب: فعّل "متاح للطلبات" **من أول مرة على تثبيت جديد** (سيناريو الإذن غير الممنوح بعد) وتأكد الإشعار "اكليتو / جاي العمل..." يظهر **وما بينمسح بالسحب** هالمرة.
5. ⚠️ لو ضلت المشكلة، محتاج فعلياً **نص الخطأ من Logcat** (فلتر `AvailabilityService` أو `ForegroundService`) وقت ما تفعّل "متاح" — بدون اللوج صعب أأكد السبب 100% وبكون عم أخمّن بدل ما أشخّص بدقة.

## 16 أغسطس 2026 (تكملة) — مهمة #4: إعادة هندسة جوهرية — تتبّع موقع أصلي حقيقي (native)

### التشخيص النهائي (بعد سلسلة اختبارات مع مؤيد)
عزلنا المشكلة خطوة بخطوة (Logcat + اختبارات معزولة بالخلفية/مسح الإشعار/إغلاق التطبيق):
1. أول تشخيص (خطأ): ظننا المشكلة صلاحية موقع مفقودة وقت `startForeground` — اللوج أثبت الخدمة اشتغلت صح (`Background started FGS: Allowed`)، فهاد مش السبب.
2. ثاني تشخيص (صحيح جزئياً، سلوك أندرويد مو باغ): من أندرويد 13+، المستخدم يقدر يمسح إشعار أي Foreground Service بالسحب افتراضياً بغض النظر عن `setOngoing(true)` — تغيير سياسة متعمّد من جوجل، الخدمة نفسها بتضل شغالة رغم مسح الإشعار.
3. **السبب الجذري الحقيقي:** بالاختبار تبيّن إنه تحديث `location_updated_at` بـFirestore **بيتوقف تماماً** بمجرد ما التطبيق يروح للخلفية (Home button)، **حتى بدون ما يتلمس الإشعار إطلاقاً**. السبب: تتبّع الموقع كان بالكامل عبر `navigator.geolocation.watchPosition` — جافاسكربت عادي جوا صفحة الويب (`akleto-driver-home.html`) — وأندرويد/الـWebView بيجمّد تنفيذ الجافاسكربت تلقائياً لما التطبيق بالخلفية، بغض النظر عن أي Foreground Service شغّال بالتوازي. الإشعار الثابت كان بس بيخلي **عملية** التطبيق حية بذاكرة أندرويد، بس ما كان عنده أي علاقة فعلية بتشغيل الجافاسكربت نفسه.

### القرار (بعد سؤال مؤيد مباشرة)
تأكيد صريح: **لازم** يضل موقع السائق وحالة توفره "حي" حتى لو الموبايل بجيبه والشاشة مطفية (زي كريم/أوبر بالظبط) — مو بس طول ما التطبيق مفتوح قدامه.

### الحل: نقل تتبّع الموقع من جافاسكربت لكود أندرويد أصلي (Native) بالكامل
`AvailabilityService.java` هلق بتسوي هيك حصراً بنفسها، بدون أي اعتماد على الجافاسكربت:

1. **تطلب تحديثات الموقع مباشرة من `LocationManager`** (GPS + Network providers)، كل ~12 ثانية — نفس الفاصلة القديمة بالجافاسكربت، بس هلق شغّالة فعلياً بالخلفية.
2. **تكتب كل تحديث موقع مباشرة لـFirestore عبر REST API** (طلب HTTPS PATCH خام بـ`HttpsURLConnection` + `org.json`، بدون أي مكتبة Firebase إضافية — كلاهما جزء من Android SDK الأساسي، صفر تبعيات Gradle جديدة).
3. **التوثيق (Auth):** قواعد أمان Firestore لمجموعة `drivers` بتتطلب `request.auth.uid` يطابق `auth_uid` السائق. الحل: JS بتولّد Firebase ID Token (JWT) من نفس جلسة Anonymous Auth الموجودة أصلاً (`auth.currentUser.getIdToken()`) وبتمرره للخدمة الأصلية عند التفعيل (`plugin.start({driverId, idToken})`)، والخدمة بتستخدمه كـ`Authorization: Bearer <token>` بكل طلب REST — هيك نفس صلاحيات JS بالضبط، بدون خرق أي قاعدة أمان.
4. **تجديد التوكن دورياً:** التوكن صلاحيته ساعة وحدة بس. JS بتجدده كل 45 دقيقة طول ما التطبيق بالمقدمة (`refreshToken` — دالة plugin جديدة، بتحدّث التوكن بالخدمة الشغّالة أصلاً بدون ما تلمس الإشعار أو تعيد تسجيل الموقع). ⚠️ **قيد معروف:** لو التطبيق ضل بالخلفية أكتر من ساعة كاملة بدون ما يرجع يُفتح ولا مرة، تحديثات الموقع الأصلية بتتوقف بصمت (التوكن منتهي) لحد ما يرجع يفتح التطبيق ويتجدد. للاستخدام الطبيعي (سائق بيفتح التطبيق بشكل متقطع، أو بيوصله طلب فيرجع يفتح تلقائياً) هاد مقبول لمرحلة الـMVP.
5. **بدون أي صلاحية أو تبعية جديدة:** بما إنها Foreground Service حقيقية بنوع `location` مُعلَن أصلاً، مش محتاجة `ACCESS_BACKGROUND_LOCATION` (القيد الحساس هاد خاص بطلب الموقع من تطبيق بالخلفية بدون Foreground Service — مو حالتنا). `ACCESS_FINE_LOCATION`/`ACCESS_COARSE_LOCATION`/`INTERNET` كلها موجودة أصلاً بالمانيفست من قبل. صفر مراجعة إضافية متوقعة من Google Play.

### ملفات اتعدّلت
- **`AvailabilityService.java`**: أضيف `LocationManager`/`LocationListener` + `ExecutorService` (Thread منفصل لكتابة الشبكة، ممنوع I/O عالـMain Thread) + دالة `pushLocationToFirestore()` (REST PATCH خام). أُضيف Action جديد `ACTION_REFRESH_TOKEN` + Extras `EXTRA_DRIVER_ID`/`EXTRA_ID_TOKEN`. `onDestroy()` هلق بتوقف تحديثات الموقع (`removeUpdates`) وتقفل الـExecutor.
- **`DriverAvailabilityPlugin.java`**: `start()` هلق بتمرر `driverId`/`idToken` من JS كـIntent extras. دالة جديدة `refreshToken()`.
- **`driver-shared.js`**: `startAvailabilityForegroundNotification()` صارت `async` وبتاخذ `driverId`، بتجيب `idToken` من `auth.currentUser.getIdToken()` وتمررهم للـplugin، وبتشغّل مؤقّت تجديد كل 45 دقيقة. `stopAvailabilityForegroundNotification()` بتلغي المؤقّت.
- **`akleto-driver-home.html`**: تحديث موقعي الاستدعاء (`toggleAvailability` + إعادة التفعيل عند فتح التطبيق) ليمرروا `session.id` وينتظروا (`await`) الدالة الجديدة. **`startLocationWatch()` (الجافاسكربت القديم) ضل زي ما هو بدون حذف** — بيستمر يخدم شاشة الخريطة/حساب مسافة الشيفت طول ما التطبيق بالمقدمة، والمسارين (JS + Native) ما بيتعارضوا (Firestore: آخر كتابة بتكسب، مفيد كطبقة تكرار إضافية مش مشكلة).

### التحقق قبل الرفع
فحصت توازن الأقواس بملفات الجافا (سليم)، وصياغة الجافاسكربت (`node --check`) لكل من `driver-shared.js` وسكربت `akleto-driver-home.html` — سليمة بالكامل.

⚠️ **لسا ما تم اختبار فعلي** — نفس القيد المعتاد (بيئتي ما فيها بناء APK حقيقي). هاد تغيير جوهري بمنطق التطبيق (أول كود Android أصلي يسوي كتابة شبكة مباشرة لـFirestore بمشروع أكليتو) فمهم نختبره بعناية.

### المطلوب من مؤيد للاختبار
1. `git pull`
2. بمجلد `mobile-driver`: `npx cap sync android`
3. ابني APK جديد بـAndroid Studio
4. فعّل "متاح للطلبات"، بعدين اضغط Home (بدون قفل من Recent Apps) واترك الشاشة تطفى — راقب `location_updated_at` بـFirestore Console لمدة دقيقة أو دقيقتين، لازم يستمر يتحدّث كل ~12 ثانية.
5. جرّب كمان بعد ما تمسح الإشعار بالسحب — نفس النتيجة متوقعة (التتبع يضل شغال، الإشعار بس اختفى بصرياً).
6. لو صار خطأ (خصوصاً حوالين `HttpsURLConnection`/`JSONException`/صلاحيات)، ابعتلي Logcat بفلتر `AvailabilityService`.

## 16 أغسطس 2026 (تكملة) — مهمة #5 (النصف التاني): خطة مونة شهرية — تنفيذ فعلي

### توضيح المفهوم (بعد نقاش)
تبيّن إنه "خطة مونة شهرية" مش نفس فكرة "اختيار المتجر بدل التوجيه التلقائي" يلي كانت موثّقة بالـbacklog الأصلي — مفهوم مختلف طلبه مؤيد مباشرة بالنقاش:

> "الزبون يختار مجموعة وجبات والتطبيق يحدد المكونات المناسبة عشان يشتريها، بس بالبداية يحدد المتجر أول بعدها الوجبات اللي متوفرة عنده المكونات."

قرارات التصميم المؤكدة من مؤيد:
1. **مدخل الميزة:** بانر إعلاني الشكل ("خطط لمونتك الشهرية 📅") فوق قائمة الوجبات بالشاشة الرئيسية — مو شاشة إلزامية، الزبون بيدخلها بس لو بدو.
2. **صرامة الفلترة:** الوجبة لازم كل مكوناتها تكون متوفرة عند المتجر المختار (بما فيها البدائل — إذا في مجموعة بدائل، يكفي خيار واحد منها يكون متوفر). أي وجبة ناقصها شي **تختفي بالكامل** من القائمة، ما تظهر معتمة.
3. **تغيير المتجر:** لو السلة فاضية، الزبون يقدر يغيّر المتجر بأي لحظة بدون أي تحذير.

### التنفيذ (`akleto-customer.html` فقط)
اكتشفت إنه فيه أصلاً بنية تحتية جاهزة تقريباً 100% من ميزة تانية موجودة (`productsScreen` — تصفّح منتجات فردية من متجر مختار)، فأعدت استخدامها بدل ما أبني من الصفر:
- `loadActiveStoresForPicker()`, `loadStoreProductsMap()`, `storePickMeta()` — استُخدمت زي ما هي بدون أي تعديل.
- نمط بطاقة اختيار المتجر (`.store-pick-card`) — نفس الـCSS الموجود، بدون تكرار.

**جديد اتضاف:**
- بانر `#monthlyPlanBanner` فوق قائمة الوجبات مباشرة — نصه بيتغيّر ديناميكياً: لو ما في متجر مختار بيقول "خطط لمونتك الشهرية"، ولو في متجر مختار بيقول "بتخطط من متجر [الاسم] — دوس لتغيير المتجر" (`updateMonthlyPlanBanner()`).
- شيت جديد `#planStoreSheet` (منفصل تماماً عن `productsScreen` — تصميم متعمّد، عشان الميزتين يضلوا مستقلين عن بعض بالحالة الداخلية ومايتشابكوش).
- `openMonthlyPlanFlow()`: لو السلة فيها شي مرتبط بمتجر (من أي مصدر — تخطيط شهري قديم، أو حتى التوجيه التلقائي العادي)، بيسأل تأكيد قبل ما يفضّيها؛ غير هيك بيفتح شيت اختيار المتجر مباشرة.
- `choosePlanStore()`: بيحمّل خريطة منتجات المتجر المختار (`loadStoreProductsMap`) ويخزّنها بـ`lockedStorePriceMap` + `cartLockedStore` — **نفس المتغيرين المستخدمين أصلاً بكل منطق السلة/الدفع/التوجيه** (`confirmAddMealToCart`, `computeMergedIngredients`, الخ) — يعني صفر تغيير لازم على منطق الدفع/الطلب، كله بيشتغل تلقائياً زي ما هو.
- `isMealCompatibleWithStore(mealId, storeMap)`: تستخدم نفس `getMealIngredients()`/`buildMealDisplayRows()` الموجودين من نظام البدائل (مهمة #5 الأساسية) — بتتأكد كل مكوّن مستقل موجود بالـstoreMap، وكل مجموعة بدائل فيها خيار واحد ع الأقل موجود.
- `getFilteredMeals()` صارت تضيف شرط `matchesStore` — بيتفعّل تلقائياً بمجرد ما `cartLockedStore` يصير مضبوط (بغض النظر شو مصدره: خطة شهرية، أو التوجيه التلقائي العادي القديم بعد إضافة أول وجبة) — سلوك موحّد ومنطقي بكلا الحالتين.
- `updateCartBadge()` (الدالة المركزية المستدعاة بعد أي تغيير بالسلة) صارت تستدعي `updateMonthlyPlanBanner()` بالنهاية — فالبانر بيتحدّث تلقائياً لما السلة تفضى (`cartLockedStore` بيرجع `null` أصلاً بنفس الدالة).

### التحقق قبل الرفع
فحصت صياغة الجافاسكربت (`node --check`) لكل من السكربتين (plain + module)، وتأكدت من عدم تكرار أي `id` جديد بالـHTML — سليم بالكامل.

### المطلوب من مؤيد للاختبار
1. `git pull` — تعديل HTML/JS بس، بينعكس مباشرة عبر GitHub Pages، بدون أي بناء APK.
2. افتح تطبيق الزبون، شوف البانر الجديد فوق قائمة الوجبات.
3. دوس عليه، اختار متجر، وتأكد إنه قائمة الوجبات فلترت فعلياً (وجبات ناقصها مكونات عند هداك المتجر لازم تختفي).
4. جرب تضيف وجبة للسلة وتأكد التوجيه للمتجر عند الدفع بيطابق المتجر يلي اخترته بالخطة.
5. فضّي السلة وتأكد البانر يرجع لنصه الافتراضي، وتقدر تختار متجر تاني بحرية.

## 16 أغسطس 2026 (تكملة) — إصلاح عاجل: باغ كسّر تطبيق الزبون بالكامل بعد جلسة خطة المونة الشهرية

مؤيد بلّغ إنه تطبيق الزبون طلع "مسجّل خروج لحاله وولا زر شغال" بعد تعديلات خطة المونة الشهرية.

### السبب (خطأي بالتعديل السابق)
لما ضفت الدوال الجديدة (`openMonthlyPlanFlow`, `choosePlanStore`, `updateMonthlyPlanBanner`, إلخ) عبر `str_replace`، استخدمت `old_str="function updateCartFab() {"` بنية إدخال الكود الجديد **قبل** هاد السطر — بس نسيت أرجّع نفس السطر بآخر النص الجديد. النتيجة: سطر `function updateCartFab() {` **انحذف كلياً**، فصار *جسم* الدالة (كل الأسطر يلي كانت جواها) كود شغّال مباشرة على المستوى الأعلى للملف (Top-level) بدل ما يكون جوا دالة — و**اسم الدالة `updateCartFab` نفسه صار غير معرّف إطلاقاً**.

بما إنه `updateCartFab()` بينستدعى من جوا `updateCartBadge()` (يلي بتنستدعى تقريباً بكل تفاعل: فتح التطبيق، تسجيل دخول، إضافة/حذف من السلة، فتح الشاشات)، أي استدعاء لها كان يرمي `ReferenceError: updateCartFab is not defined` ويوقف تنفيذ الكود بالكامل بتلك اللحظة — وهيك التطبيق كان يعلق بحالة غير مكتملة (تبدو متل "تسجيل خروج") وكل الأزرار يلي عمليتها بتعتمد على كود ما وصل يتنفّذ بعد كانت ميتة.

⚠️ **درس مهم:** `node --check` (فحص الصياغة) **ما بيكشف هاد النوع من الأخطاء** — بما إنه الأقواس ضلت متوازنة رياضياً (بس التوزيع/الـnesting غلط دلالياً)، الفحص عدّاها كصياغة سليمة 100%. لازم أعتمد بعد هيك فحص إضافي (مقارنة كل الدوال المُستدعاة مع الدوال المُعرّفة فعلياً) بعد أي تعديل `str_replace` يمسح/يستبدل سطر إعلان دالة، مو بس `node --check`.

### الإصلاح
رجّعت سطر `function updateCartFab() {` بمكانه الصحيح. فحصت بعدها بأداة تحليل ثابت مخصّصة (مقارنة كل استدعاءات الدوال بكل التعريفات المتاحة فعلياً بالملف) للتأكد إنه ما في أي دالة تانية بنفس المشكلة — طلعت نظيفة 100%، هاي كانت المشكلة الوحيدة. فحصت الصياغة (`node --check`) كمان — سليمة.

### المطلوب من مؤيد
`git pull` وجرّب التطبيق من جديد — المفروض يرجع يشتغل عادي (تسجيل الدخول، كل الأزرار، خطة المونة الشهرية). اعتذر عن هالخطأ — التعديل القادم رح أتحقق منه بأداة تحليل إضافية قبل ما أرفعه، مو بس فحص الصياغة.

## 16 أغسطس 2026 (تكملة) — إصلاح: حقل "اسم الماركة" بلوحة الأدمن كان يفقد التركيز أثناء الكتابة

مؤيد بلّغ مشكلة بحقل "اسم الماركة" داخل قائمة اختيار البدائل (`akleto-admin-meals.html`).

### السبب
حقل النص كان مربوط بـ`oninput="updateAltMemberField(...)"`، والدالة كانت تختم دايماً بـ`renderIngredientRows()` — يعني كل ضغطة حرف وحدة كانت تعيد رسم **كل** صفوف المكونات وقائمة الاختيار من الصفر، فحقل الإدخال يفقد التركيز (focus) فوراً بعد كل حرف — المستخدم ما يقدر يكتب أكتر من حرف وحد بمرة وحدة بدون ما يرجع يدوس بالحقل من جديد. نفس نمط الباغ يلي انصلح قبل هيك بمربع بحث قائمة البدائل (`filterAltPicker`) — بس فاتني تطبيق نفس الإصلاح هون وقتها.

### الإصلاح
فصلت سلوك `updateAltMemberField`:
- **`isDefaultBrand`** (زر ⭐): ضلت `renderIngredientRows()` كاملة — ضرورية هون لأنها لازم تنعكس بعدة عناصر بالقائمة مع بعض (إلغاء الافتراضي عن باقي الخيارات).
- **`altBrand`** (الحقل النصي): هلق بس بتحدّث البيانات بالذاكرة (`mealIngredientRows[...]`) بدون أي إعادة رسم — مش محتاجة أصلاً لأنه قيمة الحقل يلي المستخدم شافها فعلاً مطابقة للداتا، مافي داعي نعيد رسمها.

### التحقق قبل الرفع
فحصت الصياغة (`node --check`) + سويت فحص إضافي (تحليل ثابت يقارن كل استدعاءات الدوال بكل التعريفات الفعلية بالملف) بعد الدرس يلي تعلمته من باغ `updateCartFab` قبل شوي — نظيف 100%، ما في أي دالة تانية بنفس المشكلة.

### المطلوب من مؤيد
`git pull` وجرّب تكتب اسم ماركة بقائمة اختيار البدائل — المفروض تقدر تكتب بشكل متواصل عادي.

## 16 أغسطس 2026 (تكملة) — إصلاح: بادج اسم الماركة بيظهر بمكان غلط أول ما تفتح الوجبة

مؤيد بلّغ: اسم الماركة (يلي بيكتبها بالأدمن لمكوّن بديل واحد بالمجموعة) بيظهر بتطبيق الزبون **تحت اسم منتج مختلف** عن المنتج الحقيقي المعروض — وبعد أول سحب أفقي (تبديل يدوي) بيتصحح ويصير بمكانه الصح.

### السبب
بدالة `computeGroupDefault` (الاختيار الافتراضي أول ما تفتح الوجبة): لو مجموعة البدائل فيها خيار واحد بس عندو اسم ماركة (`altBrand`) والباقي فاضي، الكود كان يحسب `brand` بس بدون ما يفلتر `candidates` فعلياً (فرع `brands.length === 1` كان ناقص فلترة، بعكس فرع `brands.length > 1`). النتيجة: خوارزمية "الحجم الأنسب" كانت تختار الأرخص من **كل** الخيارات (حتى يلي مالهاش ماركة)، بينما بادج الماركة يضل يعرض اسم الماركة يلي انحسب لحاله بدون علاقة فعلية بالمنتج المُختار — خلط واضح.

بعد أول سحب يدوي (`altRowPointerUp`)، الكود كان يبني اختيار جديد بالكامل من الخيار المسحوب فعلياً (`brand: next.altBrand`) — فبيتصحح تلقائياً من هونيك، وهاد بالضبط اللي وصفه مؤيد ("بعد التحريك الأفقي لأول مرة تعدل").

### الإصلاح
غيّرت المنطق ليكون **دايماً** مشتق البادج من الخيار المُختار فعلياً (`best.altBrand`) بدل قيمة منفصلة محسوبة مسبقاً ممكن تنحرف عنه — بمكانين: `computeGroupDefault` (أول فتح)، و`ensureAltSelections` (إعادة الحساب عند تغيير عدد الأشخاص). هيك مستحيل يصير خلط بعد اليوم بغض النظر عن شكل بيانات المجموعة (ماركة وحدة، ماركات متعددة، أو ماركة على خيار واحد بس).

### التحقق قبل الرفع
فحصت الصياغة (`node --check`) + التحليل الثابت لمطابقة كل استدعاءات الدوال بتعريفاتها (نفس الإجراء الإضافي من الدرس السابق) — نظيف 100%.

### المطلوب من مؤيد
`git pull` وجرّب تفتح وجبة فيها مجموعة بدائل عندها ماركة على خيار واحد بس — لازم اسم الماركة يظهر صح من أول فتحة، بدون الحاجة تسحب يدوياً.

## 16 أغسطس 2026 (تكملة) — تبسيط شاشة إدخال مكونات الوجبة (طلب مؤيد: "معجّقة")

مؤيد لاحظ إنه شاشة تحرير مكونات الوجبة (`akleto-admin-meals.html`) صارت مزدحمة بصرياً بعد إضافة نظام البدائل — السبب: كل عضو بمجموعة بدائل (مثلاً "لحمة سنيورة" و"لحمة نبيل") كان يظهر كصف كامل مستقل مطابق الشكل (اسم/كمية/وحدة/⇄/✕)، فوجبة فيها كم مجموعة بدائل بتطلع بعشرات الصفوف المكررة بصرياً.

### التصميم الجديد (بالاتفاق مع مؤيد)
أعضاء أي مجموعة بدائل صاروا يظهروا **بصف مختصر واحد** بدل التكرار:
```
لحمة برغر   [سنيورة ⭐] [نبيل]   ⇄   ✕
```
- الاسم الرئيسي = اسم أول عضو بالمجموعة.
- بادجات صغيرة تلخّص كل الأنواع/الماركات الموجودة، مع ⭐ على الافتراضي.
- ⇄ بيفتح نفس قائمة الاختيار الموجودة أصلاً (بدون تغيير بآلية الاختيار نفسها).
- ✕ هلق بيحذف **المجموعة كاملة** (كل الأعضاء مع بعض) بعد تأكيد — بدل حذف عضو واحد بس.

### تعديل إضافي ضروري
بما إنه الأعضاء التانية (غير الرئيسي) صارت مالها صف مستقل، ما عاد فيه مكان تعدّل فيه كميتها/وحدتها. فأضفت **حقلي كمية ووحدة مباشرة داخل كل عنصر مُحدَّد بقائمة الاختيار المنسدلة (⇄)** — نفس مكان اسم الماركة وزر ⭐ بالضبط، فالأدمن يقدر يظبط كل عضو بالكامل (الكمية/الوحدة/الماركة/الافتراضي) من داخل القائمة نفسها بدون ما يحتاج صف مستقل.

### ملفات اتعدّلت
`akleto-admin-meals.html` فقط:
- `buildAdminDisplayRows()` (جديدة): تجمّع `mealIngredientRows` لصفوف عرض — واحد لكل مكوّن مستقل، وواحد بس لكل مجموعة بدائل (بغض النظر عن عدد أعضائها، حتى لو "مجموعة من عضو واحد" بسبب حذف جزئي سابق — بتضل تُعامل كمجموعة، تفادياً لثغرة عرض).
- `singleIngredientRowHtml(idx)` / `altGroupRowHtml(idx)`: فصل منطق رسم الصف العادي عن صف المجموعة المختصر (بدل كتلة واحدة ضخمة كانت داخل `renderIngredientRows`).
- `removeAltGroup(groupKey)` (جديدة): حذف كل أعضاء مجموعة بديل بضغطة وحدة مع تأكيد.
- `altPickerItemHtml`: أضيف حقلي كمية (`ing-alt-qty-input`) ووحدة (`ing-alt-unit-select`) لكل عنصر محدد — نفس نمط وحدات القياس الديناميكية المستخدم بالصف العادي.
- `updateAltMemberField`: أضيف تعامل خاص بحقل `recipeQty` (تحويل لرقم عشري `parseFloat`) بنفس منطق `updateIngredientRow` الأصلي.
- CSS جديد: `.ing-group-row`, `.ing-group-name`, `.ing-group-badges`, `.ing-group-badge` (+ `.default`), `.ing-alt-qty-input`, `.ing-alt-unit-select`.

### التحقق قبل الرفع
فحصت الصياغة (`node --check`) + التحليل الثابت لمطابقة كل استدعاءات الدوال بتعريفاتها الفعلية (نفس الإجراء الإضافي المعتمد بعد باغ `updateCartFab`) + توازن وسوم `<div>` بالملف كامل — كله نظيف 100%.

### المطلوب من مؤيد
`git pull` وافتح أي وجبة فيها مجموعة بدائل — لازم تشوف صف مختصر واحد بدل التكرار، وتقدر تعدّل كمية/وحدة كل عضو من جوا قائمة الاختيار (⇄) مباشرة.

## 16 أغسطس 2026 (تكملة) — إصلاح: زر ⇄ ميت على المكونات اللي بعدها ما إلها بديل

مؤيد بلّغ إنه زر ⇄ خرب على المكونات العادية (اللي لسا ما ضيفلها بديل).

### السبب
وقت ما فصلت `renderIngredientRows()` لدالتين (`singleIngredientRowHtml` و`altGroupRowHtml`) بجلسة تبسيط الشاشة قبل شوي، نسيت أنقل معاها كود رسم لوحة اختيار البدائل (`altPanel` — البحث + القائمة) لدالة `singleIngredientRowHtml` الجديدة. النتيجة: الزر كان يبدّل `row._altOpen` صح، بس ما في أي HTML بيتحقق فعلياً من هاي القيمة عشان يعرض اللوحة — فالضغط عليه ما كان يظهر أي شي، يعني عملياً "ميت".

### الإصلاح
أضفت نفس منطق رسم `altPanel` (اللي موجود بـ`altGroupRowHtml`) لدالة `singleIngredientRowHtml` كمان، بالإضافة لحالة "active" البصرية على الزر نفسه لما تكون اللوحة مفتوحة.

### التحقق قبل الرفع
فحصت الصياغة + التحليل الثابت لمطابقة الاستدعاءات + توازن وسوم `<div>` — نظيف بالكامل.

### درس مُتكرر
هاي تاني مرة بنفس الجلسات بيصير فيها باغ بسبب فصل/إعادة هيكلة دالة كبيرة لدوال أصغر وننسى ننقل جزء من المنطق كامل. لازم أراجع أي دالة جديدة أفصلها عن دالة قديمة كبيرة — قارن كل جزء بالنسخة الأصلية قبل الحذف، مو بس أثق إني نقلت كل شي.

### المطلوب من مؤيد
`git pull` وجرّب ⇄ على مكوّن عادي (بدون بديل) — لازم تفتح لوحة اختيار البدائل عادي زي المكونات اللي أصلاً فيها بديل.

## 20 أغسطس 2026 (تكملة) — 🆕 إشعارات الدفع (Push Notifications) لتطبيق الإدارة (mobile-admin)

بطلب مؤيد — أُضيفت بنية تحتية كاملة لإشعارات الدفع بتطبيق الإدارة، نفس نمط الزبون/السائق/المتجر، مع فرق جوهري واحد: الأدمن يستخدم **Firebase Auth حقيقي (إيميل/باسورد)** مش Anonymous Auth، فالتوكن يُحفظ مباشرة بمستند `admins/{uid}` (uid حساب الأدمن نفسه) بدل حقل منفصل بمجموعة تانية.

### `mobile-admin/` — إضافة مكتبة Push
- `package.json`: أُضيفت `@capacitor/push-notifications@^8.1.2`.
- `android/app/capacitor.build.gradle` + `android/capacitor.settings.gradle`: ربط module الـpush-notifications بمشروع Android (نفس نمط `mobile-store`/`mobile-driver` بالضبط).
- `AndroidManifest.xml`: صلاحية `POST_NOTIFICATIONS` جديدة (لأندرويد 13+).
- `build.gradle` (لم يُلمس — كان أصلاً فيه منطق شرطي لتطبيق `google-services` plugin لو الملف موجود، من يوم إنشاء المشروع بجلسة 6 أغسطس).

### `akleto-admin.html`
- استيراد `updateDoc` إضافي من `firebase-init.js` (ناقص من قبل بهالملف).
- دالة جديدة `initAdminPushNotifications(adminUid)` — تُستدعى تلقائياً داخل `onAuthStateChanged` بعد نجاح تسجيل الدخول (`user.uid`). تتحقق من `Capacitor.isNativePlatform()` أولاً (ما تشتغل إطلاقاً بمتصفح عادي)، تطلب صلاحية الإشعارات، تسجّل، وتحفظ `fcm_token`/`fcm_platform`/`fcm_updated_at` مباشرة على مستند **`admins/{adminUid}`** (بدل مجموعة توكنات منفصلة — أبسط وأقرب لنمط باقي التطبيقات).
- الضغط على إشعار وصل يوجّه الأدمن مباشرة لصفحة `akleto-admin-notifications.html`.

### `firestore.rules` — استثناء جديد على قاعدة `admins`
كانت `allow write: if false` بالكامل (تُدار يدوياً من الكونسول). أُضيف استثناء ضيّق: **الأدمن يقدر يحدّث مستنده الخاص هو بس** (`request.auth.uid == adminId`) — يسمح بحفظ `fcm_token` ذاتياً بدون فتح الباب لإنشاء/حذف حسابات أدمن جديدة من التطبيق (لسا `allow create, delete: if false`).

### `functions/index.js` — دالة خامسة: `onAdminNotificationCreated`
بدل ما نبني منطق مخصص لكل نوع تنبيه (طلب جديد، طلب جاهز، تسجيل سائق...)، الدالة الجديدة بتراقب **أي** مستند جديد بمجموعة `admin_notifications` (الموجودة أصلاً وتُكتب تلقائياً من تطبيقات الزبون/المتجر/السائق بكل الأحداث الموثّقة سابقاً) وتبعت push لكل أدمن عنده `fcm_token` محفوظ — بنفس العنوان والنص (`title`/`message`) المخزّنين أصلاً بمستند التنبيه وقت إنشائه. تغطية شاملة تلقائية بدون أي كود إضافي مستقبلي لكل نوع تنبيه جديد يُضاف لاحقاً.

### التحقق قبل الرفع
`node --check` على كل سكربتات `akleto-admin.html` (3 كتل) و`functions/index.js` — سليمة. فحصت توازن `{}`/`()` بـ`firestore.rules` بعد استبعاد التعليقات العربية — متوازن 100%.

### ⚠️ المطلوب من مؤيد للتفعيل الفعلي
1. **تسجيل تطبيق أندرويد جديد بمشروع Firebase `akleto-prod`** بـpackage name **`com.akalito.admin`** بالضبط (Firebase Console → Project settings → Add app → أندرويد) — نفس خطوة الزبون/السائق/المتجر بالضبط.
2. تحميل `google-services.json` الناتج ووضعه بالمسار: `mobile-admin/android/app/google-services.json` (`build.gradle` أصلاً بيتجاهل الميزة بهدوء بدونه، بس بدون هالملف الإشعارات ما رح تسجّل توكن صحيح).
3. `git pull`
4. بمجلد `mobile-admin`: `npm install` ثم `npx cap sync android`
5. `firebase deploy --only firestore:rules` (تفعيل قاعدة `admins` الجديدة)
6. `firebase deploy --only functions` (نشر `onAdminNotificationCreated` الجديدة)
7. ابني APK جديد بـAndroid Studio لتطبيق الإدارة، ثبّته، سجّل دخول، وتأكد إشعار push يوصل عند أي تنبيه جديد (مثلاً جرّب تسجيل سائق جديد أو طلب جديد وشوف هل يوصل إشعار للموبايل).

**الحالة:** ✅ الكود مكتمل ومرفوع بالكامل. ⏳ بانتظار خطوات مؤيد اليدوية السبع فوق قبل ما تشتغل فعلياً.

## 20 أغسطس 2026 (تكملة) — 🆕 Splash Screen لكل التطبيقات الأربعة + تعديل سريع بخطة المونة الشهرية

### 1) زر إلغاء اختيار متجر خطة المونة الشهرية (`akleto-customer.html`)
أُضيف زر ✕ صغير جوا بانر "خطة المونة الشهرية" — يظهر بس لما يكون في متجر مقفول على الخطة (`cartLockedStore`)، وبيلغي الاختيار مباشرة بضغطة وحدة (بدل ما كان لازم تفتح شيت اختيار متجر جديد للتغيير). لو في أصناف بالسلة، بيسأل تأكيد قبل ما يفضّيها. دالة `cancelMonthlyPlanStore()` جديدة + تحديث `updateMonthlyPlanBanner()` لإظهار/إخفاء الزر.

### 2) Splash Screen حقيقي للتطبيقات الأربعة (customer, driver, store, admin)
**المشكلة المكتشفة:** صور splash.png كانت موجودة أصلاً بكل تطبيق (توليد افتراضي قديم من `@capacitor/assets` عند إنشاء المشاريع)، بس **مكتبة `@capacitor/splash-screen` نفسها ما كانت مثبّتة إطلاقاً** بأي تطبيق. النتيجة: الـsplash الأصلي (Native) كان يختفي فور ما الـWebView يبلش يشتغل — وبما إنه التطبيقات بتحمّل صفحتها الأساسية **عن بعد** من GitHub Pages (`server.url` بـ`capacitor.config.json`)، كان الزبون/السائق/المتجر/الأدمن يشوف شاشة بيضاء فاضية لثواني لحد ما تحمّل الصفحة فعلياً من الإنترنت.

**الحل المطبّق على كل تطبيق من التطبيقات الأربعة:**
- إضافة `@capacitor/splash-screen@^8.0.2` بـ`package.json`.
- ربط module الـsplash-screen بـ`android/app/capacitor.build.gradle` + `android/capacitor.settings.gradle` (نفس نمط push-notifications بالضبط).
- إعداد `plugins.SplashScreen` بـ`capacitor.config.json`: `launchAutoHide: false` (يعني الـsplash يضل ظاهر لحد ما الكود نفسه يقرر يخفيه)، `backgroundColor: "#A23E48"` (لون البراند)، `showSpinner: false`.
- سكربت صغير جديد بأول `<body>` بكل ملف (`akleto-customer.html`, `akleto-driver.html`, `akleto-store.html`, `akleto-admin.html`) — بيستنى حدث `load` (يعني الصفحة خلصت تحميل فعلياً، مو بس DOM) قبل ما يستدعي `Capacitor.Plugins.SplashScreen.hide()`، مع مهلة أمان قصوى 6 ثواني حتى لو تأخر التحميل لأي سبب. هيك الـsplash يضل ظاهر طول فترة تحميل الصفحة عن بعد، بدل شاشة بيضاء فاضية.

**تصميم صور splash.png نفسها (104 ملف: 26 لكل تطبيق × 4):**
تم توليدها برمجياً (Python/Pillow) — خلفية بلون البراند `#A23E48` (نسخة غامقة `#5A2228` لوضع الليل)، مع رسمة الأيقونة البيضاء لكل تطبيق (استُخرجت تلقائياً من `resources/icon.png` الحالي لكل تطبيق) بالنص، بدل التصميم الافتراضي (خلفية بيضاء + أيقونة بمربع ملوّن). كل الأحجام القياسية لأندرويد (port/land × ldpi→xxxhdpi × نهاري/ليلي) اتغطّت.

**⚠️ ملاحظة مهمة اكتُشفت بالمناسبة:** أيقونتي تطبيقي **المتجر والإدارة** (`mobile-store/resources/icon.png` و`mobile-admin/resources/icon.png`) هما نفس أيقونة الدراجة الخاصة بتطبيق السائق **بالضبط** (ملف مكرر، مش تصميم مخصص) — على الأغلب placeholder قديم من وقت إنشاء المشاريع (6 أغسطس) ولسا ما تم تحديثه. صار الـsplash الجديد يعكس هالخلل (splash المتجر والإدارة عم يورّي أيقونة دراجة بدل أيقونة تمثّل دورهم الفعلي). هاي نقطة تابعة لمهمة "تعديل الهوية البصرية" المؤجّلة — تحتاج تصميم أيقونتين حقيقيتين (متجر، لوحة إدارة) قبل أي إطلاق فعلي.

### التحقق قبل الرفع
`node --check` على كل كتل السكربت بالملفات الأربعة بعد إضافة سكربت الـsplash — كلها سليمة. تأكدت من تطابق أبعاد الصور الجديدة (26 مقاس/ملف) مع نفس المسارات والأبعاد الافتراضية لـCapacitor (port/land، ldpi حتى xxxhdpi، نهاري/ليلي).

### ⚠️ المطلوب من مؤيد للتفعيل الفعلي
لكل تطبيق من الأربعة: `git pull` → `npm install` (لتثبيت `@capacitor/splash-screen`) → `npx cap sync android` → بناء APK جديد وتجربته (يفترض تشوف شاشة splash بلون البراند بدل الأبيض، وتضل ظاهرة لحد ما الصفحة تحمّل فعلياً بدون فراغ أبيض بالنص).

**الحالة:** ✅ الكود والصور جاهزين ومرفوعين بالكامل على GitHub. ⏳ بانتظار مؤيد يبني APK جديد لكل تطبيق ويجرب.

## 20 أغسطس 2026 (تكملة) — 🆕 تفعيل OTP حقيقي (Firebase Phone Auth) — زبون، سائق، متجر

### القرار المتفق عليه مع مؤيد
- OTP **مرة وحدة بس** عند إنشاء حساب جديد لأول مرة (مو كل تسجيل دخول — الجلسة تضل محفوظة محلياً بالجهاز زي ما كانت).
- الحسابات الموجودة أصلاً (بدون تحقق رقم) **تضل شغالة عادي بدون أي تأثير** — OTP بس للتسجيلات الجديدة من هلق وطلع.

### المشكلة الأمنية اللي انحلّت بالمناسبة
قبل هالتعديل، "تسجيل الدخول" بالثلاث تطبيقات كان مجرد **كتابة رقم الهاتف بدون أي تحقق** — أي حدا يعرف رقم هاتف شخص تاني كان يقدر يدخل على حسابه (زبون/سائق/متجر) مباشرة. هلق التسجيل الأول محمي برمز SMS حقيقي.

### التنفيذ التقني
**`firebase-init.js`** (مشترك بكل الصفحات) — أُضيف:
- استيراد `RecaptchaVerifier`, `signInWithPhoneNumber` من Firebase Auth SDK.
- `toE164Jordan(localPhone)` — يحوّل رقم محلي (07XXXXXXXX) لصيغة دولية (+9627XXXXXXXX).
- `sendOtpCode(containerId, localPhone)` — يبعت الكود عبر invisible reCAPTCHA، يرجع `confirmationResult`.
- `otpErrorMessage(err)` — رسائل خطأ عربية مفهومة لكل أكواد خطأ Firebase الشائعة (رمز غلط، منتهي، محاولات كتير، دومين غير مصرّح...).

**`akleto-customer.html`** — `startCustomerSession()` صار يبعت OTP بدل ما ينشئ الحساب مباشرة (بعد التأكد إنه الرقم مش مسجّل مسبقاً). شاشة جديدة `#otpScreen` (6 خانات، تأكيد، إعادة إرسال، رجوع). دالة جديدة `confirmCustomerOtp()` بتكمل إنشاء الحساب بعد نجاح التحقق، باستخدام الـuid الحقيقي المتحقق منه (`result.user.uid`) بدل الـuid المجهول.

**`akleto-driver-signup.html`** — أُضيفت خطوة جديدة بمعالج التسجيل (`panel-1v`) بين خطوة البيانات الأساسية ورفع الوثائق. `goToStep2()` صار يبعت OTP بعد التحقق من عدم تكرار الرقم (وبس للتسجيل الجديد — إعادة التقديم `resubmit` بتتخطى OTP لأنها مش تسجيل جديد). `confirmDriverOtp()` بتخزّن الـuid المتحقق منه بـ`window._signupData.auth_uid`. **لا حاجة لتعديل `createDriverAccount` بملف `driver-shared.js`** — دالة `ensureDriverAuth()` أصلاً بتتحقق بس من وجود جلسة Auth حالية (`auth.currentUser`) بغض النظر عن نوعها، فبما إنه جلسة التحقق بالـOTP بتصير قبل الوصول للخطوة الأخيرة، بتُستخدم تلقائياً بدون أي تعديل إضافي.

**`akleto-store.html`** — `loginAsStore()` انقسمت لمسارين: **موظف موجود مسبقاً** بنفس المتجر (بحسب `store_id`+`phone`) → دخول مباشر بدون OTP (زي ما كان). **موظف جديد** (أول مرة يسجّل بهالمتجر) → OTP أول عبر شاشة `#storeOtpScreen`، وبعد التأكيد `confirmStoreOtp()` بينشئ مستند `store_employees` بالـuid المتحقق منه.

### ⚠️⚠️ خطوات إلزامية بـFirebase Console — بدونها الميزة ما رح تشتغل إطلاقاً
1. **Firebase Console → Authentication → Sign-in method → فعّل "Phone" كـprovider.**
2. **Firebase Console → Authentication → Settings → Authorized domains → أضف `moayyad44.github.io`** (الدومين اللي التطبيقات الثلاث بتحمّل صفحاتها منه عبر `server.url` بـ`capacitor.config.json`) — بدون هالخطوة رح يطلع خطأ `auth/unauthorized-domain` عند أي محاولة إرسال OTP.
3. **تفعيل/ربط reCAPTCHA:** Firebase Phone Auth (نسخة الويب) بتعتمد على reCAPTCHA (v2 أو Enterprise حسب إعداد المشروع) للتحقق إنه الطلب من إنسان حقيقي. غالباً يشتغل تلقائياً بمجرد تفعيل Phone provider، لكن لو ظهرت مشاكل بالتحقق، يلزم مراجعة "Google Cloud Console → reCAPTCHA Enterprise" والتأكد من ربط مفتاح API بمشروع `akleto-prod`.
4. **⚠️ تنويه تكلفة:** Firebase Phone Auth **مو مجاني بالكامل** — فيه حصة مجانية محدودة شهرياً (تختلف حسب خطة المشروع Blaze)، وبعدها بتُحتسب تكلفة لكل رسالة SMS تُرسل. مع نمو عدد المستخدمين الجدد شهرياً، هاي تكلفة تشغيلية مستمرة لازم تُراقب من Firebase Console → Usage and billing.
5. لا حاجة لأي `git pull` أو بناء APK جديد لتفعيل هالميزة تقنياً (الكود يشتغل مباشرة بمجرد رفعه لأنه محمّل عن بعد) — **بس** الخطوات 1 و2 فوق لازم تصير يدوياً من Firebase Console قبل ما يشتغل أي OTP فعلياً، وإلا كل محاولة تسجيل جديد رح تفشل بخطأ.

### التحقق قبل الرفع
`node --check` على كل كتل السكربت بالملفات الأربعة (`firebase-init.js`, `akleto-customer.html`, `akleto-driver-signup.html`, `akleto-store.html`) — كلها سليمة. تأكدت من عدم وجود أي اعتماد على نوع مزود المصادقة (`isAnonymous`/`sign_in_provider`) بـ`firestore.rules` — لا حاجة لتعديلها.

**الحالة:** ✅ الكود جاهز ومرفوع بالكامل. ⏳ بانتظار مؤيد يفعّل Phone provider ويضيف الدومين المصرّح من Firebase Console (خطوتين 1+2 فوق) قبل ما يجرب فعلياً.

## 22 أغسطس 2026 — 🆕 مهمة مستقبلية: دومين خاص بدل GitHub Pages

### السياق
اكتُشفت أثناء اختبار OTP: رسالة التحقق SMS (عبر Firebase Phone Auth) بتظهر بصيغة "XXXXXX is your verification code for moayyad44.github.io" — لأنه reCAPTCHA (آلية الحماية اللي Firebase بتستخدمها) بتربط تلقائياً باسم الدومين اللي التطبيقات محمّلة منه، وهو حالياً دومين GitHub Pages الافتراضي.

### القرار
- **نص رسالة OTP نفسه واسم المرسل غير قابلين للتخصيص إطلاقاً** مع Firebase Phone Auth (تأكدنا بالبحث — قيد معروف ومطلوب من مطوّرين تانيين من فيربيس من زمان بدون تنفيذ).
- **التبديل لمزوّد SMS خارجي** (Twilio Verify / Unifonic / MSG91) هو الخيار الوحيد للتحكم الكامل بنص الرسالة واسم المرسل (مثلاً "AKLITO") — مؤجّل حالياً، يحتاج شغل إضافي (Cloud Function جديدة) وتكلفة إضافية.
- **القرار الحالي المتفق عليه:** الانتقال لدومين خاص (مثلاً `akalito.com` أو مشابه) بدل الاعتماد على `moayyad44.github.io` — تحسين أرخص وأسرع، هيك الرسالة بتصير "...for akalito.com" بدل اسم GitHub، أكثر احترافية (رغم إنها لسا مش نص حر متل "AKLITO" بالضبط).

### ⚠️ خطوات مطلوبة لاحقاً (لسا ما اشتغلنا عليها)
1. شراء دومين مناسب لأكليتو (مثلاً عبر Namecheap، GoDaddy، أو مزوّد أردني).
2. ربط الدومين بـGitHub Pages (عبر إعداد CNAME بالريبو + DNS records عند مزوّد الدومين).
3. تحديث `server.url` بملفات `capacitor.config.json` للتطبيقات الأربعة ليشاور على الدومين الجديد بدل `moayyad44.github.io`.
4. **إضافة الدومين الجديد لقائمة Authorized domains بـFirebase Console** (Authentication → Settings) — وإلا OTP رح ينكسر فوراً لأنه الدومين القديم مش هو نفسه.
5. `git pull` + `npx cap sync android` + بناء APK جديد للتطبيقات الأربعة بعد التحديث.

**الحالة:** ⏳ مهمة مسجّلة بانتظار قرار مؤيد بأي دومين يشتريه، ثم التنفيذ.

## 22 أغسطس 2026 — ⏸️ Splash Screen: متوقفة مؤقتاً (مش محلولة، بطلب مؤيد)

### الحالة النهائية
جرّبنا 3 محاولات إصلاح متتالية لمشكلة "splash screen ما بتظهر":
1. توليد صور splash.png بلون البراند (104 صورة) — تبيّن إنها الطريقة الغلط أصلاً، المشروع مبني على نظام splash حديث لأندرويد 12+ (`Theme.SplashScreen`) ما بيستخدم هيك صور إطلاقاً.
2. تصحيح `styles.xml` ليستخدم `windowSplashScreenBackground`/`windowSplashScreenAnimatedIcon` الصح بدل `android:background` القديمة — تحسين صحيح تقنياً بس ما حل المشكلة بالكامل.
3. ربط splash بكود Java أصلي مباشر (`installSplashScreen()` + `setKeepOnScreenCondition` مربوطة بـ`WebView.onPageFinished`) بدل الاعتماد على جسر الجافاسكربت — بسبب bug موثّق بـCapacitor نفسها بيعطّل `SplashScreen.hide()` مع صفحات محمّلة عن بعد (`server.url`). لسا ما اشتغلت حتى بعد هالإصلاح.

**مؤيد قرر يوقف الشغل على هاي المهمة حالياً** — مش أولوية، ونرجعلها لاحقاً لو حبينا.

### ⚠️ ملاحظة لأي شغل مستقبلي على هالموضوع
التغييرات الثلاثة فوق **لسا موجودة بالكود** (مرفوعة على GitHub، ما تم التراجع عنها):
- `values/styles.xml` + `values/splash_theme_colors.xml` (تصحيح ثيم splash) — بكل التطبيقات الأربعة.
- `MainActivity.java` (ربط native بالـWebView) — بكل التطبيقات الأربعة.
- 104 صورة splash.png (غير مستخدمة فعلياً بالنظام الحالي، بس موجودة وما بتأذي شي).
- مكتبة `@capacitor/splash-screen` مضافة بـ`package.json` الأربعة + إعدادات `capacitor.config.json`.

لو حدا رجع يشتغل عليها لاحقاً، الخطوة المنطقية التالية (ما جُرِّبت بعد): التأكد فعلياً هل splash عم يظهر لمدة قصيرة جداً (أجزاء من الثانية، مش ملحوظة) أو ما بيظهر إطلاقاً — عبر تسجيل شاشة (screen recording) بطيء الحركة وقت فتح التطبيق، بدل الاعتماد على الملاحظة البصرية العادية.

## 22 أغسطس 2026 — ✅ تأكيدات مؤيد على مهمات هالمحادثة

مؤيد راجع قائمة المهام المفتوحة وأكّد حالتها:

1. ✅ **زر إلغاء اختيار متجر بخطة المونة الشهرية** — **شغّال ومؤكَّد**.
2. ⏸️ **Splash Screen** — **متوقفة بطلبه** (تفاصيل التوقف موثّقة بالقسم السابق أعلاه).
3. ❌ **إشعارات الدفع لتطبيق الإدارة (mobile-admin)** — **لسا مش شغّالة**. تحتاج تشخيص إضافي (لسا ما عرفنا وين بالضبط المشكلة — احتمال خطوات نشر ناقصة زي `firebase deploy --only functions`/`firestore:rules`، أو مشكلة بربط `google-services.json`، أو الـFCM token ما عم يتسجل. تحتاج جلسة تشخيص مخصصة لما مؤيد جاهز).
4. ✅ **OTP (تسجيل أول + تسجيل دخول لاحق)** — **شغّال ومؤكَّد بالكامل**.
7. ✅ **ملف الـkeystore** — **محفوظ بمكان آمن من مؤيد**.

**المتبقي بدون تأكيد بعد (لسا ما جاوب عنها مؤيد):**
5. مسودة الشروط والخصوصية — بانتظار إرسالها لمحامي فعلي.
6. سؤال "فرد مش شركة" — بانتظار تأكيد نهائي من محامي/محاسب.
8. دومين خاص بدل GitHub Pages — بانتظار شراء الدومين.


## 24 أغسطس 2026 — 🚨 مهمة 3+8 (تكملة): إصلاح ثغرة حرجة — PIN المتجر كان مقروء للعامة

### الاكتشاف
أثناء البدء بتقييد القراءة المفتوحة (الجزء المتبقي من مهمة Auth الحقيقي)، تبيّن إنه حقل `pin` (رمز دخول موظفي المتجر، 4 أرقام) كان مخزّن مباشرة على مستند `stores/{storeId}` — وقاعدة `stores` بالقراءة `allow read: if true` (لازمة لتصفّح الزبون للمتاجر). يعني عملياً **أي حد بالعالم كان يقدر يستعلم مجموعة `stores` كاملة ويجيب كل أرقام PIN مباشرة بدون ما يحتاج يعرفها أصلاً**، ويسجّل دخول كموظف بأي متجر — أخطر ثغرة مكتشفة بالمشروع لحد الآن.

### الحل (Firestore Rules ما بتقدر تقيّد حقل واحد بس داخل مستند — إما القراءة كلها مفتوحة أو كلها مقفلة):
1. **مجموعة جديدة `store_pins/{storeId}`** (نفس معرّف المتجر) — محمية بالكامل (`allow read, write: if isAdmin();`)، فيها الحقل `pin` بس.
2. **Cloud Function جديدة `verifyStorePin({pin})`** — تستخدم Admin SDK (بيتجاوز القواعد)، تدوّر عن الـPIN بمجموعة `store_pins` وترجّع `{storeId, storeName, isActive}` لو صحيح، وإلا خطأ `not-found`. العميل ما يقرأ ولا يشوف أي PIN تاني إطلاقاً — بس يبعت الرقم يلي كتبه ويستنى نتيجة صح/غلط.
3. **`akleto-store.html`**: شاشة دخول الموظف صارت تستدعي `verifyStorePin()` (عبر Cloud Functions client SDK) بدل `getDocs(query(collection(db,'stores'), where('pin','==',pin)))` المباشرة القديمة.
4. **`akleto-admin-stores.html`**: توليد الـPIN عند إنشاء متجر جديد صار يكتب لـ`store_pins/{id}` (`setDoc` مباشر بمعرّف المتجر نفسه) بدل حقل على `stores`. عرض/نسخ الـPIN بالواجهة صار يقرأ من `pinsMap` (محمّلة من `store_pins` — الأدمن مسجّل دخول حقيقي فمقدر يقرأها). حذف متجر صار يحذف وثيقة الـPIN معه.
5. **Cloud Function ترحيل `migrateStorePins`** (مرة وحدة، محمية بفحص `admins/{uid}` صريح جوا الدالة نفسها) — تنقل أي `pin` لسا موجود بمستندات `stores` القديمة (من قبل هالإصلاح) لمجموعة `store_pins` الجديدة، وتحذف الحقل من `stores` بعدها. **زر مؤقت "ترحيل الآن"** أُضيف بأعلى `akleto-admin-stores.html` (بطاقة بحدود برتقالية) — **لازم يُضغط مرة وحدة بس بعد أول نشر لهالتحديث**، وبعدها ينحذف الزر من الصفحة (تنظيف لاحق).
6. **`firestore.rules`**: قاعدة `stores.update` القديمة كانت فيها شرط `request.resource.data.pin == resource.data.pin` (يمنع غير الأدمن يغيّر الـPIN) — انحذف بالكامل لأنه ما عاد الحقل موجود على `stores` أصلاً (لو تُرك، كان ممكن يسبب خطأ تقييم القاعدة لأي تحديث عادي من موظف المتجر — Firestore Rules بترمي خطأ عند الوصول لحقل غير موجود بمقارنة صريحة).

### التحقق قبل الرفع
`node --check` على كل الملفات المعدّلة الخمسة (`functions/index.js`, `firebase-init.js`, `akleto-store.html`, `akleto-admin-stores.html` module scripts) — سليمة بالكامل.

### ⚠️ المطلوب من مؤيد (بالترتيب بالضبط — حرج، لا تتجاوز الترتيب)
1. `git pull`
2. **`firebase deploy --only functions`** — لازم أول شي، عشان `verifyStorePin` و`migrateStorePins` يصيروا موجودين على السيرفر قبل أي شي تاني.
3. **`firebase deploy --only firestore:rules`** — عشان `store_pins` تصير محمية فعلياً.
4. افتح `akleto-admin-stores.html`، اضغط **"ترحيل الآن"** (بالبطاقة البرتقالية بأعلى الصفحة) **مرة وحدة بس** — تأكد الرسالة تقول "تم ترحيل X من Y متجر ✅" (X = عدد المتاجر اللي كان عندها PIN قديم على مستند stores).
5. تحقق يدوياً من Firebase Console → Firestore أنه مستندات `stores` ما عاد فيها حقل `pin` إطلاقاً، وأنه مجموعة `store_pins` الجديدة فيها كل الأرقام.
6. اختبار: افتح `akleto-store.html`، جرّب تسجيل دخول بموظف موجود بـPIN صحيح (لازم يشتغل عادي)، وجرّب PIN غلط (لازم رسالة "رمز PIN غير صحيح" واضحة).
7. **لا حاجة لبناء APK** — كل التعديلات ملفات ويب (JS/HTML) + سيرفر (Functions/Rules)، تنعكس مباشرة عبر GitHub Pages.
8. بعد التأكد من نجاح الترحيل والاختبار، احذف بطاقة "ترحيل الآن" من `akleto-admin-stores.html` (تنظيف — الزر ما بيأذي لو ضل، بس مو محتاج يبقى دايماً).

### الخطوة التالية (بعد تأكيد هالإصلاح)
نكمّل تقييد القراءة المفتوحة على باقي المجموعات بنفس الترتيب المقترح سابقاً: `customers` (الأبسط) → `drivers` (فصل البيانات العامة عن الحساسة كصور الوثائق) → `orders` (الأعقد).


## 24 أغسطس 2026 (تكملة) — 🔄 إلغاء نظام PIN بالكامل، استبداله بتصريح أرقام هواتف من الأدمن

### القرار
بعد إصلاح ثغرة قراءة PIN المكشوفة (أعلاه)، مؤيد لاحظ إنه نظام PIN نفسه عنده مشكلة تصميم أعمق — مش بس مشكلة قراءة:
- PIN 4 أرقام = 10,000 احتمال بس، قابل للتخمين الآلي (brute-force) بدون أي تحديد لعدد المحاولات على `verifyStorePin`.
- **الأخطر:** لو حد خمّن PIN صحيح وسجّل برقم هاتف عشوائي (مو موجود بـ`store_employees`)، النظام القديم كان يعتبره تلقائياً "موظف جديد" ويبعتله OTP لهاتفه هو — وبمجرد تأكيد الكود **يصير موظف مصرّح فعلياً بهيك متجر غريب**. يعني تخمين PIN = دخول حساب حقيقي، مو بس تسريب معلومة.

**القرار:** إلغاء PIN كمفهوم بالكامل. الأدمن هو يلي بيضيف مسبقاً أرقام هواتف الموظفين المصرّح لهم لكل متجر (`store_employees`، `create: isAdmin()` بقواعد Firestore) — ما في تسجيل ذاتي لموظف جديد إطلاقاً بعد اليوم. الموظف بس بيقدر "يفعّل" سجله المُنشأ مسبقاً (يربط `auth_uid`) أول مرة يسجّل دخول بالـOTP.

### التنفيذ (تراجع كامل عن نظام PIN اليوم + الميزة الجديدة)
1. **`functions/index.js`**: حذف `verifyStorePin` و`migrateStorePins` بالكامل (ما عاد فيهم داعي).
2. **`firestore.rules`**: حذف مجموعة `store_pins`. تعديل `store_employees.create` من `if request.auth != null` (أي حساب موثّق كان يقدر يخلق سجل موظف لأي متجر!) إلى `if isAdmin()` بس — دي الحماية الحقيقية الجديدة.
3. **`firebase-init.js`**: حذف Cloud Functions client SDK والـhelper `verifyStorePin` (كانا لنظام PIN الملغى، تنظيف تبعي).
4. **`akleto-store.html`**: شاشة الدخول صارت `<select>` لاختيار المتجر (قائمة نشطة من قراءة `stores` العامة العادية) + اسم + هاتف، بدون أي PIN. `loginAsStore()` يتحقق إنه الرقم موجود مسبقاً بـ`store_employees` لهذا المتجر بالذات (رسالة "رقمك مش مصرّح — تواصل مع الأدمن" لو مش موجود) قبل ما يبعت OTP. `confirmStoreOtp()` صار يربط `auth_uid` على السجل الموجود مسبقاً بس (حذف فرع "إنشاء موظف جديد" نهائياً).
5. **`akleto-admin-stores.html`**: تراجع كامل عن كل شغل الـPIN (الحقل، زر النسخ، بطاقة الترحيل المؤقتة). بدالها: زر "إدارة الموظفين ▾" تحت كل متجر بقائمة المتاجر، بيعرض الموظفين المصرّحين (مع تنبيه "(ما دخل لسا)" للي لسا ما فعّلوا حسابهم بأول OTP)، مع زر "إلغاء" لسحب تصريح أي رقم فوراً، وحقل إضافة رقم/اسم جديد مباشرة من نفس المكان.

### التحقق قبل الرفع
`node --check` على كل ملفات JS المعدّلة (module scripts داخل الـHTML أيضاً) — سليمة. عدّ الأقواس المتطابق بـ`firestore.rules` (53/53) للتأكد من عدم كسر البنية.

### ⚠️ المطلوب من مؤيد (بالترتيب)
1. `git pull`
2. **`firebase deploy --only functions`** — يشيل `verifyStorePin`/`migrateStorePins` من السيرفر (ما عادوا موجودين بالكود، بس لازم ينشروا عشان ينحذفوا فعلياً من Firebase أيضاً — وإلا يضلوا "يتامى" شغالين عالسيرفر رغم إنهم مو مستخدَمين).
3. **`firebase deploy --only firestore:rules`** — عشان قاعدة `store_employees.create` الجديدة (أدمن بس) تصير فعلية، وتُحذف `store_pins`.
4. **مهم جداً:** أي متجر عنده موظفين مسجّلين مسبقاً (من قبل هالتحديث) ضلوا شغالين عادي (سجلاتهم موجودة أصلاً بـ`store_employees` بـ`auth_uid` مربوط). **بس المتاجر الجديدة أو الموظفين الجدد لازم الأدمن يضيف رقمهم يدوياً الأول** من `akleto-admin-stores.html` → "إدارة الموظفين" → تعبئة الرقم (والاسم اختياري) → "تصريح رقم".
5. تحقق يدوياً من Firebase Console → Firestore إنه مجموعة `store_pins` (كانت اتنشأت بالإصلاح السابق) مو مستخدمة، احذفها يدوياً لو موجودة (تنظيف، مو حرج).
6. اختبار: من لوحة الأدمن ضيف رقم هاتف تجريبي لمتجر، افتح `akleto-store.html`، اختر نفس المتجر، دخّل نفس الرقم — لازم يوصلك OTP ويشتغل الدخول عادي. جرّب رقم مش مضاف — لازم رسالة "رقمك مش مصرّح" واضحة.
7. لا حاجة لبناء APK — كل التعديلات ويب + سيرفر، تنعكس مباشرة عبر GitHub Pages.

### 🆕 فكرة مستقبلية (تحتاج APK جديد — مؤجّلة)
مؤيد اقترح إضافة "بحث بجهات الاتصال" لتسهيل إدخال رقم الموظف بلوحة الأدمن. هاي محتاجة:
- مكتبة Capacitor لقراءة جهات الاتصال (`@capacitor-community/contacts`)
- صلاحية `READ_CONTACTS` بالأندرويد — صلاحية حساسة، بتحتاج نموذج إفصاح إضافي بـGoogle Play Console (متل صلاحية الموقع بالخلفية عند تطبيق السائق)
- **بناء ونشر APK جديد لـ`mobile-admin`** — مو مجرد تعديل صفحة ويب
**القرار الحالي:** الإدخال اليدوي للرقم (07XXXXXXXX) يشتغل فوراً بدون أي بناء تطبيق، وده يكفي حالياً. "اختيار من جهات الاتصال" ميزة تحسين تجربة استخدام لاحقة، مو أساسية.


## 24 أغسطس 2026 (تكملة 2) — تحسينات UX على نظام تصريح الموظفين الجديد

بعد ما جرّب مؤيد الميزة الجديدة، طلب تعديلين:

1. **زر "إدارة الموظفين" كان مو واضح إنه زر** (شكله متل نص عادي بقائمة المتاجر). **الحل:** نقل إدارة الموظفين بالكامل لداخل مودال تعديل المتجر نفسه (`openEditStoreModal`) — بدل زر/أكورديون منفصل بالقائمة. هلق: تضغط على أي متجر بالقائمة، المودال يفتح بمعلومات المتجر + قسم "👥 الموظفون المصرّحون بالدخول" مباشرة تحتها (قائمة الموظفين + حقل إضافة رقم جديد)، كله بمكان واحد. قسم الموظفين مخفي لما تضيف متجر جديد (لسا ما إله id لنربط الموظفين فيه) ويظهر بس بوضع التعديل.

2. **إلغاء اختيار المتجر وكتابة الاسم من شاشة دخول الموظف** — بما إنه الأدمن أصلاً حدد مسبقاً مين الموظف ووين بيشتغل (بمجموعة `store_employees`)، ما في داعي يكرر هالمعلومة يدوياً. **الحل:** شاشة "الدخول كتاجر" بـ`akleto-store.html` صارت حقل واحد بس (رقم الهاتف). `loginAsStore()` يبحث بمجموعة `store_employees` عن أي سجل بنفس رقم الهاتف (بغض النظر عن المتجر — الاستعلام صار `where('phone','==',phone)` بدون فلترة store_id مسبقة، لأنه المتجر مو معروف قبل ما نلاقي السجل)، ياخد `store_id` و`name` منه مباشرة، يجيب اسم المتجر بـ`getDoc` (استيراد `getDoc` جديد بالملف)، ويكمل لنفس مسار الـOTP.

⚠️ **افتراض مهم:** التصميم الحالي يفترض إنه كل رقم هاتف مصرّح لمتجر واحد بس. لو نفس الرقم انضاف لأكتر من متجر (نظرياً ممكن لو الأدمن غلط)، بياخد أول نتيجة يلقاها بالاستعلام بس — سلوك غير محدد لو صار تكرار. مو مشكلة حالياً لأنه الأدمن هو المتحكم الوحيد بإضافة الأرقام.

### التحقق قبل الرفع
`node --check` على module scripts بالملفين — سليمة.

### ⚠️ المطلوب من مؤيد
1. `git pull` — **لا حاجة لأي `firebase deploy`** هالمرة (تعديلات واجهة/منطق عميل بس، ما لمسنا Functions ولا Rules).
2. اختبار: افتح لوحة الأدمن، اضغط على أي متجر، تأكد قسم "الموظفون المصرّحون" ظاهر تحت معلومات المتجر مباشرة وشغّال (إضافة/إلغاء).
3. اختبار: افتح `akleto-store.html`، دخّل رقم موظف مصرّح — لازم يوديك على طول لخطوة OTP بدون ما يطلب اسم أو متجر.


## 24 أغسطس 2026 (تكملة 3) — شاشة اختيار متجر لموظف مصرّح بأكتر من متجر

مؤيد لاحظ إنه لو نفس رقم الهاتف مصرّح بأكتر من متجر (سيناريو ممكن — نفس الشخص يشتغل بأكتر من فرع)، الكود السابق كان ياخد أول نتيجة استعلام بس (سلوك عشوائي/غير محدد). الحل:

### التنفيذ (`akleto-store.html`)
- `loginAsStore()`: الاستعلام عن `store_employees` بنفس المنطق (`where('phone','==',phone)`)، بس هلق يفحص عدد النتائج:
  - **نتيجة وحدة** → نفس المسار القديم بالضبط (يحل المتجر فوراً، يرسل OTP).
  - **أكتر من نتيجة** → يحفظ قائمة كل (`empId`, `storeId`, `name`) بـ`_pendingStoreLogin.employees` بدون ما يحدد متجر، ويرسل OTP لنفس الرقم (التحقق من الرقم نفسه واحد بغض النظر عن عدد المتاجر).
- `confirmStoreOtp()`: بعد نجاح الـOTP، لو `_pendingStoreLogin.multi === true` → يستدعي `showStoreSelectScreen()` بدل ما يكمل الدخول مباشرة.
- **`showStoreSelectScreen()`** (دالة جديدة): تجيب اسم وحالة كل متجر بقائمة الموظف (`getDoc` لكل واحد)، تستبعد المتاجر المعطّلة (`is_active === false`) من الخيارات، وتعرض زر لكل متجر متبقي بشاشة جديدة `storeSelectScreen`. لو كل المتاجر معطّلة، رسالة "كل متاجرك معطّلة حالياً".
- **`selectStoreAndEnter(choice)`** (دالة جديدة): تربط `auth_uid` بسجل الموظف المختار بس (مو كل السجلات)، تضيفه لـ`authorized_uids` بمستند المتجر المختار، وتكمل الدخول العادي (localStorage + `enterStoreMode()`).

### ⚠️ قيد معروف (موثّق، مو حرج حالياً)
الجلسة المحفوظة بـ`localStorage` بعد الاختيار بتصير مربوطة بمتجر واحد بس لهاد الجهاز. لو موظف عنده متجرين وبده يبدّل بينهم بنفس الجهاز، لازم يعمل تسجيل خروج (`exitStoreMode`) ويرجع يسجّل دخول من جديد بنفس الرقم عشان تطلعله شاشة الاختيار تاني ويختار التاني. ما في زر "تبديل متجر" مباشر لحد الآن — تحسين مستقبلي لو صار محتاج فعلياً.

### التحقق قبل الرفع
`node --check` على الـmodule script — سليم.

### ⚠️ المطلوب من مؤيد
1. `git pull` — بدون حاجة لـ`firebase deploy` (تعديل واجهة/منطق عميل بس).
2. اختبار الحالة العادية (رقم بمتجر واحد) — لازم يشتغل بدون أي تغيير بالتجربة.
3. اختبار الحالة الجديدة: ضيف نفس رقم الهاتف كموظف مصرّح بمتجرين مختلفين من لوحة الأدمن، جرّب تسجيل الدخول بهاد الرقم من `akleto-store.html` — بعد الـOTP لازم تطلع شاشة فيها زرار لكل متجر، واختيار أي وحدة يدخلك عليها بشكل طبيعي.


## 24 أغسطس 2026 (تكملة 4) — صورة حقيقية لكل متجر بلوحة الأدمن (بدل أيقونة 🏪)

### الطلب
بلوحة الأدمن بس (مو تطبيق الزبون) — استبدال أيقونة 🏪 الثابتة بصورة حقيقية للمتجر يرفعها الأدمن.

### التنفيذ (`akleto-admin-stores.html`)
اتّبعنا بالضبط نفس نمط رفع صور الوجبات الموجود مسبقاً بـ`akleto-admin-meals.html` (نفس helper `uploadImage(file, folder)` من `firebase-init.js`، ونفس CSS classes `image-upload-box`/`image-preview` الموجودة أصلاً بـ`admin-style.css`):
- حقل `<input type="file">` جديد بمودال إضافة/تعديل المتجر (`sf-image` + معاينة `sf-image-preview`)، فوق حقل رقم الهاتف مباشرة.
- `previewStoreImage()`: معاينة فورية للصورة قبل الرفع (نفس `previewMealImage`).
- `saveStore()`: لو المستخدم اختار صورة جديدة، ترفع لـFirebase Storage بمجلد `stores/` (مسار جديد منفصل عن `meals/`) وتُخزَّن رابطها بحقل `image_url` على مستند المتجر. لو ما اختار صورة وهو بوضع تعديل، الرابط القديم يضل زي ما هو (ما بينمسح).
- قائمة المتاجر (`renderStores`): الصورة تظهر بدل 🏪 لو موجودة (`<img>` جوا نفس مربع `ing-thumb`)، وترجع لأيقونة 🏪 الافتراضية لو المتجر ما رفعله صورة بعد.

### ملاحظة تقنية
رفع الصورة بيعتمد على نفس قواعد Firebase Storage المستخدمة أصلاً لصور الوجبات (ما في ملف `storage.rules` بالمستودع — يبدو إدارتها مباشرة من Firebase Console). بما إنه رفع صور الوجبات شغّال فعلياً، رفع صور المتاجر المفروض يشتغل بنفس الطريقة تلقائياً (نفس bucket، مجلد مختلف بس).

### التحقق قبل الرفع
`node --check` على الـmodule script — سليم.

### ⚠️ المطلوب من مؤيد
1. `git pull` — بدون حاجة لـ`firebase deploy` (كود عميل بس، ما لمسنا Rules ولا Functions).
2. اختبار: افتح تعديل أي متجر، ارفع صورة، احفظ، تأكد إنها ظهرت بقائمة المتاجر بدل 🏪. جرّب متجر جديد بدون صورة — لازم يضل عليه 🏪 الافتراضية لحد ما تُرفع له صورة.


## 24 أغسطس 2026 (تكملة 5) — سحب صورة المتجر لتطبيق المتجر نفسه

بعد إضافة رفع صورة المتجر بلوحة الأدمن (أعلاه)، مؤيد طلب سحبها لتطبيق المتجر (`akleto-store.html`) أيضاً — بدل أيقونة 🏪 الثابتة بكل الأماكن اللي كانت تظهر فيها.

### التنفيذ
- متغيّر جديد `storeImage` + مفتاح `localStorage` جديد `STORE_IMAGE_KEY` (بنفس نمط `storeName`/`STORE_NAME_KEY` الموجود مسبقاً)، محفوظ/مسترجع بكل نقاط الدخول والخروج وتذكّر الجلسة.
- **3 أماكن عرض** استبدلت 🏪 الثابتة بالصورة الحقيقية (لو موجودة، وإلا ترجع لـ🏪 الافتراضية):
  1. هيدر الشاشة الرئيسية (بجانب اسم المتجر بالترحيب) — أيقونة صغيرة 22×22 دائرية.
  2. القائمة الجانبية (Drawer) — أفتار أكبر بأعلى القائمة.
  3. شاشة اختيار المتجر (لموظف مصرّح بأكتر من متجر) — أيقونة صغيرة جنب اسم كل متجر بقائمة الأزرار.
- **تحديث لحظي:** استغللنا الـ`onSnapshot` الموجود مسبقاً على مستند المتجر (`watchStoreOpenState`، أصلاً يراقب حالة "متاح/غير متاح") لمراقبة تغيّر `image_url` أيضاً — لو الأدمن غيّر صورة المتجر بعد ما الموظف مسجّل دخول أصلاً، الصورة بتتحدّث بالهيدر تلقائياً بدون ما يحتاج يسجّل خروج ويرجع يدخل.

### التحقق قبل الرفع
`node --check` على الـmodule script — سليم.

### ⚠️ المطلوب من مؤيد
1. `git pull` — بدون حاجة لـ`firebase deploy` (كود عميل بس).
2. اختبار: سجّل دخول بمتجر عنده صورة مرفوعة مسبقاً — تأكد الصورة ظاهرة بالهيدر وبالقائمة الجانبية بدل 🏪.
3. اختبار التحديث اللحظي: وأنت مسجّل دخول بتطبيق المتجر، روح لوحة الأدمن من جهاز/تبويب تاني وغيّر صورة نفس المتجر — لازم تتحدّث بالهيدر تلقائياً خلال ثواني بدون تحديث الصفحة.


## 24 أغسطس 2026 (تكملة 6) — تقييد قراءة `customers` (أول خطوة من مهمة تضييق القراءة المفتوحة)

بعد إكمال شغل نظام PIN/تصريح الموظفين، رجعنا لمهمة Auth الحقيقي الأساسية: تضييق `allow read: if true` المتبقية. البداية بـ`customers` (الأبسط بالترتيب المتفق عليه: customers → drivers → orders).

### المشكلة المكتشفة
`customers.read` كانت `if true` — مو بس لتسهيل فحص "هل الرقم مسجّل مسبقاً" وقت التسجيل/الدخول (قبل أي Auth)، إنما كانت بتسمح لأي حد بالعالم يعمل `getDocs(collection(db,'customers'))` بدون أي فلتر ويفرّغ **اسم ورقم هاتف كل زبون بالنظام** — تسريب بيانات حقيقي على نطاق كامل قاعدة الزبائن، مو مجرد ثغرة تخمين متل PIN المتجر (بس بدون خطر انتحال هوية مباشر لأنه معرفة الاسم/الرقم لحاله ما بيسمح بتسجيل دخول — لسا محتاج OTP فعلي لهاتف الشخص).

### الحل (نفس فلسفة verifyStorePin سابقاً، بس بدون "سر" يُخمَّن هالمرة)
1. **Cloud Function جديدة `checkCustomerPhone({phone})`** — Admin SDK، تفحص رقم هاتف واحد بالضبط وترجّع `{ exists, customerId?, name?, isBlocked? }`. ما ترجّع كامل الجدول ولا تسمح بأي فلترة/تعداد جماعي — بس معلومة الرقم المُرسَل تحديداً (نفس مبدأ "فحص وجود حساب" المعتاد بمعظم تطبيقات الدخول).
2. **`firestore.rules`**: `customers.read` صارت `isAdmin() || isOwnerCustomer(customerId)` (نفس دالة `isOwnerCustomer` المستخدمة أصلاً لمجموعة `addresses` الفرعية) — بدل `if true`.
3. **`firebase-init.js`**: رجّعنا Cloud Functions client SDK (كنا حذفناه لما ألغينا نظام PIN) + دالة مساعدة `checkCustomerPhone(phone)` جديدة.
4. **`akleto-customer.html`**: كل من `signUpCustomer()` و`signInCustomer()` صاروا يستدعوا `checkCustomerPhone()` بدل `getDocs(query(collection(db,'customers'), where('phone','==',phone)))` المباشرة. باقي المنطق (فحص is_blocked، تخزين customerId/name، التوجيه لـOTP) بقي زي ما هو بالضبط — بس مصدر المعلومة تغيّر.

### ✅ تأكيد: باقي قراءات customers ما تأثرت
- `akleto-admin-customers.html` (لوحة الأدمن، قائمة كل الزبائن): تستخدم `isAdmin()` — تشتغل عادي.
- `getDoc(doc(db,'customers',customerId))` بداخل `akleto-customer.html` (قراءة الزبون لبياناته هو بعد تسجيل الدخول، بشاشة "عناويني"): تشتغل عادي لأنها مقيّدة أصلاً بـ`isOwnerCustomer` اللي بتتحقق `auth_uid` مطابق — وهذا محقَّق فعلياً بعد OTP.
- `drivers.html` و`store.html`: صفر استخدام لمجموعة `customers` (بيانات الزبون بالطلب أصلاً denormalized جوا مستند `orders` نفسه — مافي حاجة يقرأوا `customers` مباشرة).

### التحقق قبل الرفع
`node --check` على `functions/index.js`، `firebase-init.js`، وmodule script بـ`akleto-customer.html` — كلها سليمة. عدّ الأقواس بـ`firestore.rules` (53/53) بعد التعديل.

### ⚠️ المطلوب من مؤيد (بالترتيب)
1. `git pull`
2. **`firebase deploy --only functions`** — لازم أول شي عشان `checkCustomerPhone` يصير موجود على السيرفر.
3. **`firebase deploy --only firestore:rules`** — عشان تقييد قراءة `customers` يصير فعلي.
4. اختبار: من `akleto-customer.html`، جرّب "حساب جديد" برقم غير مسجّل (لازم يوديك على OTP عادي)، وبنفس الرقم بعد ما تسجّله جرّب "حساب جديد" تاني (لازم رسالة "في حساب مسجّل مسبقاً"). جرّب "تسجيل الدخول" برقم مش مسجّل (لازم رسالة "ما في حساب"). جرّب حظر حساب من لوحة الأدمن وبعدين تسجيل دخول فيه من تطبيق الزبون (لازم رسالة "محظور").
5. لا حاجة لبناء APK — كل التعديلات ويب + سيرفر.

### الخطوة التالية
`drivers` (الأعقد شوي — لازم نفصل بيانات عامة زي الاسم/الموقع/نوع المركبة عن بيانات حساسة زي صور الوثائق والآيبان)، وبعدها `orders` (الأعقد — 3 أطراف: زبون/متجر/سائق/أدمن).


## 24 أغسطس 2026 (تكملة 7) — 🆕 مهمة مستقبلية: تفعيل الحظر/التعطيل فوراً حتى لجلسات مسجّلة دخول مسبقاً

### المشكلة المكتشفة
مؤيد سأل: "لما اعمل حظر للعميل ويكون مسجل دخول شو بصير؟" — الجواب الصادق: **ولا شي فوراً**. فحص `is_blocked` (بالزبون) حالياً بيصير بس لحظة تسجيل الدخول (عبر `checkCustomerPhone` قبل الـOTP). لو الزبون أصلاً بجلسة محفوظة (localStorage) وقت الحظر، بيضل يقدر يستخدم التطبيق ويطلب طلبات عادي — لأنه:
1. ما في مراقبة لحظية (`onSnapshot`) على مستند الزبون نفسه تكتشف تغيّر `is_blocked` وهو شغّال بالتطبيق.
2. قاعدة `orders.create` بـ`firestore.rules` **ما بتتحقق من `is_blocked` إطلاقاً** — بس بتتحقق إنه صاحب الحساب الحقيقي (`isOwnerCustomer`).

**نفس الفجوة بالضبط موجودة على الأغلب بالتطبيقين التانين** (نفس النمط، حقول مختلفة):
- **السائقين (`drivers`)**: حقل `is_active` — تعطيل سائق من لوحة الأدمن وهو بجلسة مفتوحة، على الأغلب بيضل يقدر يشتغل عادي لحد ما يسجّل خروج/دخول من جديد.
- **موظفي المتاجر (`store_employees` / `stores.is_active`)**: تعطيل متجر أو سحب تصريح موظف وهو بجلسة مفتوحة، نفس السيناريو.

### الحل المتفق عليه (يُطبَّق على التطبيقات الثلاث: الزبون، السائق، المتجر)
لكل تطبيق من الثلاثة، خطوتين:
1. **الأهم (أمان فعلي على مستوى القاعدة):** إضافة شرط الحالة النشطة (`is_blocked == false` للزبون، `is_active == true` للسائق، `is_active == true` للمتجر) على قواعد الكتابة الحساسة بـ`firestore.rules` (بالذات `orders.create` للزبون، وشروط تحديث الطلب `orders.update` للسائق والمتجر). هيك حتى لو الجلسة شغّالة بالواجهة، أي عملية حساسة بترفض من السيرفر نفسه فوراً، بغض النظر عن حالة الواجهة.
2. **تحسين تجربة (اختياري، بعد الخطوة 1):** مراقبة لحظية (`onSnapshot`) على مستند الحساب نفسه بكل تطبيق — لو انحظر/انعطّل وهو فاتح التطبيق، يطلع له مباشرة شاشة "حسابك محظور/معطّل" ويسجّل خروج تلقائياً، بدل ما يكتشف بس لما تنرفض عملية بصمت بدون تفسير واضح.

### الحالة
⏳ **لسا ما اشتغلنا عليها** — مسجّلة هون كمهمة تُنفَّذ بالتطبيقات الثلاث مع بعض بجلسة قادمة (بعد ما نخلص مهمة تضييق القراءة المفتوحة الحالية: drivers ثم orders).


## 24 أغسطس 2026 (تكملة 8) — تقييد قراءة `drivers` + فصل الوثائق الحساسة لمجموعة جديدة

ثاني مجموعة من مهمة تضييق القراءة المفتوحة (بعد `customers`). أعقد شوي لأنه اكتشفنا مستند كل سائق فيه حقول **حساسة جداً**: صور الهوية (وجهين)، رخصة القيادة، رخصة المركبة (وجهين)، شهادة عدم محكومية، و**IBAN** (رقم حساب بنكي) — كلها كانت مكشوفة بالكامل بسبب `drivers.read: if true`.

### الاستخدامات الفعلية اللي لازم تضل شغّالة (راجعناها قبل التنفيذ)
- `findDriverByPhone` (فحص رقم مكرر بالتسجيل/الدخول) — يحتاج شغل قبل أي Auth حقيقي.
- `watchActiveDrivers` (خريطة "السائقين المتاحين" — كل سائق شغّال بيشوف باقي السائقين) — بين سائقين مسجّلين دخول لبعض بس (أكدنا عدم استخدامها من تطبيق الزبون أو المتجر).
- لوحة الأدمن: تحتاج تشوف كل شي بما فيه الوثائق لمراجعة طلبات التسجيل.

### الحل (فصل الحساس عن العام، نفس فلسفة PIN المتجر)
1. **مجموعة جديدة `driver_documents/{driverId}`** (نفس معرّف السائق) — فيها بس الحقول الحساسة (`photo_url`, `id_front_url`, `id_back_url`, `license_url`, `vehicle_license_front_url`, `vehicle_license_back_url`, `clearance_certificate_url`, `iban`). محمية بـ`isAdmin() || isOwnerDriver(driverId)` (نفس الدالة المساعدة الموجودة أصلاً، بتتحقق من `drivers/{driverId}.auth_uid`).
2. **`drivers.read`**: من `if true` (أي حد بالعالم) إلى `if request.auth != null` (أي مستخدم مسجّل دخول، حتى Anonymous) — تضييق حقيقي بدون ما يكسر خريطة السائقين أو فحص الرقم المكرر.
3. **`driver-shared.js`**:
   - `findDriverByPhone()`: صارت تستدعي `ensureDriverAuth()` أول شي (تضمن وجود Auth — حتى Anonymous — قبل القراءة، بدل ما تعتمد على إنه الشاشة استدعته مسبقاً).
   - `createDriverAccount()`: الحقول الآمنة تُكتب على `drivers` متل قبل، والحقول الحساسة تُكتب بـ`setDoc` منفصل على `driver_documents/{newId}` بعد إنشاء السائق.
   - `resubmitDriverAccount()`: نفس الفصل — الحقول الآمنة على `drivers` (update)، الحساسة على `driver_documents` (`setDoc` بـ`merge:true`).
   - `updateDriverIban()`: صارت تكتب لـ`driver_documents` بدل `drivers`.
4. **`akleto-driver-account.html`** (شاشة "حسابي" بتطبيق السائق): قراءة حالة الآيبان صارت من `driver_documents/{id}` بدل `drivers/{id}`.
5. **`akleto-admin-drivers.html`**: `loadDrivers()` صارت تجيب `drivers` و`driver_documents` مع بعض (`Promise.all`) وتدمجهم بـ`driversCache` بنفس شكل الكائن القديم (`{...driverData, ...docsData}`) — عشان مودال عرض تفاصيل السائق يضل يشتغل **بدون أي تغيير** بالكود اللي بيعرضه.
6. **أُكِّد**: لا استخدام لهالحقول الحساسة بأي من (`akleto-driver.html`, `akleto-driver-home.html`, `akleto-driver-cards.html`, `akleto-driver-deliveries.html`, `akleto-driver-orders.html`, `akleto-driver-reports.html`, `akleto-driver-settle.html`, `akleto-driver-signup.html` عدا شاشة إعادة التقديم اللي بتقرأ الحقول الآمنة بس أصلاً) ولا بلوحة الأدمن الرئيسية (`akleto-admin.html` — بس ملخص اسم/حالة).

### 🆕 Cloud Function ترحيل `migrateDriverDocuments` (مرة وحدة، متل `migrateStorePins` سابقاً)
تنقل أي حقل حساس لسا موجود بمستندات `drivers` القديمة (من قبل هالتحديث) لمجموعة `driver_documents`، وتحذفه من `drivers` بعدها. محمية بفحص `admins/{uid}` صريح جوا الدالة نفسها. **زر مؤقت "ترحيل الآن"** أُضيف بأعلى `akleto-admin-drivers.html` (بطاقة بحدود برتقالية) — **لازم يُضغط مرة وحدة بس بعد أول نشر لهالتحديث**، وبعدها ينحذف الزر (تنظيف لاحق).

### التحقق قبل الرفع
`node --check` على `functions/index.js`, `driver-shared.js`, module scripts بـ`akleto-admin-drivers.html` و`akleto-driver-account.html` — كلها سليمة. عدّ الأقواس بـ`firestore.rules` (55/55) بعد إضافة قاعدة `driver_documents`.

### ⚠️ المطلوب من مؤيد (بالترتيب بالضبط)
1. `git pull` — **تأكد إنه ما في رسالة `error`/`Aborting`** (نفس مشكلة قبل، الملفات المتضاربة كانت android/capacitor).
2. `firebase deploy --only functions` — لازم أول شي عشان `migrateDriverDocuments` يصير موجود على السيرفر.
3. `firebase deploy --only firestore:rules` — عشان `driver_documents` تصير محمية وتقييد `drivers.read` يصير فعلي.
4. افتح `akleto-admin-drivers.html`، اضغط **"ترحيل الآن"** (البطاقة البرتقالية بأعلى الصفحة) **مرة وحدة بس** — تأكد الرسالة تقول "تم ترحيل X من Y سائق ✅".
5. تحقق يدوياً من Firebase Console → Firestore: مستندات `drivers` ما عاد فيها أي حقل من الحقول الحساسة، ومجموعة `driver_documents` الجديدة فيها كل الوثائق.
6. اختبار: افتح مودال تفاصيل أي سائق بلوحة الأدمن — لازم تظهر الوثائق والآيبان زي ما كانت بالضبط (بما إنه الدمج صار تلقائي بالكود). افتح شاشة "حسابي" بتطبيق السائق — تأكد حالة الآيبان ظاهرة صح. جرّب تسجيل سائق جديد كامل (بما فيه رفع الوثائق) — تأكد ينحفظ ويظهر صح بمراجعة الأدمن.
7. بعد التأكد من نجاح الترحيل والاختبار، احذف بطاقة "ترحيل الآن" من `akleto-admin-drivers.html` (تنظيف).
8. لا حاجة لبناء APK — كل التعديلات ملفات ويب + سيرفر.

### الخطوة التالية
`orders` — آخر وأعقد مجموعة بمهمة تضييق القراءة المفتوحة (3-4 أطراف: زبون/متجر/سائق/أدمن).


## 24 أغسطس 2026 (تكملة 9) — ✅ تأكيد إتمام مهمة `drivers` + تنظيف

مؤيد أكّد نجاح الترحيل واختبار صفحة السائقين بلوحة الأدمن بعد نشر Functions وRules. تفاصيل التشخيص المفيدة اللي طلعت بالطريق (بالحال احتجناها مرة تانية):

- خطأ `permission-denied` وقت تحميل صفحة السائقين **قبل الـdeploy** كان مصدره الفعلي قراءة مجموعة `driver_documents` الجديدة (مالها قاعدة أمان منشورة بعد وقتها) داخل نفس `Promise.all` — مو مجموعة `drivers` نفسها زي ما بدا بالظاهر من رسالة الخطأ. درس عام: أي `Promise.all` فيه أكتر من قراءة، لو وحدة فيهم لمجموعة جديدة ما نُشرت قواعدها بعد، بيفشل الكل برسالة عامة بتضلل عن مصدر المشكلة الحقيقي.
- تم تنظيف: حذف زر "ترحيل الآن" المؤقت من `akleto-admin-drivers.html` (خلص دوره)، وإرجاع رسائل الخطأ التشخيصية المؤقتة (كود الخطأ + UID) بصفحتي الزبائن والسائقين لرسائلها العامة الأصلية ("صار خطأ — تحقق من الإنترنت") بعد ما خلص التشخيص وما عاد محتاجينها.

### الحالة النهائية لمهمة تضييق القراءة المفتوحة
- ✅ `customers` — مكتملة ومختبرة
- ✅ `drivers` — مكتملة ومختبرة (وثائق حساسة بمكان منفصل `driver_documents`)
- ⏳ **`orders`** — التالية والأخيرة (الأعقد: 3-4 أطراف زبون/متجر/سائق/أدمن) — لسا ما بلّشنا فيها.

## 21 أغسطس 2026 — ✅ تأمين باقي مجموعات Firestore المفتوحة بالكامل (كتابة)

بطلب مؤيد ("اشتغلهم كلهم") — بعد مراجعة `firestore.rules` الفعلي بالريبو، تبيّن إنه 15 مجموعة كانت لسا `allow read, write: if true` بشكل كامل (بدون أي تحقق Auth). تم تقييدها كلها بنفس commit وحدة، كل مجموعة حسب مين فعلياً بيكتب/يقرأ فيها بالتطبيق:

**كتالوج عام (قراءة عامة، كتابة أدمن بس):** `meals`, `meal_ingredients`, `ingredients`, `measure_config`.

**قراءة مقيّدة بـAuth، كتابة أدمن بس:** `app_settings` (إعدادات العمولات), `user_notifications`.

**قراءة عامة، كتابة نطاقها محدود:**
- `counters` — كتابة لأي مستخدم مسجّل (حتى Anonymous)، بما إنه مافيه صاحب واحد محدد للمستند (عداد تسلسلي مشترك بين كل الطلبات).
- `store_ingredients` — كتابة لموظف المتجر صاحب `store_id` بس (أو الأدمن).

**كتابة/قراءة مقيّدة بصاحب العلاقة (سائق/متجر) أو أدمن:**
- `coupons` — قراءة لأي مسجّل دخول، تعديل الحقول العامة أدمن بس، الزبون مسموح له يعدّل حصراً `used_count`/`updated_at` (عبر `request.resource.data.diff(resource.data).affectedKeys().hasOnly([...])`) لدعم transaction تطبيق الزبون وقت تنفيذ الطلب.
- `admin_notifications` — إنشاء لأي مستخدم مسجّل (fire-and-forget من تطبيقات الزبون/المتجر/السائق)، قراءة/تعديل/حذف أدمن بس.
- `driver_shifts` — صاحب الوردية (`isOwnerDriver`) أو الأدمن بس، وممنوع تغيير `driver_id` بعد الإنشاء.
- `driver_balance_transactions` — السائق ينشئ طلب `pending` عن نفسه بس، وبعدها الأدمن بس يقدر يأكّد/يرفض (`update`/`delete` أدمن حصراً).
- `store_work_sessions` — موظف المتجر صاحب `store_id` بس (أو الأدمن).
- `store_commission_requests` — نفس نمط `driver_balance_transactions` بس على مستوى المتجر.

**أدمن بس بالكامل (قراءة وكتابة):** `users` — مجموعة غير موثّقة الاستخدام الفعلي بالكود حالياً، اعتماد أكثر تحفظ (Deny by default) لحد ما نتأكد شو غرضها الحقيقي.

### ⚠️ ملاحظات مهمة
- **لم يتم اختبار فعلي بعد** — نفس القيد المعتاد (بيئة العمل ما فيها تشغيل تطبيق حقيقي). القواعد مبنية بالاعتماد الدقيق على أسماء الحقول والمنطق الموثّق مسبقاً بهالملف لكل مجموعة (مثلاً `driver_id`/`store_id`/`status` بـ`driver_shifts`/`driver_balance_transactions`/`store_ingredients`/`store_work_sessions`/`store_commission_requests`)، بس أي تسمية حقل غير متطابقة 100% مع الكود الفعلي ممكن تسبب رفض غير متوقع.
- **الكتابة الوحيدة اللي احتجنا فيها استثناء "حقول محددة فقط"** هي `coupons.used_count` — لو صار أي تعديل مستقبلي على منطق تطبيق الكوبون (مثلاً حقل جديد يحتاج الزبون يعدّله)، لازم إضافته لقائمة `hasOnly([...])`.
- **باقي كل المجموعات المذكورة بالمرحلة السابقة** (`drivers`, `customers`+`addresses`, `stores`+`store_employees`, `orders`, `driver_documents`, `admins`) **بدون أي تغيير** بهالتحديث.
- **لسا معلّق من قبل (غير متأثر بهالتحديث):** تضييق **القراءة** المفتوحة (`allow read: if true`) على `stores`, `store_employees`, `orders` — هالتحديث عالج **الكتابة المفتوحة** على المجموعات الـ15 المذكورة فوق بس، مو ملف القراءة العامة المتبقية الموثّقة بالقسم السابق ("تأمين القراءة المفتوحة"، حالة `orders` لسا "⏳ لسا ما بلّشنا فيها").

### المطلوب من مؤيد
1. `git pull`
2. `firebase deploy --only firestore:rules`
3. **لا حاجة لأي بناء APK** — تعديل ملف قواعد بس.
4. اختبار سريع (الأهم): تأكد الأدمن لسا يقدر يدير الوجبات/المكونات/الكوبونات/الإشعارات عادي، وتأكد السائق يقدر يفتح "تقارير العمل" ويطلب تسديد/تحويل رصيد، وتأكد المتجر يقدر يحدّث توفر منتجاته ويطلب تحصيل عمولة.

## 21 أغسطس 2026 (تكملة) — ✅ رفع Node.js runtime لمشروع Cloud Functions من 20 لـ22

بطلب مؤيد — عالج تحذير ظهر بآخر `firebase deploy --only functions`: Node.js 20 توقّف دعمه (deprecated) من 30 أبريل 2026، وبينحذف نهائياً (decommissioned) بـ30 أكتوبر 2026 — بعدها ما رح يكون ممكن ننشر أي تحديث على الدوال بدون ترقية.

**التعديل:** `functions/package.json` — حقل `engines.node` تغيّر من `"20"` لـ`"22"`. تعديل سطر واحد بس، بدون أي تغيير على كود الدوال نفسه (`functions/index.js`) ولا على `firebase-admin`/`firebase-functions` (النسخ الحالية `^12.1.0`/`^5.0.0` بتدعم Node 22 بدون مشاكل).

**⚠️ تحذير منفصل ظهر بنفس النشر (لم يُعالَج بهالتحديث):** "package.json indicates an outdated version of firebase-functions... there will be breaking changes when you upgrade" — هاد تحذير تاني مختلف (نسخة المكتبة نفسها، مو نسخة Node) وفيه احتمال breaking changes حقيقي لو رفعناها، فتُرك لجلسة منفصلة مخصصة له بعد اختبار كافي، مش ضمن نطاق هالتعديل البسيط.

### المطلوب من مؤيد
1. `git pull`
2. `firebase deploy --only functions` — أول نشر بعد التعديل هذا لازم يطبّق نسخة Node الجديدة على كل الدوال الست (`onOrderUpdated`, `onAdminNotificationCreated`, `onOrderCreated`, `onOrderReady`, `checkCustomerPhone`, `migrateDriverDocuments`) — **متوقع تظهر "Successful update operation" لكل وحدة فيهم هالمرة، مو "Skipped"، لأنه بيئة التشغيل (runtime) تغيّرت فعلياً**.
3. تأكد بعد النشر إنه تحذير Node 20 اختفى من مخرجات الأمر.
